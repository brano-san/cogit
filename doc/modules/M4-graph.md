# M4 · Граф истории и визуальный Rebase

**Статус:** `WIP` — T4.1–T4.6 закрыты · **Вехи:** C, E · **Зависит от:** M1, M2, M9

Самый сложный модуль. Перед началом обязательно прочитать
[07-graph-rendering.md](../07-graph-rendering.md) целиком.

## Задачи

### T4.1 · Алгоритм дорожек (`graph_engine`)
- [x] Порядок коммитов приходит готовым из `git_engine`; сортировку делает обход истории
- [x] Алгоритм активных дорожек из [07-graph-rendering.md §3](../07-graph-rendering.md#3-алгоритм-назначения-дорожек)
- [x] Стабильное назначение цветов
- [x] Сжатие хвоста освободившихся дорожек
- [x] Сохранение состояния между чанками через `LayoutCursor`
- [x] Обработка особых узлов: root, merge, octopus, несколько корней

**DoD выполнен:** сценарии из
[07-graph-rendering.md §9](../07-graph-rendering.md#9-тестирование). Раскладка
переписана по образцу SmartGit (R-161): 4 снапшота заменены утверждениями о колонках и
сегментах, плюс инвариант главной линии на 300 случайных историях.

Крейт не зависит от `gix` — тестируется на синтетических списках «oid + родители».

### T4.2 · Стриминг истории
- [x] Команда `load_commits` с `Channel<GraphChunk>`
- [x] Чанки по 200 коммитов, раскладка считается на лету
- [x] Курсор не понадобился: обход стримит всё сразу и прерывается отказом от чанка
- [x] Отмена: закрытый канал останавливает обход, `generation` отбрасывает чанки устаревшего потока
- [x] Обход в `spawn_blocking` (rayon добавится, когда замер это оправдает)

**DoD:** первые 200 коммитов репозитория на 50 000 приходят быстрее 300 мс;
смена репозитория во время загрузки не приводит к смешению данных.

### T4.3 · Виртуализированный список
- [x] Фиксированная высота строки 22 px
- [x] Рендер только видимых строк плюс буфер 10 сверху и снизу
- [x] Подгрузка при приближении к концу загруженных данных (история стримится целиком)
- [x] Колонки: граф, сообщение, автор, дата, короткий хеш (ref-капсулы — T4.6)
- [x] Клавиатурная навигация, синхронная с прокруткой

**DoD:** тесты `vitest` на расчёт видимого диапазона. Стоимость кадра на 50 000 строк
измерена в `frontend/src/lib/scrolling.test.ts`: число отрисовываемых строк ограничено
видимой областью при любой глубине прокрутки. Индекс рёбер больше не нужен — сегменты
лежат в своих строках (R-161). **Настоящий FPS внутри окна
по-прежнему не измерялся** — для этого нужен инструментированный прогон приложения.

### T4.4 · Отрисовка на Canvas
- [x] `sticky`-холст размером с видимую область
- [x] Отрисовка только в `requestAnimationFrame`
- [x] Учёт `devicePixelRatio`, пересчёт при переносе окна на другой монитор
- [x] Линии и узлы с одним центром, без сдвига на полпикселя (R-114)
- [x] Кривые Безье с вертикальными контрольными точками
- [x] Порядок: серые сегменты → главная линия → кольца (R-161)
- [x] Математическое определение попадания курсора, без `getImageData`

**DoD:** линии не смещаются относительно строк при быстром скролле и при масштабе 150%.

Это главный риск проекта (R-03) — проверять придирчиво.

### T4.5 · Строка Working Tree / Index
- [x] Всегда первая строка списка
- [x] Показывает счётчики: staged, modified, untracked, conflicted
- [x] Обновляется по событиям `fs_watcher`
- [x] Клик переводит Files и Diff в режим рабочей директории
- [x] Пунктирное ребро к HEAD

**DoD:** правка файла в редакторе отражается в строке в пределах 200 мс.

### T4.6 · Ref-капсулы
- [x] Капсулы HEAD, локальных и удалённых веток, тегов (отношение `=`/`↑`/`↓` — с upstream в M1)
- [x] Цвета по типу ссылки из дизайн-системы
- [x] Обрезка при нехватке места с тултипом полного списка
- [x] Клик по капсуле выделяет её коммит; панель Branches он не фильтрует (отменено по #47 партии 2026-09-23)

**DoD:** коммит с десятью ссылками отображается без ломки раскладки строки.

### T4.7 · Фильтрация
- [x] По автору, тексту сообщения, диапазону дат, хешу, пути файла
- [x] Комбинирование условий
- [x] Стриминг результатов, как и обычная история
- [x] Индикация активного фильтра и быстрый сброс

**DoD:** фильтр по пути на репозитории 50 000 коммитов выдаёт первые результаты
быстрее 1 секунды.

### T4.8 · Визуальный Interactive Rebase
- [x] Выделение диапазона коммитов, вход в режим
- [x] Drag-and-drop порядка строк
- [x] Действия: `pick`, `reword`, `edit`, `squash`, `fixup`, `drop`
- [x] Предпросмотр итоговой истории (счётчик оставшихся коммитов)
- [x] Генерация `git-rebase-todo` и запуск через `GIT_SEQUENCE_EDITOR`
- [x] Обработка конфликта: переход в состояние `Rebase in progress`
- [x] Фиксация `ORIG_HEAD` в журнале Undo перед запуском ([INV-12](../01-architecture.md#inv-12))

**DoD:** rebase с squash двух коммитов и конфликтом в середине проходит целиком внутри Cogit;
`Abort` возвращает репозиторий в исходное состояние.

### T4.9 · Drag-and-drop в графе

Прямое манипулирование историей мышью. Каждое действие деструктивно, поэтому каждое
проходит через журнал Undo ([INV-12](../01-architecture.md#inv-12)).

- [x] **Ветка на ветку.** Бросок открывает меню: `Merge`, `Rebase onto`,
      `Fast-forward` (последний пункт появляется, только если перемотка возможна)
- [x] **Коммит внутри ветки.** Перетаскивание меняет порядок (`reorder`) или,
      при броске на другой коммит, объединяет (`squash`)
- [x] Обе операции выполняются фоновым `git rebase -i` через `GIT_SEQUENCE_EDITOR`
- [x] Предпросмотр до подтверждения: план открывается в редакторе rebase
- [x] Конфликт переводит UI в состояние из [M11](M11-commit-debugger.md)

**DoD:** перестановка двух коммитов и squash двух соседних проходят целиком в UI;
`Abort` возвращает исходное состояние.

**Замечание.** Цель перетаскивания определяется по строке и дорожке, вычисляемым из
координат курсора математически — тем же способом, что и попадание по узлу (T4.4).
Отдельная DOM-разметка для drop-зон поверх виртуализированного списка развалится
при скролле.


## Приёмка модуля

```bash
cargo nextest run -p graph_engine
npm run test -- graph
```

- [ ] Граф на настоящем репозитории просмотрен глазами рядом со SmartGit
- [x] Бенчмарк раскладки 50 000 коммитов укладывается в 500 мс — **354 мс** (20.09.2026)
- [ ] Скролл плавный, линии не смещаются
- [x] Отмена загрузки при смене репозитория работает
- [x] Interactive Rebase обратим через `Abort` и Undo

## Известные ловушки

| Ловушка | Последствие | Как избежать |
|---|---|---|
| Canvas высотой во всю историю | Превышение лимита размера холста в браузере | `sticky`-холст размером с видимую область |
| Отрисовка прямо в обработчике `scroll` | Рассинхрон холста и DOM | Только `requestAnimationFrame` |
| Игнорирование `devicePixelRatio` | Мыльные линии на HiDPI | Масштабирование холста и `setTransform` |
| Пересчёт цветов при дозагрузке чанка | Ветки меняют цвет на глазах | Сохранять счётчик цветов в курсоре |
| Переменная высота строки | Невозможно синхронизировать с холстом | Фиксированная высота |
| Раскладка графа в JS | Требует весь граф в Webview | Считать на бэкенде ([INV-02](../01-architecture.md#inv-02)) |
| Rebase без фиксации `ORIG_HEAD` | Потеря коммитов при ошибке | Журнал Undo до запуска |
