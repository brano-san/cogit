# M4 · Граф истории и визуальный Rebase

**Статус:** `TODO` · **Вехи:** C, E · **Зависит от:** M1, M2, M9

Самый сложный модуль. Перед началом обязательно прочитать
[07-graph-rendering.md](../07-graph-rendering.md) целиком.

## Задачи

### T4.1 · Алгоритм дорожек (`graph_engine`)
- [ ] Топологическая сортировка DAG
- [ ] Алгоритм активных дорожек из [07-graph-rendering.md §3](../07-graph-rendering.md#3-алгоритм-назначения-дорожек)
- [ ] Стабильное назначение цветов
- [ ] Сжатие хвоста освободившихся дорожек
- [ ] Сохранение состояния между чанками через непрозрачный курсор
- [ ] Обработка особых узлов: root, merge, octopus, несколько корней

**DoD:** snapshot-тесты `insta` для всех сценариев из
[07-graph-rendering.md §9](../07-graph-rendering.md#9-тестирование);
снапшоты — читаемая ASCII-визуализация, не дамп структур.

Крейт не зависит от `gix` — тестируется на синтетических списках «oid + родители».

### T4.2 · Стриминг истории
- [ ] Команда `load_commits` с `Channel<CommitChunk>`
- [ ] Чанки по 100–200 коммитов, раскладка считается на лету
- [ ] Курсор для продолжения обхода
- [ ] Отмена по `generation` при смене репозитория
- [ ] Обход в `spawn_blocking`, раскладка через `rayon` при большом объёме

**DoD:** первые 200 коммитов репозитория на 50 000 приходят быстрее 300 мс;
смена репозитория во время загрузки не приводит к смешению данных.

### T4.3 · Виртуализированный список
- [ ] Фиксированная высота строки 22 px
- [ ] Рендер только видимых строк плюс буфер 10 сверху и снизу
- [ ] Подгрузка при приближении к концу загруженных данных
- [ ] Колонки: граф, сообщение, ref-капсулы, автор, дата, короткий хеш
- [ ] Клавиатурная навигация, синхронная с прокруткой

**DoD:** скролл 50 000 строк держит 60 FPS; тесты `vitest` на расчёт видимого диапазона.

### T4.4 · Отрисовка на Canvas
- [ ] `sticky`-холст размером с видимую область
- [ ] Отрисовка только в `requestAnimationFrame`
- [ ] Учёт `devicePixelRatio`, пересчёт при переносе окна на другой монитор
- [ ] Смещение 0.5 px для резких линий
- [ ] Кривые Безье с вертикальными контрольными точками
- [ ] Порядок: сначала рёбра (`Crossing` → `Direct` → `Merge`), потом узлы
- [ ] Математическое определение попадания курсора, без `getImageData`

**DoD:** линии не смещаются относительно строк при быстром скролле и при масштабе 150%.

Это главный риск проекта (R-03) — проверять придирчиво.

### T4.5 · Строка Working Tree / Index
- [ ] Всегда первая строка списка
- [ ] Показывает счётчики изменённых и проиндексированных файлов
- [ ] Обновляется по событиям `fs_watcher`
- [ ] Клик переводит Files и Diff в режим рабочей директории
- [ ] Пунктирное ребро к HEAD

**DoD:** правка файла в редакторе отражается в строке в пределах 200 мс.

### T4.6 · Ref-капсулы
- [ ] `HEAD → dev`, `origin/master`, теги, отношение `=` / `↑` / `↓`
- [ ] Цвета по типу ссылки из дизайн-системы
- [ ] Обрезка при нехватке места с тултипом полного списка
- [ ] Клик по капсуле переходит к ветке в панели References

**DoD:** коммит с десятью ссылками отображается без ломки раскладки строки.

### T4.7 · Фильтрация
- [ ] По автору, тексту сообщения, диапазону дат, хешу, пути файла
- [ ] Комбинирование условий
- [ ] Стриминг результатов, как и обычная история
- [ ] Индикация активного фильтра и быстрый сброс

**DoD:** фильтр по пути на репозитории 50 000 коммитов выдаёт первые результаты
быстрее 1 секунды.

### T4.8 · Визуальный Interactive Rebase
- [ ] Выделение диапазона коммитов, вход в режим
- [ ] Drag-and-drop порядка строк
- [ ] Действия: `pick`, `reword`, `edit`, `squash`, `fixup`, `drop`
- [ ] Предпросмотр итоговой истории
- [ ] Генерация `git-rebase-todo` и запуск через `GIT_SEQUENCE_EDITOR`
- [ ] Обработка конфликта: переход в состояние `Rebase in progress`
- [ ] Фиксация `ORIG_HEAD` в журнале Undo перед запуском ([INV-12](../01-architecture.md#inv-12))

**DoD:** rebase с squash двух коммитов и конфликтом в середине проходит целиком внутри Cogit;
`Abort` возвращает репозиторий в исходное состояние.

## Приёмка модуля

```bash
cargo test -p graph_engine
cargo insta review -p graph_engine
npm run test -- graph
```

- [ ] Все snapshot-тесты раскладки проходят и просмотрены глазами
- [ ] Бенчмарк раскладки 50 000 коммитов укладывается в 500 мс
- [ ] Скролл плавный, линии не смещаются
- [ ] Отмена загрузки при смене репозитория работает
- [ ] Interactive Rebase обратим через `Abort` и Undo

## Известные ловушки

| Ловушка | Последствие | Как избежать |
|---|---|---|
| Canvas высотой во всю историю | Превышение лимита размера холста в браузере | `sticky`-холст размером с видимую область |
| Отрисовка прямо в обработчике `scroll` | Рассинхрон холста и DOM | Только `requestAnimationFrame` |
| Игнорирование `devicePixelRatio` | Мыльные линии на HiDPI | Масштабирование холста и `setTransform` |
| Пересчёт цветов при дозагрузке чанка | Ветки меняют цвет на глазах | Сохранять счётчик цветов в курсоре |
| Переменная высота строки | Невозможно синхронизировать с холстом | Фиксированная высота |
| Раскладка графа в JS | Требует весь граф в Webview | Считать на бэкенде ([INV-02](../01-architecture.md#inv-02)) |
| Rebase без фиксации `ORIG_HEAD` | Потеря коммитов при ошибке | Журнал Undo до запуска |
