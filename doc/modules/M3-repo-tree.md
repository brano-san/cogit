# M3 · Дерево репозиториев, сабмодули и папки

**Статус:** `TODO` · **Веха:** D · **Зависит от:** M1, M2

Панель Repositories (лево-верх) и всё, что связано с управлением набором проектов.

Профильный документ: [05-ui-layout.md §3.2](../05-ui-layout.md#32-repositories-лево-верх).

## Задачи

### T3.1 · Виртуальные папки-группы
- [x] `Add Group...`, переименование, удаление группы
- [x] Перемещение репозитория между группами через drag-and-drop
- [ ] Вложенные группы
- [x] Состав групп сохраняется в `tauri-plugin-store`

**DoD:** структура групп переживает перезапуск; удаление группы не удаляет репозитории с диска.

### T3.2 · Отображение репозитория
- [x] Имя, активная ветка, бейджи `↑ahead` / `↓behind`
- [x] Индикатор наличия несохранённых изменений
- [x] Обновление по событиям `repo-changed` без перезагрузки всего дерева
- [x] Обрезка длинных имён многоточием с тултипом

**DoD:** переключение ветки в терминале отражается в дереве в пределах 200 мс.

### T3.3 · Мультивыбор и групповые операции
- [x] Выбор через `Ctrl` и `Shift`
- [x] `Fetch All` для выбранных с общим индикатором прогресса
- [x] Массовое открытие и закрытие
- [x] Ошибка в одном репозитории не прерывает обработку остальных

**DoD:** `Fetch All` на пяти репозиториях, один из которых недоступен, завершается
и показывает ошибку только по проблемному.

### T3.4 · Сабмодули
- [x] Разбор `.gitmodules`; рекурсия во вложенные сабмодули — впереди
- [x] Индикация расхождения SHA (записанный в родителе ↔ фактический)
- [x] Индикация неинициализированного сабмодуля
- [x] Операции `Update` и `Initialize`; `Sync` и `Deinit` — впереди
- [x] Открытие сабмодуля как самостоятельного репозитория

**DoD:** тест на фикстуре `with_submodule()`; рассинхрон SHA виден без дополнительных действий.

### T3.5 · Worktree
- [x] Список worktree репозитория
- [x] `Add`, `Remove`, `Prune`
- [x] Индикация заблокированных и отсутствующих worktree
- [x] Переключение между worktree как между репозиториями

**DoD:** тест на фикстуре `with_worktree()`.

### T3.6 · Системные действия
- [x] `Open in Explorer` через `tauri-plugin-opener`
- [x] `Open in Terminal` / `PowerShell` / `Git Bash` через `tauri-plugin-shell`
- [x] Выбор предпочитаемого терминала в настройках
- [x] Копирование пути в буфер обмена

**DoD:** пути с пробелами и кириллицей открываются корректно.

### T3.7 · Фильтр и недавние
- [x] Фильтр по имени в шапке панели
- [ ] Список недавно открытых на стартовом экране
- [x] Удаление из списка без удаления с диска
- [x] Обнаружение перемещённого или удалённого репозитория с предложением убрать из списка

**DoD:** удаление папки репозитория не приводит к падению — строка помечается как недоступная.

### T3.8 · Worktree как сущность первого класса

Расширение T3.5: worktree перестаёт быть «ещё одной командой» и становится объектом,
между которыми приложение переключается.

- [x] Worktree показываются в дереве рядом с репозиторием, а не в отдельном диалоге
- [ ] У каждого — статус `clean` или `dirty`, обновляемый по `fs_watcher`
- [x] Клик по ветке, уже занятой другим worktree, **переключает контекст приложения**
      на этот worktree вместо падения `fatal: already checked out`
- [x] Перед переключением — предупреждение, если в текущем worktree есть незакоммиченное
- [x] Заблокированные (`locked`) и отсутствующие worktree помечены отдельно

**DoD:** попытка checkout ветки, занятой другим worktree, приводит к переключению,
а не к ошибке.

**Почему это важно.** `already checked out in worktree` — одна из самых раздражающих
ошибок Git: она сообщает о проблеме, хотя пользователь хотел ровно того, что Git
и так может сделать.


### T3.9 · Автопоиск репозиториев в папке · `DONE`

Пользователь указывает папку, приложение находит в ней все репозитории и предлагает открыть
их одним списком.

- [x] Параллельный обход (`rayon`) в `crates/git_engine/src/discover.rs`
- [x] Репозиторий — лист обхода: внутрь найденного не заходим ([R-41](../12-risks.md))
- [x] Стоп-лист тяжёлых каталогов и предел глубины 6
- [x] Bare-репозиторий (`HEAD` + `objects` + `refs`) и worktree (`.git` — файл) распознаются
- [x] Находки идут потоком через `tauri::ipc::Channel`, уже открытые помечены
- [x] Диалог с чекбоксами, фильтром и «Open N Selected»

**DoD:** 1000 каталогов с 200 репозиториями обходятся меньше чем за 1.5 с —
тест `a_broad_tree_is_scanned_inside_the_budget`.

## Приёмка модуля

```bash
cargo test -p git_engine submodule
cargo test -p git_engine worktree
```

- [ ] Дерево корректно на репозитории с вложенными сабмодулями второго уровня
- [x] Групповые операции устойчивы к ошибке в отдельном репозитории
- [ ] Перетаскивание не теряет репозитории
- [ ] Недоступный путь обрабатывается без паники

## Известные ловушки

| Ловушка | Последствие | Как избежать |
|---|---|---|
| Рекурсия по сабмодулям без ограничения глубины | Зависание на циклических ссылках | Ограничение глубины и учёт посещённых путей |
| Открытие сабмодуля как отдельного репо без учёта `.git`-файла | Не находится репозиторий | `gix::discover` умеет читать `.git` как файл |
| Блокирующий `Fetch All` последовательно | Пять репозиториев по 10 секунд = минута | Параллельно, с ограничением одновременных задач |
| Хранение абсолютных путей без нормализации | Дубликаты при разном регистре на Windows | Канонизация пути перед сравнением |
