# M5 · Дерево ссылок и управление состоянием

**Статус:** `TODO` · **Веха:** D · **Зависит от:** M1, M2

Панель References (лево-низ), все операции над ветками и стэшами, и — важнее всего —
механизм Undo.

## Задачи

### T5.1 · Иерархия ссылок
- [ ] Аккордеоны: `HEAD`, `Local Branches`, `Remote`, `Tags`, `Stashes`, `Lost Commits`
- [ ] Счётчики в заголовках секций
- [ ] Группировка удалённых веток по remote
- [ ] Вложенность по слешам в именах (`feature/auth/login`)
- [ ] Состояние раскрытия сохраняется между запусками
- [ ] Отношение к upstream: `= origin`, `↑N`, `↓N`, `↑N ↓M`

**DoD:** репозиторий с 500 ветками отображается без просадки; фильтр работает по всему дереву.

### T5.2 · Интерактивные стэши

Ключевая особенность, отличающая Cogit от большинства клиентов.

- [ ] Клик по стэшу **мгновенно** показывает его файлы в панели Files
- [ ] Выбор файла показывает diff — **без** `stash apply`
- [ ] Чтение через `gix` (стэш — обычный коммит), не через `git stash show`
- [ ] Отображение всех трёх частей стэша: изменения worktree, index, untracked

**DoD:** просмотр содержимого стэша не меняет рабочую директорию — проверяется тестом,
сравнивающим `status` до и после.

### T5.3 · Операции над стэшами
- [ ] `Stash All` (`Ctrl+S`), `Stash Selection` (`Ctrl+Alt+S`)
- [ ] `Apply`, `Pop`, `Drop`, `Branch from Stash`
- [ ] Ввод сообщения при создании
- [ ] Опции `--include-untracked`, `--keep-index`
- [ ] `Drop` фиксируется в журнале Undo вместе с OID стэша

**DoD:** случайно удалённый стэш восстанавливается через Undo.

### T5.4 · Операции над ветками
- [ ] `Checkout` (`Ctrl+G`), `Merge` (`Ctrl+M`), `Fast-Forward`
- [ ] `Rebase` (`Ctrl+R`), `Cherry-Pick`, `Revert`
- [ ] `Branch` (`F7`), `Tag` (`Shift+F7`), удаление с различением `-d` и `-D`
- [ ] `Set Upstream`, `Rename`, `Push`, `Delete Remote Branch`
- [ ] Предупреждение при checkout с несохранёнными изменениями и предложение автоstash

**DoD:** каждая операция покрыта интеграционным тестом; неуспех даёт `GitCommandError`.

### T5.5 · Git-Flow
- [ ] `Init` с настройкой префиксов
- [ ] `Feature` / `Release` / `Hotfix`: start и finish
- [ ] Определение уже инициализированного Git-Flow по конфигу
- [ ] Реализация через обычные команды Git, **без** зависимости от расширения `git-flow`

**DoD:** полный цикл feature start → finish на фикстуре.

Отказ от внешнего расширения намеренный: оно не установлено у большинства пользователей,
а его поведение — просто последовательность обычных команд.

### T5.6 · Lost Commits (reflog)
- [ ] Чтение `.git/logs/HEAD` и `.git/logs/refs/`
- [ ] Отбор коммитов, недостижимых из существующих ссылок
- [ ] Отображение с датой и причиной попадания в reflog
- [ ] Восстановление: создать ветку, cherry-pick, просмотр в графе

**DoD:** после `reset --hard` потерянный коммит виден в секции и восстанавливается в один клик.

### T5.7 · Журнал безопасности и Undo

Реализация [INV-12](../01-architecture.md#inv-12). Самая ценная часть модуля.

- [x] Запись перед деструктивной операцией: описание и что нужно вернуть (`Recovery`).
      Пока покрыты `discard`, `branch -D` и `checkout` (последний — как необратимый)
- [x] Автоматический stash перед операциями, затрагивающими рабочую директорию
- [x] `Undo` кнопкой в тулбаре; шорткат придёт с Command Palette
- [ ] Журнал виден пользователю списком — пока только последняя запись в подсказке кнопки
- [x] Честная индикация: операции, которые откатить нельзя, помечены явно

**DoD:** для каждой деструктивной операции есть тест «выполнить → Undo → состояние совпадает
с исходным» (сравнение по OID всех ссылок и по `status`).

Список операций, обязательных к покрытию: `reset --hard`, `discard`, `branch -D`,
`stash drop`, `rebase`, `merge --abort`, `cherry-pick`, `revert`, `checkout` с потерей изменений.

### T5.8 · Баннеры состояния репозитория
- [ ] Баннер над панелью Graph для каждого состояния из
      [03-git-semantics.md §4](../03-git-semantics.md#4-состояния-репозитория)
- [ ] Кнопки `Continue` / `Skip` / `Abort` по применимости
- [ ] Отдельная обработка `index.lock`: показать, какой процесс держит блокировку
- [ ] Баннер `Detached HEAD` с кнопками `Create Branch` и `Return to <branch>`

**DoD:** каждое состояние воспроизведено фикстурой и проверено вручную.

## Приёмка модуля

```bash
cargo test -p git_engine stash
cargo test -p git_engine branch
cargo test -p app_state undo
```

- [ ] Просмотр стэша не меняет рабочую директорию
- [ ] Каждая деструктивная операция откатывается через Undo
- [ ] Все состояния репозитория дают корректный баннер
- [ ] Ошибки CLI показываются целиком

## Известные ловушки

| Ловушка | Последствие | Как избежать |
|---|---|---|
| `stash show` через CLI на каждый клик | Задержка 30 мс на выбор | Читать через `gix` |
| `branch -D` без записи в журнал | Безвозвратная потеря ветки | Журнал до операции |
| Checkout с несохранёнными изменениями | Git откажет или потеряет правки | Проверка `status` и предложение автоstash |
| Reflog без фильтрации достижимых | Секция забита обычными коммитами | Отбирать только недостижимые |
| Зависимость от расширения `git-flow` | Не работает без установки | Реализовать обычными командами |
| Undo, который «почти» восстанавливает | Хуже отсутствия Undo | Тест на точное совпадение состояния |
