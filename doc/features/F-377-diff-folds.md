# F-377 · Полосы скрытых строк вместо `@@`

- [ ] В diff нет строк `@@ … @@`: промежутки между изменениями — полосы `▲ N lines hidden · show ▼` на всю ширину, в side-by-side — через обе половины; промежуток в одну-две строки показан целиком; у нового и удалённого файла полос нет.
