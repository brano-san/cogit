# Переключение вместо already checked out

Клик по ветке, занятой другим worktree, предлагает перейти в него, а не показывает ошибку git.
Worktree этого репозитория открывается в панелях, как двойной клик в Worktrees: новой строки в
Repositories не появляется, у владельца — метка worktree (R-184).
