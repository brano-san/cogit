# F-084 · Инспектор хуков

- [ ] Tools → Manage Hooks показывает все хуки Git с описанием момента срабатывания, отмечает, откуда они берутся (`.git/hooks` или `core.hooksPath`), и даёт править их прямо в окне.
