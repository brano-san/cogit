# Blame пропускает косметические коммиты

Коммиты, перечисленные в `.git-blame-ignore-revs`, не показываются владельцами строк:
строка приписывается тому, кто её написал, а не тому, кто переформатировал файл.
Комментарии, пустые строки и мусор в файле пропускаются, blame от них не ломается.
