# F-320 · Remote ▸ Synchronize

`Remote ▸ Synchronize` (`Ctrl+Shift+S`) делает Pull, затем Push текущей ветки в основной remote;
если Pull не удался, Push не запускается. Проверить: ветка отстаёт и опережает на один
коммит без конфликтов, `Ctrl+Shift+S` — после него `ahead 0, behind 0`.
