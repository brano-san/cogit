# Отсутствие финального перевода строки видно в diff

Файл, у которого появился или пропал перевод строки в конце, показывается как изменение
последней строки, а не как «без изменений»; сторона без перевода помечена флагом
`noNewline`, под который вьюер печатает `\ No newline at end of file`.
