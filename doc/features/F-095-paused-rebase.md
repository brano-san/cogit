# F-095 · Rebase с остановкой на каждом коммите

- [ ] Галочка «Pause after each commit» в диалоге rebase останавливает операцию после каждого применённого коммита, чтобы между шагами можно было прогнать сборку или тесты.
