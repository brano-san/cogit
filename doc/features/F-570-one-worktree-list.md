# F-570 · Рабочая копия одним списком без раздельного индекса

- [ ] Застейджить часть файла `a.txt`, целиком — `b.txt`, изменить `c.txt`; выключить в Files
      `Separate Working Tree and Index` — один список без заголовков `Unstaged`/`Staged`, каждый
      файл один раз: у `b.txt` бейдж в рамке, у `a.txt` — в пунктирной рамке, подсказка бейджа
      говорит «staged» / «partly staged». Клик по `b.txt` открывает застейдженный diff, по `a.txt`
      и `c.txt` — незастейдженный. Включить переключатель — снова две секции (R-590).
