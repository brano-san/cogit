# Blame сводит авторов через .mailmap

Если в корне репозитория есть `.mailmap`, blame показывает каноническое имя и почту
автора, а не ту подпись, с которой был сделан коммит; сломанный или отсутствующий
`.mailmap` blame не ломает.
