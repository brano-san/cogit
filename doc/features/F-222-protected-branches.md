# Защита от правки общей истории

Split-Off недоступен для коммита, который уже лежит на общей ветке (`main`, `master`, `develop`, `release/*` или список из `cogit.protectedBranches`), и меню объясняет, на какой именно.
