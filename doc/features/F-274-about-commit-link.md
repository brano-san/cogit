# F-274 · Commit в About ведёт на GitHub

Хеш коммита сборки — ссылка на страницу коммита на GitHub; сборка из дерева с
незакоммиченным кодом помечена `dirty` и ссылки не имеет. Проверить: собрать с правкой в
`crates/` — рядом с хешем метка `dirty`, хеш не кликается.
