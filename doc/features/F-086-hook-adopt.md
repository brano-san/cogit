# F-086 · Подключение командных хуков

- [ ] Если в репозитории лежит каталог `.githooks`, на который никто не указывает, Cogit предлагает включить `core.hooksPath` одной кнопкой — но никогда не делает этого сам.
