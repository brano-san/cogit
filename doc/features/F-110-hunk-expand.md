# F-110 · Разворот скрытых строк

- [ ] Между изменениями — полоса `▲ N lines hidden · show ▼`: `▲` открывает двадцать строк над изменением ниже, `▼` — двадцать строк под изменением выше, `show` — весь промежуток, Ctrl+клик — все скрытые строки файла.
