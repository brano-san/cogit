# Видимые разделители панелей

Между панелями идёт полоса в 6 px с точечной ручкой посередине и рамкой по кромкам; при наведении она подсвечивается, курсор меняется на `col-resize` или `row-resize`, а зона захвата шире самой полосы на 3 px с каждой стороны.
