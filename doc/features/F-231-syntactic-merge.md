# Синтаксическое слияние через tree-sitter

Если две стороны правили разные узлы разобранного файла (разные функции), конфликт снимается сам и помечается «by the parser»; при неразобравшемся файле или правке поперёк двух узлов конфликт остаётся пользователю.
