# F-012 · Side-by-side diff

- [ ] Клик по файлу показывает две колонки со старой и новой версией, выровненные построчно.
- [ ] Блок, где удалены три строки, а вставлена одна: против пары строк лента переходит от
      красного к зелёному, против двух остальных удалённых — красная до правой колонки, без
      зелёного хвоста (R-532). Чистая вставка — зелёная лента.
- [ ] Удалённые строки: справа, напротив них, диагональная штриховка без разрывов между строками
      и на границе колонки знака; у добавленных — слева. Во всех четырёх темах штрих виден, но не
      спорит с текстом (R-533).
- [ ] Между колонками — линия; наведение подсвечивает её, курсор `col-resize`. Перетаскивание
      меняет ширину колонок (не уже пятой части), ленты едут вместе с границей; после
      перезапуска и в окне сравнения ширина та же. Двойной щелчок — поровну; с фокусом на
      линии `←` / `→` двигают её, `Home` — поровну (R-535).
