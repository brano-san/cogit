# F-374 · Индикатор окончаний строк

- [ ] В тулбаре Diff окончания строк написаны заглавными (`LF → LF`, `CRLF → LF`), у нового файла — только новое (`LF`), у удалённого — только старое; tooltip `Line endings: …`.
