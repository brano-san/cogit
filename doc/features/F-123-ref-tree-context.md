# Контекстное меню дерева ссылок

Правый клик по строке дерева предлагает действия её типа — checkout, merge, rebase, удаление, apply/pop/drop для стэша, восстановление потерянного коммита.

- [ ] Добавить worktree на ветку `topic` → ПКМ по `topic` в Branches и по её метке в графе:
      `Delete (checked out in a worktree)` неактивен; после удаления worktree — снова активен.
