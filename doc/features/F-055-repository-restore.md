# F-055 · Восстановление списка

- [ ] Открытые репозитории возвращаются после перезапуска в прежнем порядке. Тот, что не открылся
  (папку перенесли, диск ещё не подключён), остаётся в Repositories закрытой строкой с меткой
  missing, и одно уведомление называет такие пути с причиной. Тесты — `stores/repository.test.ts`,
  «restoring the last session».
