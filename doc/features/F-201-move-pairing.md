# Перемещённый блок помечен с обоих концов

Оба конца одного перемещения несут один и тот же `moveId`, поэтому видно не просто «эти
строки перемещены», а какой именно удалённый блок соответствует какому добавленному;
два перемещения в одном файле получают разные номера.
