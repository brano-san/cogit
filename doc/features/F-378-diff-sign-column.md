# F-378 · Маркеры `+` и `−` в своей колонке

- [ ] В diff `+`/`−` стоят в узкой колонке между номером строки и кодом, в паре пробелов от кода, того же цвета и фона, что строка; при выделении и копировании кода маркеры не попадают в текст.
