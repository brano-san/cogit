## Cogit: правки от 25.09.2026

Номера — из исходного списка. Пометка **повторно** — пункт уже «чинился»: искать первопричину.

### Режим работы

- Идёт на ветке аудита `audit/2026-09-25`: аудит закончен на 2b9995b (точка для его A/B),
  правки сливаются следом. Без push.
- Агенты — каждый в своём worktree `D:\cogit-work\tN` на ветке `tasks-0925/tN`, бриф
  `D:\cogit-work\BRIEF6.md`, тяжёлые команды — на ядрах 16–31 через `heavy.cmd`, каталог сборки
  свой (`D:\cogit-work\build-tN`). Слияние — по одной ветке, после каждой — проверки.
- Группы:
  - t1 — A (1–9, 23, 24, 29): отдельное окно Diff;
  - t2 — B (10, 14–16, 42–44): Repositories;
  - t3 — C (11–13, 20) и 19: remote-операции и меню Branches;
  - t4 — 40 и 46: двойной клик, общий диалог Checkout, checkout с изменениями;
  - t5 — E (21, 25, 27, 36–39): граф;
  - t6 — 26: Bisect;
  - t7 — F (28, 30–34) и 35: Files, мигающий кружок на заголовках;
  - t8 — 45: Clone;
  - t9 — G (17, 18, 22, 41): общее; американское написание — последним, когда слито остальное.
- Волны: сейчас — t1, t5, t7 (не пересекаются с b1 и b3); после слияния b1, разбиения
  `commands/mod.rs` и `app_state/lib.rs` (BE-024, BE-025) и b3 — t2, t3, t4, t6, t8; последней —
  t9.
- Пункты, которые меняют решения аудита 26.09 (список новее — делается по нему, решение
  аудита помечается в `12-risks.md` как заменённое):
  - 33 — сортировка по колонкам (было FG-030 / R-476: только по пути);
  - 28 — кнопки строк видны всегда (было FG-051 / R-475: при наведении);
  - 11 — Pull без upstream делает fetch (было FS-030: Pull неактивен);
  - 40 — двойной клик открывает диалог (было FR-022: как Check Out меню);
  - 17 — американское написание (было FR-029 / R-517 и правило в `frontend/CLAUDE.md`).
- Замеры A/B — в конце, на простаивающей машине, по пунктам, которые трогают горячие пути.

### A. Отдельное окно Diff

- [x] **1. Горизонтальная прокрутка** — сейчас нельзя листать вправо-влево. Полоса прокрутки и `Shift` + колесо.
  > Итог: Уже было: окно сравнения использует тот же DiffView с прокруткой вбок (463b973, DF-037); после 3f3c84b каждая колонка прокручивается в своей ширине.
  > Проверить в сборке: длинная строка в окне сравнения, Shift+колесо.
- [x] **2. Next/prev change** — после перехода кратко подсвечивать изменение, к которому перешли.
  > Итог: Вспышка 600 мс у изменения, к которому перешли, токен `--diff-jump-flash`, при уменьшенном движении — без затухания (9e5c12b, F-541).
- [x] **3. Подсветка синтаксиса в правой панели ломает токены** — `fn`: `f` розовая, `n` белая. Вероятно, подсветка считается по кускам строки, разрезанным внутристрочными изменениями. Подсвечивать весь текст файла целиком, diff-разметку накладывать поверх.
  > Итог: Причина другая: Lezer разбирал склейку строк из ханков — битый код, ханк начинается посреди функции. Теперь `FileDiff::Text` несёт стороны целиком (если есть парсер и ≤ 5000 строк), подсветка по всему тексту, разметка diff поверх; изменённое слово сохраняет цвет токена (8ee69d8, R-530).
  > Проверить в сборке: Rust-ханк посреди функции.
- [x] **4. Разделитель между панелями** — явная вертикальная граница, перетаскиванием меняет ширину панелей; ширина запоминается.
  > Итог: Разделитель — полоса лент между колонками, 20–80 %, ←/→/Home, двойной щелчок — поровну; ширина — `diffSplit` в настройках и Preferences (3f3c84b, e73b19d, R-535).
  > Проверить в сборке: перетаскивание в окне сравнения и в главном.
- [x] **5. Подпись `commitVsParent`** справа сверху — человекочитаемо (например, `Commit a1b2c3d vs parent d4e5f6a`) и tooltip с пояснением, что с чем сравнивается.
  > Итог: «Commit a1b2c3d vs parent d4e5f6a», в подсказке — какая версия слева и справа (7aa8eeb).
- [x] **6. `Ctrl+F`** открывает поиск браузера — должен открывать наш поиск (тот же, что по кнопке `Find`). Проверить главное окно на то же.
  > Итог: `suppressBrowserFind` гасит Ctrl+F/G и F3, если страница не ответила, во всех окнах; наш поиск по Ctrl+F при фокусе в Diff не тронут (ed6e1e6, R-536). Причину появления поиска браузера по коду не нашли; следующий шаг, если он останется, — `AreBrowserAcceleratorKeysEnabled` в `webview2.rs`.
  > Проверить в сборке: Ctrl+F в окне сравнения над текстом и над бинарным файлом.
- [x] **7. Имя файла трижды** — в заголовке окна, в шапке и в начале diff. Оставить заголовок окна и одно место в шапке.
  > Итог: Имя файла — в заголовке окна и один раз в шапке (`showPath={false}`, 59281cf).
- [x] **8. Пустые области** (справа у удалённых строк, слева у добавленных) — штриховка, как в VS Code, из токенов темы.
  > Итог: Штриховка пустой половины строки, токен `--diff-filler` (86e3b99, R-533).
  > Проверить в сборке: 4 темы.
- [x] **9. Удалённая строка слева** красная, но в конце остаётся зелёный хвост — фон строки единый на всю ширину.
  > Итог: «Зелёный хвост» — лента между колонками, всегда заливалась зелёным; теперь по тому, что напротив: удаление красным, вставка зелёным, замена — градиентом (c5e461f, R-532).
  > Проверить в сборке: 4 темы.
- [x] **23. Изменённый submodule** открывает пустое окно diff — вместо окна переходить в этот submodule (сделать его активным).
  > Итог: Двойной щелчок по submodule делает его активным, окно не открывается; окно сравнения, всё же открытое на submodule, шлёт `cogit://open-module` и закрывается (a88af9f, R-537).
- [x] **24. Большие и бинарные файлы**
  - [x] Файл от 1 000 000 байт или бинарный — не текстом: сообщение, размеры в байтах и хеши старой и новой версии.
  - [x] Бинарность — с учётом `.gitattributes` (`binary`, `-diff`, `text`) и по содержимому, с объяснением причины, как у SmartGit: `File is considered as binary: invalid character 0x02 in line 1, at position 6`.
  > Итог: От 1 000 000 байт на сторону или бинарный — сводка: причина, размеры в байтах, полные id сторон (у рабочего файла — как `git hash-object` через clean-фильтры). `.gitattributes`: `binary`/`-diff` → бинарный, `text`/`diff` → текст, `text=auto`/`-text` — по байтам; по содержимому — управляющие 0x00–08, 0x0E–1A, 0x1C–1F по всей стороне, причина как у SmartGit плюс сторона (1f17ea5, R-531).
- [x] **29. Удалённый файл** в превью diff показывается как «размер N → 0 байт» — показывать как удаление: все строки удалены.
  > Итог: Удалённый текстовый файл и раньше показывался удалёнными строками; «N → 0» писали сводка бинарного и панель изображения — теперь «Not there: the file is deleted» и «Deleted · N KB» (6003bc3, R-534).

### B. Repositories

- [x] **10. Индикатор изменений** — повторно. У одного репозитория оранжевый кружок появляется только при выборе и пропадает при переходе на другой. Для submodules кружок и, вероятно, стрелки pull/push не поддержаны. Индикаторы у всех узлов, включая submodules, независимо от выбора.
  > Итог: Причина — два определения «есть изменения»: у выбранной строки полный статус, у остальных — пульс без неотслеживаемых и submodules, по размеру и mtime. Пульс считает как полный статус (3f33be9), наблюдатель слышит HEAD и refs в `.git/modules` (79ab2be), у узлов submodules свои точка и стрелки (9ecd673), метки ушедшей строки держатся до чтения (e513398) (R-540…R-542).
  > Проверить в сборке: неотслеживаемый файл; правка в submodule; коммит в submodule из терминала; уход с submodule без мигания.
- [x] **14. Стрелки pull и push** крупнее — сейчас еле видно.
  > Итог: Стрелки 10 px вместо 7, токен `--sync-arrow`, двойной ореол (d320ff2, R-547).
  > Проверить в сборке: 4 темы.
- [x] **15. Цвета стрелок**: push — оранжевая (как кружок изменений), pull — зелёная.
  > Итог: Push — цвет точки изменений, pull — `--status-add` (e38d7ae, R-548).
  > Проверить в сборке: 4 темы.
- [x] **16. Подсказки submodules** (`not initialized`, `behind` и др.) уходят за край при длинном имени коммита или ветки. Подсказка видна всегда: закреплена справа или идёт перед усекаемым текстом; усекается имя коммита.
  > Итог: Метка состояния — сразу после имени и не сжимается; усекается ветка или коммит после неё (bc2595a, R-544).
  > Проверить в сборке: behind на длинной ветке в узкой панели.
- [x] **42. При открытии репозитория** сразу раскрывать его submodules (глубина 1, без вложенных).
  > Итог: Первый уровень submodules раскрывается при настоящем открытии (диалог, Recent, перетаскивание, скан, клик по закрытой строке); переключение и восстановление сессии свёрнутое не трогают (62156c3, R-546).
- [x] **43. Закрытие репозитория, пока активен его submodule**, добавляет submodule в список отдельной записью. Закрытие родителя закрывает и его submodules, отдельной записи не появляется.
  > Итог: Причина: перечитывание показанного репозитория шло через открытие по папке, а оно вносит папку в список. Команда `reread_repository` по id; закрытие родителя закрывает показанный submodule (7f59a6f, R-543).
  > Проверить в сборке: A → submodule → правка/Refresh → Close A.
- [x] **44. Правый клик по репозиторию** никак его не выделяет — меню висит без привязки. Подсвечивать узел, пока открыто меню (активный репозиторий при этом не меняется).
  > Итог: Пока открыто меню, строка обведена, выбор не меняется (`MenuRow`, 547f6ec, R-545).

### C. Remote-операции

- [x] **11. Pull на ветке без upstream** даёт ошибку `You asked to pull from the remote 'origin', but did not specify a branch…`. Молча выполнять fetch всех remotes, без ошибки. Сверить с документацией SmartGit.
  > Итог: Pull без upstream делает `git fetch --prune --all` без ошибки и без Undo (35da55b). SmartGit в этом случае Pull выключает — отступление записано в R-552; решение аудита FS-030 для Pull заменено.
- [x] **12. Push на ветке без upstream** даёт ошибку `The current branch … has no upstream branch`. Пушить ветку в remote с одноимённой веткой, выставлять upstream (`--set-upstream`) и пушить всё, что можно. Сверить с документацией SmartGit; если remotes несколько — выбор remote.
  > Итог: У пользователя была сборка до R-414. Найдена ещё дыра: проверка смотрела только `branch.<b>.merge`, git требует и `.remote`; при нескольких remotes Push открывает Push To с выбором (bb12f8e, R-551).
  > Проверить в сборке: два remotes → Push To; один → сразу с upstream.
- [x] **13. После push новой ветки** в графе две метки `audit/2026-09-25` и `origin/audit/2026-09-25` вместо `origin=audit/2026-09-25`. После push перечитывать конфигурацию upstream и перестраивать метки.
  > Итог: Причина по логу: Push из меню ветки и Push To шли явным refspec без `--set-upstream`. У `push_to` параметр `track`, в Push To флажок Set upstream (fdc2a52, R-550).
  > Проверить в сборке: одна метка `origin=<ветка>` после push новой ветки.
- [x] **20. Несколько remotes** — у каждого (`origin`, добавленный `upstream` и т.д.) свой узел в Branches со своим меню (п.19). Проверить с двумя remotes.
  > Итог: Узел у каждого настроенного remote, в том числе пустой; имя со слешем — по самому длинному совпадению (b7c8821).

### D. Branches

- [x] **19. Контекстные меню**
  - [x] У каждого узла — и групп, и элементов — пункт `Toggle`.
  - [x] Группа `Tags` — `Add Tag` (диалог создания тега).
  - [x] Группа `Local Branches` — `Add Branch`.
  - [x] Узел remote (`origin` и др.): `Push To…` — `Pull`, `Fetch`, `Fetch More` (если ничего нового не пришло — уведомление об этом; семантику сверить с SmartGit) — `Rename` (переименовать remote) — `Delete` — `Copy URL` — `Set Depth` — `Properties` (URL и чекбокс `Perform background poll or fetch` — это та же настройка фонового fetch, что для индикаторов Repositories) — `Toggle`.
  > Итог: Toggle у всех узлов; Add Tag у Tags, Add Branch у Local Branches; у remote — Push To…, Pull, Fetch, Fetch More, Rename, Delete, Copy URL, Set Depth, Properties с фоновой проверкой по remote (`remote.<имя>.cogitBackgroundFetch`) (7960b9a, aa075e2, R-553…R-555).
  > Проверить в сборке: меню всех узлов; Fetch More на `--single-branch`; Set Depth на `--depth=1`; Rename.
- [x] **40. Двойной клик**
  - [x] Папка — раскрыть/свернуть.
  - [x] Stash — диалог `Apply Stash`: `Cancel`, `Apply`, `Apply & Drop`, чекбокс `Restore Index`. Тот же диалог — из контекстного меню.
  - [x] Тег — диалог Checkout (ниже), варианты: `Create local branch` с полем имени или `(just read-only)` — detached HEAD.
  - [x] Remote-ветка — диалог Checkout: создать локальную ветку с чекбоксом `Track remote branch` (включён по умолчанию); если есть локальная ветка, для которой эта remote-ветка — upstream, третий вариант `Checkout and fast-forward local branch '<имя>'`. Как git связывает ветки — по upstream в конфиге; случай одноимённой ветки без upstream — на твоё решение, обоснуй.
  - [x] Локальная ветка — диалог Checkout с информацией и чекбоксом `Don't show again` (дальше — сразу checkout).
  - [x] `HEAD` — ничего не делает.
  - [x] В графе: двойной клик по коммиту = как по remote-ветке без локальной (два варианта); по метке ветки = как по remote-ветке (три варианта при наличии отслеживающей локальной, иначе два); по тегу = как по коммиту.
  - [x] **Диалог Checkout — один на все случаи**, кнопки `Cancel` и `Checkout`.
  > Итог: Один диалог Checkout на все случаи (4eb63d8); двойной клик в Branches (f5c444f) и в графе (8e2bf7a); Apply Stash с Apply & Drop и Restore Index (1d61f08). Одноимённая локальная без upstream не считается отслеживающей: git связывает ветки только через upstream, перемотка без спроса изменила бы чужое (R-560…R-563); решение аудита FR-022 заменено.
  > Проверить в сборке: диалоги в 4 темах; двойной клик по линиям, метке, кнопке сворачивания.

### E. Graph

- [x] **21. Подсветка отмеченной ветки захватывает чужую историю.** Отмечен только `master`, а цветом подсвечена линия от нижнего коммита до `feature/14340_new_toolchain`. Подсвечивать только часть истории, принадлежащую ветке: от её вершины до точки, где она сходится с главной линией или с другой подсвеченной; общая история остаётся цветом главной линии.
  > Итог: Причина: цепочка ветки шла до коммита на главной линии, а влитый в feature master — боковая полоса. История главной линии — всё достижимое из её вершины; ветка красит только то, чего там нет, целиком влитая — не красится (be981bc, R-570).
  > Проверить в сборке: HEAD на feature с влитым master, отмечен master → полоса серая.
- [x] **25. Кнопка `Continue` в баннере merge** — убрать: коммит слияния делается обычными средствами. `Continue` остаётся для rebase, cherry-pick, revert.
  > Итог: У merge в баннере только Abort, текст зовёт закоммитить; Continue остался у rebase, cherry-pick, revert, am (a9501b3, R-572).
- [x] **27. Граф «плывёт» при изменении высоты Diff** — повторно. Текст стоит на месте, а линии графа смещаются. Canvas графа не синхронизирован с прокруткой или высотой списка при resize — найти причину.
  > Итог: Первопричина — порядок кадра: ResizeObserver → Svelte в том же кадре меняет CSS-высоту, перерисовка уходила в rAF уже прошедшего кадра — кадр со старым растянутым bitmap; плюс дробная CSS-высота против округлённого bitmap. Новый размер рисуется сразу, CSS-размер берётся из bitmap (f812e60, R-571).
  > Проверить в сборке: 125 % и 150 %, сплиттер Diff быстро и медленно.
- [x] **26. Bisect** — по образцу SmartGit, сверить с его документацией.
  - [x] Старт с выбором хорошего и плохого коммита; действия `Mark as Good`, `Mark as Bad`, `Skip`, `Reset`.
  - [x] Граф окрашивает хорошие коммиты зелёным, плохие — красным, текущий проверяемый выделен.
  - [x] Состояние bisect — в баннере панели Graph и меткой в Repositories, как у merge и rebase.
  - [x] В конце — найденный первый плохой коммит и предложение завершить.
  > Итог: Старт из меню коммита, Branch ▸ Bisect и палитры; Mark as Good/Bad, Skip, Reset; отметки в графе отдельным слоем с меткой словом; баннер и метка в Repositories; в конце — первый плохой коммит выбран и диалог Bisect Finished (90ae4a8, d93c3bf, c63b0fe, cd99035, R-580…R-583).
  > Проверить в сборке: bisect из меню и баннера; git bisect в терминале; 4 темы.
- [x] **36. Поиск в графе**
  - [x] Placeholder — просто `Filter`.
  - [x] При вводе появляется строка переключателей: `Author`, `Committer`, `Message`, `Refs`, `ID`, `Name`, `Content`. По умолчанию включены все, кроме `Name` и `Content`.
  > Итог: Placeholder `Filter`; при вводе — переключатели Author, Committer, Message, Refs, ID, Name, Content; Content — сравнение строк версий как мультимножеств (ловит то же, что `-G`) (d3c10a3, R-573).
- [x] **37. Кнопка настроек графа** справа от поиска, выпадающее меню: `Default Coloring`, `Branch Coloring`, `Mergeable Coloring`, `Varying Coloring` — ссылка на настройки графа в Preferences — `Follow Only First Parent`, `Show Only Selected Branches and Tags`, `Include Tracked Remote Branches`, `Show Graph While Filtering` — `Show Working Tree Permanently` (когда выключено и изменений нет — строки Working Tree нет). Семантику режимов раскраски сверить с SmartGit. Значения — те же, что в Preferences, один источник.
  > Итог: Меню: Default/Branch/Mergeable/Varying Coloring (Mergeable — новый режим), ссылка в Preferences, Follow Only First Parent, Show Only Selected, Include Tracked Remote, Show Graph While Filtering (новый обход), Show Working Tree Permanently; один источник — те же настройки (f644c97, f70970b, 1ad7f48, 59683dc, 40e0ce8, R-574, R-575).
  > Проверить в сборке: каждая раскраска и переключатель, синхронность с Preferences.
- [x] **38. Кнопки навигации** справа от поиска
  - [x] «Домой»: с Working Tree — на последний коммит HEAD; с HEAD — на Working Tree; с любого другого коммита — на Working Tree.
  - [x] «Назад» — к предыдущему выбранному коммиту; неактивна, если истории нет.
  > Итог: «Домой» и «Назад», история выбора на репозиторий до 100 шагов, Working Tree — тоже шаг (4ebcbf2).
- [x] **39. Лупа в поле поиска** — меню с `Remember Pattern` и списком сохранённых шаблонов; выбор подставляет шаблон; сохранённый можно удалить.
  > Итог: Лупа: Remember Pattern, Forget Pattern, до 30 шаблонов (e8ad24a).

### F. Files

- [x] **28. Кнопки в строках `Unstaged` / `Staged`** видны всегда, а не по наведению. Кнопки при наведении на строки файлов убрать.
  > Итог: Кнопки в заголовках Unstaged/Staged видны всегда, на строках их нет (6aef244, R-593); решение аудита FG-051 заменено.
  > Проверить в сборке: узкая панель — усекается подпись, не кнопки.
- [x] **30. Дерево / плоский список** — сейчас две соседние кнопки. Одна кнопка-переключатель, меняющая иконку и tooltip.
  > Итог: Одна кнопка, иконка и подсказка показывают, на что она переключит (20b1a8e, R-594).
- [x] **31. Нажатое состояние при наведении** — кнопки-переключатели теряют синий цвет, нажатую от отжатой не отличить. Наведение не должно скрывать нажатое состояние.
  > Итог: Причина: правило наведения специфичнее `.on`. Токены `--state-pressed` и одно правило `[aria-pressed="true"]` для всех переключателей (b8b9fed, R-592).
  > Проверить в сборке: 4 темы: Files, Branches, Investigate, окно слияния.
- [x] **32. `Separate Working Tree and Index` выключен**, а список всё равно разделён на `Staged` / `Unstaged` — исправить.
  > Итог: Настройку читала только раскладка — секции оставались. Теперь один список без заголовков, частично застейдженный файл — один раз (ffddd2d, R-590).
- [x] **33. Таблица** из четырёх колонок: имя файла, тип (файл, репозиторий, бинарный), вид изменения (`M`, `?`, `D`…), путь. Пути начинаются с одной вертикали. Сортировка по любой колонке, по умолчанию — по имени.
  > Итог: Колонки Name, Type, State, Path; сортировка по любой, по умолчанию по имени; пути с одной вертикали; Binary — по расширению, без чтения файла (62e91c2, R-595, R-596); решение аудита FG-030 заменено.
  > Проверить в сборке: заголовки над колонками, вертикаль путей, стрелка сортировки.
- [x] **34. `Customize View`** открывает пустой список — должен управлять видимостью колонок из п.33.
  > Итог: Меню обрезала полоса (FG-055), а колонок не было; теперь группа Columns (910d196, R-597).
- [x] **35. Мигающий оранжевый кружок** на заголовках Repositories и Files раз в секунду — найти источник. Если это индикатор активности — не мигать; если лишний — убрать; записать, что это было.
  > Итог: Это пометка «данные устарели» (F-223). У submodules, склонированных на месте, их `.git` писали другие клиенты и health-проверка Cogit — наблюдатель считал это изменением родителя, 125 перечитываний с шагом 2 с. Внутри вложенного `.git` изменение — только HEAD, refs, packed-refs; проба регистра ничего не пишет; точка — только при перечитывании дольше 600 мс (43cd794, 8179c53, 0f1eb7e, R-591).
  > Проверить в сборке: точка не мигает на dtv_device.

### G. Общее

- [x] **17. Американский английский везде**: `initialized`, `color`, `license` и т.д. Обновить правило в `frontend/CLAUDE.md` (сейчас там британское написание) и пройти по всем строкам интерфейса.
  > Итог: Правило американского написания — в `frontend/CLAUDE.md` и корневом `CLAUDE.md`; меню, палитра, ошибки и журнал, submodules, Preferences, темы (Light gray / Dark gray), About, Customize View (5f653b5, b58b024, 906b805, 1620f57, fd9e189, 360ad4c, R-615). Ключи настроек, id тем и команд, serde-значения IPC оставлены — `bindings.ts` не изменился. Держат тесты `lib/ui-spelling.test.ts` (все строки и разметка `.svelte`/`.ts`) и `menu::spelling_tests`.
  > Проверить в сборке: Remote ▸ Synchronize, Submodule ▸ Initialize, View ▸ Maximize Panel; Preferences ▸ Theme & Colors, Behavior; About ▸ Third-party licenses.
- [x] **18. Индикатор ошибок в левом нижнем углу** (треугольник и число) — выровнять по центру своего блока.
  > Итог: Причина: `⚠` — символ шрифта, запасной шрифт со своей базовой линией. Треугольник — SVG, у счётчика и `Cancel` одно правило `.foot-button` (4451122).
  > Проверить в сборке: 100/125/150 %, 4 темы, счётчик рядом с Cancel.
- [x] **22. Редактор конфига репозитория**: путь к файлу наполовину скрыт за редактором, у конфига пользователя всё в порядке. Свести оба в один компонент — вероятно, это две реализации.
  > Итог: Реализация одна (`ConfigEditor`). Причина: редактор брал высоту по числу строк и сжимал колонку вместе со строкой пути — длинный `.git/config` обрезал путь, короткий `~/.gitconfig` нет. Шапка не сжимается, путь режется в середине, полный — в подсказке (42d9408, R-610).
  > Проверить в сборке: конфиг репозитория на 60+ строк, узкое окно.
- [x] **41. Чередование фона строк** — как в графе — в Files, Repositories, Branches, Worktrees.
  > Итог: Тот же токен `--row-stripe` и правило, что в графе, в Files, Repositories, Branches, Worktrees; чётность — по месту строки в списке, не по DOM; тот же переключатель «Alternate row background» (0489710, R-611, F-580).
  > Проверить в сборке: полосы при прокрутке Files; Repositories с раскрытыми submodules; выделенная и под меню — сплошные.

### H. Новые функции

- [x] **45. Clone** — `Repository → Clone…`, мастер:
  - [x] Страница 1: URL удалённого репозитория или путь к локальному. Если в буфере обмена URL репозитория — подставить сразу. `Next` проверяет доступ к репозиторию.
  - [x] Страница 2: `Include submodules` и `Fetch all heads and tags` (оба включены), `Skip large files (partial clone)` (выключен), выбор ветки для checkout из списка веток remote.
  - [x] Страница 3: каталог назначения.
  - [x] Клонирование — операция в общей очереди с прогрессом; после — репозиторий открыт.
  > Итог: Repository ▸ Clone…, мастер из трёх страниц: проверка ls-remote, submodules, все ветки, partial clone `--filter=blob:limit`, ветка из списка сервера, папка; операция очереди с прогрессом и Cancel в футере, после — репозиторий открыт (650fe92, R-600…R-602).
  > Проверить в сборке: меню и буфер; неверный URL; Cancel убирает папку.
- [x] **46. Checkout с изменениями в рабочей копии**
  - [x] Как SmartGit: предложить автоматически сохранить изменения — скрытый stash → checkout → применение stash. Для пользователя — «перешёл на другую ветку вместе со своими изменениями».
  - [x] Отличие от SmartGit: stash применяется через `apply`; без конфликтов — затем удаляется (как `pop`), с конфликтами — остаётся.
  - [x] В диалоге — чекбокс «удалить stash после чистого применения», включён по умолчанию; выключен — stash остаётся всегда.
  > Итог: Скрытый stash на время операции — в `refs/cogit/backup`, после checkout `apply`; чисто и чекбокс включён — удаляется, иначе возвращается в список (`stash@{0}`) с сообщением; при отказе checkout изменения возвращаются с индексом (fd5ec68).
