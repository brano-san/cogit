# Итерация: перенос проекта, Staged, мигание графа, тулбар, уровни деревьев

| № | Пункт | Причина | Что сделано |
|---|---|---|---|
| 1 | Перенести проект на D | На C оставалось ~24 ГБ | Проект скопирован в `D:\cogit`, проверен, копия на C удалена; `core.hooksPath` стал относительным |
| 2 | Staged без файлов | Раздельный режим рисовал пустую секцию | Секция с `hideWhenEmpty` без файлов скрыта (R-188) |
| 3 | Мигает Graph & History | `graph.load` очищал строки до первой порции | Старые строки стоят, пока новый список их не догонит (R-186) |
| 4 | Две подсветки у кнопок тулбара | Ячейка и иконка подсвечивались разными цветами | Одна подсветка ячейки цветом `--state-selected`, вертикальный отступ у тулбара (R-187) |
| 5 | Уровни деревьев | Шаг 12 px при треугольнике 20 px, разная база | Общий шаг «один элемент» на токенах во всех деревьях и в группировке Files (R-185) |

## Перенос

- Скопировано robocopy: 173 405 файлов, 55,5 ГБ, ошибок нет.
- Две junction npm workspaces (`node_modules/@cogit/frontend` → `frontend`) созданы заново на
  новые пути: robocopy их пропустил ключом `/XJ`.
- Копия репозитория `.claude/worktrees/parallel-work` перерегистрирована: `git worktree repair`.
- `core.hooksPath` был абсолютным (`C:\Users\brano\cogit\.githooks`) — после удаления
  старой папки хуки молча перестали бы работать. Теперь `.githooks`.
- `src-tauri/target` (~50 ГБ по `du`) похож на старый остаток: сборка идёт в корневой
  `target`. Перенесён как есть; удалять — решение пользователя.

## Тесты

- `frontend/src/stores/graph.test.ts` — новый, 7 тестов: старые строки держатся до догоняния,
  короткая история подменяет по завершении, пустая очищает, другой репозиторий очищает сразу,
  первая загрузка идёт порциями, ошибка очищает, устаревший поток игнорируется.
- `file-view.test.ts` — `shownSections`, 3 теста.
- Тулбар и отступы деревьев — разметка и CSS, тестами не закреплены; шаг проверен арифметикой
  (R-185).
