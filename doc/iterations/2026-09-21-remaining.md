# Что осталось после слияния ветки `diff-merge`

Составлено 21 сентября 2026, на коммите после `chore: clear the branch dividers`.
Восемьдесят шагов [разделения работы](2026-09-21-split.md) закрыты все. Галочки в
модульных документах отстали от кода на десятки пунктов — сверено по коду и тестам,
142 отметки проставлено задним числом. Ниже — то, что действительно не сделано.

## Итого 32 пункта: 12 работы и 20 проверок

### Работа (12)

| # | Модуль | Что | Размер |
|---|---|---|---|
| 1 | M5 | **Git-Flow**: `Init` с префиксами, `Feature`/`Release`/`Hotfix` start и finish, определение уже инициализированного по конфигу, всё через обычный `git` без расширения | большой, 4 пункта |
| 2 | M7 | **Синтаксическое слияние через `tree-sitter`**: непересекающиеся узлы AST сливаются сами, при любом сомнении — вручную | большой |
| 3 | M2 | **Окно 3-way merge** и событие `merge-resolved` обратно в главное | средний |
| 4 | M6 | **`Discard` в скрытый стэш**, чтобы отброшенное можно было вернуть через Undo | средний |
| 5 | M3 | **Вложенные группы** репозиториев | средний |
| 6 | M3 | **Стартовый экран** со списком недавно открытых | средний |
| 7 | M3 | Статус `clean`/`dirty` у worktree, обновляемый по `fs_watcher` | малый |
| 8 | M8 | Индикатор «данные устарели» на панелях при событии ФС | малый |
| 9 | M2 | Подтверждение при закрытии с несохранёнными правками | малый |
| 10 | M2 | Общий компонент тултипа вместо атрибута `title` | малый |
| 11 | M12 | Действие недоступно для коммита, отправленного в защищённую ветку | малый |
| 12 | M1 | Фильтрация обхода через крейт `ignore` вместо списка `EXCLUDED_DIRS` | малый |

### Тесты и замеры (5)

- M1: лог не превышает лимит; `status` на `stress(10_000)` в бюджете.
- M11: повреждённый `git-rebase-todo` не роняет UI.
- M3: вложенные сабмодули второго уровня; недоступный путь без паники.

### Ручная проверка (15)

M0 — сплиттеры между запусками. M4 — snapshot-раскладки глазами, плавность прокрутки.
M2 — токены дизайн-системы, клавиатурная навигация, видимость фокуса, масштабы
100/125/150%, чек-лист [06-design-system.md §10](../06-design-system.md).
M3 — перетаскивание не теряет репозитории. M6 — обратимость `Discard` (после пункта 4).
M8 — палитра, blame, настройки, сессия. M14 — репозиторий с 500 авторами,
приглашение включить аватары при первом запуске.

## Порядок, если продолжать

1. Пункты 7–12: шесть мелких, каждый на полдня, закрывают четыре модуля.
2. Пункт 4 (`Discard` в стэш) — единственная дыра в обещании «любое действие обратимо».
3. Пункт 3 (окно merge) — четыре панели уже есть внутри главного окна, нужно отдельное.
4. Git-Flow и `tree-sitter` — два больших куска, каждый со своим разбором до кода.
