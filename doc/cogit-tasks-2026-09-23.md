## Cogit: партия правок от 23.09.2026

Ты работаешь над Cogit — desktop git GUI (Tauri + WebView2, Rust-бэкенд, Svelte 5 фронтенд), референс по UX — SmartGit. Ниже 49 пунктов из ручного тестирования, сгруппированных по областям.

### Как работать с этим файлом

- [ ] Прочитал правила ниже до начала работы
- Каждый пункт и подпункт — чекбокс. Отмечай `[x]` только после проверки в запущенной сборке, а не после правки кода.
- Под каждым пунктом верхнего уровня допиши строку `> Итог: …` — что было причиной и что сделано, либо `уже работало`, либо `не сделано: причина`.
- Номер `#N` — из исходного списка тестировщика. Не перенумеровывай. **Пункта #23 в исходном списке нет** — это не пропуск.
- Перед каждой правкой проверь текущее поведение в актуальной ветке: часть пунктов может быть уже исправлена.
- Скриншотов у тебя нет: всё, что на них было, описано словами.

### Порядок

Сначала — пункты, которые ломают работу приложения: **#8**, **#31**, **#10** (баг в первой части), **#48**, **#1**. Затем остальное в любом порядке. **#15 (Investigate)** — крупная новая функция, её делать после исправлений.

### Общие правила для всех пунктов

- [ ] **Контекстные меню** (#33–#41) — только через общий компонент меню приложения, в его стилях. Разделители из списков ниже — настоящие разделители; двойные, ведущие и замыкающие разделители не рендерятся.
- [ ] **Неприменимый пункт меню — неактивен, а не скрыт.** Скрывать можно только то, что не существует на платформе (например, PowerShell на Linux).
- [ ] **Шорткаты** пунктов, если они есть, показываются справа в меню.
- [ ] **Деструктивные действия** (Discard, Delete, Drop Stash, Reset hard, Remove) — с подтверждением.
- [ ] **«Коммит запушен»** = достижим из любой remote-tracking ветки. В бэкенде уже есть запрос `is_published` — используй его, а не новую логику. Там, где ниже сказано «с предупреждением, если запушен», — перед выполнением диалог о переписывании опубликованной истории; «выключена, если запушен» — пункт неактивен с tooltip о причине.
- [ ] **Все диалоги** — через общий шаблон модального окна приложения. Никаких `window.prompt` / `alert` / `confirm` браузера.
- [ ] **Все отдельные окна** (diff, blame) закрываются крестиком, `Esc`, `Ctrl+W`, не несут меню главного окна и не влияют на работу главного окна — урок из #8.

---

### A. Тулбар и главные меню

- [ ] **#2 Отступы в тулбаре, подписи обрезаются**
  - [ ] Подписи кнопок не обрезаются снизу: сейчас у `Tag` не видна нижняя часть буквы `g`. Причина — высота строки или `overflow: hidden` у подписи; поправить высоту строки и вертикальный отступ.
  - [ ] Горизонтальные отступы у краёв окна: крайние кнопки не прилипают к границам.
  - [ ] Проверить все кнопки тулбара на масштабах 100/125/150 %.

- [ ] **#31 Stage, Unstage, Discard, Merge, Rebase никогда не становятся активными**
  - [ ] Найти, почему правило доступности всегда возвращает «недоступно».
  - [ ] `Stage` — активна, если выбран Working Tree и в `Files` есть выбранные неиндексированные файлы (или есть хоть одно изменение, если выбора нет — тогда действие на все).
  - [ ] `Unstage` — активна, если в индексе есть выбранные файлы (или есть staged-изменения при отсутствии выбора).
  - [ ] `Discard` — активна, если выбраны изменения рабочей копии.
  - [ ] `Merge` — активна, если в графе выбран коммит или ветка, отличные от HEAD и не являющиеся его предком.
  - [ ] `Rebase` — активна, если в графе выбран коммит, отличный от HEAD.
  - [ ] Доступность пересчитывается при смене выбора в `Graph`, `Files`, `Branches`.

- [ ] **#32 У Rebase меняется цвет подписи, но не состояние кнопки**
  - Сейчас: при выборе Working Tree подпись `Rebase` обычного цвета; при выборе коммита в истории подпись белеет, а сама кнопка (иконка, фон, реакция) не меняется.
  - [ ] Одно состояние `disabled` управляет подписью, иконкой и кареткой одновременно. Проверить все кнопки тулбара на такое же расхождение.

- [ ] **#6 Создание тега: свой диалог с аннотацией**
  - Сейчас кнопка `Tag` открывает браузерное окно ввода.
  - [ ] Диалог `Add Tag` в стилях приложения: поле имени, многострочное поле сообщения (аннотации), видимое указание, на какой коммит ставится тег (короткий хеш + тема).
  - [ ] Непустое сообщение → аннотированный тег (`git tag -a`), пустое → лёгкий тег.
  - [ ] Валидация имени через `git check-ref-format "refs/tags/<имя>"`; занятое имя — ошибка под полем.
  - [ ] Тот же диалог открывается из контекстного меню коммита в графе → `Add Tag` (см. #38, #39).

- [ ] **#26 Выпадающее меню Pull**
  - Сейчас: `Pull`, `Fetch`, `Fetch All`, в неупорядоченном виде.
  - [ ] Упорядочить: `Pull`, затем блок fetch: `Fetch '<текущий remote>' (current)`, затем `Fetch '<remote>'` для каждого остального remote репозитория по алфавиту, затем `Fetch All`.
  - [ ] Пункты `Fetch '<remote>'` строятся из фактического списка remotes активного репозитория (например `Fetch 'origin' (current)`, `Fetch 'custom-controls'`).
  - [ ] Выбор поведения по умолчанию прямо в этом меню, как в SmartGit: *«Multi-remote repositories now make Pull behavior clearer. Choose the default directly – set whether Pull uses the current remote or all remotes from the toolbar popup.»* — переключатель «текущий remote / все remotes» для основной кнопки `Pull`.
  - [ ] Опция *«More control after fetch – deleting merged branches after Pull is now optional.»* — чекбокс «удалять слитые ветки после Pull», сохраняется в настройках.

- [ ] **#27 Выпадающее меню Sync**
  - [ ] Пункты `Push, then Pull` и `Pull, then Push`.
  - [ ] По умолчанию основная кнопка `Sync` выполняет `Pull, then Push`.
  - [ ] Выбор в меню задаёт поведение основной кнопки и запоминается.

- [ ] **#28 Выпадающее меню Push → Push To…**
  - [ ] Пункт `Push To…` открывает диалог по образцу SmartGit: заголовок `Push '<ветка>' to remote '<remote>'`, подзаголовок `Select the target repository where to push the ref(s).`, выбор remote (если их несколько), переключатель `Push To:` — `Tracked or matching branch` / `Custom Ref` с полем ввода имени ref, кнопки `Push` и `Cancel`.
  - [ ] У SmartGit в этом диалоге подписи обрезаны справа — не повторять.

- [ ] **#29 Переработка Stash**
  - [ ] Переименовать `Stash everything` → `Stash All`.
  - [ ] Клик по основной кнопке `Stash` открывает диалог создания stash.
  - [ ] В диалоге ошибка `Enter a name` показывается **под полем ввода**, к которому относится (сейчас она оторвана от поля).
  - [ ] Вместо одной кнопки `Stash` в диалоге три: `Stash All`, `+ Keep Index` (`git stash push --keep-index`), `+ Keep Working Tree` (stash без очистки рабочей копии: `git stash create` + `git stash store -m <сообщение> <sha>`).
  - [ ] Выпадающее меню кнопки `Stash`: `Stash Selection`, `Quick Stash All`, `Quick Stash Selection`. Пункт `Stash All` из меню убрать — его делает основная кнопка.
  - [ ] `Stash Selection` — неактивен, если в `Files` нет выбранных файлов; перед выполнением подтверждение со списком файлов, которые будут застешены, кнопки `OK` / `Cancel`.
  - [ ] `Quick Stash All` — stash всего без диалога.
  - [ ] `Quick Stash Selection` — stash выбранных файлов без подтверждения.

- [ ] **#30 Кнопка Apply Stash**
  - [ ] Новая кнопка рядом со `Stash`: применяет последний stash активного репозитория (`stash@{0}`).
  - [ ] Неактивна, если stash-ей нет. При конфликтах — уведомление через общее окно уведомлений.

- [ ] **#43 Убрать `Repository → Refresh`** из меню.

- [ ] **#44 `Edit → Configure Toolbar…`**
  - [ ] Настраивает **тулбар с кнопками** (`Pull`, `Push`, `Sync`, `Stage`…), а не строку меню.
  - [ ] Добавление, удаление, перестановка кнопок, сброс к умолчанию; сохраняется между запусками.

- [ ] **#45 Меню Remote: новые пункты**
  - [ ] `Synchronize`
  - [ ] `Submodule ▸`: `Initialize`, `Synchronize`, `Reset`, —, `Add`, —, `Deactivate`, `Deinit`, `Unregister`
  - [ ] `Subtree ▸`: `Add`, —, `Merge`, `Split`, `Reset`, —, `Push`
  - [ ] Доступность по контексту: пункты Submodule — при выбранном submodule или репозитории с submodules.

- [ ] **#46 Пункт LFS**
  - [ ] Подменю `LFS ▸`: `Install`, `Track`, —, `Lock`, `Unlock`, —, `Prune`.
  - [ ] Разместить в меню `Remote` рядом с `Submodule` и `Subtree`; если сочтёшь другое место логичнее — обоснуй в итоге.
  - [ ] Если `git lfs` не установлен — активен только `Install` (или подсказка, как установить).

---

### B. Graph

- [ ] **#1 Счётчик коммитов в заголовке Graph обновляется не всегда**
  - При отметке всех веток в `Branches` число в `Graph & History (N)` меняется не каждый раз.
  - [ ] Счётчик и строки графа — из одного источника; после каждого пересчёта (включая отменённые и перезапущенные) заголовок совпадает с фактическим числом строк.
  > Итог: источник у счётчика и строк и так был один (показанный обход в `graph.svelte.ts`), но перезагрузка, догоняющая старый граф, могла не догнать его никогда: блок строк, бывший в пути в момент последнего сообщения о прогрессе, перезапрашивался только у показанного обхода, и старые строки со старым числом оставались до следующего тика; обход, оборванный в Rust чужим поколением (две почти одновременные `load_commits` получают `begin_graph` в любом порядке) или оставшийся без последнего сообщения, замораживал промежуточное число. Сделано: окно графа несёт `total`/`complete` того же снимка, что и строки, и счётчик их принимает; догоняющий обход дозапрашивает укороченный блок; обход, закончившийся без `isLast`, сверяется с Rust (окно из 0 строк) и при потерянном поколении запускается заново один раз (R-230). Тесты: 5 новых в `stores/graph.test.ts` (3 падали до правки). Проверить в сборке: быстро ставить и снимать галочки групп веток в Branches, в том числе прокрутив граф вниз на 150–300 строк; после каждого тика число в `Graph & History (N)` равно числу строк (End в графе — последняя строка N-я).

- [ ] **#19 Stash в графе**
  - Сейчас отмеченный stash появляется в графе, но выглядит как обычный коммит.
  - [ ] Метка с именем stash-а на его строке, как метка ветки или тега, своим цветом.
  - [ ] Узел stash-а — **квадрат**, как в SmartGit, а не кольцо.
  > Итог: причина — граф не знал, что коммит — stash: меток stash-ей не было, узел рисовался общим кольцом, а цвет stash-а делили теги. Сделано: `refLabels` получает список stash-ей и ставит метку `stash@{N}` (тултип — сообщение) после меток веток и тегов; `RefCapsule` рисует её фиолетовым `--status-stash`; `GraphCanvas` рисует узел stash-а квадратом (`nodeSquare` в `graph-geometry`, тот же центр и ширина, что у кольца) с обводкой `--status-stash`. Теги получили свой токен `--c-tag`/`--status-tag` (жёлтый), чтобы метки различались (R-232); формат окна графа не менялся — stash-коммиты фронтенд знает из `stashes.entries`. Тесты: 2 новых в `lib/format.test.ts`, 2 в `lib/graph-geometry.test.ts` (падали до правки). Проверить в сборке: сделать stash, отметить его в Branches — строка stash-а с меткой `stash@{0}` и квадратным узлом; теги — жёлтые.

- [ ] **#21 Чередование фона строк графа**
  - [ ] Нечётные строки чуть темнее, чётные чуть светлее. Разница едва заметная, из токенов темы.
  - [ ] Выделение и наведение перекрывают чередование; линии графа рисуются поверх.
  - [ ] Корректно во всех темах, включая новые из #24.
  > Итог: не было реализовано. Сделано: токен `--c-row-stripe`/`--row-stripe` в каждой теме (белый полупрозрачный поверх фона панели — в тёмных 3 %, в светлых 60 %, поэтому «чётные светлее» верно везде), класс `striped` на строках коммитов и строках rebase (`striped()` в `graph-geometry`: вторая, четвёртая… строка списка, строка Working Tree — первая); в CSS полоса стоит до `:hover` и `.selected`, поэтому они её перекрывают; канвас с линиями лежит над строками, а заливку кольца узла `GraphCanvas` берёт из `nodeFill()` — панель, полоса, наведение, выделение, как у самой строки (для этого `CommitList` передаёт наведённую строку). Тесты: 3 новых в `lib/graph-geometry.test.ts` (написаны вместе с функцией, до сборки не падали — шаг «красный» не наблюдался). Проверить в сборке: во всех четырёх темах полосы едва заметны, наведение и выделение закрывают полосу целиком, внутри колец нет пятен чужого фона ни на полосатой, ни на наведённой, ни на выделенной строке.

- [ ] **#22 Состояния merge / rebase / detached HEAD**
  - Сейчас строка `Merge in progress — Resolve the conflicts and continue, or abort to go back.` с кнопками `Continue` и `Abort` висит на всю ширину окна под тулбаром.
  - [ ] Перенести баннер **внутрь панели Graph**, над списком коммитов, как в SmartGit (`The working tree is in merging-state.` с кнопкой `Abort`), кнопки `Continue` / `Abort` оставить.
  - [ ] Строка Working Tree в графе дополняется состоянием: `Working Tree (…), merging`.
  - [ ] В `Repositories` рядом с именем репозитория — метка `<merging>` цветом предупреждения.
  - [ ] Аналогично для всех длительных состояний: `<rebasing>`, `<cherry-picking>`, `<reverting>`, `<bisecting>`, `<applying patches>` (git am) — определить по файлам состояния в `.git`.
  - [ ] Предупреждение `Detached HEAD` — туда же, в панель Graph; в `Repositories` метка `<detached>`.
  - [ ] Для submodules — те же метки на их узлах.
  > Итог: причина — `StateBanner` рендерился в `App.svelte` над всей рабочей областью; строка Working Tree и дерево Repositories состояния не знали, `git am` не распознавался (его `rebase-apply` принимался за rebase). Сделано: бэкенд — `RepoState::ApplyingPatches` по `rebase-apply/applying`, состояние в `RepoOverview.state` и `Submodule.repoState` (привязки перегенерированы, `doc/04`, `doc/03` обновлены); Abort/Continue для `git am` — `git am --abort/--continue/--skip`, для bisect Abort = `git bisect reset`, Continue отказывает без запуска git. Фронтенд — баннер в `GraphPanel` над списком коммитов (под тулбаром — только если панель Graph скрыта), строка Working Tree из `workingTreeLabel` (`Working Tree (…), merging`), метки `<merging>`…`<applying patches>`/`<detached>` цветом `--status-modify` из `repoStateTag` у репозиториев и на узлах сабмодулей (на сабмодулях без `<detached>` — он у них норма, R-130; решение в R-233). Тесты: Rust — 2 в `git_engine/tests/repo_state.rs` (настоящий `git am` с конфликтом), 3 в `operations.rs`, 2 в `submodules.rs`, 2+1 проверка в `app_state/tests/repositories.rs`; TS — 9 в `lib/repo-state.test.ts` (падали до правки; Rust-тесты состояний сабмодуля и строки — до реализации не компилировались, прогон «красного» не делался). Проверить в сборке: merge с конфликтом — баннер `Merge in progress` с Continue/Abort над графом внутри панели, `, merging` в строке Working Tree, `<merging>` рядом с именем в Repositories; то же для rebase с конфликтом (`<rebasing>`), `git am` конфликтного патча (`<applying patches>`), checkout коммита (`<detached>` и баннер Detached HEAD в панели Graph); в сабмодуле начать merge с конфликтом — `<merging>` на его узле.

- [ ] **#47 Убрать автофильтрацию Branches** при клике по ветке или тегу в графе.
  > Итог: причина — клик по капсуле в `CommitList` вызывал `onref`, а `App.svelte` писал текст метки в фильтр панели Branches (так было задумано в T4.6 M4). Сделано: `onref` убран из `CommitList`, `GraphPanel` и `App.svelte`, капсула больше не кнопка — клик по ней выделяет коммит строки, как клик по любому месту строки; M4 T4.6 и F-105 поправлены. Тестов разметки в проекте нет, проверено `svelte-check`. Проверить в сборке: клик по метке ветки и по метке тега в графе — выделяется коммит, поле фильтра Branches остаётся пустым, дерево не сужается.

- [ ] **#49 Объединённая метка ветки и её upstream**
  - Сейчас на одном коммите две метки: `feature/14340_new_toolchain` и `origin/feature/14340_new_toolchain`.
  - [ ] Если локальная ветка и её upstream указывают на один коммит — одна метка вида `origin=feature/14340_new_toolchain`: блок `origin` и блок имени ветки — разные сочетающиеся цвета, `=` между ними.
  - [ ] Если разошлись — две отдельные метки, как сейчас.
  - [ ] Для нескольких remotes на том же коммите — все префиксы в одной метке.
  > Итог: причина — `refLabels` давал по метке на каждую ссылку. Сделано: `refLabels` (`$lib/format`, чистая функция) сливает локальную ветку с upstream-ом того же имени на том же коммите в метку `origin=feature/x` и добавляет в префикс одноимённые ветки других remote на этом коммите (`origin,fork=dev`, upstream первым); разошедшиеся — две метки, как раньше; upstream с другим именем не сливается (R-231). Метка вынесена в `RefCapsule.svelte`: блок remote — вторичный текст на `--surface-raised`, `=`, блок ветки — цветом ветки/HEAD; тултип — все полные имена. Поле `text` осталось полной строкой для усечения из #5. Тесты: 8 новых в `lib/format.test.ts` (5 падали до правки). Проверить в сборке: запушенная ветка — одна метка `origin=<ветка>` с двумя цветными блоками; после локального коммита — две метки; при втором remote с той же веткой на том же коммите — `origin,<remote2>=<ветка>`.

- [ ] **#37 Контекстное меню Working Tree в графе**
  - [ ] Commit
  - [ ] Stage
  - [ ] Unstage
  - [ ] Discard

- [ ] **#38 Контекстное меню коммита в графе**
  - [ ] Check Out (предупреждение о detached HEAD)
  - [ ] Merge
  - [ ] Cherry-Pick
  - [ ] Revert
  - [ ] Rebase
  - —— разделитель ——
  - [ ] Modify — с предупреждением, если запушен
  - [ ] Split — с предупреждением, если запушен
  - [ ] Squash — выключен, если запушен
  - [ ] Edit Message — с предупреждением, если запушен
  - [ ] Edit Author — с предупреждением, если запушен
  - [ ] Rebase Interactive From
  - —— разделитель ——
  - [ ] Add Branch
  - [ ] Add Tag (диалог из #6)
  - —— разделитель ——
  - [ ] Reset
  - [ ] Reset Advanced… (диалог: `mixed`, `soft`, `hard`, `keep`, `merge` с пояснением каждого режима; `hard` — с подтверждением)
  - —— разделитель ——
  - [ ] Push Up To
  - —— разделитель ——
  - [ ] Copy Message
  - [ ] Copy ID

- [ ] **#39 Контекстное меню ветки или тега в графе** (клик по метке)
  - [ ] Check Out
  - [ ] Merge
  - [ ] Cherry-Pick
  - [ ] Revert
  - [ ] Rebase
  - —— разделитель ——
  - [ ] Modify — с предупреждением, если запушен
  - [ ] Split — с предупреждением, если запушен
  - [ ] Squash — выключен, если запушен
  - [ ] Edit Message — с предупреждением, если запушен
  - [ ] Edit Author — с предупреждением, если запушен
  - [ ] Rebase Interactive From
  - —— разделитель ——
  - [ ] Add Branch
  - [ ] Add Tag
  - —— разделитель ——
  - [ ] Reset
  - [ ] Reset Advanced…
  - —— разделитель ——
  - [ ] Push
  - [ ] Push To… (диалог из #28)
  - —— разделитель ——
  - [ ] Delete
  - [ ] Rename — неактивна, если ветка/тег запушены
  - —— разделитель ——
  - [ ] Copy Name
  - [ ] Copy Message
  - [ ] Copy ID

---

### C. Branches

- [ ] **#11 Папки веток и тегов**
  - [ ] Внутри каждого уровня **сначала папки** (префиксы вида `testing/`), затем ветки.
  - [ ] Сейчас синим треугольником помечены ветки — убрать этот маркер с веток. Папки помечать иконкой папки (не синим треугольником).
  - [ ] Теги тоже группируются по папкам: в `dtv_device` много тегов `release/…` — они должны оказаться в папке `release/`.
  - [ ] **Схлопывание цепочек:** папка, в которой нет ничего, кроме одной подпапки, показывается одним узлом `testing/network/`. Если в папке есть и подпапка, и ветки — обычная вложенность.
  - [ ] Разделитель групп тегов — из настройки `Tag-Grouping` (#42), по умолчанию `/`.

> Итог: причина — папки строились попутно с ветками в порядке имён (поэтому `testing/` стояла между ветками), теги шли плоским списком, `▶`/`▷` рисовались перед ветками. Сделано: `buildRefTree` (`lib/ref-nodes.ts`) строит префиксное дерево для Local, каждого remote и Tags и выводит уровень за уровнем — сначала папки со значком папки, затем ссылки; папка с единственной подпапкой — один узел `testing/network`; треугольники убраны, текущая ветка выделена цветом и жирным; у листа тега в `label` только последний сегмент, сам тег — в `node.tag`. Разделитель тегов — `cogit.tagGroupSeparator` из конфига репозитория (контракт с #42: пишет диалог, читаем мы), по умолчанию `/`, пустая строка — без папок; бэкенд отдаёт его в `RepoSummary.tagGroupSeparator` и перечитывает при каждом открытии/обновлении (TDD: `git_engine/tests/ref_meta.rs`, `app_state/tests/open.rs`). Пока в фильтре есть текст, папки раскрыты. Тесты: `ref-nodes.test.ts` (папки первыми, цепочки, теги в папках, свой разделитель, пустой разделитель, уникальные id). Решение — R-240. Проверить в сборке не удалось (запуск сборки запрещён системой разрешений); проверить руками: ветки `testing/network/a`, `testing/network/b`, `testing/x`, `alpha`, теги `release/1.0`, `release/1.1` — порядок и значки; `git config cogit.tagGroupSeparator -` + Refresh — теги `release-1.0` в папке `release`.

- [ ] **#20 Сортировка**
  - [ ] Натуральная сортировка по умолчанию: числовые куски сравниваются как числа (`v1.0.2` раньше `v1.0.10`), остальное — посимвольно. На фронте: `Intl.Collator(undefined, { numeric: true, sensitivity: 'base' })` с посимвольным тай-брейком для детерминизма.
  - [ ] Кнопка-переключатель рядом с фильтром в строке заголовка `Branches`: вкл — натуральная, выкл — прежняя посимвольная.
  - [ ] Кнопка сортировки по времени: по возрастанию / по убыванию даты коммита вершины (для аннотированного тега — даты тега).
  - [ ] Выбор сохраняется между запусками.

> Итог: причина — имена сравнивались посимвольно, дат вершин во фронтенде не было. Сделано: `lib/ref-sort.ts` — `Intl.Collator(undefined, { numeric: true, sensitivity: 'base' })` с посимвольным тай-брейком, режимы дат «выкл / новые сверху / старые сверху»; две кнопки рядом с фильтром Branches (`RefSortButtons.svelte`): натуральная ↔ посимвольная и дата (цикл по клику). Даты — новая команда `ref_dates` (дата аннотированного тега — тега, иначе committer-дата коммита; TDD в `git_engine/tests/ref_meta.rs`), читается только при включённой сортировке по дате и при каждом перечитывании ссылок. По дате сортируются ссылки внутри уровня, папки — по имени. Выбор — в `localStorage` (`cogit.ref-sort.v1`), общий для всех репозиториев. Тесты: `ref-sort.test.ts`, `ref-nodes.test.ts` (sorting), `refs.test.ts` (сохранение, даты). Решение — R-242. Проверить руками: теги `v1.0.2`/`v1.0.10` меняются местами при выключении натуральной сортировки; по дате сверху — самая свежая вершина; после перезапуска выбор тот же.

- [ ] **#25 Ветки, взятые в worktree**
  - По образцу SmartGit: *«Branches attached to worktrees are now shown in Branches and the Graph together with their current worktree state.»*
  - [ ] В `Branches` у ветки, взятой в worktree, — иконка worktree (та же, что в панели Worktrees) и tooltip с путём.
  - [ ] Состояние worktree рядом: чистый / есть изменения / `missing`.
  - [ ] В графе метка такой ветки тоже помечена.
  > Итог (часть Graph): не было реализовано. Сделано: `refLabels` получает список worktree (`worktrees.entries`, уже загружаемый стором) и помечает метку локальной ветки, выписанной в другом worktree (не текущем), полем `worktree { path, state: clean | modified | missing }` и строкой тултипа `Checked out in worktree <путь> (<состояние>)`; `RefCapsule` ставит перед именем иконку worktree из `KindIcon` (та же, что в панели Worktrees) цветом текста метки. Тесты: 2 новых в `lib/format.test.ts` (один падал до правки). Часть Branches — у агента «branches». Проверить в сборке: `git worktree add ../wt feature` — у метки `feature` в графе основного репозитория иконка worktree, тултип с путём и `(clean)`; правка файла в `../wt` → `(modified)`; удалить папку `../wt` → `(missing)`.

> Итог (часть Branches): причина — Branches не читал список worktree, а у записи без папки бэкенд терял ветку. Сделано: `worktreeMarks` (`lib/worktree-list.ts`) из тех же данных `worktrees`, что использует граф; у локальной ветки, которую держит другой worktree (не текущий), — значок worktree с путём и состоянием в tooltip и метка `clean` / `changes` / `missing`. Бэкенд: у `missing` ветка и HEAD читаются из `.git/worktrees/<id>/HEAD` (TDD: `git_engine/tests/worktrees.rs`, R-241) — поле добавочное, источник данных для графа не менялся. Тесты: `worktree-list.test.ts`, `ref-nodes.test.ts` (#25). Метку в графе делает агент graph. Проверить руками: добавить worktree на ветку — значок и `clean`; изменить файл в нём и обновить — `changes`; удалить его папку — `missing`.

- [ ] **#33 Контекстное меню ветки в Branches**
  - [ ] Check Out
  - —— разделитель ——
  - [ ] Reveal Commit — отмечает ветку галочкой и прокручивает граф к её коммиту, выделяя его
  - [ ] Compare with HEAD — в `Files` все файлы, различающиеся между коммитом ветки и HEAD
  - [ ] Compare with Selected Commit — в графе выделены оба коммита, в `Files` diff между ними; неактивно, если в графе ничего не выбрано или выбран тот же коммит
  - —— разделитель ——
  - [ ] Merge
  - [ ] Rebase
  - —— разделитель ——
  - [ ] Push
  - [ ] Push To…
  - —— разделитель ——
  - [ ] Reset
  - [ ] Reset Advanced…
  - —— разделитель ——
  - [ ] Delete
  - —— разделитель ——
  - [ ] Copy (имя ветки)
  - —— разделитель ——
  - [ ] Toggle — вкл/выкл галочку

- [ ] **#34 Контекстное меню тега в Branches**
  - [ ] Check Out
  - —— разделитель ——
  - [ ] Reveal Commit
  - [ ] Compare with HEAD
  - [ ] Compare with Selected Commit
  - —— разделитель ——
  - [ ] Merge
  - —— разделитель ——
  - [ ] Push
  - [ ] Push To…
  - —— разделитель ——
  - [ ] Reset
  - [ ] Reset Advanced…
  - —— разделитель ——
  - [ ] Delete
  - —— разделитель ——
  - [ ] Copy (имя тега)
  - [ ] Copy Message — сообщение аннотации; у лёгкого тега неактивно
  - —— разделитель ——
  - [ ] Toggle

- [ ] **#35 Контекстное меню stash-а в Branches**
  - [ ] Apply Stash
  - —— разделитель ——
  - [ ] Reveal Commit
  - [ ] Compare with HEAD
  - [ ] Compare with Selected Commit
  - —— разделитель ——
  - [ ] Rename Stash — в git нет прямого переименования: `git stash drop` + `git stash store -m <новое> <sha>`; учти, что store кладёт запись наверх и меняет её позицию — предупредить или сохранить порядок
  - —— разделитель ——
  - [ ] Drop Stash — с подтверждением
  - —— разделитель ——
  - [ ] Copy Message
  - —— разделитель ——
  - [ ] Toggle

---

### D. Repositories

- [ ] **#50 Повторный клик по открытому репозиторию перезагружает его с нуля**
  - [ ] Клик по уже открытому и активному репозиторию — ничего не перезагружает.
  - [ ] Клик по открытому, но неактивному (после работы в submodule или worktree) — переключение из кэша состояния, без полной переинициализации.

> Итог: причина — `onselect` строки Repositories всегда вызывал `activate`: очистка панелей вместе с деревом submodules, фаза `opening`, повторное открытие в бэкенде, перезагрузка дерева submodules, проверка здоровья, список репозиториев. Сделано: чистая функция `repoClick` (`lib/repo-click.ts`) — `stay` для показанного (и уже открывающегося), `return` для владельца показанного submodule/worktree, `open` для остальных; перед переходом в submodule/worktree `repository.keep()` запоминает сводку владельца, `repository.comeBack()` показывает её сразу без фазы `opening` и тихо перечитывает (ветку могли сдвинуть в worktree), дерево submodules, проверка здоровья и список не трогаются; граф владельца перестраивается (хранилище графа держит один репозиторий). Тесты: `repo-click.test.ts`, `repository.test.ts` (comeBack: без `opening`, поздний ответ не отменяет новое переключение, без кэша — обычное открытие). Решение — R-244. Проверить руками: повторный клик по активному репозиторию — ни одного IPC-вызова `open_repository` (журнал профиля), панели не мигают; открыть submodule, кликнуть его репозиторий — дерево submodules остаётся раскрытым, «Opening repository…» не появляется.

- [ ] **#36 Контекстное меню репозитория (в т.ч. submodule)**
  - [ ] Open Repository — неактивна, если уже открыт
  - [ ] Open in Explorer — открывает саму папку. **Сейчас не работает на Windows 11 — починить**
  - [ ] Reveal in Explorer — открывает родительскую папку с выделенным каталогом репозитория
  - [ ] Open in Terminal
  - [ ] Open in PowerShell — только Windows; на Linux и macOS пункт отсутствует (платформенная абстракция закладывается сразу)
  - [ ] Open in Git Shell — только Windows, Git Bash из поставки Git for Windows; путь определять автоматически
  - [ ] Close Repository — неактивна, если закрыт
  - —— разделитель ——
  - [ ] Pull
  - [ ] Push
  - —— разделитель ——
  - [ ] Move To ▸ — список групп панели Repositories
  - [ ] Pin — закрепить вверху группы
  - [ ] Rename — меняет только отображаемое имя в списке; папку и репозиторий не трогает
  - [ ] Remove — убирает из списка, с диска ничего не удаляет
  - [ ] Для узлов submodule пункты `Move To`, `Pin`, `Rename`, `Remove` неактивны с пояснением (submodule — часть родителя, а не запись списка). Если считаешь иначе — обоснуй в итоге.
  - [ ] На Linux подписи `Explorer` заменяются на `File Manager`.

---

### E. Files

- [ ] **#3 Кнопки-фильтры `If selected…` при выборе коммита из истории**
  - Сейчас: в Working Tree все кнопки доступны — это верно. При выборе коммита из истории кнопки анимируются как нажимаемые, но ничего не делают: выделение не снимается, список не меняется.
  - [ ] В коммите из истории активны только применимые, как в SmartGit: показ неизменённых файлов (`Unchanged`); показ удалённых исходных файлов распознанных переименований (сейчас это кнопка `Missing/Removed` — в контексте коммита она отвечает именно за это); переключение дерево/плоский список (`Show Directories` / `Show Flat List`).
  - [ ] Остальные (`Untracked`, `Ignored`, `Modified`, `Skipped`, разделение Index/Working Tree) — визуально неактивны, без анимации наведения и нажатия, с tooltip о причине.
  - [ ] При возврате к Working Tree восстанавливается прежнее состояние всех кнопок.

- [ ] **#7 Возврат к информации о коммите**
  - [ ] Убрать «отжатие» файла повторным кликом — оно конфликтует с двойным кликом, открывающим diff в отдельном окне (#8).
  - [ ] Новое поведение: пользователь выбрал коммит → в `Diff` информация о коммите; выбрал файл → в `Diff` diff файла (так уже работает); **повторно кликнул по тому же, уже выбранному коммиту в графе** → в `Diff` снова информация о коммите, а выделение файла в `Files` снимается.

- [ ] **#48 Не работает кнопка `Search in file contents`**
  - Соседняя кнопка регулярных выражений работает.
  - [ ] Найти причину: не вызывается команда, не приходит результат или результат не отображается. Починить.

- [ ] **#40 Контекстное меню файла в Files (Working Tree и Index)**
  - [ ] Open File — во внутреннем редакторе. Если внутреннего редактора в проекте нет — укажи в итоге, что сделано вместо него
  - [ ] Reveal in Explorer
  - [ ] Show Changes — то же, что двойной клик
  - [ ] Log
  - [ ] Blame (окно из #10)
  - [ ] Investigate (см. #15)
  - —— разделитель ——
  - [ ] Commit
  - [ ] Stash Selection
  - —— разделитель ——
  - [ ] Stage
  - [ ] Unstage
  - [ ] Index Editor — трёхпанельное редактирование HEAD / Index / Working Tree
  - [ ] Move or Rename
  - —— разделитель ——
  - [ ] Resolve ▸ `Take Theirs`, `Take Ours` — активно только для файлов в конфликте
  - —— разделитель ——
  - [ ] Ignore
  - [ ] Discard — с подтверждением
  - [ ] Remove — диалог по образцу SmartGit:
    - [ ] Заголовок окна `Remove`, заголовок диалога `Remove files from the repository`, пояснение `Select the files you want to remove from the repository or working tree (stopped from tracking).`
    - [ ] Таблица выбранных файлов с чекбоксом у каждого и колонками `Name`, `Directory`; все отмечены по умолчанию.
    - [ ] Чекбокс `Delete local files`, **по умолчанию выключен**: выключен — файлы перестают отслеживаться и остаются на диске (`git rm --cached`); включён — удаляются и с диска (`git rm`).
    - [ ] Кнопки `Remove` и `Cancel`.
  - [ ] Delete — удалить файл с диска (в корзину), с подтверждением
  - —— разделитель ——
  - [ ] Copy Name
  - [ ] Copy Path
  - [ ] Copy Relative Path
  - Только для файлов Working Tree, дополнительно:
  - [ ] Toggle 'Assume Unchanged' (`git update-index --[no-]assume-unchanged`)
  - [ ] Toggle 'Skip Worktree' (`git update-index --[no-]skip-worktree`)

- [ ] **#41 Контекстное меню файла в коммите из истории**
  - [ ] Show Changes
  - [ ] Compare with Working Tree
  - [ ] Open File — версия из этого коммита, только чтение
  - [ ] Reveal in Explorer — неактивен, если файла больше нет в рабочей копии
  - —— разделитель ——
  - [ ] Save As… — сохранить версию файла из коммита
  - [ ] Log
  - [ ] Blame — на этом коммите
  - [ ] Investigate
  - —— разделитель ——
  - [ ] Cherry-Pick — изменения этого файла из коммита
  - [ ] Revert — изменения этого файла из коммита
  - —— разделитель ——
  - [ ] Copy Path
  - [ ] Copy Relative Path
  - [ ] Copy Name

---

### F. Diff

- [ ] **#8 Двойной клик по файлу открывает чёрное окно и ломает приложение — критично**
  - Сейчас: двойной клик по изменённому файлу открывает второе окно Cogit, полностью чёрное, без diff; закрыть его нельзя; главное окно после этого продолжает анимировать интерфейс, но данные не обновляются (даже выбор коммита в графе ничего не меняет). Выйти можно только снятием задачи.
  - [ ] Найти, почему окно не монтирует содержимое (вероятно, то же, что с ранее белым окном diff: окно ждёт события или состояния, которое уже прошло).
  - [ ] Найти, почему ломается главное окно: вероятные причины — бэкенд начинает отправлять события только последнему созданному окну; инициализация второго окна держит общую блокировку состояния; общий стор перезаписывается вторым окном.
  - [ ] Окно diff: полноценно отображает diff файла, растягивает содержимое на всю клиентскую область, закрывается крестиком / `Esc` / `Ctrl+W`, не имеет меню главного окна.
  - [ ] После открытия и закрытия окна diff главное окно работает без деградации. Проверить 10 открытий-закрытий подряд.

> Итог: **первопричина — взаимная блокировка WebView2, одна на оба симптома.** `open_compare_window` и `open_merge_window` были синхронными командами, а синхронная команда Tauri выполняется внутри обработчика IPC-запроса WebView2 на главном потоке; `WebviewWindowBuilder::build()` там создаёт окно (видно сразу — это тёмный фон `#0f1115`, прежде белый: «белое окно» из R-111 — тот же баг) и ждёт контроллер WebView2 во вложенном цикле сообщений, а WebView2 не доставляет завершение, пока не вернулся обработчик, в котором стоит вызов (wry#583, прямо описано в документации `WebviewWindowBuilder::new`). Отсюда: окно без содержимого; крестик не работает (цикл событий `tao` занят); главное окно рисует анимации (рендерер — отдельный процесс), но ни один новый IPC-запрос и ни одно событие бэкенда не доходят — главный поток навсегда в `build()`. Сделано: команды окон (`open_compare_window`, `open_merge_window`, `close_this_window`, новая `open_blame_window`) — `async`, `build()` в `spawn_blocking`. Попутно найдено и исправлено то, что влияло на главное окно и после открытия окна: закрытие **любого** окна запускало сторожа выхода приложения (отмена чтений главного окна, выход через 2 с без ответа) — теперь только для `main`; `window-state` запоминал каждое дочернее окно и отдавал его геометрию следующему — теперь только `main`; дочернее окно создаётся скрытым, меню снимается до показа (пустое меню рисовало пустую полосу), повторяет аргументы браузера главного окна (иначе WebView2 отказывает во втором окружении — так было бы в конфиге бенчмарка); `Esc` больше не закрывает окно дважды и отдаёт его сначала панели поиска внутри. Тесты: Rust — `a_window_is_never_opened_or_closed_on_the_main_thread` (разбирает все файлы команд, падал на трёх командах до правки), `a_synchronous_window_command_would_be_caught`, `every_kind_of_window_may_call_commands` (вид окна без `<вид>-*` в capabilities), `a_window_opened_here_is_never_the_main_one`, `a_child_repeats_the_main_windows_browser_arguments`; vitest — `whenCloses`. Подробно — R-201. **В запущенной сборке не проверено**: запуск с портом отладки для управления приложением среда не разрешила. Проверить: двойной клик по изменённому файлу → окно с diff на всю площадь, без полосы меню; закрыть крестиком, `Esc`, `Ctrl+W`; с открытым поиском (`Ctrl+F`) первый `Esc` закрывает поиск, второй — окно; 10 раз открыть/закрыть, между разами выбирать коммиты в графе — детали и Files обновляются; окно слияния — то же.

- [ ] **#9 Маркеры `+` и `−` в строках diff**
  - Сейчас `+`/`−` стоят вплотную к коду. В SmartGit их нет вовсе, но концепция маркеров нравится.
  - [ ] Вынести маркер в отдельную узкую колонку между номером строки и кодом, отделённую от кода отступом в пару пробелов. Переносить левее номера строки не обязательно.
  - [ ] Маркер не выделяется и не копируется вместе с кодом (`user-select: none`).
  - [ ] Цвет маркера согласован с цветом строки.

- [ ] **#10 Blame**
  - [ ] **Сначала баг:** сейчас включённый blame занимает всю панель `Diff` и из него нельзя выйти ни выбором другого файла, ни закрытием. Выйти можно только сменой коммита в графе. Проверь и исправь причину, даже если перенос в отдельное окно её скроет.
  - [ ] Затем перенести blame в **отдельное окно**, как в SmartGit:
    - [ ] Заголовок окна: `<имя файла> - Blame of <путь>@<короткий хеш>`.
    - [ ] Сверху: выпадающий список `View Commit` — версия файла из какого коммита показана (дата, хеш, автор, тема).
    - [ ] Строка `Highlight: Changes Since` + выпадающий список коммита — подсвечиваются строки, изменённые после выбранного коммита.
    - [ ] Основная область: номера строк, код, у каждого блока строк — хеш, автор, дата (как сейчас в Cogit).
    - [ ] Снизу панель `History of current line`: коммиты, менявшие текущую строку, — хеш, автор, возраст, содержимое строки в той версии.
    - [ ] Своё минимальное меню окна, не меню главного окна.
    - [ ] Требования к окнам из общих правил.

> Итог: **первопричина бага:** в `DiffPanel` ветка `{:else if blame.path}` стояла выше ветки diff, а стор `blame` сбрасывался только эффектом на `commit.oid` в `App.svelte`; выбор другого файла (и повторный клик по тому же) менял лишь стор `diff`, `blame.path` оставался — панель продолжала показывать blame; кнопки закрытия не было. Сначала исправлено на месте (сброс blame при смене `diff.path`, крестик — коммит 39f288c), затем blame из панели убран совсем: состояния blame в главном окне больше нет, залипать нечему. **Окно Blame:** заголовок `<файл> - Blame of <путь>@<короткий хеш>` ставит Rust (`open_blame_window` сам разрешает `HEAD` и любую ревизию); `View Commit` — версии файла (дата · хеш · автор · тема), плюс открытая ревизия, если она файл не меняла; `Highlight: Changes Since` — подсветка строк, чей коммит в списке версий выше выбранного; основная область — номер, код, у первой строки блока хеш/автор/дата; снизу `History of current line` — хеш, автор, возраст (`45m`/`5h`/`140d`/`3y`), строка в той версии, двойной клик открывает ту версию; текущая строка — клик или `↑↓ PgUp PgDn Home End`, история запрашивается после паузы 150 мс. Меню окна: `File ▸ Close` (`Ctrl+W`), `View ▸ Refresh` (`F5`), `View ▸ History of Current Line`; id пунктов `child:<метка>:<действие>` не попадают в команды главного окна. История строки — `git log -L N,N:<путь> <rev>` (как Investigate, R-104), а не blame по родителям — решение и цена в R-202. Единая точка входа — `openBlame(repo, path, rev)` в `frontend/src/lib/blame-window.ts`: кнопка Blame в Diff, команда палитры и пункт `Blame` меню файла (`blameOne` в `App.svelte`) идут через неё. Решения, принятые самостоятельно: история строки через `git log -L`; заголовок не меняется при смене `View Commit` (`setTitle` из вебвью ронял приложение, R-86); «Changes Since» по порядку списка версий, а не по предкам; версии до переименования в `View Commit` не попадают. Тесты: Rust — `git_engine/tests/line_history.rs` (13: порядок, текст и номер строки в каждой версии, переименование, старт с ревизии, лимит, ошибки; `file_revisions`), модуль `blame_window` (заголовок, URL с любыми символами пути, меню), `child_window` (id меню, DOM-событие); vitest — `lib/blame-window.test.ts` (18), `stores/blame-window.test.ts` (7), `onMenuAction`. **В запущенной сборке не проверено** (см. #8). Проверить: Blame из кнопки Diff, из палитры и из меню файла открывает окно с верным заголовком; блоки с хешем/автором/датой; выбрать в `Highlight` старый коммит — подсветились новые строки; стрелками сменить строку — внизу её история; двойной клик по строке истории — версия файла в `View Commit` сменилась; `F5` перечитывает, а не перезагружает страницу; в окне нет меню главного окна; крестик, `Esc`, `Ctrl+W`, `File ▸ Close` закрывают окно, главное продолжает работать.

- [ ] **#12 Поиск по diff: нет совпадений**
  - [ ] Если совпадений нет — поле поиска слегка подсвечивается красным.
  - [ ] Подсветка снимается при полной очистке поля или при появлении первого совпадения.

- [ ] **#13 Кнопки «следующее / предыдущее изменение»**
  - [ ] На первом изменении кнопка «предыдущее» неактивна, на последнем — «следующее». Если изменение одно — обе неактивны.

- [ ] **#14 Tooltip кнопки `Unified`**
  - Сейчас: `Remembered between runs` — не описывает кнопку.
  - [ ] Tooltip описывает действие: переключение между side-by-side и unified видом. Запоминание можно упомянуть второй строкой.

- [ ] **#15 Investigate — отдельная функция, как в SmartGit**
  - Сейчас кнопка ничего не делает. Blame в Cogit уже есть и по виду совпадает со SmartGit (#10). Investigate — **другая функция**: поиск происхождения строк через историю с переходами через перемещения строк и переименования файлов. В SmartGit это встроенный DeepGit от Syntevo. Перед реализацией изучи документацию DeepGit и в итоге опиши, где реализация от неё отличается.
  - Окно:
    - [ ] Отдельное окно, заголовок `<файл> [<репозиторий>] - Investigate`, своё меню: File, Edit, View, Go To, Window, Help.
    - [ ] Тулбар слева: `Back` (с выпадающей историей переходов) и `Forward`.
    - [ ] Тулбар справа — переключатель перспектив: `Log`, `Diff`, `Blame`, `Blame+Origins`, `Origins`.
  - Верхняя панель `Navigation`:
    - [ ] Узел-заголовок с именем исследуемого файла, под ним лог коммитов, менявших файл: короткий хеш, граф, тема, автор (коротко), дата.
    - [ ] Файлы, к которым пользователь перешёл дальше (после переименований и переходов к источнику строк), добавляются в тот же лог своими узлами-заголовками.
    - [ ] Выбор коммита меняет нижнюю панель по перспективе: `Blame` — содержимое файла в этой версии с blame; `Diff` — изменения файла в этом коммите; `Log` — сводка всех изменённых файлов коммита.
  - Нижняя панель, перспектива `Blame`:
    - [ ] Шапка `Blame for`: аватар автора, `File` (путь), `Commit` (хеш, автор, дата), `Msg` (тема); стрелки перехода к предыдущему/следующему изменению.
    - [ ] Колонки строк: короткий хеш, маркер типа изменения строки (в SmartGit видны `+`, `M+`, `M~` — точную семантику возьми из документации DeepGit), автор, возраст (`140d`), номер строки, код.
  - Поиск происхождения — главное отличие от Blame:
    - [ ] Выбор строки запускает в фоне поиск её происхождения.
    - [ ] Учитываются перемещения строк внутри файла и между файлами, переименования файлов и окружение строки, поэтому кандидаты обычно блоки, а не одиночные строки.
    - [ ] Результат — представление `Origin Candidates` в перспективе `Blame+Origins`; лучший кандидат выбран и подсвечен.
    - [ ] Карточка у выбранной строки: `Appeared here — Lines first appeared at this position`, оценка кандидата (`single origin, high likelihood` и т.п.), кнопка `Go Deeper`, крестик.
    - [ ] `Go Deeper` — переход к версии до коммита-кандидата (к его родителю) на исходном пути и строке и повторный поиск оттуда; итеративно, каждый шаг попадает в историю `Back`/`Forward`.
    - [ ] Вертикальная полоса у правого края `Blame (go deeper)` с индикатором состояния поиска.
    - [ ] Основа реализации: `git log --follow` для лога файла; `git blame --porcelain -M -C -C` — исходные коммит, файл и строка для каждой строки с учётом перемещений и копирования между файлами; повторный blame на `<коммит>^` для `Go Deeper`. Если найдёшь подход точнее — обоснуй в итоге.
  - Точки входа:
    - [ ] Кнопка `Investigate` в тулбаре Diff.
    - [ ] Пункты `Investigate` в контекстных меню файлов (#40, #41).
    - [ ] Шорткат `Shift+Ctrl+Alt+L`, как в SmartGit.
    - [ ] Если в diff выделена строка — Investigate открывается сразу с ней.
  - [ ] Требования к окнам из общих правил.

- [ ] **#16 Заголовки ханков `@@ … @@`**
  - Сейчас: строки вида `@@ -33,6 +37,17 @@ struct ElrsChannel {` и `@@ -0,0 +1,220 @@` у нового файла. Заголовок нарисован только в левой половине side-by-side, справа на его месте пустая строка без номера.
  - [ ] Заменить на читаемый разделитель по образцу SmartGit: `▲ N lines hidden · show ▼` с раскрытием вверх и вниз.
  - [ ] Контекст функции из заголовка ханка (`struct ElrsChannel`) показывать приглушённым текстом в разделителе.
  - [ ] Разделитель рисуется на всю ширину, в обеих половинах.
  - [ ] Не сворачивать промежутки в 1–2 строки — показывать их целиком.
  - [ ] У нового и удалённого файла заголовок не нужен совсем.

- [ ] **#17 Индикатор окончаний строк**
  - [ ] Заглавными: `LF → LF`, `CRLF → LF`.
  - [ ] У нового файла сейчас `none → lf` — показывать просто `LF`; у удалённого — окончание старой версии.
  - [ ] Tooltip: `Line endings: old → new`.

- [ ] **#18 Тулбар Diff не отделён от содержимого**
  - [ ] Визуальное отделение: фон тулбара из токенов поверхности и/или граница снизу, отступы.

---

### G. Общая вёрстка, состояния, темы

- [ ] **#4 `Opening Repository` семь раз на экране**
  - При первом открытии репозитория статус выводится во всех панелях одновременно.
  - [ ] Статус — только в футере. Панели во время загрузки показывают нейтральное состояние без текста статуса.

> Итог: причина — «Opening repository…» печатали сразу семь мест: оболочка `Panel` каждой панели, Graph, Files, сведения о коммите, список Repositories и футер дважды (слот репозитория «Opening…» и строка активности). Сделано: одно правило в `lib/repo-phase.ts` — `idleMessage` на время открытия ничего не возвращает (панели пусты, у `Panel` — `aria-busy`), «No repository open.» на стартовом экране осталось; слот репозитория в футере называет открываемую папку (`footerRepository`), статус — только строка активности футера. Тесты: `repo-phase.test.ts`. Решение — R-245. Проверить руками: запустить с большим репозиторием в сессии — до конца открытия текст статуса только в футере справа, слева в футере — имя папки.

- [ ] **#5 Адаптивное усечение текста во всех панелях**
  - **Это отменяет прежнее требование** обрезать префикс пути submodule слева (`…/cmake-conan`). Теперь: в списках — только усечение справа.
  - [ ] **Repositories:** путь submodule не обрезается слева — сейчас `import/libjam` превращается в `...rt/libjam`, так нельзя. Порядок усечения: сначала вторичный текст (ветка, тема коммита), затем имя — справа. При сильном сжатии тема коммита сейчас наезжает на имя submodule — наложение исключить.
  - [ ] **Branches:** у `origin` справа выводится URL; при сужении сейчас сжимается `origin`, а не URL. Сначала усекается URL, имя remote — последним.
  - [ ] **Files:** сначала усекается относительный путь, имя файла — последним.
  - [ ] **Worktrees:** сначала путь, затем имя.
  - [ ] **Graph:** сейчас при сужении можно полностью спрятать текст коммита и дату, а имя автора и иконка остаются. Решение — как в SmartGit, **без адаптивного скрытия колонок**, просто минимальная ширина панели:
    - [ ] минимальная ширина панели Graph = одна колонка графа + ~35 символов темы коммита; уже этого панель не сужается ни сплиттером, ни размером окна;
    - [ ] граф вытесняет текст как обычно: при многих полосах тема начинается правее, и видимых символов становится меньше — это нормально;
    - [ ] всё, что не помещается в ширину панели, просто обрезается её правым краем (тема — с многоточием); колонки автора, аватара и даты не прячутся по отдельности и не переставляются;
    - [ ] значение минимальной ширины — одна константа в одном месте.
  - [ ] Метки веток в графе усекаются **посередине**, как в SmartGit (`feature/143...ew_toolchain`, `origin/feature/...net_control_opt`): сохраняются начало и различающийся конец.
  - [ ] Одно общее правило/утилита усечения с приоритетами на всё приложение, а не решение в каждой панели отдельно.

> Итог: причина — каждая панель усекала по-своему (flex-элементы сжимались поровну, путь submodule — `direction: rtl` слева, лист пути выходил за свою flex-коробку и наезжал на тему; тема графа сжималась до нуля). Сделано одно правило: в `app.css` классы `.shrink-first` (вторичный текст: URL, путь, тема, ветка; вес сжатия 100000) и `.shrink-last` (имя) рядом с `.truncate`; `lib/truncate.ts` — `truncateMiddle` и `refLabelText` (сюда же переехали усечение путей и `fitPath` из `path-truncate.ts`). Применено: Repositories (путь submodule — одна строка `папка/имя`, усекается справа, наложения нет; ветка и тема — первыми), Branches (URL и `= origin`/↑↓ первыми, имя последним), Files (путь и «← старое имя» первыми), Worktrees (ветка/«detached at» первыми — отдельного пути в строке нет, он в tooltip), Graph — минимальная ширина панели `GRAPH_MIN_SUBJECT_CHARS = 35` в `lib/graph-panel.ts` (единственное место): одна колонка графа + 35 символов; её держат сплиттеры (`floorFraction`/`capFraction`) и окно (min-width правой области, левая колонка уступает), тема в строке не уже 35 символов, остальное обрезает правый край панели; метки веток в графе усекаются посередине при выводе (`feature/143…ew_toolchain`, длина `REF_LABEL_MAX = 30`). Требование обрезать путь submodule слева отменено — R-243 (R-124 помечен отменённым). Тесты: `truncate.test.ts`, `graph-panel.test.ts`, `perspectives.test.ts` (floorFraction). Проверить руками: сузить левую колонку — в Repositories сначала исчезают ветка/тема, путь `import/libjam` режется справа и не наезжает; в Branches у `origin` сначала уходит URL; в Files — путь; сплиттер Graph и окно не сужают граф уже ~35 символов темы; длинная метка ветки в графе — с многоточием посередине, полный текст в tooltip.

- [ ] **#24 Две промежуточные темы**
  - Светлая слишком светлая, тёмная слишком тёмная.
  - [ ] Добавить две темы между ними (например, «светло-серая» и «тёмно-серая»), итого четыре.
  - [ ] Все цвета через токены; чередование строк (#21), граф, метки, подсветка diff корректны во всех четырёх.
  > Итог: тем было две (`dark`, `light`). Сделано: `THEMES` в `$lib/settings` — Light, Light grey, Dark grey, Dark, один список для типа, проверки файла настроек и выбора в Preferences (ключевые слова поиска `grey`/`gray`); в `app.css` блоки `lightGrey` (поверх светлой: серые поверхности и границы, акценты на шаг темнее для контраста ≥ 4.5:1) и `darkGrey` (поверх тёмной: серые поверхности, текст, красный и фиолетовый светлее); полоса строк графа `--c-row-stripe`, цвет тегов `--c-tag` заданы во всех темах. Проверка токенов: литералы в компонентах — только запасные значения `var(--x, …)`; тень меню тулбара — литерал в `Toolbar.svelte`, оставлен владельцу тулбара (R-234). Граф, метки, подсветка синтаксиса и diff читают токены, поэтому перекрашиваются вместе с темой. Тесты: 3 новых в `lib/settings.test.ts` (падали до правки), `design-tokens.test.ts` зелёный. Проверить в сборке: в Preferences выбрать по очереди все четыре темы — окно, граф (линии, кольца, квадрат stash, полосы), метки веток/тегов/stash/`origin=…`, diff с добавлениями и удалениями, баннер состояния и метки Repositories читаются в каждой.

---

### H. Repository Settings

- [ ] **#42 Диалог настроек репозитория**
  - Референс SmartGit: `Repository → Settings…`, диалог `Edit the repository settings` с пояснением «Here you can edit the Git settings for the repository. The default settings can be found in the preferences.» и вкладками `User`, `Fetch and Pull`, `Push`, `Signing`, `Encoding`, `Tag-Grouping`.
  - [ ] Пункт меню `Repository → Settings…`, активен при открытом репозитории.
  - [ ] Вкладка `User`: `user.name`, `user.email` на уровне репозитория (пусто — наследуется глобальное значение, показать какое).
  - [ ] Вкладка `Fetch and Pull`: переключатель `Merge fetched remote changes` / `Rebase local branch onto fetched changes` (`pull.rebase`); чекбоксы `Prune obsolete remote tracked branches` (`fetch.prune`), `Always fetch new commits, tags and branches from submodules`, `Update registered submodules`, `Initialize new submodules`.
  - [ ] Вкладка `Push`: поведение push по умолчанию (`push.default` и связанные опции).
  - [ ] Вкладка `Signing`: подпись коммитов и тегов (`commit.gpgsign`, `tag.gpgSign`, `user.signingkey`, `gpg.format`).
  - [ ] Вкладка `Encoding`: кодировка сообщений коммитов и отображения файлов.
  - [ ] Вкладка `Tag-Grouping`: разделитель группировки тегов по папкам — используется в #11.
  - [ ] Для каждой опции явно: пишется в `.git/config` репозитория или хранится в настройках Cogit.
  - [ ] У SmartGit в этом диалоге подписи обрезаны справа («changes» не помещается) — не повторять.

---

### Итог работы

- [ ] Под каждым пунктом верхнего уровня есть строка `> Итог: …`.
- [ ] Отдельным списком — решения, принятые самостоятельно (где в пункте было «обоснуй» или «на твоё решение»).
- [ ] Отдельным списком — пункты `не сделано` с причиной.
- [ ] Для #8 и #10 — явно названа первопричина бага.
- [ ] Покрытие проверено: в файле 49 пунктов — #1–#22 и #24–#50.
