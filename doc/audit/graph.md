# Аудит: graph_engine и отрисовка графа

25.09.2026 · просмотрено: `crates/graph_engine/src/{lib,lanes,paint,view}.rs`, `crates/graph_engine/tests/{lanes,long_links,paint,view}.rs`, `crates/git_engine/src/{graph_walk,topo}.rs` (для контекста `search.rs`, `history.rs`, `mailmap.rs`, `repo.rs::branches`), `crates/app_state/src/{graph_cache,graph_layout,graph_overlay,graph_wire}.rs`, `src-tauri/src/commands/mod.rs` (`load_commits`, `graph_window`, `graph_row_of`, `graph_overlay`, `close_repository`), `frontend/src/components/graph/{GraphCanvas,FoldToggle}.svelte` (для контекста `CommitList.svelte`), `frontend/src/lib/graph-{geometry,links,modes,mode-conflicts,style,wire,anchor}.ts`, `frontend/src/stores/graph{,-overlay,-folds}.svelte.ts`, `doc/07-graph-rendering.md`, `doc/04-ipc-contract.md` §5, `doc/12-risks.md` R-164, R-300…R-303, R-330, R-331, R-340…R-342, R-370.

## Проход 1

GR-001 · bug · medium · crates/app_state/src/graph_layout.rs:47 · У коммита на shallow-границе (и у ребёнка нечитаемого коммита) родители остаются в `parents`, но `hidden` заполняется только для отфильтрованного списка; раскладка ждёт родителя, которого обход никогда не отдаст: лейн 0 (его не рубят) тянется ниже последнего коммита главной линии до конца списка, прочие такие полосы рубятся в обрубок со ссылкой на отсутствующий коммит, а при `longLinkRows = 0` каждая идёт до низа и расширяет граф · `git clone --depth 30 --no-single-branch <url>` (commit-graph в shallow-клоне git не пишет), открыть, отметить `origin/*`: линия HEAD продолжается под последним своим коммитом до конца; клик по обрубку у граничного коммита выбирает несуществующий коммит (ошибка в Files/Diff). Трасса: graph_walk.rs:163 (родителей shallow-коммита не обходит, но отдаёт) → search.rs:226 → graph_layout.rs:47 (`hidden` пуст вне `flat`) → lanes.rs:114/149 (`far` → обрубок) или lanes.rs:132 (полоса ждёт вечно) · В `lay_out` для не-flat считать `hidden` = родители коммитов из `.git/shallow` (открыть `shallow_commits` из git_engine, один раз на загрузку) и родители, которых обход не смог прочитать; `parents` в `WalkedHistory` не урезать, чтобы перестройка после `--unshallow` была верной
GR-002 · bug · medium · crates/graph_engine/src/paint.rs:109 · Узел под обрубленной связью первого родителя получает в `trace` новую полосу: `own_in` смотрит только на входящие сегменты без стрелки, и `node_lane` = `fresh()`, хотя обрубок над ним уже взял полосу потомка (строка 136). «Ветка коммита» (`focusLane`) обрывается на стрелке, вопреки 07 §5 («ветка коммита … идут через разрыв, как по сплошной линии») · Preferences: Branch of commit включён, порог 40; ветка, ответвившаяся больше чем на 40 строк ниже вершины; клик по вершине подсвечивает её полосу и оба обрубка, а коммиты ветки ниже стрелки — нет; клик по ним подсвечивает только низ. Трасса: lanes.rs:326 (`open_tip` — новая полоса у родителя) → paint.rs:109–116 → paint.rs:190 (линия первого родителя идёт по новой полосе) → lib/graph-modes.ts:118 · Если `own_in` пуст, а в строке есть обрубок `Top`, чей ведущий потомок — ребёнок по первому родителю, брать его полосу (`stubs[(lead, r)]`) как `node_lane`; в тесте `traced_lanes_are_the_layouts_lanes_on_random_histories` учесть, что такая полоса продолжает цвет потомка (или давать новой полосе в lanes.rs цвет обрубленного потомка из `cut_into`)
GR-003 · bug · medium · frontend/src/components/graph/GraphCanvas.svelte:120 · Пунктир от Working Tree рисуется к строке 0 в колонке `rowAt(0).lane` (CommitList.svelte:225), а не к HEAD. По R-164 HEAD идёт первым только когда в графе нет его потомков; иначе строка 0 — чужой коммит, и пунктир связывает рабочую копию с ним · Локальная ветка отстаёт от upstream после fetch; отметить `origin/main` в Branches: пунктир из Working Tree уходит в вершину `origin/main` (строка 0, колонка 1), а HEAD ниже в колонке 0 остаётся без линии сверху. Трасса: topo.rs:149 (HEAD после потомков) → CommitList.svelte:225 → GraphCanvas.svelte:120–131 (`foot = nodeCentre(headLane, firstCommitRow)`) · Передавать в GraphCanvas строку HEAD (`graph.loadedIndexOf(repository.current.head.oid)`) и вести пунктир по колонке 0 до неё — выше HEAD колонка 0 по построению пуста; отступ строки Working Tree — от полосы HEAD
GR-004 · inconsistency · medium · src-tauri/src/commands/mod.rs:598 · `graph_window` выполняется прямо на async-потоке Tauri, а с R-302 читает из gix тему и автора до 128 коммитов, держа `graph.read()` и мьютекс текстов (graph_cache.rs:594–609); пока окно читает с диска, `on_chunk` идущего обхода и `trim` ждут запись. Нарушает «Never block: heavy work goes to spawn_blocking» (CLAUDE.md); `graph_overlay` рядом уже в `blocking` · Большой репозиторий без прочитанных текстов, прыжок прокрутки во время обхода: каждое окно — 128 `find_commit` на воркере tokio под блокировкой кэша, обход стоит на `self.graph.write()` в graph_cache.rs:513 · Обернуть в `blocking("graph_window", …)`; под read-блокировкой копировать срез `CommitRow` и `Arc<Mutex<Texts>>`, отпустить кэш и дочитывать недостающие тексты уже вне его
GR-005 · bug · medium · crates/app_state/src/graph_cache.rs:665 · Известно (регресс `repo.close` на large) — точная точка: `forget_graph` удаляет `Graph` выражением `self.graph.write().graphs.remove(&repo);`, возвращённый `Option<Graph>` освобождается до гарда, то есть под write-блокировкой кэша: сотни тысяч аллокаций (`CommitRow` со строками, `WalkedHistory`, `Texts`, `PaintMemo` с индексом по oid) на пути закрытия, окна и раскраска других репозиториев ждут. То же при вытеснении в `trim` (строка 359) · Закрыть открытый large после показа графа: шаг `forget-state` в `close_repository` (lib.rs:994) растёт на время освобождения ~25 МБ графа · `let gone = self.graph.write().graphs.remove(&repo);` отдельной инструкцией и `std::thread::spawn(move || drop(gone))`; в `trim` собирать вытесненные в `Vec` и освобождать так же, вне блокировки
GR-006 · ux · low · frontend/src/stores/graph-overlay.svelte.ts:73 · При новом обходе того же репозитория раскраска сбрасывается целиком: `#stale` переносится только при смене запроса, а смена поколения даёт пустые карты, и до ответа `graph_overlay` отмеченные ветки, приглушение Ancestry и фокус ветки мигают серым, хотя строки внутри `kept` те же (R-301) · Отметить ветку (цветная), сделать коммит или fetch: App.svelte:1021 перезагружает граф, при подмене `graph.walk` меняет поколение, CommitList вызывает `graphOverlays.show` с новым ключом, все строки рисуются без цвета до прихода окна раскраски · Для того же репозитория оставлять прежние блоки в `#stale` для строк `< kept` (отдать `kept` показанного обхода из graph store) и снимать их по приходу нового окна, как при смене запроса
GR-007 · bug · low · crates/app_state/src/graph_cache.rs:434 · Ответ из кэша возвращается без `trim` и перезапускает поток `graph-text`, который дочитывает тексты уже после последней обрезки; `walks` при этом не растёт, поэтому потоки нескольких репозиториев идут одновременно, а сумма графов и текстов превышает `GRAPH_CACHE_BYTES` до следующего обхода без кэша (R-303 обещает учёт текстов в лимите) · Открыть по очереди 3–4 больших репозитория, затем переключаться между ними: каждое переключение — ветка 434–461, prefill без остановки, `trim` не вызывается · Вызывать `cache.trim(repo, GRAPH_CACHE_BYTES)` и в ветке ответа из кэша; по окончании prefill проверять бюджет (передать потоку доступ к обрезке или учитывать тексты в `Graph.bytes` и обрезать при следующем `build_graph`)
GR-008 · bug · low · crates/app_state/src/graph_cache.rs:417 · Ключ кэша графа — запрос и `refs_fingerprint`, но граница shallow в него не входит: `git fetch --deepen`/`--unshallow`, не сдвинувший ссылок, оставляет в кэше обрезанную историю, и `load_commits` отвечает ею · Shallow-клон, в терминале `git fetch --unshallow` при неизменном remote; переключиться на другой репозиторий и обратно (или Reload): `answers()` истинно, граф по-прежнему кончается на старой границе, пока не сдвинется какая-нибудь ссылка · Добавить в отпечаток отметку `.git/shallow` (путь через `rev-parse --git-path shallow`/gix, длина и mtime или хэш списка `shallow_commits`)
GR-009 · dead-code · low · crates/graph_engine/src/view.rs:28 · После R-341 («ViewFilter теперь только для сворачивания») `ViewFilter::first_parent`, поле `first_parent` и ветка `if self.first_parent { continue; }` в `admit` в продукте не вызываются (`graph_view` возвращает `None` при `first_parent`); пять тестов `first_parent_*` в tests/view.rs проверяют путь, которого нет, и создают ложное покрытие режима · `grep ViewFilter::first_parent` — только tests/view.rs:40–133; graph_layout.rs:99 никогда не строит его · Удалить конструктор, поле, ветку и тесты `first_parent_*` из view.rs (режим покрывают `graph_view.rs` и `graph_parity.rs` через `ByTime`)
GR-010 · inconsistency · low · doc/07-graph-rendering.md:357 · §10 «Только первые родители»: «Решает `graph_engine::ViewFilter` в порядке обхода, до раскладки …» — в коде первые родители решает обход `ByTime` (search.rs:179) и `truncate(1)` в graph_layout.rs:36 (R-341, обновление при слиянии с g1). Там же §3 (строки 135–136) говорит, что тему и автора граф читает только окном, — фоновое дочитывание `graph-text` (R-303) в 07 не упомянуто · Сравнить 07 §10 и §3 с graph_layout.rs:23–41, search.rs:179, graph_cache.rs:561–577 · Переписать абзац §10 по R-341 (обход по первым родителям плюс обрезка родителей строк), в §3 добавить фоновое дочитывание текстов и его остановку по новому обходу
GR-011 · dead-code · low · crates/git_engine/src/graph_walk.rs:17 · `WalkedHistory::from_rows`, `len`, `is_empty` нигде не вызываются, а номер строки `at` (u32 в `(u32, i64, Vec<ObjectId>)`) пишется для каждого коммита (search.rs:245, счётчик `listed`) и никогда не читается: `Reuse::read` берёт только время и родителей; кортеж из-за выравнивания на 8 байт больше · `grep from_rows\|history.len` по крейтам — нет вызовов; `get` (строка 63) отдаёт `at`, `Reuse::read` (строка 77) его отбрасывает · Удалить `from_rows`, `len`, `is_empty`, поле `at` и счётчик `listed` в `stream_rows`
GR-012 · ux · low · frontend/src/components/graph/GraphCanvas.svelte:148 · Кольцо заливается фоном выделения только для `selectedRow` (одна строка — `selection.oid`), а строка «сравнить с» (`comparedFrom`) тоже получает класс `.selected` (CommitList.svelte:539): её кольцо залито фоном панели или полосы, внутри кольца «дырка» другого цвета, вопреки 07 §5 «Узлы» · Выбрать коммит, Ctrl+клик по второму для сравнения: у строки первого фон выделения, а его кольцо — цвета панели · Передавать в GraphCanvas набор выделенных строк (selection и comparedFrom) и строить `nodeFill` по нему
GR-013 · simplify · low · crates/app_state/src/graph_layout.rs:118 · `mainline_of` на каждом обходе без кэша зовёт `branches()`, который для каждой локальной ветки с upstream считает расхождение (`merge_base` и два обхода, repo.rs:213), хотя нужны только oid HEAD или `master`/`main`; `branches()` вызывается даже при живом HEAD, а `.ok()?` у `branches()` и `head()` глотает ошибку без лога (правило «Every fallback logs its Err»), и колонка 0 тогда молча не резервируется · Репозиторий с десятками разошедшихся с upstream локальных веток: каждая отметка ветки или перестройка графа платит за ahead/behind всех веток до первой строки · Сначала `head()`; при Unborn/неотмеченном HEAD — прямой `find_reference("refs/heads/master"|"refs/heads/main")`; ошибки логировать `tracing::error!(error = ?err, context = "graph mainline")`
GR-014 · bug · low · frontend/src/lib/graph-modes.ts:118 · Выбранная кликом по линии полоса хранится номером (`lanePick = { oid, lane }`, CommitList.svelte:394), а номера полос `paint::trace` раздаёт заново сверху вниз при каждом новом графе (paint.rs:90); `lanePick` не сбрасывается при смене обхода, и после перестройки `focusLane` подсвечивает другую ветку · Branch of commit включён: выбрать коммит, кликнуть по линии чужой ветки в его строке; затем отметить ещё одну ветку в Branches (или fetch принёс новую вершину выше) — коммит остаётся выбранным, а подсвечена уже другая полоса · Сбрасывать `lanePick` при смене `graph.walk` (репозиторий или поколение) либо хранить выбор как колонку и половину строки и пересчитывать полосу через `laneAt` по текущей раскраске

## Проход 2

25.09.2026 · другая оптика: пограничные состояния (stash, поиск, переключение репозитория, Windows), ошибки и учёт памяти, обещания doc/07 и doc/features. Дополнительно просмотрено: `crates/app_state/src/lib.rs` (`search_graph`, `begin_graph`, `close_repository`), `crates/git_engine/src/{search,history,shared,mailmap}.rs`, `crates/app_state/tests/{graph,graph_cache,performance}.rs`, `frontend/src/components/graph/CommitList.svelte`, `frontend/src/lib/ref-nodes.ts`, `frontend/src/App.svelte` (`reloadGraph`, `filterGraph`), doc/04 §5, doc/07 §2–§10, R-300…R-303, R-330, R-341, R-342, R-370, F-331, F-430…F-444, F-470. Опыт с git — `D:\cogit-work\tmp-graph2`.

GR-015 · bug · medium · crates/app_state/src/graph_cache.rs:172 · Ключ кэша графа — запрос и `refs_fingerprint` (history.rs:60: имена и цели ссылок и HEAD), но отмеченный stash едет в `visibleRefs` ревизией `stash@{N}` (ref-nodes.ts:248), которая разрешается по reflog `refs/stash`, а reflog в отпечаток не входит. `git stash drop stash@{1}` не двигает `refs/stash` (проверено: `git rev-parse refs/stash` до и после один и тот же, а `stash@{1}` указывает уже на другой коммит), запрос тот же — `answers()` истинно, граф отдаётся из кэша со старой вершиной · Сделать три stash, отметить `stash@{1}` в Branches (F-331), затем Drop на `stash@{1}` (RefActions.svelte:716) или `git stash drop stash@{1}` в терминале; после перезагрузки графа (или ухода на другой репозиторий и возврата) в графе остаётся коммит удалённого stash — без метки и квадрата, обычным кольцом, со своим `index on …`, а новый `stash@{1}`, хотя отмечен, не показан; так до сдвига любой ссылки. Трасса: graph.svelte.ts:196 (`visibleRefs` те же) → graph_cache.rs:417 (отпечаток тот же) → :434 (ответ из кэша, search.rs:117 не вызывается) · Включать в ключ разрешённые вершины: до обхода `handle.walk_tips(query)` и хранить/сравнивать их oid вместе с `refs` (N `rev_parse`, без чтения коммитов), либо подмешивать в отпечаток штамп `logs/refs/stash` (путь через `rev-parse --git-path`), когда в `visible_refs` есть ревизия с `@{`
GR-016 · bug · low · crates/app_state/src/graph_cache.rs:496 · В кэше один граф на репозиторий: поиск (отфильтрованный список, `filters_rows`) замещает им граф, а по концу обхода списка прежний граф выбрасывается (`graph.base = None`, :543). У списка нет `Texts` (`texts: None`, :470), и `WalkedHistory` у него только из совпавших коммитов (search.rs:244 — запись после фильтра). Поэтому очистка поиска не отвечается из кэша (R-300), берёт из прошлого обхода время и родителей лишь совпавших коммитов (R-301), остальные читает заново, а тексты — с нуля (`same_names` ложно → новый `Texts`, `kept = 0`, все блоки перезапрашиваются) — вопреки R-303 «кэш текстов … общий для графов репозитория с тем же mailmap: перестроенный граф начинает с прочитанного» · large без commit-graph, граф показан и тексты дочитаны (в логе `graph texts read ahead`); ввести фильтр по сообщению в поиске графа, дождаться результата, очистить: в логе снова обход всей истории и `graph texts read ahead` с rows ≈ 50 000. Тест: `build`, `build_with(filter)`, `build` (tests/graph_cache.rs) — у последнего прогресса `kept == 0`, тексты читаются заново · Держать неотфильтрованный граф рядом со списком: ключ `(RepoId, query.filters_rows())` в `GraphCache.graphs` (`trim` считает оба) либо переносить вытесняемый завершённый граф в поле `unfiltered` отфильтрованного и отдавать его из `answers()`/`reusable()` при возврате к запросу без фильтра
GR-017 · race · low · frontend/src/stores/graph.svelte.ts:108 · `setView` (и `setLongLinkRows`, :218) перезагружают `this.#shown.repo`, а при переходе на другой репозиторий `#shown` — ещё прежний (R-300: старые строки на экране, новый обход копится в `#next`); `#load` для репозитория, который уже не открыт, выходит сразу (:168). Смена режима или порога во время загрузки нового репозитория до него не доходит: его граф дорисовывается со старыми `view`/`longLinkRows`, а `modes` в CommitList уже новые (включено сворачивание — свёрток нет; включены «первые родители» — граф полный) до следующей перезагрузки · Открыт A; кликнуть большой B в Repositories и, пока граф B грузится (на экране ещё строки A), переключить в Preferences ▸ Graph & History «First parents only» или порог длинных связей: CommitList.svelte:130–133 → `graph.setView` → `load(A)` → return на graph.svelte.ts:168; граф B — без режима · Перезагружать тот репозиторий, что грузится или открыт: `const repo = this.#next?.repo ?? this.#shown?.repo` (или `repository.current?.repo`) в обоих методах
GR-018 · dead-code · low · crates/app_state/src/lib.rs:564 · `AppState::search_graph` в продукте не вызывается с R-193/R-300 (`load_commits` → `build_graph`; вызовы — только tests/graph.rs, первая половина tests/graph_view.rs и tests/performance.rs), но на нём стоят тесты раскладки и замеры «обещания продукта»: путь с чтением текста каждой строки (`text: true`), без кэша, повторного обхода и ленивых текстов — не то, что запускает пользователь. Известно флапающий `performance::fifty_thousand_commits_meet_the_product_promise` (performance.rs:153–176, бюджет 500 мс; отладочная сборка по 07 §9 — 356–368 мс) меряет как раз его: `row_of` на каждый из 50 000 коммитов добавляет к обходу около четверти (R-302: 219–231 против 171–177 мс release без commit-graph) и съедает запас под нагрузкой. `GraphChunk` держит `Serialize`/`specta::Type`, хотя по IPC больше не ходит · `grep -rn search_graph crates src-tauri` — только определение и тесты; сравнить performance.rs:153 (`search_graph`) с :192 (`build_graph`) · Перевести тесты graph.rs/graph_view.rs и бюджеты performance.rs на `build_graph` + `graph_window` (как tests/graph_cache.rs), `search_graph` удалить, с `GraphChunk` снять `Serialize`/`specta::Type`; в 07 §9 записать, что меряется продуктовый путь
GR-019 · ux · low · crates/git_engine/src/graph_walk.rs:164 · Отмеченный stash (F-331, ревизия `stash@{N}`) обходится как любая вершина: `ByTime` идёт по всем его родителям, и в граф попадают служебные коммиты stash — `index on <ветка>: …` (второй родитель) и при `-u` `untracked files on …` (третий, корень, отдельной полосой); узел stash рисуется merge-коммитом с линией в них. F-331 обещает одну строку с квадратным узлом, как в SmartGit · Внести правку, Stash, отметить `stash@{0}` в Branches: под квадратом `On master: …` — кольцо `index on master: …` в соседней колонке и ромб с HEAD; с Include untracked — ещё коммит-корень. Родители проверены `git log --format='%h %p %s' stash@{0}` · Для вершин из reflog `refs/stash` (ревизии `stash@{N}`, их разрешает `tips_for`) следовать только первому родителю: передать в `ByTime` множество таких oid и брать для них `take(1)`, а в `lay_out` урезать их `parents` до первого, как при `first_parent`
GR-020 · inconsistency · low · doc/07-graph-rendering.md:152 · Таблица §4 «Особые узлы»: «Стэш — в основном графе не показывается, только в References» и «любой коммит — одинаковое полое кольцо» (:150). В коде с 85e0733 отмеченный stash обходится (ref-nodes.ts:248, `rev: stash@{N}`), получает метку и рисуется квадратом цвета `--status-stash` (GraphCanvas.svelte:141, F-331); CLAUDE.md требует приводить 07 в соответствие в том же коммите · Сравнить 07:150–152 с GraphCanvas.svelte:139–147 и doc/features/F-331-stash-in-graph.md · Строка «Стэш — только отмеченный в Branches: метка `stash@{N}`, квадратный узел `--status-stash`» и оговорка про квадрат в строке «Любой коммит»
GR-021 · inconsistency · low · doc/07-graph-rendering.md:292 · §7: «Наведение на строку — подсветка строки; узлы её родителей и потомков получают обводку». Обводки нет: `hoverRow` меняет только заливку кольца (GraphCanvas.svelte:148, graph-geometry.ts:101), а родителей фронтенд и не получает (graph-wire.ts:3: «parents stay in Rust, the list never draws from them») · Навести курсор на merge-коммит: подсвечены фон строки и заливка его кольца, кольца родителей и потомков не меняются · Убрать вторую половину фразы из §7 либо сделать: отдавать в `graph_overlay` строки родителей и потомков строки под курсором и обводить их кольца
GR-022 · ux · low · frontend/src/components/graph/CommitList.svelte:572 · Кнопка складки (FoldToggle) показывается по `modes.collapseMerged` и `graphFolds.expanded`, без проверки, что на экране граф, а не отфильтрованный список. В результатах поиска раскрытый ранее merge получает треугольник «Fold the merged branch»; клик меняет `expanded` → `graph.setView` → поиск обходится заново (`view` входит в ключ кэша, хотя `filters_rows()` его не видит), а свернуть в плоском списке нечего — треугольник просто исчезает · Включить «Collapse merged branches», раскрыть merge в графе, найти его поиском по теме: в строке результата — открытый треугольник; клик — полный повторный поиск (на large с фильтром по пути — секунды), список не меняется · Показывать FoldToggle только при `isEmptyQuery(graph.query)`; для отфильтрованного запроса не передавать `view` в `load_commits` (или не сравнивать его в `Graph.answers`), чтобы смена режима не перезапускала поиск
GR-023 · inconsistency · low · crates/app_state/src/graph_cache.rs:353 · Бюджет кэша (R-300: «граница памяти — байты») не видит `PaintMemo` (graph_overlay.rs:45): у раскрашенного графа это вторая копия каждого oid в `index: HashMap<String, u32>`, `parents: Vec<Vec<Option<u32>>>` на каждую строку и колонки `Paint` — по оценке 7–8 МБ на 50 000 строк, около трети к учтённым ~25 МБ; `trim` складывает только `graph.bytes` и тексты · Открыть по очереди 4 больших репозитория с отмеченной веткой (или с Branch of commit / Collapse merged — раскраска считается при любом запросе): по учёту кэш в пределах 96 МБ, на деле больше на память раскрасок, вытеснение не срабатывает · Считать в `trim` память раскраски (`PaintMemo::bytes()` по длинам `index`, `parents`, `paint`); индекс строить без копии строк oid (`HashMap<ObjectId, u32>` или позиция в `laid.commits`)
GR-024 · race · low · crates/app_state/src/graph_cache.rs:265 · Поток `graph-text` открывает репозиторий сам (`RepoHandle::open(root)`), мимо `SharedRepo` (R-324), и держит этот хэндл — с отображёнными в память pack-файлами — на всё дочитывание, от первой пачки до последней, хотя `SharedRepo` (shared.rs:9–12) обещает «nothing stays mapped between calls: Windows refuses to replace or delete a mapped file, and git … deletes packs». После открытия большого репозитория дочитывание всех строк занимает сотни миллисекунд (R-303: ~190 мс на 50 000), на истории в сотни тысяч коммитов — секунды · Windows: открыть большой репозиторий и сразу запустить в терминале `git gc`, `git repack -ad` или `git maintenance run`: старые pack, отображённые потоком `graph-text`, не удаляются (`unable to unlink …`), мусорные pack остаются · Открывать хэндл на пачку и отпускать после неё (`RepoHandle::open` на каждые 256 строк, ~0,8 мс) либо передать потоку `Arc` на `HandleCache` и брать `SharedRepo::handle()` (у него своё хранилище объектов) на пачку
