# Аудит: app_state, src-tauri, IPC

25.09.2026, ветка `audit/2026-09-25` (a779a38). Просмотрено: `crates/app_state/src/{lib,handles,queue,safety,rewrite,ref_ops,network,remote_ops,stashing,flow,worktrees,file_actions,hooking,presets,avatars,credentials,settings,logging,environment,terminal,desktop,repo_rows,investigation,licences}.rs`; `src-tauri/src/**` (`lib`, `events`, `commands/*`, `menu/*`, `child_window`, `blame_window`, `window_place`, `shutdown`, `session_end`, `renderer_failure`, `webview2`, `accelerators`, `operations`, `profile`, `logging`, `diagnostics`, `recycle_bin`, `webview_memory`), `src-tauri/build.rs`, `tauri*.conf.json`, `capabilities/default.json`; `crates/fs_watcher/src/*`, `crates/avatars/src/*`, `crates/build_info/src/*`; `frontend/src/lib/ipc/*` (сверка `bindings.ts` с `collect_commands!` — 187 команд, имена и сигнатуры совпадают); `doc/04-ipc-contract.md`, `doc/01-architecture.md`, R-300…R-390. Для проверки поведения читались также `app_state/src/graph_cache.rs` (только `graph_window`/`forget_graph`), `git_engine/src/{shared,runner,interactive,maintenance,pulse}.rs` и исходники tauri 2.11.5, tauri-plugin-window-state 2.4.1, gix 0.87.1, notify 8.2.0 из `Cargo.lock`.

## Проход 1

BE-001 · bug · medium · src-tauri/src/commands/mod.rs:598 · `graph_window` — `async` без `blocking`: с 6060e47 `AppState::graph_window` дочитывает тему и автора непрочитанных строк из ODB (`crates/app_state/src/graph_cache.rs:604-607` → `Texts::fill` → `commit_text`) и на каждый вызов берёт новый `RepoHandle` с новым хранилищем объектов (`handles.rs:34` → `SharedRepo::handle`, индексы паков грузятся заново, R-324) — блокирующий дисковый I/O на воркере tokio, который несёт IPC, под `graph.read()` и мьютексом `Texts` · набор large (50k), открыть граф и сразу утащить ползунок в конец, пока фоновый читатель (R-303) туда не дошёл: каждое окно в 128 строк — до 128 чтений коммитов на async-воркере, при быстрой прокрутке таких окон несколько одновременно · обернуть тело в `blocking("graph_window", …)` вместе с `graph_wire::encode` и base64

BE-002 · ux · medium · src-tauri/src/menu/mod.rs:500 · `rebuild` ставит пересобранную строку меню через `app.set_menu`, а в tauri 2.11.5 (`app.rs:971-979`) её получает каждое окно с `menu() == None`; окна Compare и Merge своё меню сняли (`src-tauri/src/child_window.rs:98`, `remove_menu`) — после пересборки у них появляется всё главное меню, вопреки «Child windows: no main-window menu»; его пункты шлют `menu-command` главному окну · открыть Compare with…, затем Edit ▸ Preferences… ▸ OK (`frontend/src/App.svelte:954` зовёт `setKeymap` при каждом применении настроек) — в окне сравнения полоса Repository/Edit/View/… · ставить пересобранное меню только главному окну (`get_webview_window("main")?.set_menu(menu)`), `app.set_menu` оставить стартовому `setup`; либо после `set_menu` снимать меню с окон, открытых `child_window::open`

BE-003 · bug · medium · crates/avatars/src/queue.rs:186 · вторая неудача запроса (`Fetched::Failed`) записывается `store_missing` в `index.json` на 7 дней (`MISSING_TTL`) — ровно то, от чего предостерегает `crates/avatars/src/source.rs:64` («remembering a bad moment as a miss would blank the author for seven days»); перезапуск не помогает · включить аватары без сети (или при недоступном gravatar.com), пролистать граф, вернуть сеть, перезапустить — у всех показанных авторов неделю инициалы вместо картинок · неудачи помнить только в памяти очереди на сессию (множество в `State`), в индекс писать лишь ответ 404 / пустой 200

BE-004 · bug · medium · crates/app_state/src/handles.rs:37 · зарегистрированный корень переоткрывается поиском вверх (`SharedRepo::open` → `discover_opts`), так же `overview_of` (`lib.rs:1025`), `submodule_outline` (`repo_rows.rs:9`) и `git_engine::pulse` (`crates/git_engine/src/pulse.rs:24`): если папка перестала быть репозиторием, а выше есть другой, `RepoId` молча начинает указывать на внешний репозиторий; тест `crates/app_state/tests/handles.rs:79` проходит только потому, что фикстура лежит во временной папке вне репозиториев · `git init outer && git init outer/inner` с коммитами, открыть `outer/inner`, удалить `outer/inner/.git` в Проводнике, F5 — Files «inner» показывает статус `outer` (пути от его корня), Stage/Discard/Reset идут в `outer`, строка Repositories — ветка `outer` вместо `missing` · после открытия сверять корень рабочего дерева с зарегистрированным (`RepoNotFound`/`missing` при несовпадении) или открывать зарегистрированный корень без поиска вверх

BE-005 · bug · medium · crates/app_state/src/graph_cache.rs:665 · `close_repository` (`crates/app_state/src/lib.rs:994`) → `forget_graph` освобождает весь разложенный граф репозитория (R-351: +23 MiB на large — сотни тысяч мелких аллокаций строк, `Texts`, `history`) синхронно, на пути ответа и под `self.graph.write()`, блокируя `graph_window`/`graph_overlay` других репозиториев; до 9c2a5c7 граф неактивного репозитория к закрытию уже был заменён — это конкретная причина «синхронного освобождения кэша» в известном регрессе `repo.close` · large: открыть, дождаться графа, переключиться на другой, закрыть первый — шаг `forget-state` в строке лога `repository closed` (`Steps::report_close`) · вынимать `Graph` из карты под блокировкой, а ронять вне её и вне ответа (`std::thread::spawn(move || drop(graph))`); так же вытесненные `trim`

BE-006 · inconsistency · medium · crates/app_state/src/rewrite.rs:89 · INV-12 и ccb4227: merge, rebase, cherry-pick, revert и pull при остановке на конфликте пишут Undo через `record_move`, а `interactive_rebase` (и `split_off`, `rewrite.rs:145`) выходят по `?` раньше `record` — после Continue отменить нечем; `edit_author` (`crates/app_state/src/ref_ops.rs:108`) — тот же `rebase -i` с `exec`, но пишет `Recovery::None`, хотя ветку можно вернуть как `Moved` · Local ▸ Rebase Commits After This One…, переставить коммиты с конфликтом, разрешить, Continue, Undo Last Operation — «nothing to undo»; Edit Author — запись в журнале без Undo · в `interactive_rebase`/`split_off` звать `record_move(repo, &handle, before, …, &result)` вместо `?`; в `edit_author` — `Recovery::Moved { name, oid }` из `before`

BE-007 · inconsistency · medium · src-tauri/src/commands/mod.rs:205 · правило «Over ~500 items: Channel» и INV-02 («возврат монолитного JSON запрещён») нарушают неограниченные `Vec` через `invoke`: `commit_tree_files` (все файлы дерева, так и записано в `doc/04-ipc-contract.md:415`), `blame` (`mod.rs:1091`, запись на каждую строку файла), `worktree_files` (`mod.rs:699`), `commit_files` (`mod.rs:651`), `compare_files` (`src-tauri/src/commands/ref_ops.rs:43`), `ref_dates` (`mod.rs:1186`) · Files ▸ Unchanged на коммите репозитория со 100 тыс. файлов — один ответ со 100 тыс. путей разбирается вебвью целиком; окно Blame файла на 50 тыс. строк — то же · отдавать через `Channel` чанками по 100–200 (как `investigate_blame`) или окнами по видимой области, как `graph_window`

BE-008 · inconsistency · medium · src-tauri/src/commands/mod.rs:837 · две абстракции платформенных действий вопреки CLAUDE.md («go through one abstraction»): `open_in_terminal` берёт программу из `app_state::terminal` и сам спавнит голый `std::process::Command` в слое Tauri (INV-09) с унаследованными std-хэндлами, мимо `desktop::spawn`, хотя `crates/app_state/src/desktop.rs:158` объясняет, что консольную программу из GUI надо пускать через `cmd /C start`; Git Bash — `bash.exe --login -i` в conhost (`crates/app_state/src/terminal.rs:73`) против `git-bash.exe` у Open Git Shell (`desktop.rs:168`) · Preferences ▸ Terminal = PowerShell / Command Prompt / Git Bash, Open in Terminal в отладочной сборке — оболочка садится в консоль `tauri dev` вместо своего окна; Git Bash из Open in Terminal и из Open Git Shell — разные программы · выбор терминала выразить как `desktop::Launch` (через `cmd /C start`, `hidden`) в одном модуле `app_state::desktop`, спавнить `desktop::spawn`, в команде — один вызов

BE-009 · dead-code · low · src-tauri/src/commands/repo_rows.rs:27 · `background_fetch` без вызова: обёртка `backgroundFetch` (`frontend/src/lib/ipc/repo-rows.ts:22`) не используется с c0e99dd (заменена `pull_probe`, R-354), а `doc/04-ipc-contract.md:420` и R-353 описывают её как работающую; команда к тому же пишет ссылки мимо очереди репозитория (нет `mutating`) и, если её оживить, будет гоняться с Pull/Fetch за ref-lock. Так же без вызова `repo_status` (`mod.rs:896`, `frontend/src/lib/ipc/index.ts:315`; `frontend/src/stores/repository.test.ts:478` проверяет, что его не зовут, R-316). `delete_untracked`, `set_upstream`, `diff_files`, `file_before` уже записаны «на решение» в `doc/refactor-night.md:776` · поиск `backgroundFetch(` и `repoStatus(` по `frontend/src` без тестов — только определения · удалить команды, строки `collect_commands!`, обёртки и строки контракта (фолбэк `scripts/bench/scenarios.mjs:48` нужен лишь сборкам без `pull_probe`, базовая a6b23f1 его имеет)

BE-010 · bug · low · crates/app_state/src/handles.rs:34 · после R-324 все `RepoHandle` одного `SharedRepo` делят один снимок индекса: `IndexStorage` — `Arc` (gix 0.87.1 `worktree/mod.rs:18`, клонируется с `ThreadSafeRepository`, `try_index` — «shared across all clones»), а перечитывается он только при строго более новом mtime (gix-fs 0.22.1 `snapshot.rs:125`); две записи индекса в пределах разрешения mtime с чтением статуса между ними оставляют статус старым до следующей записи, раньше снимок жил одну команду · репозиторий на флешке FAT32 (mtime 2 с): Stage файла A, сразу Stage файла B — после обновления B остаётся в Unstaged · в `git_engine` для статуса читать индекс `open_index()` (без общего снимка) или сверять вместе с mtime размер файла

BE-011 · duplicate · low · crates/app_state/src/lib.rs:1001 · `close_repository` шлёт `AppEvent::RepoClosed` второй раз: `unregister` (`lib.rs:1306`) уже послал его; подписчиков `RepoOpened`/`RepoClosed` в продукте нет (`src-tauri/src/events.rs:99` — `_ => {}`) · подписаться на шину и закрыть репозиторий — два `RepoClosed` подряд · убрать эмит в `close_repository` (или оба события, если шине они не нужны)

BE-012 · race · low · crates/app_state/src/queue.rs:211 · `settle` шлёт `Done` до `queue.release(repo)`; пересыльщик (`src-tauri/src/events.rs:85`) на этом событии спрашивает `session_end_blocker()` и может ещё увидеть занятую полосу — `ShutdownBlockReasonDestroy` не зовётся, причина удержания сеанса висит до следующего `Done` или `WM_QUERYENDSESSION` · Windows под нагрузкой: выход из сеанса во время операции (R-168), операция закончилась, очередь пуста — причина «1 operation is still running» остаётся установленной · в `settle` сначала `release`, затем `emit`

BE-013 · ux · low · crates/app_state/src/lib.rs:1244 · нормальные состояния приходят как «Not a Git repository» / «Invalid repository state» / «Internal error»: снятый с регистрации `RepoId` — `RepoNotFound("id 5")`, во фронтенде «Not a Git repository: id 5» (`frontend/src/lib/ipc/index.ts:167`); любой `InvalidState` получает префикс «Invalid repository state:» (`index.ts:169`), хотя так приходят «Git Bash from Git for Windows was not found» (`src-tauri/src/commands/desktop.rs:69`), «PowerShell is a Windows program», ошибки keyring (`src-tauri/src/commands/network.rs:118`); сбой записи `settings.json` и «нет программы для .txt» у списка лицензий — `Internal` (`mod.rs:404`, `mod.rs:485`, `mod.rs:490`) · открыть окно Blame, в главном Ctrl+W (Close Repository), в Blame F5 — «Not a Git repository: id 3»; мутация, стоявшая в очереди на момент закрытия, — то же · для снятой регистрации — своё сообщение («репозиторий закрыт в Cogit — откройте снова»), нейтральный префикс у `invalidState`, ошибки файлов — `GitError::Io`

BE-014 · inconsistency · low · src-tauri/src/lib.rs:271 · `tauri_plugin_window_state` с флагами по умолчанию (`StateFlags::all()`, включая `VISIBLE`): его `on_window_ready` при создании главного окна (tauri 2.11.5 `manager/window.rs:117` и `app.rs:2524` — синхронно, до пользовательского `setup`) делает `show()` + `set_focus()` (plugin 2.4.1 `lib.rs:262`, и при первом запуске без сохранённого состояния) — `"visible": false` (`src-tauri/tauri.conf.json:24`), «settle … before the window is shown» (`lib.rs:336-341`) и «subscribed before the window is shown» (`lib.rs:327`) не выполняются · сохранить окно с заголовком выше экрана (R-113) и запустить — окно показывается там, потом `settle` его двигает · `.with_state_flags(StateFlags::all() - StateFlags::VISIBLE)`

BE-015 · bug · low · crates/fs_watcher/src/watcher.rs:191 · в linked worktree пути общей папки классифицируются как свои: `<main>/.git/index`, `HEAD`, `ORIG_HEAD`, `MERGE_HEAD` главного worktree после неудачного `strip_prefix(git_dir)` снимаются с `common_dir` и дают `Index`/`Head` этого worktree · открыть основной репозиторий и его linked worktree (Worktrees ▸ Open), в основном Stage — у worktree `repo-changed { kind: index }` и лишнее перечитывание · для путей, снятых с `common_dir`, принимать только общее: `config`, `packed-refs`, `refs/**`, `hooks/**`, `logs/refs/stash`

BE-016 · ux · low · src-tauri/src/commands/ref_ops.rs:19 · подпись операции в футере берётся из `OperationKind::title()` и не совпадает с действием: `reset_to` — «Checking out», `rollback_to` — «Undoing» (`mod.rs:1244`), `abort_operation`/`continue_operation`/`skip_operation` — «Merging» и при rebase/cherry-pick (`mod.rs:947-949`), `cherry_pick`/`revert`/`split_off` — «Committing» (`mod.rs:1000`, `mod.rs:1276`) · Reset ▸ Hard на коммите графа — в футере «Checking out»; Abort при остановленном rebase — «Merging» · `mutating_titled` со своей подписью («Resetting», «Rolling back», «Aborting», «Cherry-picking», «Reverting», «Splitting»)

BE-017 · bug · low · crates/app_state/src/lib.rs:229 · счётчик `RowCache.forgotten` общий на все репозитории: событие наблюдателя любого репозитория отбрасывает строки всех остальных, прочитанные в это время (`keep` сравнивает общий счётчик), хотя комментарий обещает «while its repository changed» · держать открытыми большой A и «шумный» B (идёт сборка, пишутся файлы) — `repositories` при переключениях снова читает A с полным статусом, `rows_read()` растёт · поколение на репозиторий (`HashMap<RepoId, u64>`), сравнивать только своё

BE-018 · inconsistency · low · crates/app_state/src/queue.rs:75 · «the place in the queue is taken when the command is called, not when its future happens to be polled» не так: `admit` вызывается внутри `async fn enqueue`, то есть при первом опросе будущего, которое Tauri запускает `async_runtime::spawn` (tauri 2.11.5 `ipc/mod.rs:375`) на многопоточном рантайме; две команды, пришедшие подряд, входят в полосу в любом порядке · фронтенд без `await` шлёт `stagePaths` и сразу `commit` — коммит может встать в полосу первым · брать место синхронной командой (идёт на главном потоке в порядке IPC), которая делает `admit` и сама спавнит работу; либо исправить комментарий и гарантию «in order» в CLAUDE.md/04

BE-019 · dead-code · low · crates/app_state/src/lib.rs:429 · `AppState::tracked` в продукте не вызывается (только `crates/app_state/tests/events.rs`); нумерует операции счётчиком журнала безопасности `next_entry_id`, а очередь — своим `Queue.next_id` с нуля, так что при использовании id в одном потоке `operation-changed` совпадали бы · поиск `.tracked(` — только тесты · удалить вместе с тестами или брать id из `Queue`

BE-020 · inconsistency · low · crates/app_state/src/lib.rs:462 · `ScanHit.root` — `display()`, на Windows с `\`, тогда как `RepoSummary.root` и `RepoOverview.root` приводятся к `/` (`lib.rs:547`, `lib.rs:1016`); crates/CLAUDE.md: IPC-пути — `/` на всех платформах · Repository ▸ Scan Folder for Repositories… на Windows — корни в диалоге и в `scan.chosen` с обратными слешами, строковое сравнение с корнями списка не совпадает · `found.path.to_string_lossy().replace('\\', "/")`

BE-021 · inconsistency · low · doc/04-ipc-contract.md:161 · контракт: «`avatars` … заодно этот вызов задаёт окно очереди»; в коде окно задаёт отдельная `avatar_window` (`src-tauri/src/commands/avatars.rs:10`), `avatars` только читает кэш, а `avatar_window` в таблице §«Аватары» нет · сравнить §«Аватары» с `commands/avatars.rs` · описать `avatar_window`, у `avatars` убрать про окно очереди

BE-022 · bug · low · src-tauri/src/lib.rs:398 · Help ▸ Copy Diagnostics (и её клавиша из обработчика WebView2, `src-tauri/src/webview2.rs:69`) выполняется на главном потоке: `diagnostics::report` читает весь текущий лог (`src-tauri/src/diagnostics.rs:24`, до 10 МБ) и режет его на строки — дольше 50 мс на большом логе или под антивирусом, окно замирает · долгая сессия с логом ~10 МБ, Help ▸ Copy Diagnostics · собирать текст в отдельном потоке (хвост читать с конца файла), в буфер обмена — по готовности

BE-023 · inconsistency · low · src-tauri/src/commands/mod.rs:565 · INV-09 («тело команды — достать состояние, вызвать метод крейта, смапить ошибку»): в слое Tauri живут политика троттлинга прогресса `load_commits` (`PROGRESS_EVERY`, `mod.rs:543-571`), протокол отмены и подсчёт `search_file_contents` (`mod.rs:235-262`), нарезка на чанки `investigate_log`/`investigate_blame` (`src-tauri/src/commands/investigate.rs:52`, `investigate.rs:88`), сборка путей `move_to_trash` (`src-tauri/src/commands/desktop.rs:86`), экспорт и запуск `open_read_only` (`src-tauri/src/commands/file_ops.rs:130`) — без тестов вне GUI · прочитать тела — 20–40 строк логики · перенести в `app_state` функции с замыканием-приёмником (как `build_graph`), в команде оставить вызов

BE-024 · split · low · src-tauri/src/commands/mod.rs:1 · 1524 строки и после fddd068 («split the command module by panel»): ~80 команд независимых областей — граф (`load_commits`…`graph_overlay`), репозитории и конфиг (`open_repository`, `repositories`, `close_repository`, `scan_for_repositories`, `read/write_git_config`, `repository_health`), staging и commit, blame и окна, rebase/rollback/split, настройки, клавиатура, лог и память вебвью · — · вынести `graph.rs`, `repositories.rs`, `staging.rs`, `history.rs`, `app.rs` (app_info, settings, keymap, report_*), `windows.rs`; `blocking`/`mutating` оставить в `mod.rs`

BE-025 · split · low · crates/app_state/src/lib.rs:271 · 1524 строки: реестр и открытие репозиториев, журнал команд (`command_log`, `command_sink`, `record`), кэш строк дерева (`RowCache`, `overviews`), staging/branches/tags, наблюдатель, конфиг, submodules, blame/conflicts/investigate, `Steps` — независимые части в одном файле, хотя stash/flow/rewrite уже вынесены (93b9bc4) · — · вынести `journal.rs`, `rows.rs`, `staging.rs` (stage/discard/commit/checkout/branches/tags), `watching.rs`

## Проход 2

Оптика: пограничные состояния (конфликт, пауза rebase, пустой репозиторий, закрытие и повторное открытие, выход с открытыми окнами), пути Undo каждой записи журнала безопасности, свежий код 24.09 (ccb4227, c0e99dd, 51c491e, f972796), кэш аватаров. Поведение git проверено консольным git в `D:\cogit-work\tmp-backend\r1…r6`; поведение Tauri — по исходникам tauri 2.11.5, tauri-macros 2.6.3, tauri-runtime-wry 2.11.4.

BE-026 · bug · medium · crates/app_state/src/ref_ops.rs:25 · Reset Hard, Discard и Roll back невозможны, пока в индексе есть конфликт: резервный `git stash push` (ref_ops.rs:25 → `crates/git_engine/src/stash.rs:152`; `lib.rs:638` и `rewrite.rs:38` → `stash.rs:53`) при любой unmerged-записи падает «error: could not write index / f: needs merge», даже с pathspec на неконфликтный файл; у Reset ошибка stash выходит сырой через `?`, а не через `backup_failed`, и главный выход из неудачного слияния — Reset Hard на HEAD — не выполняется; R-284 описывает отказ только для репозитория без коммитов и для длинных списков · Merge ветки, где один файл конфликтует, а второй слился сам (staged > 0), → Graph ▸ Reset Advanced… ▸ Hard на HEAD (`frontend/src/components/menus/RefActions.svelte:612`) → «Could not reset: git stash push failed with exit code 1», ничего не сброшено; то же при остановленном rebase. Консольный git (r1: `UU f`, `M  g`): `stash push` и `stash push -- g` → exit 1 · при `status.conflicted > 0` строить копию для Undo без stash — коммит дерева через временный индекс (`GIT_INDEX_FILE=<tmp>` + `add -A` + `write-tree` + `commit-tree`) — или идти без копии с `Recovery::None` после подтверждения, которое говорит, что Undo не будет; дописать случай в R-284

BE-027 · bug · medium · crates/app_state/src/safety.rs:113 · Undo операции, остановившейся на конфликте (запись `Moved` с ccb4227, `rewrite.rs:245`), пока операция не закончена, всегда падает: при слиянии `move_branch_back` → `reset --keep` (`crates/git_engine/src/branches.rs:38`) — «fatal: Cannot do a keep reset in the middle of a merge.», при rebase HEAD отсоединён → `branch --force` (`branches.rs:40`) — «fatal: cannot force update the branch 'topic' used by worktree at …»; кнопка Undo в тулбаре при этом активна с подписью «Undo Merge side» (`frontend/src/App.svelte:488`, `:1271`), то есть «отменить слияние посреди конфликта» — естественный путь, и он кончается сырым отказом git · Merge `side` с конфликтом → Undo → «Could not undo: … Cannot do a keep reset in the middle of a merge.»; Rebase с конфликтом или Rebase Commits… с паузой → Undo → «cannot force update the branch». Консольный git: r1 (`reset --keep HEAD` во время merge → exit 128), r3 (`branch --force topic <oid>` во время rebase → exit 128) · в `reverse` для `Moved`: если `handle.state()` — прерванная операция, сначала `abort_operation()` (он и возвращает ветку), затем `move_branch_back`; либо отказывать своим сообщением «сначала завершите или прервите операцию»

BE-028 · bug · medium · crates/app_state/src/rewrite.rs:38 · Undo отката (Roll back) не срабатывает никогда: копия есть только когда у путей были незакоммиченные правки, `stash_paths` уносит их, `rollback_to` (:41) оставляет те же пути изменёнными (версия из `rev`), и Undo (`safety.rs:111` → `stash apply --index`, затем `stash apply`, `crates/git_engine/src/stash.rs:74-78`) отказывает «Your local changes to the following files would be overwritten by merge»; без правок копии нет и запись пишется без Undo; тестов на Undo отката нет (`crates/app_state/tests` — только `protected.rs:76`) · правка a.txt → в Files коммита Roll back a.txt (`frontend/src/App.svelte:1774`) → Undo → «Could not undo», в журнале запись осталась. Консольный git (r4: `stash push -- a.txt`, `restore --source=HEAD~1 -- a.txt`, обе попытки apply → exit 1) · хранить в записи пути отката и в `reverse` сначала вернуть их к HEAD (`git restore --source=HEAD --worktree -- <пути>`, для всего дерева — `.`), затем применить stash; тест «правка → Roll back → Undo → файл как был»

BE-029 · bug · medium · crates/app_state/src/stashing.rs:66 · Undo «Drop stash@{n}» не возвращает запись в список, а применяет выброшенный stash к рабочему дереву: `Recovery::Stash { oid }` (:72) → `stash_apply(oid)` (`safety.rs:111`) — тот же путь, что у Discard; список остаётся без записи, в дереве появляются правки stash (трёхстороннее слияние, при другой базе — конфликт), хотя `crates/git_engine/src/stash.rs:163` обещает «so Undo can put the entry back», а DoD M5 — «случайно удалённый стэш восстанавливается через Undo» (`doc/modules/M5-references.md:39`); теста нет · References ▸ stash ▸ Drop → Undo → stash в списке не появился, файлы рабочего дерева изменились · своя `Recovery::DroppedStash { oid, message, index }`: `git stash store --message <message> <oid>` и перестановка на прежнее место, как в `rename_stash` (R-252); тест drop → Undo → `stashes()` совпадает с исходным, `status` чистый

BE-030 · bug · medium · crates/app_state/src/ref_ops.rs:43 · Undo «Reset <ветка> from X to Y (hard)» применяет stash, снятый на X, поверх Y и ветку не возвращает: `before` прочитан и попал только в подпись, в записи — один stash (для soft/mixed/keep/merge — `Recovery::None`); если выброшенные правки лежат в строках, которые меняли ушедшие коммиты, Undo оставляет в файле маркеры конфликта и падает; INV-12 (`doc/01-architecture.md:256`) и DoD M5 (`M5-references.md:80`: «состояние совпадает с исходным по OID всех ссылок и по status») этого не допускают, R-253 решил иначе без правки инварианта; тест `a_hard_reset_keeps_the_changes_it_throws_away_for_undo` (`tests/undo.rs:382`) правит файл, который ушедшие коммиты не трогали · c1: f=v1, c2: f=v2, правка f → Reset Hard на c1 → Undo → ветка на c1, в f `<<<<<<< Updated upstream` / `v1` / `v2 work`. Консольный git (r6) · записывать вместе `Moved { name, oid: before }` и stash: в `reverse` `reset --hard before` (дерево после hard reset чистое), затем `stash apply`; для soft/mixed — `Moved`

BE-031 · bug · low · crates/app_state/src/lib.rs:739 · Undo удаления ветки теряет её upstream: `git branch -d` удаляет секцию `branch.<name>` конфига, `Recovery::Branch` хранит только имя и oid, Undo делает `git branch <name> <oid>` (`safety.rs:112`) — у восстановленной ветки нет отслеживания, ahead/behind и Pull для неё пропадают; upstream при этом уже прочитан (`branches()` на `lib.rs:730` даёт `Branch.upstream`) · Branches ▸ ветка с upstream ▸ Delete → Undo → ветка без upstream. Консольный git (r5: после `branch -d topic` и `branch topic <oid>` `git config --get-regexp ^branch\.topic` пусто) · хранить `upstream` в `Recovery::Branch` и после создания звать `set_upstream(name, Some(upstream))`, если remote-tracking ref ещё есть

BE-032 · ux · medium · src-tauri/src/lib.rs:277 · Закрытие главного окна (крестик, Repository ▸ Exit, Alt+X — `close_this_window`, `frontend/src/App.svelte:748-757`) при открытом дочернем окне (Blame, Compare, Merge, Investigate) не завершает приложение: tauri-runtime-wry 2.11.4 (`src/lib.rs:4310-4323`) шлёт `ExitRequested` только после уничтожения последнего окна, а `on_window_event` смотрит лишь на `CloseRequested` главного; процесс живёт с дочерним окном без главного, `RunEvent::Exit` (`lib.rs:350`: `children::stop_all`, «cogit stopped») и `shutdown::exiting` не выполняются, удержание сеанса Windows ушло вместе с окном `main` · открыть Blame любого файла, в главном окне Alt+X → Exit — главное окно исчезло, окно Blame и `cogit.exe` остались; следующий запуск — второй процесс · по `WindowEvent::Destroyed` окна `main` звать `app.exit(0)` (или закрывать остальные `webview_windows()` перед его уничтожением)

BE-033 · race · low · crates/app_state/src/lib.rs:998 · `close_repository` не ждёт и не передаёт полосу: мутация закрываемого id идёт дальше со своим `RepoHandle`, а повторное открытие того же пути регистрирует новый `RepoId` (`find_or_register`, `lib.rs:1287`) с пустой полосой (`queue.rs:77`) — запись нового id идёт параллельно с ещё идущей записью старого в тот же репозиторий; вариант CC-007 (полоса по `RepoId`) · Commit с медленным pre-commit хуком (git уже держит `index.lock`) → Ctrl+W → снова открыть репозиторий → Stage → «fatal: Unable to create '…/.git/index.lock': File exists» · ключ полосы — `common_dir` репозитория, а не `RepoId`; либо закрытие ждёт полосу, либо новая регистрация того же корня наследует занятую полосу

BE-034 · bug · low · crates/avatars/src/cache.rs:200 · `save()` переписывает весь `index.json` после каждого результата (`store` :144, `store_missing` :159, истёкший `lookup` :111) из четырёх потоков очереди (`queue.rs:126-129`) одновременно, без блокировки на записи и не атомарно (`std::fs::write` — обрезка и запись): снимок, снятый раньше, может лечь последним и выбросить свежие записи, а при пересечении двух записей, где последней ложится более короткая, в файле остаётся хвост — `read_index` (:221) его не разбирает и начинает кэш заново, PNG вне индекса `evict` (:175) больше не видит, и каталог растёт за 64 МиБ; перезапись всего индекса на каждый адрес — квадратичный объём (3 000 авторов по ~130 байт — около 0,6 ГБ записи за одну прокрутку) · большой репозиторий с тысячами авторов, аватары включены, пролистать граф: Process Monitor показывает перезапись `index.json` на каждый ответ; после неудачного пересечения в логе «avatar cache index unreadable, starting over» · писать под отдельным мьютексом через временный файл и `rename`, а на результат только помечать индекс грязным и сбрасывать по простою очереди, `drain` и `Drop` (как `touched`)

BE-035 · inconsistency · low · src-tauri/src/commands/mod.rs:782 · `#[tauri::command(async)]` на обычной `fn` выполняет тело не в пуле блокирующих потоков, а на воркере tokio, который несёт IPC (tauri-macros 2.6.3 `wrapper.rs:378` → tauri 2.11.5 `ipc/mod.rs:375`, `async_runtime::spawn`): `command_log` клонирует и сериализует весь журнал (до 100 записей по 2+2 МБ), `command_outcome` — одну запись, `read_settings`/`write_setting` (`mod.rs:385`, `:391`) — чтение, правку, запись и `rename` settings.json под `parking_lot::Mutex` (`crates/app_state/src/settings.rs:55`); комментарий `commands/tests.rs:24` («hands that one to the thread pool») и подсказка теста `tests.rs:231` советуют этот атрибут для тяжёлого, а `tests.rs:172` пишет обратное · Output открыт, в журнале десятки больших записей, идёт fetch: каждый `command-recorded` перечитывает журнал на async-воркере, ответы других команд ждут · `async fn` + `blocking(...)` для этих четырёх; поправить комментарий и подсказку в `tests.rs`

BE-036 · bug · low · crates/app_state/src/lib.rs:503 · Открытые из дерева submodules и worktrees (`open_submodule`, `open_worktree` → `open_with` с `listed = false`) никто не закрывает: фронтенд зовёт `close_repository` только для записи списка или текущего (`frontend/src/App.svelte:2646`, `:2870`), `comeBack` (`frontend/src/stores/repository.svelte.ts:196`) уходит без закрытия, закрытие владельца их не трогает; каждый посещённый остаётся зарегистрированным до выхода со своим рекурсивным `RepoWatcher` (поток дебаунсера и поток ReadDirectoryChangesW) над поддеревом, которое уже слушает наблюдатель владельца, с `SharedRepo` в `HandleCache`; правка файла в submodule — два `repo-changed` · обойти двойным кликом 20 submodules и вернуться к родителю — у процесса +40 потоков наблюдателей, `repositories_held()` растёт · закрывать не-listed репозиторий при уходе из него и при закрытии владельца (владелец — поле `OpenRepo`)

BE-037 · simplify · low · crates/app_state/src/lib.rs:730 · Чтобы узнать oid одной ветки, `delete_branch` зовёт `branches()`, который для каждой локальной ветки с upstream считает ahead/behind (merge-base и два обхода, `crates/git_engine/src/repo.rs:181-230`); `delete_merged_branches` (`network.rs:75`) повторяет это на каждую удаляемую ветку, занимая полосу, — N×M расчётов расхождения; так же `flow_finish` (`flow.rs:52`) и `overview_of` (`lib.rs:1032`), которому нужен только ahead/behind HEAD · репозиторий с сотней веток, Pull с «Delete merged branches» — после pull полоса занята на N полных `branches()` · oid ветки — `find_reference("refs/heads/<name>")` + peel (тег того же имени не мешает), для строки дерева — расхождение одной HEAD, как `track()` в `crates/git_engine/src/pulse.rs:146`

BE-038 · ux · low · src-tauri/src/child_window.rs:84 · Дочерние окна строятся без `.theme(...)`: у главного `"theme": "Dark"` (`src-tauri/tauri.conf.json:22`), а заголовок Compare, Merge, Blame и Investigate следует теме Windows — в светлом режиме ОС белая полоса заголовка над тёмным `BACKGROUND` (:20) рядом с тёмным главным; тема приложения (одна из четырёх) не учитывается ни там, ни там, хотя CLAUDE.md требует для дочерних окон фон темы · Windows в светлом режиме → Compare with… — светлый заголовок у окна с тёмным содержимым · `.theme(Some(tauri::Theme::Dark))`, как у главного, а лучше передавать тему и фон из страницы в `open_*_window`

BE-039 · ux · low · crates/app_state/src/safety.rs:111 · Копия для Undo — обычная запись списка стэшей, а не «hidden stash», как пишет `lib.rs:622`: `stash push` (`crates/git_engine/src/stash.rs:49`, `:139`) кладёт «cogit: discard …», «cogit: before hard reset …», «cogit: before rollback …» в `stash@{0}`, сдвигая стэши пользователя, а Undo делает `stash apply` и запись не снимает — после Undo та же правка остаётся в Stashes, и её повторный Apply или `git stash pop` в терминале вносит её второй раз · Discard файла → Undo → в References ▸ Stashes осталась «cogit: discard a.txt»; `git stash pop` в терминале берёт её, а не стэш пользователя · после успешного `reverse` снимать эту запись (`stash drop` по её oid в reflog stash) или держать копию вне `refs/stash` (свой ref `refs/cogit/backup/<id>`); поправить комментарий

BE-040 · bug · low · src-tauri/src/shutdown.rs:87 · Сторож закрытия выходит через 2 с молчания страницы, не глядя на очередь: `app.exit(0)` → `RunEvent::Exit` → `children::stop_all` (`src-tauri/src/lib.rs:351` → `crates/git_engine/src/children.rs:105`, `taskkill /T /F`) — идущие commit, rebase, stash или pull убиваются посреди работы, хотя обычный путь закрытия спрашивает про операции (`exitFlow.ask(…, listOperations)`, `frontend/src/App.svelte:2897`); остаются `index.lock` и недоделанный rebase · длинная мутация (rebase с медленным хуком) и занятый рендерер → крестик → через 2 с приложение закрыто, в репозитории `index.lock` или состояние rebase · при непустой очереди (`session_end_blocker()`) по таймеру не выходить: спрятать окно и выйти, когда полосы опустеют (`Operation` с `Done`), убивать только чтения
