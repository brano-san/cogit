# Аудит: git_engine

Дата: 25.09.2026. Просмотрено целиком: `crates/git_engine/src/` — ancestry, apply, blame, blame_origins, blobs, branches, bypass, children, commit, commit_write, config_file, conflicts, discover, error, file_log, file_ops, find, flow, gitlink, health, history, hook_run, hooks, interactive, lfs, lib, line_history, line_match, listing, mailmap, maintenance, merging, module_ops, network, operations, origin_search, outcome, output_text, overlap, phases, presets, progress, published, pulse, ref_meta, reflog, replay, repo, repo_settings, reset, runner, search, shared, staging, stash, stash_rename, state, status, submodules, subtrees, surgery, tags, worktree, worktrees, bin/probe-hook, bin/probe-status (без graph_walk.rs и topo.rs). Для трасс — вызывающие места в `crates/app_state/src/{network,safety,repo_rows,worktrees,rewrite,lib}.rs` и фронтенде, исходники gix 0.87.1 и std Rust 1.98.1. Опыты — консольный git 2.51.0.windows.1 в `D:\cogit-work\tmp-git-engine\`.

## Проход 1

GE-001 · bug · high · crates/git_engine/src/network.rs:217 · Токен хоста remote передаётся глобальным `-c http.extraHeader=Authorization: Basic …` на всю команду; git передаёт `-c` дочерним git через GIT_CONFIG_PARAMETERS, поэтому fetch подмодулей (fetch.recurseSubmodules=on-demand по умолчанию, pull, submodule update) шлёт тот же заголовок с токеном на хост подмодуля; кроме того, `redact_arg` (runner.rs:362) маскирует только ключ ровно `http.extraheader`, так что URL-ограниченный ключ не маскировался бы · Репозиторий с https-remote github.com и сохранённым токеном, подмодуль на другом https-хосте → Fetch → network.rs:31-34 → `git -c http.extraHeader=… fetch --progress --prune origin` → fetch в подмодуле получает конфиг. Проверено: `git -c remote.origin.uploadpack=<скрипт> fetch --recurse-submodules=yes` — скрипт вызван из `.git/modules/sub`, т.е. `-c` дошёл до fetch подмодуля · Ограничить заголовок URL-ом remote: `-c http.<scheme>://<host>/.extraHeader=…` (как actions/checkout); в `redact_arg` маскировать любой ключ, оканчивающийся на `.extraheader`, без учёта регистра
GE-002 · bug · high · crates/git_engine/src/hook_run.rs:94 · Dry run хука и команда проверки (T10.3, T11.3) запускают `Command::new("bash")`: std Rust на Windows ищет программу в System32 и Windows до PATH родителя, а Git for Windows по умолчанию кладёт в PATH только `Git\cmd`, где bash нет, — находится WSL-bash (`System32\bash.exe` или алиас `WindowsApps\bash.exe`) либо ничего. Точная причина пункта GE-2-17 из doc/refactor-night-2.md («вероятно, без воспроизведения»): на этой машине `where.exe bash` → `C:\Users\brano\AppData\Local\Microsoft\WindowsApps\bash.exe`; тесты проходят, потому что запускаются из Git Bash с `/usr/bin` первым в PATH · Cogit из меню Пуск на машине с WSL → Hooks ▸ Run `pre-commit` → hook_run.rs:94 → bash WSL получает путь `C:\…\.git\hooks\pre-commit` → «No such file» или исполнение в дистрибутиве; `run_check` (hook_run.rs:153, `bash -lc`) выполняет команду внутри WSL · Искать bash рядом с git: от `git --exec-path` (`…/mingw64/libexec/git-core` → `<корень Git>/bin/bash.exe`) тем же способом, что `app_state::terminal::bash_of`, и запускать по абсолютному пути
GE-003 · bug · medium · crates/git_engine/src/stash.rs:76 · `stash_apply` после любой ошибки `stash apply --index` повторяет `stash apply` и возвращает ошибку второй попытки, а первую не логирует: если первая применила stash с конфликтом (exit 1, маркеры записаны, в индексе unmerged), вторая падает с «error: could not write index / f: needs merge», и вывод `CONFLICT (content)…` в ошибку не попадает (INV-05) · Discard файла (app_state/src/lib.rs:639 делает stash) → правка того же места и коммит → Undo (app_state/src/safety.rs:111 → stash.rs:74-78). Проверено консольным git: первая попытка «CONFLICT… Index was not unstashed.», вторая «could not write index … needs merge», конфликт остаётся в дереве · Повторять без `--index` только при отказе, который ничего не менял (stderr «Conflicts in index. Try without --index.» или отсутствие unmerged-записей после первой попытки); иначе вернуть первую ошибку
GE-004 · bug · medium · crates/git_engine/src/staging.rs:20 · Unstage до первого коммита идёт через `git rm --cached -r`, который отказывается снимать файл, изменённый после add: «the following file has staged content different from both the file and the HEAD … (use -f to force removal)» · Новый репозиторий: создать a.txt, Stage, дописать строку, Unstage → staging.rs:19-20 → GitCommandError (проверено консольным git) · `rm --cached -r -f --` (с `--cached` файл на диске не трогается) или `git reset -q -- <paths>`, который на unborn-ветке работает (проверено)
GE-005 · bug · medium · crates/git_engine/src/staging.rs:22 · Staged-переименование приходит одной строкой (`path` — новое имя, `old_path` — старое, worktree.rs:200-217), а unstage и `commit --only` получают только новые пути: `restore --staged -- new` снимает добавление, удаление старого остаётся в индексе; «Unstage all» (frontend/src/components/menus/RefActions.svelte:769 `worktree.staged.map((file) => file.path)`) оставляет `D old`, коммит выбранного переименования записывает копию и оставляет удаление незакоммиченным · `git mv old.txt new.txt` → Unstage строки `R old.txt → new.txt` → в Staged остаётся `D old.txt` (проверено: после `restore --staged -- new.txt` статус `D old.txt`, `?? new.txt`) · В `unstage` и в `commit` с `only` добавлять `old_path` каждой staged-записи Renamed, чей `path` в списке (из `worktree_files().staged`), либо передавать пары с фронтенда
GE-006 · bug · medium · crates/git_engine/src/output_text.rs:128 · `redact_assignment` заменяет хвост любой строки вида `…=…`, если левее первого `=` встречается token/password/secret/apikey, — это не учётные данные в URL или заголовке (единственное исключение CLAUDE.md и INV-05), а обычный вывод; маска пишется и в лог (runner.rs:65-69), текст теряется везде. Так же `header_value_at` (output_text.rs:114) режет всё после слова `authorization:` в любой фразе · Push новой ветки в GitLab-проект `group/api-token-service`: строка `remote:   https://gitlab.com/group/api-token-service/-/merge_requests/new?merge_request%5Bsource_branch%5D=feature` становится `…%5D=***` — ссылки на создание MR, ради которой INV-05, нет ни в окне, ни в логе · Убрать `redact_assignment` из `normalise` или применять только к строкам дампа окружения (`^\s*(export\s+)?[A-Z_][A-Z0-9_]*=`); `header_value_at` — только к строкам, начинающимся с `Authorization:` после префикса `remote:`
GE-007 · bug · medium · crates/git_engine/src/runner.rs:379 · URL с учётными данными без двоеточия (`https://<token>@github.com/…` — обычная форма токена GitHub) не маскируется: `redact_url` и `redact_urls` (output_text.rs:99-103) требуют `user:secret@` и возвращают адрес как есть; сам git в сообщениях убирает весь userinfo · Remote ▸ Subtree ▸ Push с repository `https://ghp_xxx@github.com/org/lib.git` → subtrees.rs:105-119 → токен в команде журнала, логе и окне ошибки; `git subtree push` к тому же печатает `git push using: <url>`; так же `submodule add -- <url>` (module_ops.rs:81) · Маскировать весь userinfo без двоеточия (`https://***@host`), как git `transport_anonymize_url`, в обеих функциях
GE-008 · bug · medium · crates/git_engine/src/network.rs:69 · Force push идёт `--force-with-lease` без ожидаемого значения и без `--force-if-includes`, а фоновый fetch самого Cogit (`background_fetch`, pulse.rs:58-68, `fetch --all`) сдвигает remote-tracking refs: lease сверяется с только что полученной вершиной, и push затирает коммиты, которых пользователь не видел; комментарий network.rs:68 обещает обратное · Preferences ▸ Background fetch каждые N минут (frontend/src/App.svelte:2941 → stores/repo-pulse.svelte.ts:133) → коллега пушит в ветку → фоновый fetch обновил origin/<ветка> → Push с force → коммиты коллеги потеряны на сервере · Добавить `--force-if-includes` (git ≥ 2.30) рядом с `--force-with-lease` либо передавать явный `--force-with-lease=<ref>:<oid, который видел пользователь>`
GE-009 · bug · medium · crates/git_engine/src/merging.rs:33 · Merge и Rebase получают короткое имя ветки и отдают его git как ревизию: при ветке и теге с одним именем git берёт тег (refs/tags раньше refs/heads) — «Merge feature» сливает тег, выводя лишь warning · Ветка `feature` с новым коммитом и тег `feature` на старом → меню ветки ▸ Merge → frontend/src/components/menus/RefActions.svelte:372 `source: at.ref?.name` → merging.rs:33 → `git merge --no-edit feature` → «warning: refname 'feature' is ambiguous… Already up to date.» (проверено консольным git); то же для Rebase (RefActions.svelte:382, App.svelte:1458 → operations.rs:29) · Передавать полное имя (`Branch.fullName`: `refs/heads/…`, `refs/remotes/…`); в сообщение слияния git сам пишет короткое
GE-010 · bug · medium · crates/git_engine/src/conflicts.rs:96 · «Resolve with Ours/Theirs/Base» пишет в рабочее дерево сырой blob стадии мимо smudge- и eol-фильтров: для файла под LFS на диске оказывается текст pointer-а вместо содержимого (clean-фильтр пропускает pointer как есть, индекс верный), при `core.autocrlf=true` (умолчание Git for Windows) файл получает LF, и каждый следующий `git add` даёт warning «LF will be replaced by CRLF» в уведомления · Конфликт в `*.psd` под LFS → Resolve with Theirs → conflicts.rs:80-84 → :96 → файл на диске — 130 байт `version https://git-lfs…` · Для ours/theirs — `git checkout --ours|--theirs -- <path>`, для base — `git checkout-index --stage=1 -f -- <path>` (оба применяют фильтры), затем `git add -- <path>`
GE-011 · bug · medium · crates/git_engine/src/blobs.rs:164 · Сторона рабочего дерева читается `std::fs::read`, который идёт по символической ссылке: для symlink (Linux, macOS, Windows с core.symlinks=true) diff сравнивает цель ссылки из индекса с содержимым файла-цели, висячая ссылка выглядит удалённой (NotFound → None); `write_worktree_text` (file_ops.rs:124) и `text_lines` для рабочего дерева (blame_origins.rs:116) так же пишут и читают через ссылку — правка стороны дерева в Index Editor меняет файл-цель, возможно вне репозитория · `ln -s README.md link`, коммит, `ln -sf CHANGELOG.md link` → diff строки `link` (mode Symlink) показывает «README.md» против всего текста CHANGELOG.md · Сначала `symlink_metadata`; для ссылки — `std::fs::read_link` и путь цели байтами с `/`, как git хранит blob 120000; запись в ссылку — отказом или заменой самой ссылки
GE-012 · ux · medium · crates/git_engine/src/file_ops.rs:51 · Rename/Move, меняющий только регистр имени, отказывает «already exists»: на регистронезависимой ФС (Windows, macOS) `symlink_metadata(root/"README.md")` находит сам `Readme.md`, хотя `git mv` такое переименование умеет · Files ▸ Rename `Readme.md` → `README.md` → app_state/src/file_actions.rs:33 → file_ops.rs:46-52 → InvalidState · Пропускать проверку, когда `from.eq_ignore_ascii_case(to)` (или цель — тот же файл); неотслеживаемый файл переименовывать через временное имя
GE-013 · bug · medium · crates/git_engine/src/worktrees.rs:294 · `worktrees()` для каждой записи, включая главную и текущую, открывает репозиторий и вызывает `has_changes()` — полный обход статуса; `worktree_holding` (T3.8) вызывается перед каждым переключением ветки и ради одного имени ветки платит N+1 обходов, даже без связанных worktree; так же `merged_gone_branches` (ancestry.rs:46) и `linked_handle` (worktrees.rs:223) · Двойной клик по ветке → frontend/src/App.svelte:1210 `worktrees.holding` → worktrees.rs:96-100 → describe(main) → has_changes по всему дереву большого репозитория до начала `git switch` · Лёгкий список без `dirty` (ветка из HEAD записи, как `recorded_head`) для `worktree_holding`, `merged_gone_branches`, `linked_handle`; `dirty` — только для панели Worktrees
GE-014 · bug · medium · crates/git_engine/src/health.rs:139 · Путь из `.git/worktrees/<id>/gitdir` проверяется `Path::new(&target).exists()` относительно текущего каталога процесса, а при `worktree.useRelativePaths` (git ≥ 2.48) он относителен каталогу записи — каждый такой worktree попадает в отчёт как DanglingWorktree; gix (`Proxy::base`) читает его правильно · `git -c worktree.useRelativePaths=true worktree add ../wt` → в gitdir `../../../../wt/.git` (проверено, git 2.51) → health_report → ложное предупреждение о пропавшем worktree · Относительный `target` разрешать от каталога записи (`admin.join(target)`), как `gix_discover::path::from_plain_file_relative_to_file`
GE-015 · bug · low · crates/git_engine/src/pulse.rs:73 · `remote_heads` разбирает stdout записи журнала (`run_git_with_env` → `GitOutput::record` обрезает до 20 000 строк и маскирует, runner.rs:65-71) вопреки правилу R-280 «разбираемый вывод — через read_git», и `ls-remote --heads <remote>` тянет все ветки сервера ради одной · Remote с более чем 20 000 ветками: pull_probe (pulse.rs:87-126) не находит merge-ветку из выкинутой середины → `None`, индикатор pull молчит · `ls-remote --heads <remote> <merge-ref>` (протокол v2 фильтрует на сервере) через вызов в духе `read_git` с окружением QUIET
GE-016 · ux · low · crates/git_engine/src/find.rs:54 · Find Object (Ctrl+P) считает запрос из четырёх и более hex-символов хешем и ищет только по префиксу OID: слова «added», «cafe», «decade», «feed», «fade» по сообщениям не ищутся, а поиск префикса проходит всю историю с чтением текста каждого коммита — `search_commits` не остановится, пока не наберёт `limit` · Ctrl+P → «added» → ни одного коммита, хотя «Added …» в половине сообщений · Для hex-запроса искать и по сообщению, а коммит по префиксу получать одним `rev_parse_single`, без обхода
GE-017 · ux · low · crates/git_engine/src/find.rs:101 · `head_paths` берёт все записи обхода дерева HEAD, включая каталоги и gitlink-и, и отдаёт их как FoundKind::File «in the current tree»; `tree_files` (listing.rs:147) каталоги отфильтровывает · Ctrl+P → «src» → среди результатов «файл» `src`, который не открыть · Фильтровать `entry.mode.is_tree()` (и `is_commit()`), как `tree_files`
GE-018 · bug · low · crates/git_engine/src/branches.rs:94 · Удаление удалённой ветки шлёт `push --delete <remote> <короткое имя>`: если на сервере есть ветка и тег с этим именем, git отказывает «dst refspec topic matches more than one» · Сервер с `refs/heads/topic` и `refs/tags/topic` → Branches ▸ origin/topic ▸ Delete → branches.rs:93-94 → ошибка (проверено консольным git) · Отдавать `refs/heads/<short>`, точнее — имя из обратного отображения fetch-refspec (`upstream_branch_and_remote_for_tracking_branch`)
GE-019 · bug · low · crates/git_engine/src/maintenance.rs:23 · После каждого коммита запускается `git maintenance run --auto --no-quiet --detach|--no-detach`, но `--[no-]detach` у `maintenance run` есть только с git 2.47; на более старом git (Ubuntu 24.04 — 2.43, Debian 12 — 2.39) команда падает с «unknown option» (exit 129), и запись с ошибкой уходит в журнал после каждого коммита · Linux со штатным git 2.43 → Commit → commit_write.rs:49 → maintenance.rs:8-60 → уведомление об ошибке `git maintenance run …` · Флаг добавлять только при git ≥ 2.47 (версия из `git_version()` один раз за процесс), иначе `maintenance run --auto --no-quiet`, как git до 2.47 делал сам
GE-020 · inconsistency · low · crates/git_engine/src/bypass.rs:54 · Журнал обходов хуков пишется в `git_dir()`, который у связанного worktree свой (`.git/worktrees/<id>`): F-094 обещает журнал клона и сводку по репозиторию, а коммиты с No verify из worktree не видны в сводке главного и пропадают при `worktree remove/prune` · Коммит с No verify в связанном worktree → Hooks в главном: счётчик не изменился; удалить worktree — запись исчезла · `common_dir()` в `bypass_log` и `record_bypass` (bypass.rs:22, 54)
GE-021 · bug · low · crates/git_engine/src/worktree.rs:226 · Файл после `git add -N` (intent-to-add) не попадает ни в Staged, ни в Unstaged: gix TreeIndex такие записи пропускает (gix-diff 0.67.1 index/function.rs:283), а `EntryStatus::IntentToAdd` превращается в `None`; `status()` и `has_changes()` (status.rs:58, 83) считают дерево чистым, хотя `git status` показывает «new file» среди неиндексированных · `git add -N new.txt` в терминале → в Files файла нет, строка репозитория «чистая», застейджить из Cogit нельзя · Показывать IntentToAdd как `FileStatus::Added` в unstaged и учитывать в `RepoStatus.unstaged` и `has_changes`
GE-022 · bug · low · crates/git_engine/src/hook_run.rs:85 · На Windows dry run выполняет хук как `bash <файл>`, игнорируя shebang: хук на Python или Node (`#!/usr/bin/env python3`) bash разбирает как свой скрипт и падает, хотя git запускает его по shebang нормально — dry run показывает не то, что сделает коммит · Хук commit-msg на Python → Hooks ▸ Run → hook_run.rs:83-88 → «syntax error near unexpected token» · Запускать `bash -c '"$0" "$@"' <path> <args…>` (exec в MSYS учитывает `#!`), как делает git
GE-023 · bug · low · crates/git_engine/src/pulse.rs:24 · `pulse` открывает строку Repositories через `RepoHandle::open`, т.е. с поиском вверх: папка из списка, у которой пропал `.git`, но которая лежит внутри другого репозитория (например, домашний каталог — dotfiles-репозиторий), получает ветку и «грязь» внешнего репозитория вместо `missing`, а `background_fetch` и `pull_probe` (app_state/src/repo_rows.rs:24, 37) работают во внешнем · Удалить `.git` у репозитория из списка, лежащего в `~` с dotfiles-репозиторием → строка показывает ветку dotfiles; при фоновом fetch — `git fetch --all` в `~` · Открывать ровно корень строки: `gix::open_opts(root, env_free())` (годится и для bare), без discovery
GE-024 · bug · low · crates/git_engine/src/shared.rs:82 · `SharedRepo` штампует только файлы, из которых конфиг прочитан, плюс собственные `config`, `config.worktree`, `HEAD`; глобальный (`~/.gitconfig`, `$XDG_CONFIG_HOME/git/config`) и системный конфиг, которых не было при открытии, не штампуются — появившись позже, они не делают снимок устаревшим, и чтения через gix (core.hooksPath, commit.template, mailmap.file, унаследованные значения Repository ▸ Settings) остаются старыми до смены HEAD · Машина без `~/.gitconfig`, репозиторий открыт → в терминале `git config --global core.hooksPath ~/hooks` → панель Hooks по-прежнему показывает `.git/hooks` · Добавить в `config_sources` пути глобального и системного конфига по правилам git (как `user_config_by_rules`) независимо от их наличия
GE-025 · bug · low · crates/git_engine/src/repo.rs:229 · `count_divergence` считает ahead/behind от одной merge-base: без общей базы (история upstream переписана filter-repo или BFG, orphan-ветка) возвращает `None`, и ветка показывает 0/0 «в синхроне», а pulse — `tracked=false`; при нескольких базах (criss-cross) коммиты второй базы считаются в обе стороны · Переписать историю на сервере и force push, Fetch → Branches: main 0↑ 0↓, а `git status` говорит «have diverged, 120 and 118 commits» · Два обхода без merge-base: `rev_walk([local]).with_hidden([upstream])` и наоборот, как `rev-list --left-right --count`
GE-026 · ux · low · crates/git_engine/src/conflicts.rs:83 · В конфликте modify/delete сторона, где файл удалён, отсутствует, и «Resolve with Theirs/Ours» отвечает InvalidState «has no Theirs side», хотя выбор этой стороны означает удаление файла; нормальное состояние репозитория подаётся как ошибка (CLAUDE.md, Errors) · Ветка удалила файл, текущая изменила → merge → Resolve with Theirs → ошибка · Если выбранной стороны нет — разрешать удалением: `git rm -- <path>`
GE-027 · simplify · low · crates/git_engine/src/find.rs:33 · Find Object на каждый запрос вызывает `branches()`, который для каждой локальной ветки с upstream считает ahead/behind (merge-base и два обхода, repo.rs:181-185, 213), хотя поиску нужны только имена и OID; запросы не отменяются, устаревшие продолжают занимать blocking-пул · Репозиторий с сотнями разошедшихся веток → набор в Ctrl+P → каждый символ пересчитывает расхождение всех веток · Собирать имена и OID прямо из `references().local_branches()/remote_branches()`, без `fill_upstream`
