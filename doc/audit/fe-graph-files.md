# Аудит: фронт: Graph, Files, Diff-выбор

25.09.2026 · ветка `audit/2026-09-25` (a779a38) · просмотрено: `frontend/src/components/graph/{CommitList,GraphFilter,PauseCheckBar,RebaseEditor,RefCapsule}.svelte`, `components/file-list/{CommitBox,FileList,FilePane,FilesToolbar,IndexEditorDialog,RemoveFilesDialog,SplitOffDialog}.svelte`, `components/panels/{Graph,Files,Commit}Panel.svelte`, `components/panels/CommitDetailsPane.svelte`, `stores/{files-view,commit-tree,commit,worktree,finder,stash-view,stash-dialog,overlap}.svelte.ts`, `lib/{graph-row,graph-columns,graph-panel,file-actions,file-dialogs,file-menu,file-search,file-switches,file-view,files,commit-message,commit-scope,selection,multi-select,stage-all,content-search.svelte,query,skipped,split-off,rebase-plan,rewrite-plans,overlap,truncate,staleness,disk-change,mutation}.ts`; для контекста — проводка в `App.svelte` (Files, Commit, меню файлов, Find Object, палитра), `stores/graph.svelte.ts`, `lib/format.ts`, `components/common/{VirtualList,Checkbox,Select,Avatar}.svelte`, `components/layout/{FindObject,GraphField}.svelte`, `crates/git_engine/src/{staging,stash,commit_write,interactive,find,branches}.rs`, `crates/app_state/src/lib.rs` (`discard_paths`), `src-tauri/src/{accelerators,webview2}.rs`, `src-tauri/src/menu/mod.rs`, `doc/11-keybindings.md`, `doc/12-risks.md` R-99, R-210, R-270, R-300, R-310…R-316, R-330, R-331, R-370, `doc/modules/M8-tools.md` T8.2, `doc/features/F-018, F-101, F-103`. Опыты с консольным git — в `D:\cogit-work\tmp-fe-graph-files\` (FG-002, FG-004, FG-005).

## Проход 1

FG-001 · bug · high · frontend/src/components/graph/CommitList.svelte:579 · Капсулы меток строки ключуются `(label.text)`, а `refLabels` (lib/format.ts:106, 122) даёт одинаковый `text` локальной ветке и тегу с одним именем на одном коммите; Svelte 5.57 бросает `each_key_duplicate` и в production (node_modules/svelte/src/internal/client/dom/blocks/each.js:356-361), `svelte:boundary` в приложении нет — отрисовка строк графа ломается на этом коммите · `git branch v1.0 && git tag v1.0` на одном коммите → открыть репозиторий, прокрутить к коммиту → исключение в консоли, строки графа перестают обновляться · ключ `` `${label.kind}:${label.text}` ``; тест refLabels с веткой и тегом одного имени
FG-002 · bug · high · crates/app_state/src/lib.rs:639 · Discard (кнопка строки Unstaged, «Discard all», меню файла, тулбар) делает резервный `git stash push --include-untracked -- <пути>`, а stash с pathspec откатывает к HEAD и индекс этих путей: проиндексированные правки файла тоже исчезают (уходят в скрытый stash). doc/12-risks.md:3628 и :4062 обещают «Discard работает только с неиндексированными изменениями (`restore --worktree`)», staged-правки остаются · файл в состоянии MM → Discard в Unstaged → файла нет и в Staged, `git diff --cached` пуст (проверено консольным git: после `stash push -- f.txt` файл равен HEAD) · после резервного `stash push` вернуть индексную сторону: `git checkout <stash>^2 -- <пути с индексированными правками>` (индекс и дерево = прежний индекс, проверено); тест «discard оставляет staged-правки»; учесть счётчик процессов `mutation_speed` (R-24)
FG-003 · bug · medium · frontend/src/stores/worktree.svelte.ts:71 · `mutate` глотает ошибку записи в `worktree.error`, и App сообщает её как «Could not read the working tree» (App.svelte:417): отказ pre-commit-хука, ошибки stage/unstage/discard приходят с заголовком про чтение, а `runMutation` (lib/mutation.ts:26) считает шаг успешным. Обратное: `commitStaged` (App.svelte:1114) по тому же флагу считает неудачей сбой перечитывания после успешного коммита — сообщение и Amend остаются, повтор делает второй коммит или amend · `exit 1` в .git/hooks/pre-commit → Commit → уведомление «Could not read the working tree» · разделить ошибку записи и чтения: `mutate` пробрасывает ошибку записи (или хранит `writeError`), App сообщает её с «Could not commit / stage / discard», `commitStaged` смотрит результат записи, а не общий флаг
FG-004 · bug · medium · frontend/src/components/file-list/CommitBox.svelte:48 · Черновик сеется из `commit.template` целиком, а коммит идёт `git commit -m` (crates/git_engine/src/commit_write.rs:32), где очистка — `whitespace`: строки-комментарии шаблона (`# ...`) попадают в сообщение; `ready` (строка 25) разрешает коммит нетронутого шаблона. App.svelte:2847 обещает «the way `git commit` would», а git с редактором их вырезает · `git config commit.template t.txt` с `# Explain why` → дописать тему → Commit → `git log -1 --format=%B` содержит `# Explain why` (проверено) · при коммите убирать строки, совпадающие с комментариями шаблона (по `core.commentChar`), или показывать комментарии шаблона подсказкой, а не текстом поля; `--cleanup=strip` для всех не включать — он съест темы вида `#123 fix`
FG-005 · bug · medium · frontend/src/lib/commit-scope.ts:33 · «Commit What You See» передаёт в `--only` только `file.path`: у проиндексированного переименования старый путь не попадает, `git commit --only -- new` коммитит добавление, а удаление старого пути остаётся в индексе — переименование расщепляется · staged `git mv a.txt b.txt` и ещё один staged файл, фильтр скрывает второй → «Commit 1 shown» → в коммите только `b.txt`, `D a.txt` остаётся staged (проверено консольным git) · добавлять `file.oldPath` показанных строк `renamed` в `paths` и в список подтверждения; тест в commit-scope.test.ts
FG-006 · bug · medium · frontend/src/lib/file-search.ts:35 · Фильтр файлов проверяет выражение на склейке `имя путь Статус [старый путь]`, поэтому в режиме `*` (regex) якоря не работают: `\.ts$` упирается в «Modified», `^src/` — в имя файла, оба ничего не находят; в подстрочном режиме — ложные совпадения через стык частей · Files → `*` → ввести `\.svelte$` при изменённых .svelte → «Nothing matches the filter» · проверять шаблон по каждой части отдельно (`parts.some(test)`); тест с `^` и `$`
FG-007 · bug · medium · frontend/src/components/graph/CommitList.svelte:329 · Клавиатура берёт текущую строку из `graph.loadedIndexOf`, который видит только загруженные блоки (KEEP = 48 блоков по 128 с вытеснением дальних, stores/graph.svelte.ts:344-347; перезагрузка наследует только префикс, :252-257). Если блок выбранного коммита не загружен, `nextRow(null, "ArrowDown")` = 0 — выделение и прокрутка прыгают на первый коммит · на 50k: выбрать коммит, утащить ползунок на 10 000 строк (или дождаться перезагрузки графа после fetch, прокрутив список) → ↓ → выбран верхний коммит · при `null` от `loadedIndexOf` и непустом `selection.oid` брать `await graph.indexOf(selection.oid)`; с 0 начинать, только если коммита в графе нет
FG-008 · ux · medium · frontend/src/components/file-list/FilesToolbar.svelte:103 · «✕ N files hidden» считает строки, скрытые на клиенте, но `showEverything` включает все живые переключатели, в том числе Unchanged и Ignored на рабочей копии и Unchanged на коммите: вместо N скрытых приходит всё дерево и все игнорируемые (target/, node_modules/) с полным перечитыванием на бэкенде · коммит → фильтр «foo» → клик по «✕ 12 files hidden» → список = всё дерево коммита; на Working Tree — плюс все ignored · очищать текст и включать только переключатели, чьи ворота (`GATED`, lib/file-view.ts:88) реально скрыли строки; Unchanged и Ignored не трогать
FG-009 · bug · medium · frontend/src/App.svelte:558 · Команда `commit` палитры — `run: () => {}`; в неё же приходит пункт `Local ▸ Commit…` с ускорителем `CmdOrCtrl+Return` (src-tauri/src/menu/mod.rs:110 → onMenuCommand → палитра, App.svelte:3002). Ни меню, ни палитра ничего не делают; глобальных Ctrl+Enter, Ctrl+Shift+Enter и Ctrl+K из doc/11-keybindings.md:66-68 нет — Ctrl+Enter работает только внутри поля · staged файлы и готовое сообщение → `Local ▸ Commit…` или палитра «Commit Staged» → ничего · экспортировать из CommitBox `submit()`/`focus()`: `commit` — фокус в поле и коммит, если готово; Ctrl+Shift+Enter — то же с Amend; Ctrl+K — фокус
FG-010 · race · medium · frontend/src/components/file-list/CommitBox.svelte:29 · Пока идёт `createCommit`, кнопка и Ctrl+Enter активны: `busy` = `worktree.loading`, а он поднимается только в перечитывании после записи, хуки же идут секунды. Повтор запускает второй коммит: без Amend — ошибка «nothing to commit», с Amend — второй amend и вторая запись в журнале · pre-commit `sleep 5` → Commit, затем ещё Ctrl+Enter → в Output два `git commit` · локальный флаг `committing` в `submit` (в `ready` и `disabled`), сброс в `finally`
FG-011 · ux · medium · frontend/src/App.svelte:880 · Find Object (Ctrl+P): выбор ветки делает `switchTo` — checkout без вопроса; для удалённой ветки (find.rs:34 берёт все ветки) — `git switch -- origin/x` и ошибка. doc/modules/M8-tools.md:27 (T8.2): «Переход к объекту в соответствующей панели», а не переключение · Ctrl+P → «feat» → клик по `feature/x` → рабочая копия переключена; по `origin/feature/x` → «Could not switch branches» · ветка и тег → `commit.select` вершины + `graph.requestReveal` (и выделение узла в Branches); checkout — отдельной командой
FG-012 · ux · medium · frontend/src/App.svelte:879 · Find Object: коммит и тег выбираются `commit.select` без `graph.requestReveal` — граф не прокручивается к найденному (`selectRef` прокручивает, App.svelte:1185); файл на Working Tree не открывается вовсе (`&& commit.oid`, строка 882) · Ctrl+P → хеш старого коммита → клик → Files и Diff сменились, выделенной строки в графе не видно; Ctrl+P → изменённый файл рабочей копии → ничего · `graph.requestReveal(oid)` для commit и tag; для file без выбранного коммита — `openWorktreeDiff` / `openStagedDiff` по списку, где он есть
FG-013 · bug · medium · frontend/src/components/file-list/FileList.svelte:163 · Отметки (`marked`) не сужаются до показанных строк: пути, ушедшие после коммита или Discard, и скрытые фильтром остаются отмеченными, и на них действуют `scopeOf` (кнопки строк), `fileScope` (App.svelte:1984, контекстное меню) и Stash Selection через `onmarked`; тулбар их отсекает (`splitMarked`), остальные — нет · Ctrl+клик a и b → фильтр «a» → Discard на строке a → откат и скрытого b; или отметить, закоммитить, снова изменить a → Stash Selection… из меню a включает закоммиченный b → ошибка pathspec · `$derived` пересечения `marked` с `order`, наружу и в `scopeOf` — только оно
FG-014 · bug · low · frontend/src/components/file-list/FileList.svelte:170 · `scoped()` считает любой вызов с одним путём действием строки, поэтому «Stage all / Discard all / Unstage all» в заголовке секции с одной видимой строкой применяются ко всему отмеченному набору, включая скрытые и пути другой секции · Unstaged a, b; отметить a и b; фильтр оставляет a → «Discard all» → откатываются a и b · признак «из заголовка» (отдельный `runAll`) вместо вывода из длины массива
FG-015 · ux · medium · frontend/src/components/file-list/FilePane.svelte:86 · Выбор и отметки — по пути, а частично проиндексированный файл стоит и в Unstaged, и в Staged: подсвечены обе строки (не видно, какая сторона в Diff), отметка одной отмечает обе, Shift-диапазон считается от первого вхождения (`order.indexOf`, lib/multi-select.ts:20) и захватывает строки Unstaged · файл b в обеих секциях, клик по x в Staged, Shift+клик по b в Staged → отмечен и c из Unstaged · ключ выбора и отметки — `секция:путь`; `selected` сравнивать с парой (путь, вид diff `workTreeVsIndex` / `indexVsHead`)
FG-016 · inconsistency · medium · frontend/src/App.svelte:3352 · Счётчик заголовка Files — `onWorkingTree ? worktree.total : commit.files.length`, а тело показывает и stash (onWorkingTree = true → число рабочей копии), и сравнение двух коммитов (число файлов выбранного), и коммит с Unchanged (добавленные строки не в счёте) — нарушено «One source of truth per panel» (frontend/CLAUDE.md) · References → клик по stash → в заголовке Files число изменений рабочей копии, в списке — файлы stash · FilesPanel отдаёт наружу число строк списка, который показывает (тот же выбор ветки `{#if}`)
FG-017 · ux · medium · frontend/src/components/panels/FilesPanel.svelte:168 · Пустые состояния врут во время загрузки и при ошибке: «The working tree is clean.» — пока идёт первое чтение (после `worktree.clear()` при открытии) и когда чтение упало (списки обнулены, stores/worktree.svelte.ts:44-46); «This commit changed no files.» (строка 90) — пока грузятся файлы первого выбранного коммита и при ошибке `commitFiles`; «Comparing…» (134) — статус в панели; CommitDetailsPane.svelte:54 пишет «Select a commit to see what it changed.» при уже выбранном коммите до прихода деталей. frontend/CLAUDE.md: «panels show no status text while loading» · открыть большой репозиторий → в Files мелькает «The working tree is clean.» до прихода списка · передавать `loading`/`error`: пока грузится — пусто (или SkeletonRows), при ошибке — нейтральный текст без утверждения о содержимом
FG-018 · bug · medium · frontend/src/components/file-list/FileList.svelte:138 · На каждую букву фильтра все секции заново сортируются `sortFiles` с `localeCompare` (lib/files.ts:81): на коммите с Unchanged (всё дерево, десятки тысяч путей) это O(n log n) ICU-сравнений на главном потоке — вероятно больше 50 мс на нажатие (CLAUDE.md, Performance) · коммит в репозитории на 50k файлов → включить Unchanged → печатать в фильтре · сортировать один раз на `section.files` (отдельный `$derived` до фильтра — фильтр порядок сохраняет), сравнение — закэшированный `Intl.Collator().compare` или по кодовым единицам; A/B на `files.filter`
FG-019 · ux · low · frontend/src/components/panels/FilesPanel.svelte:152 · Кнопки строк Unstaged не проверяют применимость, которую знает меню (lib/file-menu.ts:64-65): «Ignore» на отслеживаемом файле дописывает бесполезную строку в .gitignore, «Discard» на неотслеживаемом удаляет его (меню запрещает), «+x» без смены режима молча ничего не делает и перечитывает список (App.svelte:2839) · изменённый tracked-файл → Ignore → в Unstaged появился .gitignore, файл на месте · одно правило применимости для меню и кнопок; неприменимая кнопка — disabled с причиной в подсказке
FG-020 · bug · low · frontend/src/components/file-list/FileList.svelte:208 · `hidden = total - shownCount`, но `shownCount` включает синтетические строки источников переименований (lib/file-view.ts:104-106), которых нет в `total`: «N files hidden» занижено, при равенстве значок пропадает, хотя файлы скрыты · коммит с 3 переименованиями → Rename Sources → фильтр, скрывающий 3 файла → значка нет · считать скрытые по исходным строкам, до добавления синтетических
FG-021 · bug · low · frontend/src/lib/file-view.ts:104 · Синтетическая строка `deleted` добавляется для любого `oldPath`, в том числе у `copied`: источник копии не удалён, а показан как Missing/Removed; если источник ещё и изменён, путь в списке дважды · коммит с копией файла при включённом обнаружении копий (commit.rs:291 выдаёт Copied) → Rename Sources → источник помечен D · добавлять строку только при `status === "renamed"`
FG-022 · ux · low · frontend/src/components/graph/RefCapsule.svelte:17 · Метка режется дважды: по символам до 30 с «…» посередине (`refLabelText` / `truncateMiddle`), затем `middleCut` и CSS-многоточие у `lead`; на промежуточной ширине видны два многоточия (`feature/JIRA-1…t-…-toolchain`), а длинное имя режется, даже когда места хватает · ветка из 40+ символов, сужать Graph → два «…» в одной метке · убрать символьный пре-срез, ширину капсулы ограничить CSS (`max-width` в ch), оставить один `middleCut`
FG-023 · bug · low · frontend/src/components/graph/CommitList.svelte:398 · Клик по строке, блок которой ещё не пришёл, или по строке ребейза (Index / todo / onto) даёт `oid = null` и `pick(repo, null)` — выбор уходит на Working Tree, Files и Diff переключаются; клик по кнопке Working Tree обрабатывается дважды (её `onclick` и всплытие в `onclick` списка) с лишним замером `select-commit` · быстрая прокрутка большого репозитория, клик по пустой строке → выбран Working Tree · выходить, если `commitRow !== null` и строки нет, и для заголовочных строк кроме 0; `stopPropagation` у кнопки Working Tree
FG-024 · ux · low · frontend/src/components/graph/CommitList.svelte:237 · При возврате к Working Tree эффект колонки Overlap выходит, не очищая `overlap.rows`: колонка показывает пересечения с прежним коммитом и метку «base» на нём · включить Overlap, выбрать коммит, клик по Working Tree → колонка не изменилась · при `base === null` звать `overlap.clear()` или рисовать ячейку только при `overlap.base === selection.oid`
FG-025 · ux · medium · frontend/src/components/graph/GraphFilter.svelte:19 · Поле фильтра хранит свои `text`/`applied` и не знает о `graph.query`: `File ▸ Log` (App.svelte:2043) фильтрует граф по `path`, а поле пустое, без счётчика и ✕; Enter на пустом поле ничего не делает (`text === applied`), Esc тоже (строка 33) — снять фильтр из поля нельзя · правый клик по файлу → Log → граф отфильтрован, поле пустое; вернуть весь граф — только ввести символ, Enter и ✕ · выводить текст поля из `graph.query` (обратный формат запроса) или давать App его установить; `reset()` доступен, пока `graph.query` не пуст
FG-026 · bug · low · frontend/src/lib/query.ts:70 · `since:` / `until:` берут полночь UTC (`T00:00:00Z`), а не местного дня, как `git log --since`: в UTC+3 коммит 1 января в 01:30 не попадает в `since:2026-01-01` · пояс UTC+3, коммит 2026-01-01 01:30 местного → `since:2026-01-01` его не показывает · `new Date(y, m - 1, d).getTime() / 1000`; тест с TZ
FG-027 · inconsistency · low · frontend/src/components/file-list/CommitBox.svelte:83 · Amend и No verify — нативные `<input type="checkbox">`, хотя есть общий `Checkbox.svelte` (frontend/CLAUDE.md: «no native form controls»); в тёмных темах — белый системный квадрат · тёмная тема → панель Commit Message · `<Checkbox bind:checked={amend} label="Amend" />` и так же No verify
FG-028 · inconsistency · low · frontend/src/components/graph/RebaseEditor.svelte:53 · Редактор интерактивного ребейза и SplitOffDialog.svelte:35 — свои модалки вместо общего `Dialog`: Enter не подтверждает; нативные `<select>` (RebaseEditor:86), чекбоксы (RebaseEditor:128, SplitOffDialog:56) и радио (SplitOffDialog:72) · Rebase Commits After This One… → Enter не запускает, список действий — системный · перевести на `Dialog` с `onconfirm`, `Select.svelte`, `Checkbox.svelte`
FG-029 · dead-code · low · frontend/src/lib/file-view.ts:37 · `Toggle` и `TOGGLES` используются только в file-view.test.ts; тулбар строится из `stateSwitches` (lib/file-switches.ts:232), подсказки уже разошлись («not yet version controlled» против «untracked») · — · удалить `TOGGLES`, `Toggle` и их тест
FG-030 · dead-code · low · frontend/src/lib/files.ts:3 · `SortKey` «name» и «status», `STATUS_ORDER` и `rank` не вызываются: `sortFiles` зовут только с «path» (FileList.svelte:138) · — · оставить сортировку по пути (см. FG-018), остальное удалить вместе с тестами
FG-031 · bug · low · frontend/src/stores/commit-tree.svelte.ts:11 · Кэш дерева — только по `oid`, и сбой запоминается как `[]` (строка 18): пока выбран тот же коммит, повторное включение Unchanged не перечитывает, неизменённые файлы молча не показаны, ошибка не сообщается; `clear()` (24) никто не вызывает · сбой `commit_tree_files` → Unchanged выкл/вкл → список без неизменённых, без уведомления · при ошибке сбрасывать `oid = null` и сообщать в очередь уведомлений; `clear()` — при уходе из репозитория или удалить
FG-032 · ux · low · frontend/src/components/graph/CommitList.svelte:611 · `Date.now()` в разметке не реактивен: «just now», «5 minutes ago», «today» считаются при монтировании строки и не обновляются, пока строка не уйдёт с экрана · формат Relative, закоммитить, подождать 10 минут → у верхнего коммита «just now» · `now` в `$state`, обновлять раз в минуту
FG-033 · bug · low · frontend/src/App.svelte:1103 · CommitBox разрешает Amend, когда фильтр скрыл все staged (`amend || !scope.empty`, CommitBox.svelte:25), а `commitStaged` на `scope.empty` молча возвращает `false` — уже после вопроса «Amend a published commit» (1094) · staged файл скрыт фильтром, Amend, новое сообщение → кнопка ничего не делает, без сообщения · для Amend при пустом scope — `--amend --only` без путей (только сообщение) или выключать кнопку с причиной; проверку `scope.empty` — до вопроса о публикации
FG-034 · ux · low · frontend/src/App.svelte:1104 · Подтверждение «Commit what you see» перечисляет все `scope.paths` в системном `ask` (сотни строк без прокрутки), а не через `confirmation.ask` + `listedMessage` (≤ 12 и «and N more»), как Discard и Delete (doc/12-risks.md:4180) · staged 500 файлов, фильтр скрывает один → «Commit 499 shown» → системное окно выше экрана · `confirmation.ask({ message: listedMessage(...) })`; туда же вопрос «Amend a published commit» (1094)
FG-035 · ux · low · frontend/src/components/panels/FilesPanel.svelte:99 · Список stash идёт с `separateIndex: true` и тремя секциями без `split` / `onsplit`: между панелями рисуются сплиттеры, которые не двигаются (FileList.svelte:233 зовёт `onsplit?.()`), пустые Index и Untracked всегда занимают место · References → stash без индекса → тянуть разделитель — ничего · `hideWhenEmpty` для Index и Untracked и свои доли либо без сплиттеров для stash
FG-036 · bug · low · frontend/src/stores/finder.svelte.ts:10 · Find Object запускает поиск на бэкенде на каждую букву (FindObject.svelte:42) без паузы и без отмены: на 50k коммитов параллельные обходы по сообщениям копятся, отбрасывается только ответ · быстро набрать 10 символов в Ctrl+P → 10 поисков в логе · пауза ~150 мс и отмена предыдущего по id операции, как у ContentSearch
FG-037 · simplify · low · frontend/src/App.svelte:3359 · Любое переключение вида Files (Directories, Untracked, Modified, `*`, ⌕, раздельные списки) перечитывает `worktree_files`, хотя на бэкенд влияют только четыре ключа `backendView` (lib/file-view.ts:79) · Show Directories → лишнее полное чтение статуса · перечитывать, только если `backendView(next)` отличается от текущего; A/B `files.toggle-tree`
FG-038 · split · low · frontend/src/components/graph/CommitList.svelte:1 · 853 строки с независимыми частями: строки ребейза, разметка строки коммита (капсулы, колонки, обрубки, drag&drop, меню), клавиатура и прокрутка к выбранному, загрузка аватаров и Overlap, якорь при смене плотности · — · вынести `CommitRow.svelte`, `RebaseRows.svelte`, клавиатуру и reveal — в чистые функции `lib/graph-*.ts` с тестами
FG-039 · inconsistency · medium · frontend/src/components/file-list/FilePane.svelte:91 · Список Files не выполняет doc/11-keybindings.md: §10 (↑/↓, Shift+↑/↓, Home/End, PageUp/PageDown, Enter — «расхождения здесь недопустимы») нет вовсе; Ctrl+A в files (строка 70, «выделить все файлы») уходит браузеру (App.svelte:923, `allowsSelectAll`); Space отмечает строку, а doc/11:69 — «переключить стейджинг» (код следует F-101) · фокус на строке Files → ↓ ничего не выбирает; Ctrl+A выделяет текст страницы · обработчик клавиш в FileList: курсор по `order`, Shift — диапазон, Ctrl+A — отметить все показанные, Enter — открыть diff; doc/11:69 привести к F-101
FG-040 · race · low · frontend/src/components/graph/CommitList.svelte:557 · Контекстные меню строки и метки (557-563, 582-590) и drag&drop не проверяют то, что проверяет `pick` (145): пока на экране строки прежнего репозитория (R-300), меню открывается для `repository.current` с oid другого репозитория · переключиться на большой репозиторий и сразу правый клик по строке прежнего графа → меню нового репозитория, действия падают «unknown revision» · тот же выход при `graph.shownRepo !== repository.current?.repo`
FG-041 · ux · low · frontend/src/App.svelte:1025 · Новый запрос фильтра сохраняет прокрутку: `graph.home` растёт только при смене репозитория (stores/graph.svelte.ts:238), отфильтрованный список открывается с прежнего смещения, а не сверху · прокрутить граф на 3000 строк → `author:x` → первые совпадения выше экрана · при смене запроса в `filterGraph` прокручивать к началу (увеличивать `home`)
FG-042 · bug · low · frontend/src/lib/file-dialogs.ts:35 · Index Editor считает сторону CRLF, если в ней есть хоть один `\r\n`, и `forDisk` (40) переводит все `\n` в `\r\n`: у файла со смешанными окончаниями сохранение меняет и нетронутые LF-строки (crates/CLAUDE.md: «keep the original ending») · файл со строками LF и CRLF → Index Editor → поправить одну строку → Save → `git diff` меняет все LF-строки · помнить окончание каждой строки или для смешанных сохранять без перевода
FG-043 · ux · low · frontend/src/components/graph/CommitList.svelte:505 · Строка Working Tree подсвечена выбранной, пока Files показывает stash (выбор stash делает `commit.clear()`, App.svelte:1176) · References → клик по stash → в графе выделен Working Tree, в Files — stash · `class:selected={selection.oid === null && stashView.contents === null}`
FG-044 · ux · low · frontend/src/components/file-list/FilePane.svelte:71 · `VirtualList` умеет `reveal`, но FilePane его не передаёт: выбранный извне файл (Find Object → файл коммита, App.svelte:882) или переехавший при пересортировке после stage не прокручивается в видимую часть · коммит с 3000 файлами → Ctrl+P → файл из конца списка → Diff открыт, строки в Files не видно · `reveal` = индекс строки `selected` в `rows`
FG-045 · inconsistency · low · frontend/src/components/file-list/FilesToolbar.svelte:225 · «Customize View» — американское написание, а строки UI британские (frontend/CLAUDE.md: `Colours`) · навести на кнопку меню вида в Files · «Customise View» (и комментарий в строке 47)

## Проход 2

25.09.2026 · тот же код (a779a38), другая оптика: пограничные состояния (конфликт, пустой репозиторий, stash, новые папки), пути ошибок, свежие коммиты 23–24.09 по файлам области. Дополнительно просмотрено: `crates/git_engine/src/{listing,worktree,commit_write,stash,surgery,published}.rs`, `components/menus/RefActions.svelte` (stash, split, rebase-i), `App.svelte` (`commitStaged`, `replan`, `openRebase`, `openSplit`, `fileContext`, `activate`), `doc/05-ui-layout.md` §3.4–3.5 и §7, `doc/modules/M4-graph.md`, `M6-staging.md` T6.8, `doc/features/F-072, F-101, F-103, F-370`, `doc/refactor-night-2.md` (FC-2-05, FC-2-08 — там записаны и не исправлены). Опыты с консольным git — `D:\cogit-work\tmp-fe-graph-files\p2-only` (FG-046) и `p2-conflict` (FG-047).

FG-046 · bug · high · crates/git_engine/src/commit_write.rs:37 · «Commit What You See» коммитит показанные staged-файлы через `git commit --only -- <пути>`, а `--only` берёт содержимое путей из рабочего дерева, а не из индекса: у файла в состоянии MM (или частично проиндексированного через Stage lines) в коммит уходят и неиндексированные правки, которых пользователь не индексировал и в Staged не видел; lib/commit-scope.ts:33 и App.svelte:1113 передают эти пути без проверки · a.txt: `git add` версии v1, затем правка v2 в рабочем дереве; b.txt staged; фильтр Files «a» → «Commit 1 shown» → `git show HEAD:a.txt` = v2 (проверено консольным git) · коммитить через временный индекс: `GIT_INDEX_FILE=<tmp> git read-tree HEAD`, перенести записи показанных путей из настоящего индекса (`git ls-files -s -z -- <пути>` → `git update-index -z --index-info`) и `git commit` с этим индексом (хуки видят его, как и при `--only`); до того хотя бы выключать «Commit N shown», если показанный staged-путь есть и в Unstaged; тест в git_engine/tests/commit_write.rs «only commits the staged side of a partly staged file»
FG-047 · bug · medium · crates/app_state/src/lib.rs:639 · Во время любого конфликта (merge, rebase, cherry-pick с unmerged-записями в индексе) Discard невозможен ни для одного файла, в том числе неконфликтного: резервный `git stash push --include-untracked -- <пути>` отказывает («error: could not write index», «f.txt: needs merge», exit 1), `backup_failed` отвечает «Nothing was changed…», хотя `git restore --worktree -- other.txt` в этом состоянии работает; меню (lib/file-menu.ts:65), кнопка строки и тулбар предлагают Discard и для самого конфликтного файла, где падает и `restore` («path 'f.txt' is unmerged») · конфликт merge по f.txt, правка other.txt → Discard на other.txt → «Nothing was changed: the changes could not be saved for Undo before discarding them» (проверено консольным git) · при unmerged-записях резервировать без stash — обратным патчем `Recovery::Patch`, как Discard строк, или blob'ами `git hash-object -w` — и выполнять `restore --worktree` для неконфликтных путей; для конфликтных выключать Discard с причиной «resolve first», как Index Editor (file-menu.ts:50); тест в app_state/tests/undo.rs
FG-048 · bug · medium · crates/git_engine/src/listing.rs:209 · Поиск по содержимому не находит текст в файлах новой неотслеживаемой папки: `changed_file_paths` берёт `worktree_files()` с `UntrackedFiles::Collapsed`, папка приходит одной записью `newdir/`, `readable("newdir/")` падает на `fs::read` каталога, и папка пропускается; в области `all` (`all_files` папки разворачивает) совпадение приходит как `newdir/a.ts`, а `keepFile` (frontend/src/lib/content-search.svelte.ts:38) ищет в hits путь строки `newdir/` — строка скрыта, статус «Found in 0 files» · `mkdir newdir && echo needle > newdir/a.ts` → Files → ⌕ → «needle» → «No file in this list contains the text.»; с включённым Unchanged — то же · `changed_file_paths` — с `UntrackedFiles::Files`, как `all_files`; `keepFile` для строки-папки (путь на `/`) — оставлять, если есть попадание с этим префиксом; тесты в listing и content-search.test.ts
FG-049 · bug · medium · frontend/src/components/menus/RefActions.svelte:725 · Drop Stash того stash'а, что открыт в Files, оставляет Files и Diff на удалённом stash: `stashView` сбрасывают только смена коммита (`commit.onchange`, App.svelte:321), `selectRef`, `forgetPanels` и переходы RefActions, а `dropStashTarget` зовёт лишь `afterMutation`, который его не трогает; `toolbarFacts.onWorkingTree` = false (App.svelte:502) — Stage, Discard и Stash Selection в тулбаре выключены, пока пользователь не щёлкнет Working Tree · References → клик по stash@{0} → его меню → Drop → в References stash'а нет, а Files по-прежнему показывает его Working tree / Index / Untracked, дифф его файлов открывается · после изменения списка stash'ей (`afterMutation` или `stashes.refresh`) сбрасывать `stashView`, если в `stashes.entries` нет `contents.worktreeRev`; тест стора
FG-050 · bug · medium · frontend/src/components/panels/FilesPanel.svelte:177 · Мультивыбор в списке файлов коммита не доходит до меню: у FileList коммита (и stash) нет `onmarked`, `markedFiles` в App пишет только список рабочей копии и после его размонтирования хранит его последние отметки, а `fileScope` (App.svelte:1985) в коммите берёт их. Ctrl+клик a и b в коммите → меню по a действует только на a (F-101: действие — на всё выделение); если путь есть среди старых отметок рабочей копии — меню считает их (count > 1, почти всё «one file only»), Copy Path копирует пути рабочей копии, которых в коммите нет; `pickedFiles` (App.svelte:523, LFS Lock/Unlock) тоже берёт их · Working Tree: Ctrl+клик a и b → выбрать в графе коммит, менявший a → правый клик по a → Show Changes, Open File, Blame выключены «one file only», Copy Path даёт a и b · передавать `onmarked` из списков коммита и stash; при размонтировании FileList отдавать `[]` (`$effect(() => () => onmarked?.([]))`); `fileScope` — по отметкам того списка, в котором щёлкнули
FG-051 · ux · medium · frontend/src/components/file-list/FilePane.svelte:227 · Кнопки строки (`.act`) скрыты только `opacity: 0`, но занимают место (`flex: 0 0 auto`) в каждой строке: в Unstaged пять подписей STAGE, +X, DISCARD, IGNORE, DELETE с отступами — около 250 px, и имя файла (`.shrink-last`) режется ради невидимых кнопок; в узкой колонке Files (тулбар сам считает её обычной, FilesToolbar.svelte:46) от имени остаётся несколько символов · Files шириной ~400 px → в Unstaged вместо имён «…», в Staged (одна кнопка) имена почти целиком · держать кнопки поверх правого края строки (`position: absolute; right: 0` с фоном строки) и показывать по `:hover` / `:focus-within`, не резервируя ширину
FG-052 · ux · low · frontend/src/components/file-list/FilePane.svelte:296 · Правило `.renamed { color: var(--status-ref); font-size: 10px }` написано для подписи «← старое имя», но строка файла получает тот же класс из статуса (`class="row {file.status}"`, строка 84; `.row.renamed .badge` на строке 271 это подтверждает): вся строка переименованного файла — 10 px и цветом ссылки · `git mv a.txt b.txt` → Files → строка b.txt мельче соседних и синяя · класс подписи переименовать (`.from`) или писать `.row > .renamed`
FG-053 · ux · low · frontend/src/components/file-list/FilePane.svelte:174 · Строки Files ставятся с шагом `LIST_ROW_HEIGHT` = 24 (lib/graph-geometry.ts:7; строки 75 и 88; VirtualList по умолчанию тоже 24), а высота строки в CSS — 22 px: между строками зазор 2 px, в нём прерываются подсветка наведения, выделение и полоса отметок, клик в зазор никуда не попадает. Шаг сменился с 22 на 24 в 8079819 (тогда `GRAPH.rowHeight`), высоту строк не поправили · Files с несколькими файлами → провести мышью, отметить подряд Ctrl+кликом → полосы с разрывами · высота `.row, .folder` — `var(--h-row)` (24 px, как `LIST_ROW_HEIGHT`)
FG-054 · ux · low · frontend/src/components/graph/CommitList.svelte:453 · Пустой граф из-за фильтра без совпадений или снятых в References всех веток (`visibleRefs = []`) показывает «No commits yet — The first commit you make in this repository shows up here.», и вместе со списком пропадает строка Working Tree; doc/05-ui-layout.md §7: «Пустой результат фильтра — текст в центре панели + кнопка сброса фильтра». Известно как FC-2-08 (doc/refactor-night-2.md:260), осталось «не сделано» · фильтр графа `author:nobody` → Enter → «No commits yet», строки Working Tree нет · при `!isEmptyQuery(graph.query)` или пустом `graph.visibleRefs` — «No commits match the filter» с кнопкой Clear filter; строку Working Tree рисовать и при пустом списке
FG-055 · ux · medium · frontend/src/components/file-list/FilesToolbar.svelte:283 · Меню Customize View не видно: у `.bar` высотой 32 px `overflow: hidden`, а `.menu` позиционирована под полосой (`top: calc(100% + 2px)`, строка 441) внутри неё и обрезается целиком; подложка `position: fixed` при этом перекрывает окно и съедает следующий клик. В узкой панели (`crowded`, < 420 px) шесть переключателей состояний живут только в этом меню — Untracked, Ignored, Unchanged, Modified, Skipped, Missing там включить нельзя вовсе. Известно как FC-2-05 (doc/refactor-night-2.md:257), не исправлено · сузить Files меньше 420 px → кнопка ☰ → ничего не появляется, следующий клик по списку только закрывает невидимое меню · вынести меню из полосы (`position: fixed` по координатам кнопки или общий выпадающий компонент); `overflow: hidden` оставить на ряду кнопок
FG-056 · bug · low · frontend/src/App.svelte:1904 · Плашка «Some of these commits are already on a remote» в редакторе rebase проверяет базу, а не переписываемые коммиты: `openRebase` зовёт `isPublished(id, rev)` для коммита, после которого идёт план (сам он не переписывается), `replan` (1872) — для `a^`. База в origin/main — обычный случай, и предупреждение о force-push появляется, хотя ни один коммит плана не отправлен · два локальных коммита поверх origin/main → правый клик по вершине origin/main → Interactive Rebase → красная плашка про force-push · проверять `isPublished` старейшего коммита плана (первого в todo до перестановок): потомки неопубликованного коммита тоже не опубликованы
FG-057 · ux · low · frontend/src/components/graph/RebaseEditor.svelte:60 · После перетаскивания коммита заголовок редактора называет не ту базу: `mergeBaseOf` отдаёт строку `` `${a}^` `` (App.svelte:1880), `shortOid` берёт её первые 7 символов — «Rebase 2 commits onto a1b2c3d» показывает сам перетаскиваемый коммит, а не его родителя · перетащить коммит на соседний → Squash → в заголовке short id коммита из плана · разрешать базу в oid в `mergeBaseOf` или выводить `${shortOid(a)}^`
FG-058 · bug · low · frontend/src/App.svelte:3525 · SplitOffDialog получает `oid={commit.oid}` и `changed={commit.files…}` вживую, а `runSplit` (2742) режет `commit.oid` на момент нажатия; диалог не берёт фокус (своя модалка без автофокуса, FG-028), подложка останавливает только мышь, и до первого клика в диалоге клавиши остаются у списка графа (CommitList.svelte:325): ↓, PageDown, Home за диалогом выбирают другой коммит, заголовок и список файлов меняются, `splitPublished` остаётся от прежнего — Split Off разрезает не тот коммит и без предупреждения о публикации · правый клик по неопубликованному коммиту → Split Off… → не щёлкая в диалоге, PageDown (пролистать список файлов) → «Split off files from <другой>», плашки нет даже для опубликованного · при `openSplit` снимать `{ oid, files, published }` в своё состояние и отдавать диалогу его; переводить фокус в диалог (автофокус поля сообщения или общий Dialog)
FG-059 · ux · low · frontend/src/components/file-list/FileList.svelte:96 · Ctrl+F в Files сравнивается по `event.key.toLowerCase() === "f"`: в русской раскладке `event.key` = «а», фокус в фильтр не переходит; проект уже решил это через `event.code` (lib/child-window.ts:16, lib/keymap.ts:67). Так же DiffView.svelte:307 и Ctrl+A в App.svelte:922 · раскладка ЙЦУКЕН → фокус в Files → Ctrl+F → ничего · `event.code === "KeyF" || event.key.toLowerCase() === "f"`, один помощник рядом с `closesChildWindow`
FG-060 · ux · low · frontend/src/components/file-list/CommitBox.svelte:25 · В репозитории без коммитов Amend доступен и делает кнопку активной даже без staged (`stagedCount > 0 || amend`); `commitStaged` (App.svelte:1093) спрашивает `isPublished(id, "HEAD")`, на нерождённом HEAD это ошибка, `publishedOrAssume` превращает её в «опубликован» — «This commit is already on a remote… force-push», затем git: «You have nothing to amend» · `git init` → сообщение, галочка Amend → Amend → вопрос про remote → ошибка · выключать Amend (и `ready` с ним) при `repository.current.head.kind === "unborn"`, CommitPanel передаёт это в CommitBox
FG-061 · bug · low · frontend/src/components/file-list/CommitBox.svelte:48 · Шаблон `commit.template` засевает поле только при смене `draftKey` или `template`: после удачного коммита `submit` очищает сообщение, `loadTemplate` (App.svelte:2853) кладёт ту же строку — эффект не перезапускается, и до смены репозитория или перезапуска поле пустое; F-103: «пустое поле сообщения заполняется этим шаблоном» · `git config commit.template t.txt` → закоммитить → поле пустое; после перезапуска шаблон снова на месте · после успешного коммита ставить `message = template ?? ""`
FG-062 · ux · low · frontend/src/lib/file-switches.ts:55 · Для stash выключенная кнопка Untracked объясняет «A stash has no untracked files», а список stash'а тут же показывает секцию Untracked с его файлами (FilesPanel.svelte:112) · `git stash -u` → клик по stash в References → навести на кнопку Untracked · для контекста stash — «Untracked files of a stash are always listed», как у Modified
FG-063 · bug · low · frontend/src/lib/truncate.ts:33 · `middleCut` и `charCut` (строка 15) режут по UTF-16-единицам: эмодзи (суррогатная пара) на месте разреза делится между `.lead` и `.tail` капсулы (или теряет половину в `truncateMiddle`) и рисуется двумя знаками замены · `git branch "a🔥b"` → капсула «a��b» (tail = 2 единицы) · резать по кодовым точкам (`Array.from(text)`); тест в truncate.test.ts с эмодзи на границе
FG-064 · ux · low · frontend/src/components/panels/CommitDetailsPane.svelte:23 · Тему коммита в Diff нельзя выделить и скопировать: по умолчанию `user-select: none` (app.css:333), у тела (`.body`), хеша и автора (`dd`) выделение включено, у `.subject` — нет · выбрать коммит → выделить мышью первую строку сообщения → не выделяется · `user-select: text` для `.subject`
FG-065 · ux · low · frontend/src/App.svelte:1715 · Выбранный коммит, восстановленный при запуске (`session.selected`), выбирается `commit.select` без `graph.requestReveal`: Files и Diff показывают коммит, строки которого на экране нет, граф открыт сверху (а ↓ затем прыгает к первому коммиту, FG-007) · выбрать коммит глубоко в истории → перезапустить Cogit → граф наверху, выделения не видно · `graph.requestReveal(restoreOid)` рядом с `commit.select`
FG-066 · bug · low · frontend/src/App.svelte:1998 · Меню файла коммита ищет статус строки в `commit.files`, а список показывает ещё синтетические строки источников переименований (lib/file-view.ts:105) и Unchanged (lib/file-switches.ts:128): для них статус подменяется на "modified", и у источника переименования, которого в коммите уже нет, активны Open File, Save As, Blame, Investigate, Cherry-Pick, Revert — и падают · коммит с `git mv a.txt b.txt` → Files → кнопка Missing (Rename Sources) → правый клик по a.txt → Open File → «Could not open the file» · отдавать из FileList в `oncontext` саму запись строки (статус, oldPath), а не искать путь в `commit.files`
