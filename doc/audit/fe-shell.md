# Аудит: фронт: тулбар, меню, диалоги, Preferences, уведомления, окна

25.09.2026 · ветка `audit/2026-09-25` (a779a38) · просмотрено: `frontend/src/{App,BlameWindow,InvestigateWindow}.svelte`, `{main,blame,investigate}.ts`, `components/layout/{Toolbar,ToolbarEditor,SettingsPanel,KeymapEditor,GraphField,Notifications,CommandPalette,FindObject,PromptDialog,StashDialogs,SafetyJournal,HooksPanel,OutputPanel,CommandOutput,DropMenu,StatusBar,StateBanner,Splitter,Panel}.svelte`, `components/common/{Dialog,ConfirmDialog,ExitDialog,AboutDialog,ConfigEditor,QueueNav,Select,TooltipLayer}.svelte`, `components/menus/{RefActions,AddTagDialog,EditAuthorDialog,EditMessageDialog,PushToDialog,ResetDialog}.svelte`, `components/investigate/InvestigateToolbar.svelte`, `stores/{errors,notices,output,health,settings,toolbar,layout,repository,session,desktop,network,hooks,avatars,recovery,safety,confirm,prompt,exit,blame-window,remote-ops}.svelte.ts`, `lib/{toolbar,toolbar-layout,toolbar-prefs,preferences,settings,settings-file,keymap,menu-state,palette,notices,updates,child-window,wiring,leaving,names,panel-focus,perspectives,remote-menu,repo-menu,repo-click,tag-dialog,stash-modes,tooltip,exit,unsaved,health,operations,pull-request,undo-stack,mutation}.ts`, `lib/investigate/{session.svelte,menu}.ts`; сверка с `src-tauri/src/{menu/mod.rs,accelerators.rs,webview2.rs,lib.rs}`, `crates/app_state/src/{rewrite,ref_ops}.rs`, `doc/11-keybindings.md`, `doc/12-risks.md` (R-122, R-167, R-184, R-255, R-351), `doc/modules/M8-tools.md`, `doc/refactor-night*.md`; дубли с `fe-repos.md`, `fe-graph-files.md`, `diff.md` (FR-019, FR-026, FG-009, FG-011, FG-012, DF-011) сюда не вошли

## Проход 1

FS-001 · race · high · frontend/src/components/menus/RefActions.svelte:596 · Диалоги RefActions (Reset Advanced, Edit Message, Edit Author, Add Tag, Push To) и `RemoteOpsDialog` не закрываются при уходе с репозитория: 4e23719 закрыл по `repository.onLeave` только диалоги App и confirm/prompt/stash (`lib/leaving.ts`), а эти при отправке берут `repoId()` уже нового репозитория и применяют к нему oid/ветку старого (`resetWith` 596, `saveMessage` 539, `saveAuthor` 552, `createTagFrom` 573, `sendPushTo` 646); RemoteOps держит старый id и меняет A, пока на экране B · В A: меню коммита X ▸ Reset Advanced…; не закрывая диалог, Ctrl+O → B (клон того же проекта) или бросить папку B на окно; выбрать Hard → `resetTo(B, X, "hard")` сдвигает ветку B; так же Push To… отправляет одноимённую ветку B · В RefActions подписаться на `repository.onLeave` и обнулить `tagDialog/pushDialog/resetDialog/messageDialog/authorDialog`; в `leaveRepositoryDialogs` добавить `remoteOps.close()`, в onLeave App (App.svelte:2156) — `finderOpen = false`
FS-002 · bug · high · frontend/src/components/layout/SettingsPanel.svelte:473 · (известно: FA-2-07, не исправлено) правки Keyboard пишутся только в `draftKeys`: OK/Esc/✕ закрывают без применения и раскладка теряется, применяется она лишь попутно при смене другого поля; Cancel её не откатывает (снимок `settingsAtOpen` без keymap, App.svelte:935); новое: Restore Defaults на странице Keyboard (522) — `restoreCategory` пропускает поле `keymap`, клавиши не сбрасываются, зато применяется незаконченный черновик · Preferences ▸ Keyboard ▸ Fetch → Ctrl+Shift+G → OK → в меню прежний Ctrl+Shift+F · `onchange` KeymapEditor → сразу `onapply(draft, next)` (R-122); снимок keymap при открытии для Cancel; Restore Defaults на keymap → `{}` и применить
FS-003 · bug · medium · frontend/src/App.svelte:391 · `prUrl` берёт base из upstream самой ветки: у `feature` → `origin/feature` base = head = `feature`, `pullRequestUrl` отдаёт null, Create/Copy Pull Request выключены с ложной причиной «No GitHub, GitLab or Bitbucket remote»; у неотправленной ветки upstream нет, `ahead` = 0, и `openPullRequest` (858) не предлагает push (F-066, M8 T8.8), а открывает compare для ветки, которой нет на remote; заголовок — тема выделенного в графе коммита, а не последнего коммита ветки · `git push -u origin feature` → палитра: Create Pull Request неактивна; новая ветка без push → в браузере пустая форма · base — ветка по умолчанию remote (`refs/remotes/<remote>/HEAD`, иначе main/master); push предлагать при `!tracked.upstream || tracked.ahead > 0`; заголовок — тема HEAD; причина недоступности — по настоящему условию
FS-004 · bug · medium · frontend/src/components/layout/PromptDialog.svelte:34 · `validate?.(text) ?? (пусто → "Enter a value.")`: `optional()` (lib/names.ts:17) возвращает null, `??` проваливается в проверку пустоты — пустое значение не принимается; f239fac («an empty release tag are accepted») проверил только `names.ts`, диалог не менял · Палитра ▸ Git-Flow: Finish This Branch… на `release/1.0` → стереть имя тега → «Enter a value.», Finish неактивна: release без тега не завершить · `const problem = $derived(validate(text))` (validate обязателен в `PromptRequest`), пустоту проверяет сам валидатор (`textProblem`)
FS-005 · bug · medium · frontend/src/components/menus/RefActions.svelte:758 · Меню строки Working Tree: Stage/Unstage/Discard зовут `worktree.*` напрямую и `afterMutation()` без путей (проп в App.svelte:3515 отбрасывает аргумент) → `diff.dropIfAffected([])` ничего не делает, открытый diff остаётся прежним (watcher после своих записей молчит); `wt-commit` (759) при скрытой панели Commit Message ничего не делает — поля нет в DOM · Открыть a.txt в Diff, ПКМ по Working Tree ▸ Discard → подтвердить: файл откатан, Diff показывает старые изменения, Stage lines по нему падает · Проводить через `mutate(step, paths, true)` App (проп `mutate`) или передавать пути в `afterMutation(paths)`; `wt-commit` — через `commitFiles([])` App (показывает панель)
FS-006 · bug · medium · frontend/src/App.svelte:941 · Cancel в Preferences откатывает только `settings.apply(before)`: `diff.whitespace` и дифф, посчитанный с algorithm/contextLines/wordDiff/detectMoves черновика, не пересчитываются; `applySettings` при закрытом Diff выходит раньше (958), и Whitespace вообще не доходит до `diff.whitespace` до перезапуска · (1) Файл в Diff → Preferences ▸ Diff ▸ Ignore all whitespace → Cancel: настройка «Show every change», дифф без пробельных изменений; (2) без файла в Diff выбрать Ignore all → OK → открыть файл: пробельные изменения видны · До раннего выхода присваивать `diff.whitespace = settings.current.ignoreWhitespace`; `revertSettings` — через `applySettings(before, keymapAtOpen)`
FS-007 · bug · medium · frontend/src/stores/hooks.svelte.ts:58 · `edit()` глотает ошибку чтения существующего хука (`readHook(...).catch(() => "")`): редактор открывается пустым, `saved = ""`, Save перезаписывает настоящий хук набранным текстом · Хук занят антивирусом или нет прав на чтение → Hooks ▸ Edit → пусто → ввести строку → Save: хук заменён · При ошибке — `this.report(err, "Could not read the hook")` и `editing = null`, редактор не открывать
FS-008 · bug · medium · frontend/src/lib/updates.ts:69 · Install из Check for Updates (App.svelte:274): `downloadAndInstall` и `relaunch` мимо `mayClose` — несохранённый хук и разрешение конфликта теряются без вопроса, идущий push/rebase обрывается (`children::stop_all` на `RunEvent::Exit`) без окна Exit со списком операций · Запустить долгий push → Help ▸ Check for Updates → Install: приложение перезапускается, push прерван · Перед `downloadAndInstall` пройти ту же проверку, что `mayClose` (App.svelte:2884: `unsavedSummary` + `exitFlow.ask("command", …, listOperations)`); отказ — не ставить
FS-009 · ux · medium · frontend/src/App.svelte:1609 · Abort и Skip (кнопки баннера, палитра Abort Operation In Progress) выполняются сразу, без подтверждения; `abort_operation` пишет `Recovery::None` (crates/app_state/src/rewrite.rs:12) — разрешённые конфликты и прогресс rebase не вернуть; frontend/CLAUDE.md: destructive items confirm · rebase с пятью разрешёнными конфликтами → промах по Abort рядом с Continue → всё потеряно, в Safety Journal «cannot be undone» · `confirmation.ask({ …, warning: true })` перед abort и skip
FS-010 · ux · medium · frontend/src/components/common/Dialog.svelte:92 · Клик по подложке (и Esc) закрывает редакторы с набранным текстом — ConfigEditor, IndexEditorDialog, EditMessageDialog — без вопроса; `mayClose` (App.svelte:2888) учитывает только хук и merge, закрытие окна теряет эти правки тоже молча · Edit Git Config: User → дописать секцию → щёлкнуть мимо окна → правки пропали · Проп `dirty` у Dialog: при dirty подложка не закрывает, Esc/✕ спрашивают; `configEdit`/`indexEditing`/`messageDialog` — в `unsavedSummary`
FS-011 · ux · medium · frontend/src/App.svelte:861 · Осталось 11 системных `ask()` plugin-dialog (861, 1094, 1106, 1212, 1242, 1406, 1769, 1802, 2390, 2400, 2892) и вопрос обновления (283–284) — против frontend/CLAUDE.md «Dialogs use the shared modal» (долг записан в R-255; FG-034 — частный случай 1106); разрушительные — Roll back (1769), Rewrite history (1802), Amend a published commit (1094), Close anyway (2892) — стоят на «Да», Enter подтверждает, тогда как общий ConfirmDialog при `warning` ставит фокус на Cancel · Выделить коммит → палитра Roll Back Tree To This Commit → Enter: файлы рабочего дерева перезаписаны · `confirmation.ask({ title, message, confirm, warning })` вместо `ask`; для обновления — тот же стор и `notices.inform` вместо `message`
FS-012 · ux · medium · frontend/src/components/layout/KeymapEditor.svelte:27 · Захват принимает сочетания, которые окно никогда не выполнит: именованные клавиши (Up, Return, Space, Delete, Tab), знаки кроме `, . - = +` (Ctrl+/, Ctrl+[), клавишу без модификатора; для не-букв берётся `event.key` раскладки (в русской Ctrl+, → `CmdOrCtrl+Б`); `virtual_key` (src-tauri/src/accelerators.rs:98) их не знает, перехватчик WebView2 их не забирает, а muda непарсящиеся молча выбрасывает — редактор показывает клавишу назначенной · Keyboard ▸ Fetch → Ctrl+Up (и применить) → в меню «Ctrl+Up», нажатие ничего не делает · Принимать только то, что разбирает `accelerators::parse` (модификатор + буква/цифра/F-клавиша/`,.-=`; F-клавиши и без модификатора), знаки — по `event.code`, иначе подсказка «cannot be used here»
FS-013 · ux · medium · frontend/src/components/layout/FindObject.svelte:32 · У Find Object нет клавиатуры: Enter в поле ничего не делает, стрелки не двигают выбор, строки `tabindex="-1"` — результат выбирается только мышью · Ctrl+P → `main` → Enter: ничего; Tab уводит фокус за окно поиска · Курсор, ↑/↓, Enter и `scrollIntoView`, как в CommandPalette
FS-014 · bug · low · frontend/src/App.svelte:954 · `applySettings` зовёт `settings.setKeymap` на каждое изменение любого поля: `set_keymap` → `menu::rebuild` + `set_menu` на главном потоке и запись файла — ползунки Lane width, Context lines, Background fetch перестраивают строку меню на каждом `input`, поле Git executable — на каждой букве; `sameKeymap` (lib/preferences.ts:393) для этой проверки есть, но не используется · Preferences ▸ Graph ▸ протащить Lane width 8→40: ~32 перестройки меню подряд · `if (!sameKeymap(keymap, settings.keymap)) { await settings.setKeymap(keymap); pushMenuState(true); }`
FS-015 · ux · low · frontend/src/components/layout/CommandPalette.svelte:19 · ↑/↓ меняют `cursor` без прокрутки: при ~95 командах в 60vh подсвеченная строка уходит из вида, Enter выполняет невидимую команду · Ctrl+Shift+P → 25 раз ↓ → подсветки не видно, Enter что-то запускает · `scrollIntoView({ block: "nearest" })` у активной строки при смене cursor
FS-016 · ux · low · frontend/src/App.svelte:3003 · Команды меню идут через `runCommand` и попадают в «недавние» палитры, включая саму Ctrl+Shift+P (id `palette`): при пустом запросе первой и подсвеченной стоит «Find Command», Enter лишь переоткрывает палитру · Ctrl+Shift+P, Esc, Ctrl+Shift+P → первая строка «Find Command» · Для menu-command вызывать `command.run()` без записи в `recentCommands`; `palette` в недавние не писать
FS-017 · ux · low · frontend/src/components/layout/Toolbar.svelte:368 · В узком окне `.crowded .label` прячет и кнопки-каретки split, а меню переполнения (188–196) даёт только основные действия — Push To…, Fetch '<remote>', выбор Pull scope, Delete merged, порядок Sync, Stash Selection/Quick Stash, Interactive Rebase… из тулбара недоступны · Сузить окно до появления «…» → у Push нет каретки, в «…» нет Push To… · Каретку оставлять видимой (без подписи) или давать в меню переполнения подменю `menuOf(id)`
FS-018 · bug · low · frontend/src/components/layout/SettingsPanel.svelte:144 · Undo страницы Toolbar пишет шаг до нормализации: Separator в конец или рядом с другим разделителем `normalizeLayout` выбрасывает, раскладка не меняется, а шаг в истории есть — Undo активна и ничего не делает · Preferences ▸ Toolbar, без выделения Add → Separator → Undo: изменений нет · `record(history, toolbarLayout, normalizeLayout(next), sameLayout)`; ToolbarEditor не предлагать разделитель, который нормализация уберёт
FS-019 · inconsistency · low · frontend/src/App.svelte:1216 · `switchTo` для ветки, взятой другим worktree, делает `activate(elsewhere.path)` — worktree открывается как самостоятельный репозиторий и попадает в список Repositories; R-184: worktree открывается как submodule (`open_worktree`, без списка), `activate(path)` назван прежним вариантом · Двойной клик в Branches по ветке, выведенной в worktree → Yes → новая строка в Repositories · `openWorktreeRow(elsewhere)`
FS-020 · ux · low · frontend/src/App.svelte:1483 · Fetch (тулбар, меню, Fetch '<remote>') завершается `afterRefChange` → `commit.clear()` и `diff.clear()`: fetch не трогает локальные ссылки, но выделенный коммит и открытый diff пропадают; Fetch All (2238) зовёт `afterRefChange()` без репозитория и чистит панели, даже если текущий в выборке не был · Выделить коммит, открыть файл → Ctrl+Shift+F → выделение и Diff сброшены · После fetch — `repository.refreshRefs()` + `graph.load` без сброса выбора; Fetch All — `afterRefChange(id)` только если текущий был в списке
FS-021 · ux · low · frontend/src/stores/layout.svelte.ts:61 · При развёрнутой панели View ▸ <Panel> и Ctrl+1…7 меняют скрытость невидимо, а галочки (`layout.visible`, App.svelte:3036) показывают только развёрнутую — клик по «неотмеченной» Graph Panel, чтобы её показать, на деле её скрывает · Развернуть Diff (Shift+F11) → Ctrl+3 → Shift+F11: Graph исчез · `togglePanel` сначала снимает `maximized` (или галочки читают `hidden` перспективы)
FS-022 · ux · low · frontend/src/components/layout/StashDialogs.svelte:55 · В Stash All основная кнопка «Stash All» — левая из действий, крайняя справа — Cancel; правило «primary action rightmost» (frontend/CLAUDE.md, R-167) · Ctrl+S → кнопки: Stash All, + Keep Index, + Keep Working Tree, Cancel · Cancel первой, режимы после, основная — последней
FS-023 · bug · low · frontend/src/App.svelte:3602 · `void network.storeToken(token)` и `forgetToken` без catch: отказ хранилища ключей — необработанный reject, ни уведомления, ни изменения в Preferences · Preferences ▸ Authentication при недоступном хранилище → Store: ничего не происходит · `.catch((err) => errors.report(err, "Could not store the token"))`, так же для Forget
FS-024 · ux · low · frontend/src/stores/health.svelte.ts:28 · С R-351 переключение между открытыми репозиториями идёт через `activate` → `health.check` сбрасывает `#later`: «Remind me later» живёт до первого клика по другому репозиторию, предупреждение всплывает при каждом возвращении · A с предупреждением → Remind me later → клик по B → клик по A: окно уведомлений снова открыто · Хранить `#later` по корню и сбрасывать только при настоящем открытии

## Проход 2

25.09.2026 · другая оптика: клавиши и слои модальных окон, пути повтора и ошибок, пограничные состояния (пустой репозиторий, detached HEAD, ветка без upstream, юникод), обещания doc/features, doc/11 §1, R-91, R-254 против кода; дочитаны `components/layout/{CommandOutput,OutputPanel,HooksPanel,SafetyJournal,StartScreen,DropMenu}.svelte`, `components/common/{Checkbox,VirtualList}.svelte`, `stores/{flow,avatars,recovery,desktop,stash-dialog}.svelte.ts`, `lib/{retry,push-to,toolbar-layout,trace,repo-phase,remote-menu}.ts`, `lib/investigate/session.svelte.ts`, сверка с `src-tauri/src/{webview2,accelerators}.rs`, `crates/git_engine/src/{network,outcome,flow,branches}.rs`, `crates/app_state/src/{network,hooking}.rs`. Поведение git (push/pull новой ветки и detached HEAD, stash в пустом репозитории и на чистом дереве) проверено git 2.51 во временной папке вне репозитория (удалена). Дубли с FG-009, FG-028, FR-014, FR-024, DC-004, DC-012, DC-020, CC-003 не вошли или отмечены ссылкой.

FS-025 · bug · medium · frontend/src/components/common/Dialog.svelte:59 · Открытый диалог не отсекает окно: аккорды меню (перехват WebView2, src-tauri/src/webview2.rs:66) и пункты строки меню выполняются под ним (doc/11 §1: «modal > панель > global»), а все слои слушают `svelte:window` в порядке монтирования — `Esc` закрывает сразу все (Dialog для Esc не смотрит `defaultPrevented`; так же HooksPanel.svelte:53, SafetyJournal.svelte:15, OutputPanel.svelte:23), `Enter` исполняет `onconfirm` нижнего диалога (он слушает раньше, ставит `preventDefault`, и верхний пропускает клавишу) · (1) Tools ▸ Manage Hooks… → Save as preset → Esc: закрыт и запрос имени, и окно Hooks; (2) Edit Author… → поправить имя → Ctrl+S (привычное «сохранить») → поверх открывается Stash All → ввести имя, Enter: выполнен Edit Author (история переписана), диалог Stash остался; (3) любой диалог → Ctrl+W: закрыт репозиторий под ним; Preferences → Ctrl+, : `openSettings` (App.svelte:934) переснимает `settingsAtOpen`, Cancel больше не откатывает сделанное до этого · Стек модальных слоёв (счётчик в Dialog и у самописных модалок): клавиши обрабатывает только верхний, `Esc`/`Enter` — с `stopImmediatePropagation`; пока стек не пуст, `onMenuCommand` (App.svelte:2994) пропускает команды (кроме `exit`), а перехватчик не забирает аккорды (флаг через команду)
FS-026 · bug · medium · frontend/src/components/layout/KeymapEditor.svelte:69 · Захват клавиш в Preferences ▸ Keyboard не получает ни одного сочетания, уже занятого меню: перехватчик WebView2 забирает его до страницы (`SetHandled(true)`, src-tauri/src/webview2.rs:66–69) и выполняет команду — переназначить занятую клавишу нельзя, а попытка запускает действие; то же с клавишей, назначенной раньше самим пользователем · Keyboard ▸ Fetch → «Press keys…» → Ctrl+S: поверх открывается Stash All, у Fetch клавиша прежняя; Ctrl+W — закрыт текущий репозиторий; Alt+X — окно Exit; F5 — перечитан репозиторий · На время захвата выключать перехват: команда вроде `suspend_accelerators(bool)` (флаг рядом с `Keymap`, `claimed()` отвечает `None`), включать при `capturing !== null`, снимать на blur, Esc и закрытии Preferences
FS-027 · bug · medium · frontend/src/components/common/Dialog.svelte:60 · `Esc`, уже погашенный элементом внутри диалога, всё равно закрывает диалог: Dialog не проверяет `event.defaultPrevented`, а App.svelte:906 закрывает Preferences на любой Esc; отмена захвата клавиши (KeymapEditor.svelte:29 гасит Esc) и очистка полей поиска Preferences и Keyboard (`type="search"`) закрывают весь Preferences, недоделанная раскладка при этом теряется (FS-002) · Preferences ▸ Keyboard ▸ Fetch → «Press keys…» → Esc (передумал): Preferences закрыт, правки Keyboard пропали · В Dialog: `if (event.key === "Escape") { if (event.defaultPrevented) return; … }`; убрать `settingsOpen = false` из `onkeydown` App (закрытие — дело Dialog); поле поиска с текстом гасит Esc само
FS-028 · bug · medium · frontend/src/lib/retry.ts:28 · Retry в окне вывода повторяет не ту операцию: `retryOf` сверяет только ярлык и первое слово-не-флаг после подкоманды, а `operation_label` (crates/git_engine/src/outcome.rs:129) называет «Push» и `git push --delete origin topic` (Delete Remote Branch, crates/git_engine/src/branches.rs:94), и Push To / Push Up To / push тега (`git push --progress origin <refspec>`); Retry (App.svelte:2980) делает `runNetwork("push")` — обычный push текущей ветки. R-91: Retry — только для операций, которые при повторе значат то же, «удаление ветки… — не повтор» · Delete у ветки `origin/topic` без прав на сервере → Show Output → Retry: на origin уходит текущая ветка (в том числе неопубликованные коммиты), удаление не повторено · Повторять только команду без refspec и без `--delete`/`--tags` (`git push [--progress] <remote>`, `git fetch [--progress] <remote>`), либо хранить в записи намерение (`{kind, remote, refspec}`) и повторять его
FS-029 · inconsistency · medium · frontend/src/App.svelte:1476 · `runNetwork("pull")` всегда `--ff-only` к `network.primary`: так тянет Pull из меню строки Repositories для открытого репозитория (`syncListed`, 2656) и для остальных (2670), и Retry неудавшегося Pull (2980) — мимо `Preferences ▸ When a pull cannot fast-forward`, Pull scope, «Delete Merged Branches after Pull» и отслеживаемого remote, которые учитывает Pull тулбара (`runRemoteSteps`, 1494); R-91: Retry — «то же действие приложения, что и кнопка на тулбаре» · Preferences ▸ «Merge the remote branch in», ветка разошлась с upstream → ПКМ по репозиторию ▸ Pull → «Not possible to fast-forward»; ветка отслеживает `upstream/main` → Pull из меню идёт в origin и падает «did not specify a branch» · Pull открытого репозитория в `syncListed` и в `retryFor` — через `pullNow()`; для остальных — тот же `remotePlan` с `currentRemote(upstream)` и `pullMode` из настроек
FS-030 · ux · medium · frontend/src/lib/toolbar.ts:365 · Pull, Push и Sync (тулбар, палитра App.svelte:538–540, Remote ▸ Synchronize) требуют только remote: в отсоединённом HEAD (и посреди rebase) и на ветке без upstream они активны, и каждый клик кончается ошибкой git; первый push новой ветки кнопкой Push невозможен (`git push <remote>` без refspec, crates/git_engine/src/network.rs:65), Push To upstream не ставит, а задать его негде — пункт ушёл из меню Branches в a70b891 (R-254: «не сделано»), хотя F-130 его обещает · Проверено git 2.51: новая ветка → Push → «The current branch feature has no upstream branch»; Pull → «You asked to pull from the remote 'origin', but did not specify a branch»; detached HEAD → «You are not currently on a branch» · Правила `pull`/`sync` — ветка и upstream, иначе причина («HEAD is not on a branch», «The branch tracks no remote branch»), `push` — ветка; Push без upstream — `push --set-upstream <remote> HEAD` (флаг в `push`) или флажок в Push To; палитре — те же правила через `commands`
FS-031 · bug · medium · frontend/src/components/layout/CommandOutput.svelte:108 · Немодальное окно вывода (R-89: с ним сверяются с графом и файлами) забирает клавиши всего окна без проверки фокуса: Ctrl+A гасится везде и ставит `allSelected`, после чего Ctrl+C в любом поле копирует весь отчёт команды вместо выделенного; Ctrl+F открывает его поиск вместе с поиском Diff; Ctrl+= / Ctrl+- меняют его шрифт; любой Esc (закрыть диалог, палитру, поиск Diff) закрывает и его; OutputPanel.svelte:23 так же прячет панель Output на любой Esc · Show Output у ошибки → щёлкнуть в поле сообщения коммита → Ctrl+A (текст не выделяется) → выделить слово мышью → Ctrl+C: в буфере весь вывод git · Обрабатывать клавиши, только когда фокус внутри окна (`section.contains(event.target)`), Esc — если не погашен; в OutputPanel — так же
FS-032 · bug · low · frontend/src/stores/hooks.svelte.ts:124 · Save as preset с именем без латиницы и цифр молча ничего не делает: id строится заменой `[^a-z0-9]+` на `-`, пустой id — `return` без сообщения, а запрос имени проверяет только непустоту (`textProblem`, App.svelte:3580) · Manage Hooks → Save as preset у pre-commit → «Проверка» → Save: пресета нет, уведомления нет · Проверять id в `validate` запроса («Use Latin letters or digits»), либо строить id из `\p{L}\p{N}`; пустой id — `report`
FS-033 · race · low · frontend/src/stores/hooks.svelte.ts:54 · `edit()` ставит `editing = name` до чтения, а `body`/`saved` остаются от прошлого хука (Cancel сбрасывает только `editing`, App.svelte:3548): пока `readHook` не ответил, редактор под именем B показывает текст A, Save активна и пишет его в B, `dirty` при этом false · Edit pre-commit → Cancel → Edit commit-msg и сразу Save (медленный диск, антивирус) → commit-msg перезаписан скриптом pre-commit · До ответа `body = saved = ""` и флаг `loading`; Save и поле выключены, пока он поднят
FS-034 · ux · low · frontend/src/lib/toolbar.ts:409 · Пустой репозиторий и чистое дерево: Tag (тулбар и палитра) активен без единого коммита и отвечает уведомлением-ошибкой «There is no commit to tag yet» (RefActions.svelte:294, в футере горит Error); Stash и Quick Stash All на нерождённой ветке активны — git «You do not have the initial commit yet»; Stash All меню, палитры и Ctrl+S (App.svelte:546, только `noRepo`) открывает диалог и на чистом дереве — git «No local changes to save», ничего не происходит, хотя кнопка тулбара в том же состоянии выключена («The working tree is clean») · `git init`, создать файл → Tag → ошибка; на чистом дереве Ctrl+S → имя → Enter → ничего · Одно правило для тулбара и палитры: `tag` и `stash*` требуют `head !== null`, Stash All палитры — ещё и `changes`
FS-035 · inconsistency · low · frontend/src/App.svelte:544 · Сочетания в палитре (`shortcut: "Ctrl+S"` и др.) и в подсказках тулбара (lib/toolbar.ts:49–118, `data-tip-hint`) — строки-константы: после переназначения в Preferences ▸ Keyboard палитра и тулбар показывают прежние клавиши, меню — новые; на macOS пишут «Ctrl» вместо ⌘ (CLAUDE.md: Ctrl/⌘ — через одну абстракцию; `prettyKeys` в lib/keymap.ts:72 есть) · Keyboard ▸ Stash All → Ctrl+Shift+H (применить) → Ctrl+Shift+P: у Stash All «Ctrl+S», подсказка кнопки Stash — «Ctrl+S» · Показывать `prettyKeys(effective(bindings, keymap)[id], onMac)` по id команды, у тулбара — по id действия
FS-036 · ux · low · frontend/src/components/layout/StatusBar.svelte:120 · Статусная строка ничего не усекает: `.item` не сжимаются (nowrap, `min-width: auto`), футер `overflow: hidden` — при длинных имени ветки и upstream индикатор состояния (`Pushing… 45%`, `Error`) и кодировка уезжают за правый край, а статус операции есть только в футере (frontend/CLAUDE.md) · Окно ~1000 px, ветка `feature/PROJ-1234-…` на 50 символов с одноимённым upstream → справа не видно ни «Error», ни хода push · `min-width: 0` и общая утилита усечения у имени репозитория, ветки и upstream; индикатор и кодировка — `flex: none`
FS-037 · inconsistency · low · frontend/src/components/layout/HooksPanel.svelte:69 · Hooks и Safety Journal (SafetyJournal.svelte:26) — самописные модалки вместо общего Dialog (frontend/CLAUDE.md, R-108): фокус не переносится внутрь и не держится там — Tab уходит в панели под подложкой, стрелки двигают выделение графа, если фокус был в нём; Esc — мимо общего пути (FS-025) · Local ▸ Safety Journal… из меню при фокусе в графе → ↓: выделение в графе под журналом сдвинулось · Перевести на `Dialog` (`flush`, `footer`), кнопки — `.btn`
FS-038 · inconsistency · low · frontend/src/components/layout/SettingsPanel.svelte:227 · В Preferences — нативные радио (Avatars, Date format, Pull mode, Algorithm, Whitespace; в GraphField.svelte — Time, Row height), галочки `<input type="checkbox">` (Coloured lanes, Detect moves, Confirm exit, Auto update, Word diff) и ползунки `<input type="range">`; Dialog лишь красит их `accent-color`, `color-scheme` не задан — на тёмных темах системные светлые кружки и квадраты; то же OutputPanel.svelte:33 (Problems only). F-243: «в Preferences нет ни одного контрола, нарисованного Windows»; дополняет FR-014 · Тёмная тема → Preferences ▸ Diff View · `Checkbox` вместо `<input type="checkbox">` (GraphField для колонок его уже берёт), общий `Radio` из FR-014, свой ползунок или `color-scheme` тёмных тем
FS-039 · ux · low · frontend/src/components/layout/Toolbar.svelte:61 · Выпадающие меню тулбара (Pull, Push, Sync, Stash, Rebase, «…») не закрываются по Esc и не знают клавиатуры: у Toolbar нет обработчика клавиш, Esc в App.svelte:906 их не трогает — закрыть можно только щелчком мимо или выбором пункта · Открыть меню Pull → Esc: меню висит · Пока `open !== null` — Esc → `open = null` на `svelte:window`, ↑/↓ по пунктам, фокус на первый пункт при открытии с клавиатуры
FS-040 · inconsistency · low · frontend/src/BlameWindow.svelte:35 · Дочерние окна читают настройки один раз при открытии (`settings.load()`; InvestigateWindow.svelte:58, CompareWindow.svelte:19, MergeWindow.svelte:18), смена темы и формата даты в Preferences до уже открытых окон не доходит — F-335: «выбор сразу перекрашивает окно, окна diff и merge» · Открыть Blame, в главном окне Preferences ▸ Theme → Light: Blame тёмный до переоткрытия · Событие об изменении настроек (из `write_setting` или `settings.apply`) и перечитывание ключа `settings` в дочерних окнах
FS-041 · inconsistency · low · frontend/src/App.svelte:527 · `Remote ▸ Synchronize` (Ctrl+Shift+S) — `syncNow()` с порядком кнопки Sync (`toolbar.prefs.syncOrder`): после выбора «Push, then Pull» в меню Sync Synchronize сначала пушит; F-320: «делает Pull, затем Push… если Pull не удался, Push не запускается», синонимы палитры — «pull then push» (lib/remote-menu.ts:102) · Меню Sync ▸ Push, then Pull → Ctrl+Shift+S: первым идёт push · Либо Synchronize — всегда `syncNow("pullThenPush")` без записи порядка, либо F-320 и синонимы — «в порядке кнопки Sync»
