# Аудит: diff_engine и панель Diff

25.09.2026 · просмотрены: `crates/diff_engine/src/**` (lib, text, eol, words, patch, headers, moves, merge, syntax, images, language, batch), `crates/diff_engine/tests/{patch,newline,text,budget,moves_across}.rs`, `crates/app_state/src/diffing.rs`, `crates/app_state/tests/{stage_lines,discard_lines}.rs`, `crates/git_engine/src/{blobs,apply,conflicts}.rs` (как вход конвейера), `src-tauri/src/commands/{mod,conflicts}.rs` (diff-команды), `frontend/src/components/diff/**`, `components/panels/DiffPanel.svelte`, `lib/{diff-rows,diff-fold,diff-band,diff-toolbar,diff-search.svelte,highlight,merge-view,merge-params,compare-params,selection,child-window,staleness,disk-change,mutation}.ts`, `stores/{diff,compare-view,conflicts,settings}.svelte.ts`, `MergeWindow.svelte`, `CompareWindow.svelte`, места вызова в `App.svelte`; сверка с `doc/08`, `doc/11 §7`, `doc/12` (R-04, R-10, R-74, R-100, R-101, R-106, R-271), `doc/features/F-0xx/F-2xx/F-3xx` по diff и слиянию. Опыты `git apply` — во временной папке вне репозитория (удалена).

## Проход 1

DF-001 · bug · high · crates/diff_engine/src/patch.rs:93 · Stage lines: невыбранная удалённая последняя строка без перевода в конце становится контекстом с маркером `\ No newline at end of file`, и выбранные `+`-строки после неё приклеиваются к ней — в индекс уходит `xy` вместо `x\ny` · Индекс `a\nx` (без перевода в конце), рабочий файл `a\nx\ny\n`; в Unified выбрать только `+y`, Stage lines → патч ` a`/` x`/`\ No newline…`/`+y`; проверено `git apply --cached --unidiff-zero`: индекс стал `a\nxy\n`, коммит получит склеенную строку · В `build_patch` в прямом режиме невыбранный `Delete` с `no_newline`, за которым в патче идут `+`-строки, писать как `-x` + маркер и добавлять `+x` с переводом строки (как git-gui), либо отказывать с объяснением; тест в `app_state/tests/stage_lines.rs`
DF-002 · bug · high · frontend/src/App.svelte:1016 · После внешней правки (редактор, `git add` в терминале) панель Diff не перечитывается: `applyDiskChanges` зовёт `afterMutation()` без путей, `diff.dropIfAffected([])` ничего не делает, а строка 1018 гасит точку «устарело» у Diff (F-223 обещает гасить после перечитывания); Stage lines и Stage блока строят патч из старых ханков и индексируют текст, которого на диске уже нет · Открыть Unstaged-diff файла, в редакторе поправить строку внутри изменённого блока, вернуться, нажать Stage у блока → в индекс уходит прежняя версия строки, точка устаревания не горит; трасса `onDiskChange` → App.svelte:1016 → `afterMutation` → stores/diff.svelte.ts:188 `return` · В `applyDiskChanges` при `plan.worktree`/`refs` вызывать `diff.reload()` для спеков `workTreeVsIndex`/`indexVsHead`/`commitVsWorkTree` (с сохранением прокрутки) и гасить `diff` только после него; для страховки в `selection_patch` сверять присланные ханки с `diff_text` текущих сторон и отказывать при расхождении
DF-003 · bug · high · frontend/src/components/diff/MergeView.svelte:147 · `Edit result by hand` доступен при нерешённых конфликтах, `mergedText` подставляет в них base без маркеров (lib/merge-view.ts:83), а нижняя `Save resolution` не выключается при `left > 0`: правки обеих сторон в нерешённых областях молча теряются; комментарий теста «The caller refuses to save while anything is unresolved» для этого пути неверен · Файл с двумя конфликтами → ничего не выбирать → Edit result by hand → в поле base без маркеров → Save resolution → записан и проиндексирован base · Для нерешённых конфликтов в тексте ручного режима писать стандартные маркеры `<<<<<<<`/`|||||||`/`=======`/`>>>>>>>`; нижнюю кнопку выключать, пока в тексте остались маркеры (или пока `left > 0` и регион не правлен)
DF-004 · bug · high · crates/app_state/src/diffing.rs:167 · Превью слияния читает три стороны с потерями (`ConflictSides::to_text` → `from_utf8_lossy`, git_engine/src/conflicts.rs:35) и не проверяет бинарность; DiffPanel показывает MergeView для любого файла с регионами (их всегда ≥ 1), Save пишет этот текст: бинарный (PNG) или не-UTF-8 (cp1251) конфликтный файл получает U+FFFD вместо байтов и CR→LF и индексируется; ConflictView с «сырыми» Take ours/theirs для таких файлов не показывается · Конфликт по .png или файлу в cp1251 → в Diff четыре панели с мусором → Take all ours → Save resolution → файл испорчен (EF BF BD вместо исходных байтов) и добавлен в индекс · `merge_preview` возвращает отдельный ответ «бинарный / не UTF-8» (NUL в первых 8000 байт или ошибка `from_utf8` на любой стороне), панель для него предлагает только Take ours/theirs (`resolve_with`, сырые блобы); `resolve_conflict_text` для таких файлов отказывает
DF-005 · bug · high · crates/app_state/src/diffing.rs:14 · Переименование и копия диффаются по одному пути на обеих сторонах: `diff_sides(spec, path)` ищет новое имя и в родителе/HEAD, поэтому переименованный с правками файл показан как добавленный на 100 % (CommitVsParent, CommitVsCommit, IndexVsHead), хотя список Files пишет «← old.txt 95%»; у `DiffSpec`/`diff_file` нет старого пути · `git mv a.txt b.txt`, поменять строку, закоммитить; выбрать коммит, кликнуть b.txt → все строки `+` · Добавить в `diff_file` `old_path: Option<String>` (у фронтенда он есть, App.svelte:2001, `FileEntry.oldPath`) и читать старую сторону по нему; то же для Staged (worktree.rs:208 уже отдаёт переименования)
DF-006 · bug · medium · crates/diff_engine/src/text.rs:328 · При `Context lines` = 0 (ползунок разрешает 0, SettingsPanel.svelte:346) ханк чистой вставки получает `old_start = 0` (git пишет `-5,0`), а `foldDiff` считает промежутки от `oldStart + oldLines`: полоса над ханком пропадает (`oldTo = -1`), хвостовая считает `oldTotal + 1` строк от строки 0, `eolLabel` считает старую сторону отсутствующей · Preferences → Context lines 0; файл из 10 строк, вставить 2 строки после 5-й → над изменением нет полосы, внизу «11 lines hidden», в тулбаре только новое окончание строк · В `build` для стороны нулевой длины начало = `from` (как git), в `lib/diff-fold.ts:98,101,170` для `oldLines === 0` брать `oldStart + 1` как начало следующего промежутка и `oldStart` как конец предыдущего; `build_patch` берёт начало с другой стороны и не пострадает
DF-007 · bug · medium · frontend/src/stores/diff.svelte.ts:173 · Одно окончание строк на весь патч: `lineEnding = eol.old` для всех операций, `Mixed` → `\n`. В файле со смешанными окончаниями любой ханк, задевающий CRLF-строку, не накладывается (проверено: `git apply --cached` — «patch does not apply»); Unstage lines (цель — индекс, новая сторона) берёт окончание HEAD, и при разных EOL у HEAD и индекса патч не ложится; doc/08 §7 п. 3 обещает вернуть исходные окончания · Закоммитить `a\r\nb\nc\r\nd\n`, поменять `b` → Stage lines на изменении → ошибка git apply · Собирать строки патча на бэкенде из сырых байтов нужной стороны по номерам строк (Context/Delete — старая, Insert — новая; в обратном режиме — сторона-цель): `selection_patch` уже читает обе стороны; затем убрать `PatchRequest.line_ending`
DF-008 · bug · medium · crates/diff_engine/src/patch.rs:105 · При Whitespace trailing/all строки `Context` несут старый текст (`text.rs` `build` → `text(old_lines, …)`); в обратных патчах (Unstage lines, Discard lines — цель = новая сторона) контекстная строка, отличающаяся только пробелами, не совпадает с целью, и `git apply` отказывает · Unstaged, включить `Ignore ws`, переотступить блок и добавить рядом строку → Discard lines на добавленной → «patch does not apply» · Брать текст контекста со стороны-цели по номеру `new` (то же исправление, что DF-007), либо выключать построчные действия при whitespace ≠ none
DF-009 · bug · medium · frontend/src/components/diff/DiffView.svelte:257 · Discard lines и Discard блока зовут `diffStore.discardLines` напрямую, мимо `mutate`: нет `worktree.load`/`afterMutation` (не обновляются Files, счётчики, журнал безопасности и Undo; watcher заглушён `quiet`), ошибка уходит в полосу внутри Diff, а не в единую очередь уведомлений; diff перечитывается через `reload()` и не очищается, когда показывать нечего · Выбросить единственное изменение файла кнопкой Discard у блока → в Diff «No change in this file.», файл остаётся в Unstaged, запись журнала не видна до следующего обновления · Провести через App, как `stageLines`: `mutate(() => diff.discardLines(keys, of), [path])` (новый колбэк `ondiscard` у DiffPanel/DiffView), убрать `discardError`
DF-010 · ux · medium · frontend/src/components/diff/DiffView.svelte:631 · В Side by side (вид по умолчанию) нет чекбоксов в жёлобе и подсветки выбранных строк: построчный стейджинг (F-057) возможен только в Unified; кнопки Stage/Unstage/Discard lines в тулбаре действуют на выбор, сделанный раньше в Unified и в split невидимый · Вид по умолчанию → выбрать строку нечем; выбрать строки в Unified, переключиться → подсветки нет, Discard lines выбросит невидимые строки · Добавить в split жёлоб ■/□ у левой (delete) и правой (insert) ячейки и `class:staging` у строки пары
DF-011 · ux · medium · frontend/src/components/diff/DiffView.svelte:301 · F6/Shift+F6 в главном окне обрабатываются дважды: App.svelte:915 переводит фокус панели, DiffView (`svelte:window`) прыгает к следующему изменению — оба на одно нажатие; `Ctrl+F`, `F6`, `Ctrl+Alt+Shift+L` по doc/11 §7 имеют область `diff`, но ловятся на окне независимо от фокуса (`Ctrl+F` в поле сообщения коммита уводит фокус в поиск diff) · Открыть diff, фокус в Graph, нажать F6 → фокус уходит на следующую панель, и одновременно прокручивается diff · Обрабатывать клавиши DiffView только при фокусе панели Diff (`focused === "diff"` через проп или стор), а обход панелей в App пропускать, когда фокус в diff; обход панелей по F6 внести в doc/11 или перенести на другой аккорд
DF-012 · bug · medium · crates/git_engine/src/blobs.rs:190 · Символьные ссылки: `blob_in` берёт только `is_blob()`, режим 120000 отсеивается → в коммитах обе стороны `None` → `pointer_diff` → ошибка «… is absent from both sides of the diff»; в Staged ссылка выглядит добавленной (HEAD `None`, индекс `Some`); в Unstaged `blob_on_disk` идёт по ссылке и сравнивает путь-цель с содержимым целевого файла · Коммит с изменённой символьной ссылкой → клик по ней в Files → ошибка в Diff · `is_blob_or_symlink()` в `blob_in`, для рабочего дерева читать саму ссылку (`std::fs::read_link`), показывать смену цели текстом
DF-013 · bug · medium · crates/app_state/src/diffing.rs:14 · Размер проверяется после чтения: `diff_sides` читает рабочий файл целиком (`std::fs::read`) и блоб в память, и только `diff_bytes` отвечает `TooLarge` (text.rs:23); клик по неотслеживаемому файлу в несколько гигабайт читает его целиком — секунды пустой панели и рост памяти вплоть до OOM · Положить в рабочее дерево файл на 4 ГБ, кликнуть его в Unstaged → память процесса растёт на 4 ГБ · До чтения проверять размер (`fs::metadata` для диска, размер из заголовка объекта через `find_header` gix для блобов) и отвечать `TooLarge` без чтения
DF-014 · inconsistency · medium · crates/diff_engine/src/text.rs:207 · `.gitattributes` в конвейере diff не читается нигде: бинарность только по NUL, нормализация EOL безусловная; doc/08 §2 (бинарный по `.gitattributes`) и crates/CLAUDE.md («honouring .gitattributes and core.autocrlf») обещают иное; файлы с `binary`/`-diff` показываются текстом и предлагают построчный стейджинг · В `.gitattributes` `*.dat binary`, поменять текстовый .dat → текстовый diff вместо «Binary file» · В `app_state` читать атрибуты пути через gix (`attributes`) и передавать `binary`/`text`/`eol` в `diff_engine` опциями; если так задумано — поправить doc/08 и CLAUDE.md
DF-015 · bug · medium · frontend/src/lib/merge-view.ts:85 · Результат слияния всегда LF с переводом в конце: `merge3_with_syntax` нормализует окончания (crates/diff_engine/src/merge.rs:99), `mergedText` склеивает `\n` и добавляет `\n`, `resolve_with_text` пишет как есть; CRLF-файл (без autocrlf) коммитится с изменением каждой строки, файл без перевода в конце его получает · CRLF-файл с конфликтом → Save resolution в MergeView → `git diff --cached` меняет все строки (убраны `^M`) · Отдавать вместе с регионами преобладающее окончание стороны ours (`detect_line_ending`) и флаг «нет перевода в конце»; `mergedText` склеивает этим окончанием и сохраняет состояние последней строки
DF-016 · bug · medium · crates/app_state/src/diffing.rs:170 · Конфликт modify/delete: отсутствующая сторона подменяется `""` (`unwrap_or_default`), `merge3` видит «удалены все строки»; выбор ours (удаление) пишет пустой файл и `git add` — файл остаётся в дереве пустым, а не удаляется · В одной ветке удалить файл, в другой изменить, слить → MergeView → ours → Save → в коммит уходит пустой файл · Передавать в превью наличие сторон (`Option`); для отсутствующей стороны предлагать «Delete file» (`git rm`) вместо текстового решения или показывать ConflictView с «Absent on this side»
DF-017 · bug · medium · crates/git_engine/src/conflicts.rs:96 · `write_resolution` пишет блоб стадии или текст в рабочий файл сырыми байтами (`std::fs::write`) без `convert_to_working_tree`: при `core.autocrlf=true` файл становится LF, у LFS-файла в рабочем дереве оказывается указатель вместо содержимого, smudge/ident/working-tree-encoding не применяются (файл области git-engine, но это последний шаг слияния) · autocrlf=true, конфликт → Take ours → в рабочем файле LF, хотя checkout давал CRLF · Для Take ours/theirs — `git checkout --ours/--theirs -- <path>` + `git add` (фильтры применит системный git); для текста — записать через `git hash-object -w --stdin --path=<path>` + `update-index` + `checkout-index`
DF-018 · inconsistency · medium · crates/app_state/src/diffing.rs:29 · `diff_files`/`DiffBatch` (а с ними `link_moves_across_files`, `renumber`, `MoveScope::AcrossFiles`, `claim_diff_request`/`newest_diff`) фронтенд не вызывает: `diffFiles` в `frontend/src` есть только в bindings, `moveScope` не читается нигде; F-202 («вьюер рисует их по-разному») и doc/08 §9 (500 файлов < 2 с через rayon) описывают недостижимое поведение · Перенести блок из одного файла в другой в коммите → в Diff оба файла показывают обычные удаление и вставку · Либо грузить файлы коммита через `diffFiles` (кэш `FileDiffEntry` по пути в сторе) и рисовать `acrossFiles`, либо удалить команду, второй проход и F-202
DF-019 · ux · medium · crates/diff_engine/src/text.rs:23 · Изображения больше `MAX_TEXT_BYTES` (1 МиБ — предел для текста) получают `TooLarge` раньше проверки на картинку: изменённый скриншот на 1,5 МБ показывает «File is too large to diff» вместо Side by side/Swipe/Onion skin (F-073) · Изменить PNG на 2 МБ → в Diff только размер текстом · Проверять `image_mime` до предела размера (со своим, большим пределом для data URL, например 20 МиБ), `TooLarge` оставить для текста
DF-020 · bug · low · frontend/src/components/diff/DiffView.svelte:271 · В split правая контекстная ячейка подсвечивается токенами чужой строки: `cells()` строит `{kind: "context", old: cell.line}`, где у правой стороны `cell.line` — новый номер, а `tokensFor` ищет `"c" + old` в `oldAt`; ниже первой неравной вставки/удаления цвета правой колонки берутся от строки со старым номером, равным этому новому · TS-файл, вставить 3 строки в начало, Side by side → в контексте ниже цвета синтаксиса справа сдвинуты · Для правой стороны брать `tokens.new[tokens.newAt.get("c" + cell.line)]` или токены левой ячейки пары (текст тот же)
DF-021 · ux · low · frontend/src/components/diff/DiffView.svelte:975 · `.code.moved` стоит раньше `.code.del`/`.code.add` при той же специфичности, поэтому код перемещённой строки красится как обычное удаление/вставка; цветом перемещения отмечена только колонка знака шириной 3.5ch (F-076: «показывается как перемещение, а не как удаление плюс вставка») · Перенести функцию из 5 строк внутри файла → строки кода красные/зелёные, фиолетовая только колонка `−`/`+` · Перенести правило `.code.moved` ниже `.code.del`/`.code.add` (или `.code.del.moved`, `.code.add.moved`)
DF-022 · ux · low · crates/diff_engine/src/moves.rs:231 · Перемещения: серия считается по списку всех удалённых/вставленных строк файла без учёта соседства и содержания: три `}`, удалённые в трёх местах, и блок из трёх `}` в другом месте (после trim) помечаются перемещением; комментарий «Git's own floor» неверен — git (`--color-moved`) отбрасывает блоки короче 20 буквенно-цифровых символов (`COLOR_MOVED_MIN_ALNUM_COUNT`) · Было `A\n}\nB\n}\nC\n}\nD\n`, стало `A\nB\nC\nD\n}\n}\n}\n` → все шесть строк «moved», с пунктирной лентой · Обрывать серию на несмежных строках (индекс ханка/строки должен идти подряд) и добавить минимум 20 буквенно-цифровых символов на блок, как в git
DF-023 · race · low · frontend/src/stores/diff.svelte.ts:72 · `lineActions` и `stageable` читают `this.spec` (последний запрошенный), а `#lines` берёт ханки из `#shown`: пока грузится следующий diff, на старом включены кнопки другой стороны индекса · Показан Unstaged-diff файла A → кликнуть A в Staged (большой файл, медленно) → на старом diff навести на блок и нажать Unstage → ханки рабочее-дерево-против-индекса уходят в `git apply --cached --reverse` (обычно отказ; при контексте 0 блок удаления вставляется в индекс повторно) · Выводить `lineActions`/`stageable` из `this.#shown?.spec.kind`
DF-024 · race · low · frontend/src/components/panels/DiffPanel.svelte:62 · SubmoduleDiff получает `diff.path` (только что запрошенный), а не `diff.shownPath`: пока грузится следующий файл, под его именем показаны коммиты прежнего подмодуля, и `Initialise this submodule` (строка 66) инициализирует новый путь · Кликнуть неинициализированный подмодуль, затем другой файл и сразу Initialise → `git submodule update --init` не для того пути (ошибка) · `path={diff.shownPath}` и `oninitsubmodule(diff.shownPath)`
DF-025 · ux · low · frontend/src/components/diff/DiffView.svelte:503 · Подтверждение Discard — полоса внутри diff, а не общий модальный диалог (frontend/CLAUDE.md: кнопка закрытия, `Esc` отменяет, `Enter` подтверждает): `Esc`/`Enter` не действуют; текст «This cannot be undone» ложен — `discard_selection` журналирует `Recovery::Patch`, Undo возвращает строки (R-106 снят R-74, тест `the_journal_calls_a_line_discard_undoable`); если за время вопроса diff сменился (`from !== this.diff`, stores/diff.svelte.ts:141), подтверждение молча ничего не делает · Выбрать строки → Discard lines → `Esc` не закрывает полосу; текст говорит, что откатить нельзя, а Undo в журнале безопасности возвращает строки · Общий `Dialog` с текстом «можно вернуть из журнала безопасности»; при сменившемся diff сообщать «The diff changed; pick the lines again»
DF-026 · ux · low · frontend/src/components/diff/DiffView.svelte:519 · «No change in this file.» при реальных изменениях: смена только режима (chmod +x: блобы равны → `Unchanged`) и добавленный/удалённый пустой файл (`None` против `[]` → `old == new`); список Files показывает эти файлы изменёнными · `chmod +x script.sh`, закоммитить → клик по файлу в коммите → «No change in this file.» · `diff_file` передаёт наличие сторон и режимы (у `FileEntry` уже есть `modeChange`): отдельные сообщения «Mode changed: 100644 → 100755», «Empty file added/deleted»
DF-027 · inconsistency · low · frontend/src/components/diff/DiffView.svelte:534 · У `TooLarge` нет кнопки «показать всё равно», которую обещает doc/08 §2; в `DiffOptions` нет способа обойти `MAX_TEXT_BYTES` · Изменить текстовый файл на 1,2 МБ → только «File is too large to diff (1258291 bytes)» · Кнопка, перезапрашивающая diff с флагом обхода предела (`DiffOptions.max_bytes`), либо убрать обещание из doc/08 §2
DF-028 · ux · low · frontend/src/components/diff/DiffView.svelte:527 · Плашка «только окончания строк» печатает значения enum: «Only the line endings changed: crlf → lf», тогда как doc/08 §3 и тулбар (`eolLabel`, «EOL in capitals») пишут CRLF/LF · Перевести файл из CRLF в LF → плашка строчными буквами · Выводить через таблицу `ENDING` из `lib/diff-toolbar.ts`
DF-029 · bug · low · crates/diff_engine/src/patch.rs:40 · `carries_undecoded_bytes` считает заменой байта любой символ U+F780–U+F7FF, в том числе буквальные символы Private Use Area в валидном UTF-8 (иконочные шрифты, Font Awesome занимает U+F000–U+F8FF): построчный стейджинг такого файла отклоняется с ложным «is not valid UTF-8» · CSS с буквальным символом U+F7A2 в `content`, поменять строку → Stage lines → ошибка «not valid UTF-8» · В `selection_patch` проверять `std::str::from_utf8` у реальных сторон (они уже прочитаны), а не символы текста
DF-030 · bug · low · frontend/src/stores/compare-view.svelte.ts:49 · Повторный клик сравнивает только путь: если в Diff открыт тот же файл под другим спеком (коммит, рабочее дерево), клик по нему в списке сравнения очищает панель вместо показа сравнения; список при этом подсвечивает файл выбранным (`selected={diff.path}`) · Открыть diff файла X в коммите C, Compare with HEAD для C → кликнуть X в списке → панель пустая, сравнение — только со второго клика · `if (diff.shows(spec, path))` вместо `diff.path === path`
DF-031 · inconsistency · low · frontend/src/CompareWindow.svelte:19 · Окно сравнения игнорирует Preferences: `diff.load` стартует до завершения `settings.load` (контекст, алгоритм, word-diff, перемещения — по умолчанию), `diff.whitespace` из `settings.ignoreWhitespace` не ставится (в главном окне ставится, App.svelte:335); подмодуль в окне рисуется пустым DiffView (вид `submodule` не обработан ни в CompareWindow, ни в DiffView) · Preferences: Context lines 10, Ignore whitespace all → двойной клик по файлу → в окне 3 строки контекста и видны пробельные правки · `await settings.load(); diff.whitespace = settings.current.ignoreWhitespace;` до `diff.load`; ветка `submodule` → SubmoduleDiff
DF-032 · inconsistency · low · frontend/src/components/diff/DiffView.svelte:297 · Аккорды `Ctrl+Alt+T` (Stage выделенных строк), `Ctrl+Alt+Shift+T` (Unstage) и `Ctrl+Space` (развернуть свёртку) из doc/11 §7 не реализованы нигде — ни в DiffView, ни в меню · Выбрать строки в Unified, нажать `Ctrl+Alt+T` → ничего · Добавить в `onkeydown` (с проверкой фокуса Diff из DF-011) или убрать из doc/11
DF-033 · ux · low · frontend/src/components/diff/MergeView.svelte:125 · Выбор для конфликта нельзя поменять: кнопки theirs/ours/both показаны, только пока `result === null`; после ошибочного клика остаются лишь `Take all …` (сбрасывает все выборы) или Cancel; выбор стороны с пустыми строками, наоборот, возвращает кнопки, хотя конфликт уже считается решённым · Три конфликта, в первом по ошибке выбрать ours — переключить только его на theirs нечем · Показывать кнопки на первой строке любого конфликтного региона с подсветкой текущего выбора и действием «снять выбор»
DF-034 · dead-code · low · crates/diff_engine/src/lib.rs:118 · `DiffRow::Collapsed` не строится нигде (свёртки ушли на фронтенд, R-271), но живёт в IPC-типе и обрабатывается в patch.rs:38,111, lib/diff-rows.ts:75, lib/diff-fold.ts:116, DiffView.svelte:164 · `grep "Collapsed {" crates/diff_engine/src` — только объявление и сопоставления · Удалить вариант и ветки, перегенерировать bindings, поправить doc/08 §6
DF-035 · dead-code · low · frontend/src/lib/diff-rows.ts:209 · `lacksFinalNewline` используется только в `diff-rows.test.ts`; комментарий про `git apply`, молча добавляющий перевод строки, относится к сборке патча на фронтенде, которая переехала в Rust (patch.rs) · `grep lacksFinalNewline frontend/src` без тестов — только объявление · Удалить функцию и её тесты

## Проход 2

25.09.2026 · другой оптикой: пограничные состояния рабочего дерева (skip-worktree, неотслеживаемые папки и вложенные репозитории, удалённый submodule, одиночный CR), пути ошибок слияния в главном окне и в окне ⧉, взаимодействие выделения строк с перечитыванием после блочных действий, отрисовка длинных строк и подсветка, обещания F-200/F-209/F-212 против кода. Опыты `git apply` / `git submodule` — во временной папке `D:\cogit-work\tmp-diff` (удалена).

DF-036 · bug · high · frontend/src/lib/highlight.ts:110 · `mergePieces` квадратичен: для каждого куска строки линейно ищет покрывающий токен (`tokens.find`) и перебирает `inline`/`hits` (`some`), а на куске между токенами `find` проходит весь массив. На длинной строке (минифицированный JS/JSON 100–900 КБ в одну строку — ниже `MAX_TEXT_BYTES`, и Lezer её парсит: строк всего две) токенов ~10⁵, сравнений ~10¹⁰ на одну видимую строку — WebView замирает на секунды и дольше (правило «ничего не блокирует UI дольше 50 мс») · Закоммитить `dist/app.min.js` одной строкой ~300 КБ, поменять одно имя, кликнуть файл в Files → панель Diff и окно не отвечают; трасса DiffView.svelte:596/618 (unified) или :654/:672 → `cells()` :272 → highlight.ts:86 · Резать строку одним проходом с указателями по уже упорядоченным `tokens`/`inline`/`hits`; строки длиннее порога (например, 2 000 символов) не подсвечивать, как редакторы; тест на время для строки 500 КБ в `highlight.test.ts`
DF-037 · ux · medium · frontend/src/components/diff/DiffView.svelte:887 · Код строк diff обрезается многоточием (`.code { overflow: hidden; text-overflow: ellipsis }`, строка — `position: absolute; left: 0; right: 0`), горизонтальной прокрутки нет: правка за правым краем колонки не видна совсем, а в side-by-side (вид по умолчанию) колонка — половина панели. Совпадение Find за краем тоже не видно. То же в MergeView (`cell mono truncate`, MergeView.svelte:120–123, четыре колонки): конфликт, различающийся в конце строки, решается вслепую · Строка в 150 символов, изменить последнее слово, Side by side при панели ~900 px → обе половины показывают одинаковый текст с «…», видна только заливка строки · Дать строкам ширину самой длинной (`min-width: max-content`) и общий горизонтальный скролл колонки (в split — синхронный у обеих), либо переключатель `Wrap`; в MergeView — горизонтальный скролл колонок или tooltip с полной строкой
DF-038 · ux · medium · frontend/src/components/diff/DiffView.svelte:522 · Плашка `whitespaceOnly` велит «switch the whitespace button off», но кнопка режима пробелов живёт внутри `{#if diff.kind === "text"}` (:427–498, кнопка :462) и для такого файла не рисуется — тупик. При `Ignore whitespace = all` в Preferences (App.svelte:335 делает его режимом сессии) ни один файл, где менялись только пробелы, нельзя посмотреть, не уйдя на другой файл · Preferences → Ignore whitespace: all; поменять в файле только отступы; кликнуть его в Unstaged → плашка, в тулбаре только путь · Вынести кнопку пробелов из ветки `text` (она нужна именно для `whitespaceOnly`) или дать плашке свою кнопку `Show whitespace changes` → `onwhitespace("none")`
DF-039 · bug · medium · frontend/src/components/diff/DiffView.svelte:229 · Выделение строк хранится номерами (`d:<old>`, `i:<new>`) и нарочно переживает блочные Stage/Unstage (комментарий :228, F-212 обещает сохранить выделение), но после них диф перечитывается и номера на изменённой стороне индекса сдвигаются: в Unstaged — у удалений (сторона индекса), в Staged — у вставок. Эффект сброса (:334) смотрит только на `path` и `spec.kind`. Отметки молча переезжают на соседние строки: Stage/Discard lines действуют на другие строки или падают с `nothing selected` при счётчике «2 selected» · Индекс — 30 строк; в рабочем файле вставить 2 строки после 3-й (блок A) и удалить строки 20–25 (блок B); отметить `−` у строк 24 и 25; навести на блок A → Stage; после перечитывания отмечены бывшие строки 22 и 23 (номера 24/25 теперь у них), Stage lines стейджит их удаление · После блочного действия сдвинуть ключи ниже блока на его `new_lines − old_lines` (на той стороне, что меняет действие) и выкинуть ключи самого блока; либо сбрасывать выделение при любом новом `diff` того же файла, кроме перезапросов вида (пробелы, контекст, moves)
DF-040 · bug · medium · frontend/src/App.svelte:1662 · Повторный клик по уже открытому конфликтному файлу (в том числе второй клик двойного щелчка, которым F-099 открывает окно) снова вызывает `conflicts.open`, а та ставит `regions = []` (stores/conflicts.svelte.ts:50): MergeView размонтируется и собирается заново, выборы theirs/ours/both и текст «Edit result by hand» пропадают без вопроса. Переход к другому конфликтному файлу делает то же молча, хотя закрытие окна считает открытое слияние несохранённой работой (App.svelte:2890) · Открыть конфликтный файл, выбрать стороны у трёх конфликтов, кликнуть тот же файл в Files ещё раз → все конфликты снова unresolved · В `openWorktreeDiff` не переоткрывать `conflicts.path === path`; при уходе на другой файл с непустыми выборами спрашивать общим модалом (как `mayClose`) или хранить выборы по пути в сторе
DF-041 · bug · medium · frontend/src/App.svelte:2954 · Обработчик `mergeResolved` закрывает любой конфликт главного окна, не сверяя `event.path` с `conflicts.path`, — ровно то, что c012b2a исправил для `take`/`write`. Выборы и ручная правка другого файла теряются · Конфликтные A и B: A → ⧉ (окно слияния); в главном окне открыть B и выбрать стороны; в окне A — Save resolution → MergeView для B в главном окне закрывается, выборы потеряны · `if (conflicts.path === event.path) conflicts.close();`, обновление панелей оставить как есть
DF-042 · bug · medium · frontend/src/components/panels/DiffPanel.svelte:41 · Открытый конфликт перекрывает любой другой diff: ветка MergeView/ConflictView проверяется первой, а `conflicts.close()` зовёт только `openWorktreeDiff` (App.svelte:1669). `openStagedDiff` (:1651), `openDiff` (:1642), `openStashDiff` (:1189), `compareView.open` (stores/compare-view.svelte.ts:46) и смена коммита (`commit.onchange`, App.svelte:317) конфликт не закрывают — `diff` грузит выбранный файл, а панель продолжает показывать слияние · Во время merge открыть конфликтный файл, затем кликнуть автоматически слитый файл в Staged (или коммит в графе и его файл) → в Diff по-прежнему четыре панели слияния; выход только через Cancel · Закрывать конфликт в `diff.load` любого другого файла (или в каждом `open*Diff`, `commit.onchange`, `selectRef`, `compareView.open`), при непустых выборах — спросить (см. DF-040)
DF-043 · bug · medium · frontend/src/App.svelte:3443 · Ошибки разрешения конфликтов теряются: `void conflicts.take(...).then(...)` (:3443), `void conflicts.write(...).then(...)` (:3451) и `void conflicts.open(...)` (:1664) без `catch`, глобального `unhandledrejection` во фронтенде нет. Упавший `git add` (занят `index.lock`, файл заблокирован редактором) или `conflictText` для файла, уже разрешённого в терминале, не доходит до очереди уведомлений, сырой вывод git нигде не показан — кнопка просто «не работает» (CLAUDE.md, Errors: одна очередь, ничего не теряется) · Открыть конфликтный файл, создать `.git/index.lock`, нажать Take ours или Save resolution → ничего, очередь пуста · `.catch((err) => errors.report(err, "Could not resolve the conflict"))` у `take`/`write`, у `open` — «Could not open the conflict»
DF-044 · bug · medium · frontend/src/MergeWindow.svelte:37 · Ошибка Save в окне слияния пишет `failed = String(err)`, и ветка `{:else if failed}` (:47) заменяет MergeView текстом: выборы и ручная правка уничтожаются, повторить нельзя. `String(CogitError)` — только «CogitError: git add -- x failed with exit code 128»: stderr из `detail` не показан, хотя сырой вывод git нельзя заменять (CLAUDE.md, Errors). Так же показана ошибка `mergePreview` (:22) · Открыть слияние в окне (⧉), отредактировать результат вручную, создать `.git/index.lock`, Save resolution → в окне одна строка без вывода git, правка потеряна · Показывать ошибку полосой над MergeView, не размонтируя его, с полным `detail` (как в очереди уведомлений) и возможностью снова нажать Save
DF-045 · bug · medium · crates/git_engine/src/blobs.rs:166 · Файл с `skip-worktree`, которого нет на диске (sparse checkout или `Skip worktree` из меню Cogit, lib/file-actions.ts:49), при включённом показе skipped (lib/file-switches.ts:25) кликается в Unstaged, и `diff_sides(WorkTreeVsIndex)` читает отсутствие файла как `None`: Diff показывает удаление всего файла с активными Stage/Discard, хотя для `git status` он чист. Блочный Stage выбирает все строки → патч `+++ /dev/null` → `git apply --cached` удаляет файл из индекса (проверено: `git status` → `D  f.txt`), следующий коммит удалит его из репозитория · `git update-index --skip-worktree f.txt && rm f.txt`; в Cogit включить показ skipped, кликнуть файл → «удалены все строки»; навести на блок → Stage → файл застейджен как удалённый · Для записей индекса с `SKIP_WORKTREE` (и `ASSUME_VALID`) отдавать отдельное состояние без построчных действий («skipped by sparse checkout» / «assumed unchanged»), либо брать рабочую сторону из индекса, как git
DF-046 · inconsistency · medium · frontend/src/components/diff/DiffView.svelte:581 · Вьюер нигде не читает `noNewline` (во `frontend/src/components/diff` ни одного вхождения), хотя F-200 обещает, что под флагом «вьюер печатает `\ No newline at end of file`». Когда меняется только перевод строки в конце, видно `− b` / `+ b` с одинаковым текстом, без подсвеченных слов и без объяснения · Файл `a\nb` → дописать перевод строки в конце → Diff показывает удаление и вставку одинаковой строки `b` · Под строкой с `noNewline` рисовать служебную строку `\ No newline at end of file` (или значок в колонке знака с tooltip) в обоих видах, учесть её в `foldDiff`/`splitRows`
DF-047 · inconsistency · medium · crates/app_state/src/diffing.rs:204 · Нормальные состояния рабочего дерева кончаются ошибкой или неправдой. Свёрнутая неотслеживаемая папка (`generated/`, слэш добавляет git_engine/src/worktree.rs:86) — клик в Unstaged → обе стороны `None` → `Invalid repository state: generated/ is absent from both sides of the diff` в панели и в очереди. Вложенный неотслеживаемый репозиторий (`Kind::Repository`, слэш не добавляется) открывает `checked_out_commit` (blobs.rs:86), и SubmoduleDiff пишет «is a submodule … Open it from the Repositories panel», хотя это не submodule и в Repositories его нет (CLAUDE.md, Errors: нормальное состояние — сказать, что случилось) · `mkdir generated && touch generated/a generated/b` → клик по `generated/` → ошибка; `git clone <любой> vendor/x` внутри репозитория → клик по `vendor/x` → «is a submodule» · Для пути-папки без gitlink ни на одной стороне возвращать отдельный `FileDiff` («untracked folder, N files» / «nested repository, not a submodule») и не звать `submodule_pointer`
DF-048 · bug · low · crates/git_engine/src/blobs.rs:67 · Удалённый submodule выглядит существующим: у коммита, удалившего gitlink, `recorded` пуст, `recorded.or_else(previous)` подставляет старый коммит, а `previous` отфильтрован как равный — SubmoduleDiff пишет «Now <старый>» без «Was» и без слова об удалении, а раз папки на диске нет — «It has never been checked out» и кнопку `Initialise this submodule`, на которую git отвечает `pathspec 'lib' did not match any file(s) known to git` (проверено). То же для застейдженного `git rm lib` в Staged · `git rm lib && git commit`; выбрать коммит, кликнуть `lib` (D) → «Now abc1234», Initialise → ошибка · Передавать в `FileDiff::Submodule` `recorded: Option` (или флаг `removed`), в SubmoduleDiff писать «Removed (was …)» и не предлагать Initialise, когда gitlink нет в индексе
DF-049 · ux · low · frontend/src/components/diff/DiffView.svelte:80 · Открытие любой полосы выключает подсветку синтаксиса для всего diff: первое открытие просит контекст на весь файл (`expandedContext(…, true)`), `tokens` собирает все строки всех ханков, и при > 5 000 строках `highlightLines` отдаёт пустые токены (highlight.ts:35) · Файл .ts на 6 000 строк, изменить одну строку → diff подсвечен; нажать `show` на полосе → весь diff монохромный · Порог применять к строкам вокруг изменений и раскрытым (`revealed`), а не ко всему загруженному файлу, или подсвечивать асинхронно по видимому диапазону
DF-050 · inconsistency · low · frontend/src/components/diff/DiffView.svelte:161 · Поиск идёт только по отрисованным строкам: `searchTexts` строится из `unified`/`split`, полосы в нём — `[null, null]`, строки между ханками не загружены вовсе. F-209 обещает поиск «по всему файлу, а не только по видимой части», а поле при нуле совпадений ещё и краснеет (F-373), утверждая, что текста в diff нет · Правка в строке 10, слово `needle` в строке 200; Ctrl+F → `needle` → «no matches», поле красное · Искать и по строкам полос (после загрузки всего файла) и при переходе к совпадению раскрывать полосу (`revealed`), либо подписать область поиска «shown lines»
DF-051 · bug · low · crates/diff_engine/src/eol.rs:59 · Одиночный CR считается переводом строки (`replace('\r', "\n")`), а git делит строки только по LF: у `x\ny\rz\nw\n` git видит 3 строки, Cogit — 4. Номера строк после встроенного CR расходятся с git (Investigate / `git log -L`, blame), а для файла с окончанием `Cr` Stage/Discard lines пишут строки патча с терминатором `\r` (patch.rs:171) — git отвечает `corrupt patch at line 4` (проверено). Заголовки ханков считают строки третьим способом — `str::lines()`, которое CR не делит (headers.rs:10) · Файл с `\r` посреди строки: выделить строку ниже → Investigate открывает соседнюю; файл `a\rb\rc\r` → Stage lines → ошибка git · Нормализовать только `\r\n`, оставляя одиночный `\r` частью строки, как git; при `LineEnding::Cr` отказывать в построчных действиях с понятным сообщением
DF-052 · bug · low · crates/diff_engine/src/patch.rs:33 · `carries_undecoded_bytes` проверяет строки всех ханков запроса, хотя в патч идут только ханки с выбранными строками: один байт Latin-1 где угодно в diff (даже в контексте несвязанного ханка) запрещает построчный Stage/Unstage/Discard чистого ханка с «is not valid UTF-8: stage or discard it whole» · Файл с `é` в Latin-1 в строке 400 (в своём ханке) и правкой в строке 10; Stage у блока строки 10 → отказ · Проверять только ханки, где есть выбранная строка (те, что `build_patch` не пропускает по `!changed`)
DF-053 · bug · low · frontend/src/lib/highlight.ts:28 · `.tsx` и `.jsx` подсвечиваются парсерами без JSX: `language_for_path` отдаёт им `typescript`/`javascript` (crates/diff_engine/src/language.rs:38–39), а PARSERS знает только `dialect: "ts"` и JS без `jsx`. Разметка читается как сравнения и регулярные выражения (`</div>` открывает regex), раскраска дальше сбивается — та же причина, что a19f83c починил для слияния, но не для подсветки · Diff `.tsx` со строкой `return <div className="a">x</div>;` → раскраска после тега неверная · Отдавать имена `tsx`/`jsx` (с бэкенда или по расширению пути) и регистрировать `javascript.configure({ dialect: "jsx" })` и `{ dialect: "ts jsx" }`
DF-054 · bug · low · frontend/src/lib/diff-rows.ts:116 · Поиск ищет в `text.toLowerCase()` и отдаёт смещения в нём, а подсветка режет исходную строку: символ, чей нижний регистр длиннее (`İ` → `i̇`, две единицы UTF-16), сдвигает подсветку каждого совпадения правее себя на единицу · Строка `İstanbul foo`, Ctrl+F → `foo` → подсвечено `oo` и следующий символ · Искать по исходной строке регулярным выражением с флагами `giu` (экранировав запрос) или вести карту смещений при переводе в нижний регистр
DF-055 · ux · low · frontend/src/CompareWindow.svelte:24 · Заголовок и шапка окна сравнения выводят сырое имя варианта `DiffSpec`: «src/a.rs — workTreeVsIndex — Cogit», в шапке (:41) `commitVsWorkTree` без коммита, с которым сравнивают · Двойной клик по файлу в Unstaged или Compare with Working Tree из меню файла → в заголовке `workTreeVsIndex` / `commitVsWorkTree` · Человеческие подписи: «Working tree ↔ index», «Staged ↔ HEAD», «<short oid> ↔ parent», «<short oid> ↔ working tree», «<a> ↔ <b>»
