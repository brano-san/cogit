# Аудит: документация против кода

25.09.2026 · ветка `audit/2026-09-25` (a779a38) · просмотрено: `CLAUDE.md`, `crates/CLAUDE.md`, `frontend/CLAUDE.md`, `doc/01-architecture.md`, `doc/03-git-semantics.md`, `doc/04-ipc-contract.md`, `doc/07-graph-rendering.md`, `doc/11-keybindings.md`, `doc/features/README.md` и все `doc/features/F-*.md` (310), `doc/12-risks.md` R-300…R-390, `doc/refactor-night.md` и `doc/refactor-night-2.md` (открытые находки прошлых итераций); код для сверки: `src-tauri/src/{lib,accelerators,webview2}.rs`, `src-tauri/src/menu/mod.rs`, `src-tauri/src/commands/{mod,investigate,network,repo_rows,worktrees}.rs`, `crates/graph_engine/src/*`, `crates/app_state/src/{graph_cache,graph_layout,graph_overlay,handles,repo_rows,file_actions,lib}.rs`, `crates/git_engine/src/{graph_walk,topo,history,network,pulse,staging,worktrees,published,surgery,repo,search}.rs`, `crates/fs_watcher/src/watcher.rs`, `frontend/src/lib/ipc/bindings.ts`, `frontend/src/App.svelte`, `frontend/src/lib/{keymap,child-window,graph-geometry,graph-style,graph-modes,graph-mode-conflicts,repo-state,format,settings,preferences,toolbar,file-menu,ref-nodes,staleness,stage-all}.ts`, `frontend/src/stores/{graph,graph-overlay,graph-folds,repo-pulse,refs,worktree}.svelte.ts`, `frontend/src/components/{graph/CommitList,graph/GraphCanvas,graph/RefCapsule,diff/DiffView,diff/MergeView,file-list/FileList,file-list/FilePane,file-list/CommitBox,branch-tree/RefTree,repo-tree/RepositoryList,repo-tree/WorktreeList,layout/SettingsPanel,layout/GraphField,layout/CommandOutput}.svelte`, `frontend/src/MergeWindow.svelte`. Пересечения с `doc/audit/{graph,diff,fe-graph-files,fe-repos}.md` (GR-004, GR-009, GR-010, DF-011, DF-032, FG-009, FG-039, FR-013, FR-020) сюда не повторены или отмечены ссылкой.

## Проход 1

DC-001 · inconsistency · high · crates/git_engine/src/network.rs:89 · 03 §3 п.6 (doc/03-git-semantics.md:121) обещает сетевым операциям таймаут и отмену пользователем, а `stream_git` (fetch/pull/push, network.rs:25–87) ждёт процесс `git` без срока и без способа прервать; `cancel_operation` гасит только чтения (04:467). Зависший fetch держит очередь записей репозитория до выхода из приложения. Известно: refactor-night-2 GE-2-22 (п.5 «Требует решения пользователя»), не сделано · `nc -l 9418` и remote `git://127.0.0.1/x` → Fetch → «Fetching» в футере навсегда; Commit и Stage того же репозитория стоят в очереди; кнопки отмены нет нигде · сторож в `stream_git`: таймаут по отсутствию вывода (например 120 с без байта в stderr) → `children::stop_tree(pid)` и `GitCommandError`; команда `cancel_network(id)` и кнопка отмены у операции в футере; иначе — отступление в 12-risks и правка 03 §3 п.6
DC-002 · inconsistency · high · frontend/src/lib/child-window.ts:26 · 04 §7 (doc/04-ipc-contract.md:749) и 11 §9 (doc/11-keybindings.md:149) требуют подтверждения при закрытии окна слияния с несохранёнными правками; `onWindowKey` закрывает по `Ctrl+W` сразу, по `Esc` — если событие не погашено, а MergeWindow.svelte:12 ставит его без проверки `choices`/`edited` (MergeView.svelte:31–32); крестик окна тоже не спрашивает — ручное разрешение пропадает молча. Известно: refactor-night-2 FA-2-17 = FC-2-09, не сделано · открыть конфликтный файл кнопкой ⧉ → выбрать стороны, дописать текст в «Resolved content» → `Esc` в поле (textarea его не гасит) или `Ctrl+W` → окно закрыто без вопроса, выбор и правки потеряны · MergeView отдаёт `dirty` (`choices` не пусты или `edited !== null`); окно слияния перехватывает `whenCloses` и `onCloseRequested` и спрашивает через `confirmation.ask` («Discard the resolution?»)
DC-003 · bug · medium · crates/git_engine/src/history.rs:60 · Кэш графа (R-300) отвечает, если совпали запрос и `refs_fingerprint` (graph_cache.rs:172), а отпечаток хэширует только цели ссылок и HEAD; `visible_refs` же бывает селектором reflog `stash@{N}` (04:205, lib/ref-nodes.ts:248). `stash drop` не верхней записи не меняет цель `refs/stash`, но `stash@{N}` теперь другой коммит — кэш отдаёт прежний граф; сам граф после drop не перечитывается вовсе (lib/staleness.ts:10: `stash` метит только `refs`) · сделать три stash; в Branches отметить `stash@{1}`; меню `stash@{1}` → Drop; переключиться на другой репозиторий и обратно → в логе «commit graph from the cache»: в графе строка выброшенного коммита без метки, отмеченного теперь `stash@{1}` (бывший `@{2}`) нет, пока не сдвинется любая ссылка · в ключ кэша включить то, во что разрешились `visible_refs` (oid каждого через `rev_parse_single`, дёшево), либо размер и mtime `logs/refs/stash`; `stash` в AFFECTS добавить `graph`, когда отмечен stash; тест в app_state/tests/graph_cache.rs; рядом GR-008 (граница shallow — та же дыра в ключе)
DC-004 · inconsistency · medium · doc/11-keybindings.md:59 · ≈30 аккордов реестра не назначены нигде: §2 `Ctrl+=`/`Ctrl+-`/`Ctrl+0` (zoom hotkeys WebView2 у Tauri выключены по умолчанию, своего обработчика нет), `F1`; §3 `Ctrl+Alt+Shift+F` (`fetch-all` без акселератора, menu/mod.rs:97); §4 `Ctrl+T`, `Ctrl+Shift+T`, `Ctrl+Z`, `Ctrl+Shift+Enter`, `Ctrl+K`; §5 все девять; §6 `Ctrl+Shift+A`, `Ctrl+Alt+P`; §8 `Ctrl+Shift+L/E/X/Y`; §9 `F6`, `Ctrl+1…3`, `Ctrl+Shift+1…3` окна слияния (§7 — DF-032). Подсказки тулбара (lib/toolbar.ts:73–118) и контекстное меню файла (lib/file-menu.ts:42–65) их показывают. Известно: refactor-night-2 FA-2-21 (п.4 решений), не сделано · Files → выбрать файл → `Ctrl+T`: ничего, хотя подсказка Stage и пункт меню файла пишут `Ctrl+T`; `Ctrl+=` — масштаб не меняется · нужные (Stage, Unstage, Discard, Blame, Copy SHA, Fetch All) назначить в `SECTIONS` menu/mod.rs — окно заберёт их через `accelerators::table`; остальные убрать из 11 и из `shortcut`/accelerator в toolbar.ts и file-menu.ts тем же коммитом
DC-005 · inconsistency · medium · doc/11-keybindings.md:182 · 11 §10: навигация «одинаково во всех панелях-списках» — ↑/↓, `Ctrl+↑/↓`, `Shift+↑/↓`, Home/End, PageUp/PageDown, ←/→, Enter, `Ctrl+A`, поиск по вводу. В коде клавиатура есть только у графа, и то частично (CommitList.svelte:325 + graph-geometry.ts:218: стрелки, Page, Home/End; нет Shift/Ctrl, Enter и поиска по вводу); Files — только Space (FilePane.svelte:91), Branches — ни одного keydown (RefTree.svelte), Repositories и Worktrees — только Enter (RepositoryList.svelte:329, WorktreeList.svelte:37; ←/→ лишь у узла submodule, :171). По панелям: FG-039, FR-013, FR-020 · щёлкнуть строку в Files или Branches → ↓: выделение стоит; набрать `ma` в Branches — ничего · одна чистая функция «клавиша → курсор/выделение» (lib/list-keys.ts с тестами, по образцу `nextRow`) для FilePane, RefTree, RepositoryList, WorktreeList и графа; либо сократить §10 до сделанного — решение одно на все панели
DC-006 · inconsistency · medium · src-tauri/src/lib.rs:334 · 11 §9.2: аккорды окна Investigate «срабатывают через меню, а не через webview», но перехват `AcceleratorKeyPressed` ставится только окну `main`, а сам проект установил, что акселераторы меню при фокусе в WebView2 не срабатывают (accelerators.rs:3–9, doc/iterations/2026-09-22-status.md:30); `virtual_key` (accelerators.rs:98) к тому же не знает стрелок — `Alt+←/→/↑/↓` не разобрать и с перехватом. Страница эти клавиши пропускает (NavigationPanel.svelte:80, BlamePanel.svelte:73). Известно: refactor-night-2 ST-2-02 = FA-2-22, «вероятно» · открыть Investigate, щёлкнуть в Blame → `Ctrl+2`, `Alt+←`, `Ctrl+D`, `F1` — проверить в сборке, по коду не сработают · `webview2::install_accelerators` для дочерних окон с таблицей из `commands::investigate::MENU` и диспетчером `child:<метка>:<действие>`; `Left/Right/Up/Down/Return` в `virtual_key`
DC-007 · inconsistency · low · doc/07-graph-rendering.md:131 · 07 §3 «Порядок коммитов»: новый обход берёт «время, родителей и строку коммита» из прошлого графа — после R-302 копируются только время и родители (`WalkedHistory`, graph_walk.rs:11; тексты — отдельным кэшем `Texts`, graph_cache.rs:70); doc-комментарий `GraphView` (crates/git_engine/src/search.rs:35 и bindings.ts:904) отсылает к `graph_engine::ViewFilter`, который первые родители больше не решает. §10 и мёртвый `ViewFilter::first_parent` — GR-010, GR-009 · сравнить 07:129–133 с graph_walk.rs:69–82 · в §3 убрать «и строку коммита», добавить фоновое дочитывание текстов (R-303); комментарий `GraphView` — «first parents: the walk (`graph_walk::ByTime`); collapse: `ViewFilter`», перегенерировать bindings
DC-008 · inconsistency · low · doc/07-graph-rendering.md:152 · Таблица §4: «Стэш — в основном графе не показывается, только в References» и «Любой коммит — одинаковое полое кольцо» (:149). В коде (F-331) отмеченный stash попадает в граф, узел — квадрат цвета `--status-stash` (CommitList.svelte:253, 314; GraphCanvas.svelte:135–142; graph-geometry.ts:78–83) · отметить stash в Branches → в графе квадратный узел с меткой `stash@{N}` · строку «Стэш» — «отмеченный в Branches: квадрат `--status-stash`, метка `stash@{N}` (F-331)»; в «Любой коммит» — «кроме stash»
DC-009 · inconsistency · low · doc/07-graph-rendering.md:292 · Таблица §7 обещает несуществующее: наведение не обводит узлы родителей и потомков (GraphCanvas.svelte:148 — только заливка строки под курсором); `Ctrl`+клик ничего не добавляет к сравнению (CommitList.svelte:380–398 не смотрит `ctrlKey`; сравнение — пунктами `Compare with HEAD` / `Compare with Selected Commit`, lib/ref-menus.ts:153–156); тултип капсулы — имя, двойники на remote и worktree, без даты (RefCapsule.svelte:14, lib/format.ts:97–118) · навести на строку графа — родители не выделены; `Ctrl`+клик по второму коммиту — выбор просто переходит · привести таблицу к коду (наведение — подсветка строки; сравнение — через меню коммита; тултип — полное имя, remote-двойники, worktree) или завести задачи на недостающее
DC-010 · inconsistency · low · doc/03-git-semantics.md:178 · Таблица состояний §4 расходится с `stateBanner` (frontend/src/lib/repo-state.ts): у detached HEAD нет `Return to <branch>` (:101 — только `createBranch`), у заблокированного индекса нет `Retry` и `Show which process` (:69–74 — `actions: []`), cherry-pick и revert получают ещё `Skip` (:33–37), в таблице — только Continue/Abort · `git checkout HEAD~1` или `touch .git/index.lock` → в баннере Graph одна кнопка / ни одной · таблицу 03 §4 — по коду (F-037, F-038, F-039 с кодом уже совпадают), либо кнопки: Retry = `repository.refresh()`, Return to — последняя ветка из reflog HEAD
DC-011 · inconsistency · low · doc/03-git-semantics.md:214 · §6: «короткий хеш вычисляется на бэкенде через `repo.object_hash()`, а не обрезанием до фиксированной длины»; в коде короткий хеш — только `shortOid` на фронтенде, `oid.slice(0, 7)` (lib/format.ts:3, 25–26; 41 вызов), бэкенд короткого хеша не отдаёт. В §3 (:127–137) блок `GitCommandError` без полей `id`, `repo`, `operation`, `summary` из error.rs:9 · сравнить §6 с lib/format.ts · записать решение в 12-risks (7 символов на экране, полный oid в копировании и переходах) и переписать §6, либо отдавать короткий id из gix (`Id::shorten_or_id`) в `CommitRow`/`CommitDetails`; блок §3 дополнить полями
DC-012 · inconsistency · low · doc/features/F-216-merge-save-shortcut.md:3 · F-216: «в панели слияния `Ctrl+S` записывает результат»; 11 §6 (:111–115): «в любом редактируемом поле он означает Save». В главном окне `Ctrl+S` всегда забирает окно (webview2.rs:119 → `stash`, menu/mod.rs:111): в панели слияния Diff (DiffPanel.svelte:42 — MergeView без `saveShortcut`) и в поле сообщения коммита открывается диалог Stash All; сохраняет только отдельное окно слияния · конфликтный файл в панели Diff → `Ctrl+S` → диалог Stash All вместо записи результата · F-216 — «в отдельном окне слияния»; 11 §6 и строку `Ctrl+S` в §11 — «в главном окне Stash All везде, включая поля ввода; Save — только окно 3-Way Merge»
DC-013 · dead-code · low · src-tauri/src/commands/repo_rows.rs:26 · После R-354 фоновая проверка идёт через `pull_probe` (stores/repo-pulse.svelte.ts:43–54), а команда `background_fetch` (регистрация src-tauri/src/lib.rs:205), её тело `app_state::repo_rows::background_fetch` (crates/app_state/src/repo_rows.rs:22–34) и обёртка `backgroundFetch` (frontend/src/lib/ipc/repo-rows.ts:22–24) ниоткуда не вызываются; 04:420 описывает её как действующую · `git grep -n "backgroundFetch(" frontend/src` — только определение · удалить команду, регистрацию, функцию app_state, обёртку и строку 04, перегенерировать bindings; `RepoHandle::background_fetch` оставить — на нём тесты tests/pulse.rs:107–142 и замер tests/pull_probe.rs:223
DC-014 · bug · low · frontend/src/lib/settings.ts:111 · Ширина дорожки (07 §5, «настройка Lane width»): настройка и ползунок допускают 8–40 px (settings.ts:111, SettingsPanel.svelte:360–361), а `setLaneWidth` зажимает в 12–48 (graph-geometry.ts:3, 57): позиции 8–11 подписаны «8px…11px», граф рисуется с шагом 12; 41–48 недостижимы · Preferences ▸ Graph & History ▸ Lane width → 8 → подпись «8px», колонки графа не сужаются · один диапазон: экспортировать `LANE_WIDTH` из graph-geometry.ts и брать его в `RANGES` и в `min`/`max` ползунка
DC-015 · inconsistency · low · src-tauri/src/commands/mod.rs:205 · INV-02 и CLAUDE.md («Over ~500 items: `tauri::ipc::Channel`», «Возврат монолитного JSON запрещён») нарушены без записи об исключении: `commit_tree_files` отдаёт все файлы дерева одним `Vec<String>` (04:415 так её и описывает), `worktree_files`, `commit_files`, `compare_files` — все строки разом (на наборе dirty — 2 003 `FileEntry`, R-316) · Files → коммит большого монорепозитория → переключатель Unchanged → один ответ на десятки тысяч путей · записать в INV-02 исключение для списков файлов с замером и пределом, либо стримить через `Channel` порциями по 200, как `search_file_contents`; в 04 у этих команд — ссылка на исключение
DC-016 · inconsistency · low · doc/01-architecture.md:50 · §2–§4 описывают несуществующее: `RepoManager::open(path)` и методы `refs`, `commit_range` (есть `RepoHandle::open` и `SharedRepo`, app_state/src/handles.rs); `git_engine` «зависит от tokio (process)» (:52) и в §4.4 запускает `tokio::process::Command` (:158) — tokio в git_engine нет, git идёт через `std::process` (`children::spawn`, network.rs:94) в пуле блокирующих задач (commands/network.rs:44); список `ChangeKind` (:77) без `Mailmap` (fs_watcher/src/lib.rs:48); команды получают не `State<AppState>` (:83), а `State<AppContext>` · сверить с crates/git_engine/Cargo.toml и src-tauri/src/commands/network.rs · поправить §2 (`RepoHandle::open`, `SharedRepo`; зависимости gix, rayon, globset, ignore, regex, toml), §3 (сеть — процесс в `spawn_blocking`), §4.4, `ChangeKind`, `AppContext`
DC-017 · inconsistency · low · doc/04-ipc-contract.md:176 · Неверные места вне общей оговорки :73–80: `load_commits` с `channel: Channel<GraphChunk>` (:176) — на деле `onProgress: Channel<GraphProgress>` (bindings.ts:23); §5 п.2 (:629) «копирует строки прошлого графа» — после R-302 только время и родителей; `CommitRequest` без `only` (:399); «`Result<_, CogitError>` … вариант `GitCommand`» (:409) — `GitError`, вариант `command`; абзац о несуществующей `list_all_repo_files` (:429); ссылки INV-10 (:436) и INV-01 (:330, :559) вместо INV-02 и 01 §3; вторая строка `list_presets | —` (:538) противоречит первой (:106; код берёт `repo`); строки `has_token`/`store_token` (:564–565) оторваны от таблицы и не рендерятся таблицей; флаги `update-index` у `repair_worktree` (:99) — в коде `-q --unmerged --ignore-submodules --refresh` (worktrees.rs:162–168) · сравнить перечисленные строки с bindings.ts и кодом · исправить эти строки; `has_token`/`store_token` перенести в таблицу «Хуки и учётные данные»
DC-018 · inconsistency · low · doc/features/F-114-panel-shortcuts.md:3 · Устаревшие файлы фич: F-114 — `Ctrl+5` показывает Commit Message, Diff — `Ctrl+6`, Worktrees — `Ctrl+7` (menu/mod.rs:46–53); F-117 — пункта `Tools → Reveal Log File` нет, есть `Help ▸ Open Log Folder` (menu/mod.rs:154); F-019 — «10 МБ и двумя архивами» заменено файлом на запуск (F-260, INV-04); F-045 — `Sync` в тулбаре — Pull, затем Push (lib/toolbar.ts:61–66), Fetch — в меню Pull (F-311); F-030 и F-040 — кнопок Checkout/Apply/Pop/Drop при наведении в Branches нет (RefTree.svelte:109–146; действия — контекстное меню F-355 и двойной клик); F-261 — деревья submodules переживают перезапуск (R-352, F-450); F-263 — настройка называется `Coloured branch lines` (lib/preferences.ts:142) · пройти «Проверить» этих файлов в сборке · переписать тексты по коду; у F-019 и F-261 — пометка «Заменено F-260 / F-450», как у F-248
DC-019 · inconsistency · low · doc/features/README.md:142 · Строки F-136 «Контекстное меню репозитория» и F-137 «Открыть в терминале» ссылаются на файлы, которых нет и не было (`git log --all -- "doc/features/F-136*"` пуст); остальные 310 файлов и ссылки совпадают один к одному · открыть ссылки README · создать F-136/F-137 или убрать строки (меню репозитория уже описывает F-360)
DC-020 · bug · low · frontend/src/components/diff/DiffView.svelte:299 · Аккорды страницы сравниваются по `event.key`, а не по месту клавиши: DiffView (`Ctrl+F`, `Ctrl+Shift+D`, `Ctrl+Alt+Shift+L`, :299–310), FileList.svelte:96 (`Ctrl+F`), MergeView.svelte:57 (`Ctrl+S` окна слияния), App.svelte:922 (`Ctrl+A`), CommandOutput.svelte:120–130 (`Ctrl+F/A/C`); в русской раскладке `event.key` — «а», «в», «ы», и аккорды 11 §7, §9 молчат. FS-2-07 (refactor-night-2) исправлен только в lib/child-window.ts:16 · включить русскую раскладку, открыть diff → `Ctrl+F` — поиск не открывается; в окне слияния `Ctrl+S` не сохраняет · общий `chordOf(event)` по `event.code` (как `keyName` в lib/keymap.ts:66–69) и заменить им сравнения во всех перечисленных местах

## Проход 2

Второй, независимый проход: пограничные состояния (диалог открыт, конфликт, свежий профиль, длинный вывод), пути ошибок и логирование, настройки Preferences против бэкенда, свежий код 23–24.09 (журнал, rebase-планы, меню, каталог фич). Проверено и совпало с кодом (находок нет): лимиты 04 (`file_revisions` 500, `line_history` 200, `investigate` 1…1000, `investigate_log` 200/10 000, поиск 2 МБ/8000/100), DTO и события 04 §6, константы и блоки 07 §5–§6 (128/64/48, 300 мс, 96 МБ), `delete_merged_branches`, `write_repo_settings`, `stash_drop` в журнале, пути `git log -L` с не-ASCII именами (опыт в `D:\cogit-work\tmp-docs`: `log -L` не экранирует имена, в отличие от `git diff`).

DC-021 · inconsistency · medium · frontend/src/App.svelte:2994 · 11 §1 (doc/11-keybindings.md:23): «Разрешение конфликта: `modal` > конкретная панель > `global`», а аккорды меню окно забирает у WebView2 раньше страницы (src-tauri/src/webview2.rs:66–69, таблица — все акселераторы строки меню) и исполняет, не глядя на открытый диалог: `onMenuCommand` (App.svelte:2994–3004) проверяет только `unavailable`, состояние меню (`pushMenuState`, :3034) модалку не учитывает. За любым диалогом срабатывают `Ctrl+W` (закрыть репозиторий), `Ctrl+O`, `Ctrl+S`, `Ctrl+Shift+U/O/S` (Pull/Push/Sync), `Alt+X`, `F5`, `Ctrl+1…7`; редактор клавиш (KeymapEditor.svelte:27, захват по `keydown` кнопки) не получает ни одного занятого аккорда, поэтому обещанные F-127 конфликты с клавиатуры не создать. Частный случай `Ctrl+S` в полях — DC-012 · Repository ▸ Edit Git Config ▸ Repository… → дописать строку → `Ctrl+W`: репозиторий закрыт под редактором; Preferences ▸ Keyboard → кнопка клавиши у Fetch → `Ctrl+O`: вместо записи клавиши открывается выбор папки · счётчик открытых модалок (общий `Dialog`, `confirmation`, `prompt`, палитра) в сторе; пока он больше нуля, `onMenuCommand` команды не исполняет; на время захвата в KeymapEditor — команда `suspend_accelerators(bool)`, которую проверяет `webview2::claimed`
DC-022 · bug · medium · crates/git_engine/src/runner.rs:321 · Настройка `Git executable *` (lib/preferences.ts:81; F-078 «путь к git … сохраняется»; R-107 и R-122: «`logLevel` и `gitPath` читаются на старте») пишется в settings.json и нигде в Rust не читается: `git_command()` всегда `Command::new("git")` из PATH, `read_log_level` (crates/app_state/src/logging.rs:192) берёт только `logLevel`; `git grep -n gitPath -- crates src-tauri` пуст. Звёздочка «вступит после перезапуска» обещает то, что не наступит; About (`git_version`, runner.rs:387) показывает git из PATH · Git не в PATH (Portable Git) → Preferences ▸ Commands ▸ General ▸ Git executable = `D:\PortableGit\cmd\git.exe` → OK → перезапуск → любая запись падает «program not found», About без версии git · читать `settings.gitPath` на старте рядом с `read_log_level` и отдавать в `git_engine::runner` (`OnceLock<PathBuf>` для `git_command()`, проверка `--version` с уведомлением при отказе); либо убрать поле вместе со строкой F-078 и фразами R-107/R-122
DC-023 · inconsistency · medium · crates/git_engine/src/conflicts.rs:95 · INV-12 («перед любой операцией, меняющей … рабочую директорию, состояние фиксируется в журнале безопасности») и frontend/CLAUDE.md («Destructive items confirm») не выполнены у `Resolve ▸ Take Theirs / Take Ours` (lib/file-menu.ts:58–62; App.svelte:2052 — по всем выделенным файлам) и кнопок `Take ours`/`Take theirs` (ConflictView.svelte:46–49 → stores/conflicts.svelte.ts:62): `write_resolution` перезаписывает рабочий файл стороной из индекса и сразу `git add`, `AppState::resolve_conflict` (crates/app_state/src/lib.rs:1212–1220) не пишет запись журнала, подтверждения нет — правки, сделанные в конфликтном файле руками, пропадают без Undo · merge с конфликтом в a.txt → во внешнем редакторе частично разрешить a.txt и сохранить → Files → правый клик → Resolve ▸ Take Ours → ручные правки исчезли, Undo и Safety Journal пусты · до записи сохранить текущий файл (`git hash-object -w`) и записать `Recovery` (вернуть байты, конфликт — `git checkout -m -- <path>`), либо спрашивать через `confirmation.ask`, как Discard; в `resolve_with` — та же проверка «путь конфликтует», что в `resolve_with_text` (:87)
DC-024 · inconsistency · medium · crates/git_engine/src/runner.rs:69 · 03 §3 обещает, что при обрезке окна «целиком вывод остаётся в лог-файле» (doc/03-git-semantics.md:142–145, так же output_text.rs:155), и п.7 (:122–123): на финише — код возврата и `elapsed`, при ошибке полные потоки на `error`. В коде полные потоки — только `tracing::debug!` (runner.rs:69); строка ошибки — команда, код, время, `summary`, без потоков (runner.rs:262–268); успешный финиш на `info` не пишется (runner.rs:246–259, network.rs:127–137); отказ fetch/pull/push (`run_streaming`, network.rs:78–86) на `error` не пишется вовсе. При `Log level` = info (значение Preferences, lib/settings.ts:76) середина обрезанного вывода не сохраняется нигде · pre-commit хук печатает 30 000 строк и выходит с 1 → Commit → окно ошибки: первые 2 000 и последние 5 000 строк с пометкой «скрыто»; в `cogit-*.log` на info — только «running git» и «git failed», скрытых 23 000 строк нет · когда `output_text::trim` что-то вырезал, писать полные потоки на `warn` при любом уровне (или файлом рядом с логом, названным в пометке окна); на финише — `info` с `exit_code` и `duration_ms`; `tracing::error!` в `run_streaming`; иначе поправить 03 §3
DC-025 · inconsistency · medium · crates/git_engine/src/interactive.rs:92 · Reword и squash с новым сообщением (Edit Message меню коммита — RefActions.svelte:546 → `rewordPlan`; визуальный rebase) уходят в план строкой `exec git commit --amend --no-verify -m …` (так же `edit_author`, :184): хук `commit-msg` новое сообщение не проверяет (родной `git rebase -i` на reword его запускает), а обход не попадает ни в журнал обходов, ни в Output — `record_bypass` зовёт только `commit` (commit_write.rs:46). Расходится с F-220 («каждый коммит с `--no-verify` пишет в Output строку-предупреждение»), F-094 и 04:562 (о `--no-verify` там ни слова) · Tools ▸ Manage Hooks ▸ Presets → Conventional Commits → в графе коммит → Edit Message → «stuff» → rebase проходит, хук не запускался, в Output и в счётчике обходов ничего · убрать `--no-verify` из exec reword/squash (отказ хука останавливает rebase на этой строке — обычный путь ошибки) или перед amend звать `git hook run --ignore-missing commit-msg -- <файл>`; если обход оставить — `record_bypass` на каждый такой коммит и строка в 04 и F-220
DC-026 · bug · low · crates/app_state/src/logging.rs:180 · Два умолчания `Log level`: Preferences показывает `info` (lib/settings.ts:76), а бэкенд без ключа в settings.json берёт `debug` (`log_filter(None)`, закреплено crates/app_state/tests/logging.rs:10). Документ настроек пишется целиком при любом изменении (stores/settings.svelte.ts:55, :90), поэтому уровень сам падает с debug на info после смены, например, темы — уровень никто не трогал (F-096: применяется «выбранный в настройках») · чистый профиль → в логе DEBUG-строки «git output», в Preferences — «info»; сменить тему, перезапустить → DEBUG-строк нет · одно умолчание: `unwrap_or("info")` в `log_filter` с правкой теста или `logLevel: "debug"` в DEFAULT_SETTINGS
DC-027 · inconsistency · low · doc/features/F-056-submodules.md:3 · Ещё устаревшие файлы фич (вне списка DC-018): F-056 — «секция Submodules … с кнопками Update и Open» — секции нет (F-242), submodules — узлы дерева Repositories, действия — Remote ▸ Submodule (F-321); F-071 — Delete «отказывается трогать отслеживаемый» — кнопка Delete и `Delete…` меню переносят в Корзину и отслеживаемые (FilesPanel.svelte:157, lib/file-menu.ts:67, F-366); F-096 — «10 МБ плюс два архива» (как F-019: файл на запуск, INV-04); F-097 — «в тулбаре видно, что именно выполняется» — статус только в футере (F-112, frontend/CLAUDE.md; в Toolbar.svelte статуса нет); F-133 — `Tools → Safety Journal` — пункт в меню Local (src-tauri/src/menu/mod.rs:119); F-232 — «аватары начинаются в состоянии „не решено“, запросов нет» — по умолчанию `gravatar` (lib/settings.ts:77, R-145; `ask` — только у старого файла настроек), противоречит F-251; F-249 — «History of this file», «Show in Explorer», «Copy the path», у файла коммита «только чтение и копирование» — меню по F-362/F-365 (lib/file-menu.ts:20–118: Log, Reveal in Explorer, Copy Path/Relative Path/Name, у коммита Cherry-Pick/Revert); F-251 — аватар «перед именем автора» — после (`graphColumns`, lib/settings.ts:80; F-266) · пройти «Проверить» этих файлов в сборке · переписать по коду; F-056, F-071, F-096, F-097, F-232, F-249 — пометка «Заменено F-242 / F-366 / F-260 / F-112 / F-251 / F-362, F-365», как у F-248
DC-028 · inconsistency · low · doc/11-keybindings.md:69 · 11 §4: `Space` в `files` — «Переключить стейджинг строки списка»; в коде пробел на строке Files ставит или снимает отметку мультивыбора (FilePane.svelte:91–95 → `onmark`), как и описывает F-101 («пробел добавляет файл под курсором»); стейджинга по пробелу нет нигде · Files → строка → `Space` → строка отмечена, индекс не изменился · строку §4 — «Отметить или снять отметку строки (мультивыбор, F-101)»; стейджинг — `Ctrl+T` (DC-004)
DC-029 · inconsistency · low · doc/features/README.md:162 · Каталог фич («идти по списку и проверять руками», база для статусов 310 фич фазы 3) после первой пустой строки перестаёт быть таблицей: пустые строки 162, 168, 172, 174, 176, 178, 180, 183, 185, 189, 194–195 рвут её, и ~170 строк F-221…F-451 без шапки GFM выводит абзацами с `|`; фраза «Новая фича — новый файл…» (:184) стоит посреди строк; порядок сбит (F-214–F-220 после F-234, F-200–F-213 после F-220, F-440–F-444 перед F-430, F-470 перед F-450) · открыть README в любом просмотрщике Markdown · одна таблица по номерам без пустых строк, фраза — над таблицей
