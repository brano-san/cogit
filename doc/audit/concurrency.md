# Аудит: конкурентность (сквозная)

25.09.2026 · ветка `audit/2026-09-25` (a779a38) · просмотрено: `crates/app_state/src/{lib,queue,handles,graph_cache,graph_layout,graph_overlay,diffing,safety,rewrite,worktrees,repo_rows,settings,avatars}.rs`; `crates/fs_watcher/src/{lib,watcher}.rs`; `crates/git_engine/src/{runner,children,shared,repo,pulse,maintenance,commit_write,staging,config_file,worktrees,discover,history,mailmap,network}.rs` (последние пять — нужные функции); `crates/avatars/src/{queue,cache}.rs`; `src-tauri/src/{lib,events,operations,shutdown,session_end,renderer_failure,diagnostics,webview_memory,recycle_bin}.rs`, `src-tauri/src/commands/{mod,repo_rows,avatars,worktrees,desktop,tests}.rs`, `src-tauri/src/menu/context.rs`; `frontend/src/stores/{repository,graph,graph-overlay,worktree,diff,commit,stashes,recovery,network,conflicts,submodules,repo-pulse,safety,refs,output,hooks}.svelte.ts`; `frontend/src/lib/{pulse-queue,mutation,operations,wiring,settings-file,content-search.svelte,leaving}.ts`, `frontend/src/lib/ipc/{index,repo-rows}.ts`; `frontend/src/App.svelte` (каскад после мутации, watcher, открытие/переключение/закрытие, Undo, autostash); `scripts/bench/scenarios.mjs`; `doc/12-risks.md` R-197, R-300…R-303, R-314, R-324, R-351, R-353, R-354; `doc/04-ipc-contract.md` §4–5; для поведения зависимостей — исходники notify 8.2 / notify-debouncer-mini 0.7 (drop не ждёт потоков), gix-ref 0.67 (packed-refs на Windows не отображается).

Проверено и чисто: порядок захвата блокировок (`graph` → `texts`/`memo`, `watchers` → `repos`, `safety` → `repos`, обратных порядков нет); guard'ов через `.await` нет (единственный `await` под permit'ом — `mutating_titled`, это `OperationPermit`, не lock); rayon только в `spawn_blocking` (`diff_many`, `overlap_window`, scan); отмена поиска по содержимому и `diff_files` (`newest_diff`); эпохи/поколения в сторах `graph`, `worktree`, `diff`, `commit`, `stashes`, `recovery`, `network.refresh`, `conflicts`, `submodules`, `refs`, `content-search`, `PulseQueue`; `unsafe` в `session_end`, `renderer_failure`, `webview2`, `recycle_bin` — инварианты соблюдены.

## Проход 1

CC-001 · race · medium · crates/app_state/src/lib.rs:982 · Закрытие не атомарно относительно открытия того же пути. `close_repository` сначала снимает watcher и чистит кэши (:986–996), `unregister` — потом (:998), а `open_with` регистрирует по пути в самом конце, после всех чтений (`find_or_register`, :542 → :1281). Фронтенд перечитывает открытый репозиторий именно через `open_repository` (`repository.refresh` → `open(root)`, frontend/src/stores/repository.svelte.ts:231; так делают watcher и F5). (а) Перечитывание, идущее во время закрытия, после `unregister` регистрирует репозиторий заново под новым id (`listed = true`) и запускает ему watcher; фронтенд ответ отбрасывает по `#ticket` (close() его сдвинул), но бэкенд держит закрытый репозиторий открытым, и следующий `refreshList` возвращает его строку как открытую. (б) Открытие, успевшее в `find_or_register` до `unregister`, получает id, который тут же удаляется: панели показывают репозиторий, каждая команда падает `RepoNotFound`, новый watcher снимается веткой `late` (:1005). По той же щели чтение, начатое до `unregister`, заново заполняет `graph`/`cached_rows`/`handles` для закрытого id · На large закоммитить из терминала (watcher → `applyDiskChanges` → `repository.refresh()`, `open_repository` идёт сотни мс) и сразу Ctrl+W, затем кликнуть другой репозиторий — закрытый снова открыт в Repositories, его watcher жив; трасса App.svelte:1003 → repository.svelte.ts:231 → commands/mod.rs:495 → lib.rs:542/1281 параллельно с commands/mod.rs:1037 → lib.rs:998 · Первым шагом `close_repository` делать `unregister` (удалить из `repos` под write-lock, `handles.forget`), потом watcher и кэши; в `open_with` регистрировать, только если корень не закрывали после начала открытия (счётчик закрытий по корню, сверяемый под `repos.write()`), а `build_graph` вставлять граф, только если id ещё зарегистрирован (проверка под `graph.write()`); перечитывание открытого репозитория — командой по `RepoId` (`repo_refs` + `working_state`, как в R-316), которая ничего не регистрирует
CC-002 · race · medium · crates/git_engine/src/maintenance.rs:29 · Авто-обслуживание после коммита (R-314) запускается в своём потоке после того, как `commit` вернулся и `mutating` отпустил полосу. На Windows `--detach` не отделяется (сказано в самом R-314), поэтому `maintenance run --auto` → `gc --auto` (pack-refs, reflog expire, repack, commit-graph) идёт в этом потоке параллельно со следующей записью очереди — нарушение «One operation queue per repository: writes in order» (CLAUDE.md). Работа не видна ни `list_operations` (диалог выхода), ни `session_end_blocker`, а `children::stop_all` на выходе убивает её посреди работы (`taskkill /F`): остаётся `objects/maintenance.lock`, и дальше `maintenance run --auto` молча пропускается (git не пишет предупреждение при `--auto`) · `git config gc.autoPackLimit 1` и пара pack'ов; Commit и сразу (в очереди) Delete Branch / Create Tag / Push — ссылки пишутся одновременно с `pack-refs`/`reflog expire`, возможна «Unable to create '…/packed-refs.lock': File exists»; закрыть Cogit сразу после такого коммита — выход не предупреждает, gc убит; трасса commit_write.rs:49 → maintenance.rs:29 (поток) при уже вызванном `permit.finish` (commands/mod.rs:111) · Выполнять обслуживание операцией полосы: `commit` возвращает признак «нужно обслуживание», а команда после `permit.finish` ставит `enqueue(repo, Other, "Maintenance")` и запускает `maintenance run --auto` под этим permit'ом (через `blocking`), так следующая запись ждёт, а выход и завершение сеанса её видят
CC-003 · race · medium · frontend/src/App.svelte:1272 · Undo тулбара и палитры показывает одну запись, а отменяет, возможно, другую: подсказка — `lastUndo?.description` (App.svelte:3072), а вызов — `safety.undo` → `undo_last` (frontend/src/stores/safety.svelte.ts:18), который выбирает запись в момент исполнения в полосе (crates/app_state/src/safety.rs:81). Мутация, стоящая в очереди перед Undo и пишущая свою запись (Discard, Delete Branch/Tag, reset, rebase…), будет отменена вместо показанной; двойной клик отменяет две записи · Идёт долгий Push; Discard файла A (подтвердить) встаёт в очередь; Undo с подсказкой «Delete branch foo» — после push выполняется discard, затем `undo_last` возвращает изменения A, ветка foo не восстановлена · Передавать id показанной записи: `safety.undoOne(id, lastUndo.id)` (`undo_entry`), как уже делает журнал (App.svelte:2806); `undo_last` и `safety.undo` удалить
CC-004 · race · medium · frontend/src/stores/repository.svelte.ts:164 · `refreshStatus` принимает ответ `working_state` без проверки очерёдности: сверяется только `open.repo !== repo`, нет ни счётчика (у `refreshRefs` он есть — `#refsRead`, :246–250), ни `#ticket`. Чтения идут параллельно в `spawn_blocking`, поэтому ранний ответ, пришедший позже, затирает свежий `status` (заголовок строки Working Tree — frontend/src/components/graph/CommitList.svelte:255, пункты меню Working Tree — frontend/src/components/menus/RefActions.svelte:186) и отдаёт устаревший `conflicted` в `conflicts.refresh(id, known)` (App.svelte:470), где `known` применяется синхронно, мимо его `#listing` · Большой рабочий каталог; застейджить два файла подряд (или stage во время внешнего изменения — `applyDiskChanges` тоже зовёт `afterMutation`): `refreshStatus` №1, читавший статус во время `git add` №2, приходит после №2 — Files уже верный (`worktree.load` с поколением), а Working Tree в графе показывает счётчики до второго stage · `const asked = ++this.#statusRead; const ticket = this.#ticket;` и отбрасывать ответ при `asked !== this.#statusRead || ticket !== this.#ticket`; отброшенный ответ — `null`, тогда `conflicts.refresh` перечитает список сам
CC-005 · inconsistency · medium · src-tauri/src/commands/mod.rs:591 · (совпадает с GR-004) `graph_window` — `async fn` без `blocking()`, а с R-302 это чтение с диска: `AppState::graph_window` (crates/app_state/src/graph_cache.rs:590–609) берёт `handle()` (при сменённом конфиге — `SharedRepo::open`, иначе свежий `odb::Store`, индексы паков грузятся заново — R-324) и под `self.graph.read()` + `texts.lock()` читает `commit_text` до 128 коммитов. Блокирующий ввод-вывод на IPC-воркере tokio («Never block», CLAUDE.md) и под глобальной блокировкой кэша графов: `on_chunk` идущего обхода ждёт `graph.write()` (:513), а за ожидающим писателем (parking_lot fair) встают `graph_window`/`graph_row_of`/`graph_overlay` всех репозиториев · large без commit-graph, прыжок в конец списка во время обхода: каждое окно — 128 `find_commit` на воркере tokio, обход стоит на graph_cache.rs:513 · `blocking("graph_window", …)`; под блокировками только клонировать `Arc<Laid>` и `Arc<Mutex<Texts>>` и собрать oid без текста, тексты читать вне блокировок и вставлять под `texts.lock()`, как `Prefill::read`
CC-006 · race · medium · crates/app_state/src/lib.rs:784 · (продолжение TS-002: там — причина флапа на коммите с хуком) Окно тишины — три таймера по 400 мс (вход в мутацию, выход каждого процесса git в `command_sink` :823, drop `Quiet`), а проверяется в момент доставки события (`Route::classify`, crates/fs_watcher/src/watcher.rs:381). Всё, что один процесс git дольше 400 мс пишет по ходу работы — rebase/cherry-pick нескольких коммитов (HEAD, index, `rebase-merge/` на каждом шаге), pull (refs/remotes после сетевой части, затем merge), stash apply большого изменения, — приходит как внешнее изменение: `applyDiskChanges` запускает второй каскад (reopen, `worktree_files`, `lost_commits`, `load_commits`) посреди мутации. R-197 утверждает «тишина покрывает всю мутацию» · Interactive Rebase 30 коммитов: в логе `RepoChanged` во время rebase и лишние `open_repository`/`load_commits` до его конца · Держать тишину охранником: `RepoWatcher::hold()` → счётчик активных мутаций, `classify` отбрасывает события при счётчике > 0 и `DEFAULT_QUIET` после последнего снятия; `command_sink` оставить для процессов вне `Quiet`
CC-007 · race · low · crates/app_state/src/queue.rs:84 · Полоса — по `RepoId`, а связанный worktree (`open_worktree`, crates/app_state/src/worktrees.rs:22) и submodule (`open_submodule`), открытые в панелях, получают свои id и полосы. Операции полосы владельца пишут прямо в них: Repair — `update-index --refresh` в папке worktree (crates/git_engine/src/worktrees.rs:157), Remove с force — stash его изменений и `git worktree remove` (crates/app_state/src/worktrees.rs:59), Update Submodule — checkout в submodule; параллельно идут операции их собственных полос (commit/stage держат `index.lock`, push/fetch пишут общие ссылки worktree) · Открыть worktree W, Commit с медленным pre-commit хуком (или Push); вернуться к основному и сделать Repair/Remove W — `update-index` упирается в `index.lock` (только строка в логе), remove удаляет папку, в которой идёт git · Ключ полосы — общий каталог (`common_dir`) для worktree, корень верхнего репозитория для submodule: `admit`/`release` по этому ключу (или карта id → id владельца)
CC-008 · race · low · frontend/src/App.svelte:1249 · Autostash при переключении ветки — три отдельные операции полосы (stash push → checkout → `stashes.apply(id, 0, true)`, :1249/:1256/:1267), и изменения возвращаются из `stash@{0}` по номеру. Операция со stash, вставшая в очередь между шагами (Stash / Apply Stash / Drop из тулбара, Undo с восстановлением из stash), сдвигает номер: pop применит и удалит чужой stash, автосташ останется в списке · Пока полоса занята долгой операцией: Switch на ветку при мешающих изменениях → «Stash them…» → сразу Apply Stash (stash@{0}) из тулбара — после checkout pop берёт уже не автосташ · Одна операция бэкенда под одним permit'ом (`checkout_with_autostash`: `stash create` + `store` → switch → `stash apply`/`drop` по oid созданного stash), без номера
CC-009 · race · low · frontend/src/stores/repo-pulse.svelte.ts:45 · Фоновая проверка сервера перестаёт ждать `pull_probe` через 120 с (`within`, :10), но `git ls-remote` и поток `spawn_blocking` живут дальше, а каждый следующий тик (`fetchEvery`, :131–134) снова ставит тот же корень: к серверу, который принял соединение и молчит (HTTP без `http.lowSpeedLimit`, ssh без `ServerAliveInterval`), каждый тик добавляет ещё один висящий процесс и поток; поздний ответ после таймаута ещё и пишет `remoteAhead` (:48) · `backgroundFetchMinutes = 1`, remote за прокси, который держит соединение без ответа: через час — десятки `git.exe ls-remote` в диспетчере задач · Не ставить probe для корня, пока предыдущий не закончился (множество «в полёте» в `RepoPulseStore`), и ограничить процесс: `-c http.lowSpeedLimit=1 -c http.lowSpeedTime=60`, `ssh -o ConnectTimeout=20 -o ServerAliveInterval=15` рядом с `BatchMode` (crates/git_engine/src/pulse.rs:54, :128)
CC-010 · race · low · crates/app_state/src/lib.rs:799 · `command_log` клонирует весь журнал (до 100 записей по 2 МБ stdout + 2 МБ stderr, crates/git_engine/src/output_text.rs:158) под `journal.read()`; всё это время каждый процесс git стоит в `command_sink` на `journal.write()` (:832), а синхронные команды главного потока берут тот же lock: `clear_command_log` (:816) ждёт клон напрямую, `command_problems` (:805) — за ожидающим писателем (parking_lot fair), то есть окно не перерисовывается. После каждой мутации при открытом Output `output.refresh()` и `output.refreshProblems()` идут одновременно (App.svelte:471/475); сама `command_log` — `#[tauri::command(async)]` на воркере tokio (src-tauri/src/commands/mod.rs:784) · Журнал с несколькими большими выводами (push с многословным хуком, lfs), Output открыт, несколько stage подряд — подвисания окна; Clear в Output во время обновления · Хранить записи как `Arc<GitOutput>` (клон под lock — только счётчики), сериализовать вне lock; `command_log` через `blocking()`
CC-011 · race · low · crates/git_engine/src/config_file.rs:128 · Edit Git Config пишет `*.cogit-new` и `std::fs::rename` поверх, минуя `config.lock`: запись git (`git config`, `branch --set-upstream-to`, `remote add`), идущая одновременно — из терминала, из полосы связанного worktree (config общий, полоса своя — CC-007) или при User scope, где полосы нет вовсе (src-tauri/src/commands/mod.rs:323), — теряется или затирает правку. На Windows `rename` без повтора: процесс git, открывший `config` на чтение без `FILE_SHARE_DELETE`, даёт «Access is denied (os error 5)» · Edit Git Config (User) → Save, пока в терминале в цикле `git config --global x.y $i` — одна из записей пропадает · Держать `config.lock` как git: `OpenOptions::create_new` на `<path>.lock`, записать и проверить его, затем `rename`; `rename` повторять несколько раз на `PermissionDenied`
CC-012 · race · low · crates/app_state/src/queue.rs:211 · `settle` сначала рассылает `Operation(Done)`, потом `queue.release(repo)` (:213). Пересыльщик событий на другом потоке (src-tauri/src/events.rs:238) по этому событию спрашивает `session_end_blocker()`; успев до `release`, он видит операцию ещё запущенной и не вызывает `session_end::release` — причина, поставленная `ShutdownBlockReasonCreate`, остаётся после конца последней операции · Выключение Windows во время Push: Cogit держит сеанс; push закончился — изредка строка «1 operation is still running» не снимается · В `settle` вызывать `release` до `emit(Done)`
CC-013 · inconsistency · low · src-tauri/src/lib.rs:392 · Help ▸ Copy Diagnostics выполняется в обработчике меню на главном потоке (`on_menu_event`, :324 → `dispatch_menu_command`, :365): `diagnostics::report` читает часть лога до 10 МБ целиком (`read_to_string` + `lines().collect()`, src-tauri/src/diagnostics.rs:24), листает папку логов (`latest_part`) и спрашивает версию WebView2 через COM. Нарушает «Nothing blocks the UI for more than 50 ms» (CLAUDE.md) · Долгая сессия с логом около 10 МБ, Help ▸ Copy Diagnostics — окно замирает на время чтения · Собирать отчёт в `tauri::async_runtime::spawn_blocking` и писать в буфер обмена по готовности; хвост читать с конца файла (`seek`), а не весь файл
CC-014 · dead-code · low · src-tauri/src/commands/repo_rows.rs:27 · IPC-команда `background_fetch` (с `app_state::repo_rows::background_fetch`, crates/app_state/src/repo_rows.rs:22, и обёрткой `backgroundFetch`, frontend/src/lib/ipc/repo-rows.ts:22) с R-354 ничем не вызывается. Это запись (`git fetch --all` пишет remote-tracking ссылки и паки) по пути — мимо полосы и `Quiet`: любой новый вызов гонялся бы за ref-lock с Fetch/Pull пользователя; doc/04-ipc-contract.md:420 описывает её как действующую · `grep -rn "backgroundFetch(" frontend/src` — только определение; бенчмарк зовёт её лишь в сборках без `pull_probe` (scripts/bench/scenarios.mjs:47–48) · Удалить команду, обёртки в app_state и TS, регистрацию (src-tauri/src/lib.rs:205) и строку контракта; `RepoHandle::background_fetch` оставить тестам `git_engine/tests/{pulse,pull_probe}.rs` или заменить в них на fetch фикстурой
CC-015 · dead-code · low · crates/app_state/src/lib.rs:429 · `AppState::tracked` вызывается только из `app_state/tests/events.rs`, а его `Operation.id` берётся из `next_entry_id` (общего с журналом Undo, :431), тогда как у операций полосы — из `Queue::next_id` (с 0): номера пересекаются, а фронтенд ведёт запущенные операции по `id` (`applyOperation`, frontend/src/lib/operations.ts:12) — Done одной сняла бы с футера другую, ещё идущую · Любой будущий вызов `tracked` во время операции полосы с тем же номером — «Pushing…» пропадает при окончании «Working» · Удалить `tracked` и его тесты (события фаз уже проверяются через `enqueue`) или брать id из `queue.next_id`

## Проход 2

Просмотрено дополнительно: `crates/app_state/src/{network,stashing,settings}.rs`, `crates/git_engine/src/{network,conflicts,graph_walk,mailmap,commit_write}.rs`, `crates/fs_watcher/src/watcher.rs` против notify 8.2 (windows.rs:192), gix 0.87.1 (`repository/impls.rs`, `repository/index.rs`, `status/iter`) и gix-fs 0.22.1 (`snapshot.rs`), tauri 2.11.5 (`app.rs`: выход через `process::exit`); `frontend/src/{MergeWindow.svelte,App.svelte}` (watcher, drop, Undo, закрытие), `frontend/src/stores/{graph,output,network,worktrees,flow,settings,toolbar,safety,stash-view,compare-view,graph-overlay}.svelte.ts`, `lib/{disk-change,mutation,pulse-queue,repo-click,settings-file}.ts`; doc/03 §3, doc/04 §5, R-285, R-300…R-303, R-324, R-351…R-354. Опыты — `D:\cogit-work\tmp-concurrency` (удалена): FileSystemWatcher (те же флаги `CreateFileW`, что у notify) — переименование наблюдаемой папки с наблюдаемым потомком и её родителя → «Access denied», когда наблюдается только корень — сам корень переименовывается, родитель по-прежнему нет; удаление и повторное создание наблюдаемого каталога проходит (POSIX-удаление); 200 пар перезаписей файла через lock + rename — у 83 одинаковый `LastWriteTime` (шаг ≈1 мс).

CC-016 · bug · medium · crates/fs_watcher/src/watcher.rs:65 · Наблюдатель держит открытые хэндлы каталогов (`ReadDirectoryChangesW`), а Windows не даёт переименовать или переместить каталог, в котором открыт потомок, и никакой из его предков. У обычного репозитория кроме корня (:64) отдельно наблюдаются `.git` (:65 и ещё раз на `HEAD` и `index` — файл notify наблюдает через его каталог, :71–79) и `.git/refs` (:77), хотя рекурсивный watch корня уже слышит всё в `.git` (для hooks это учтено, :80–88), — поэтому саму папку репозитория нельзя переименовать, убрать в Корзину или перенести на другой диск (там копия + удаление), а папку выше — вообще. С R-351 наблюдатель живёт у каждого репозитория, на который переключались за сессию, и у каждого submodule/worktree, открытого в панелях (`open_with(…, listed = false)`, crates/app_state/src/lib.rs:542–543), а их из интерфейса не закрыть. R-351 называет цену наблюдателя только в памяти · Открыть A, переключиться на B; в Проводнике переименовать A, удалить в Корзину или перенести на другой диск → «The action can't be completed because the folder or a file in it is open in another program», хотя A не на экране; то же для папки, где лежат репозитории, пока Cogit запущен · Не ставить отдельные watch на `git_dir`, `HEAD`/`index`/`refs`, когда они внутри корня (`path.starts_with(root)`, как для hooks), — тогда корень переименовывается; наблюдатель неактивного репозитория снимать, когда панели уходят (при возврате граф сверяет `refs_fingerprint`, R-300, строку дерева — перечитать), unlisted submodule/worktree закрывать при уходе с них; ограничение записать в R-351
CC-017 · inconsistency · medium · frontend/src/App.svelte:986 · Каскады наблюдателя не дедуплицируются во времени: после 120 мс тишины `onDiskChange` запускает `applyDiskChanges`, не проверяя, закончился ли предыдущий. Каждый проход с `workingTree`/`index` — `worktree.load` (полный статус), `worktrees.refresh`, `afterMutation` (`working_state` — второй полный статус, `list_submodules`, `lost_commits`, stashes, remotes…), а старые чтения бэкенд не отменяет. Запись в неисключённые папки (R-285: `.gitignore` не применяется — `.vs/`, `obj/`, `bin/`, `.svelte-kit/`, `.next/`, логи) идёт непрерывно, и когда проход дольше паузы между пачками событий, проходы накладываются: несколько обходов статуса одновременно. CLAUDE.md: «deduplicate refresh cascades», R-144: «один план — одна волна». Заодно `freshen` старого прохода (:1011, :1018) гасит точки «устарело», поставленные событиями, пришедшими во время него · dirty/large с открытым Visual Studio (пишет в `.vs/`) или `npm run dev` с `.svelte-kit/`: в логе перекрывающиеся `worktree_files`/`working_state`/`list_submodules`, `slow ipc command`, загрузка CPU; трасса App.svelte:986 → :989 → :1009/:1016, пока предыдущий :989 ещё ждёт · Один проход за раз: флаг `applying`; событие во время прохода только копит `pending`, по окончании при непустом `pending` — следующий проход (как `#again` в `repository.refresh`); `freshen` — только для видов, не пришедших заново за время прохода
CC-018 · inconsistency · high · crates/git_engine/src/network.rs:95 · doc/03-git-semantics.md:121 (§3 п. 6): «сетевые операции получают таймаут с возможностью отмены пользователем», doc/modules/M1-core.md:29 отмечает таймаут сделанным, — а Fetch/Pull/Push/Push To запускаются без всякого предела: нет ни таймаута, ни `http.lowSpeedLimit/Time`, ни `ServerAliveInterval` для ssh, а `cancel_operation` по контракту гасит только чтения (doc/04-ipc-contract.md:467). Сервер, принявший соединение и замолчавший (обрыв VPN посреди передачи — без RST соединение висит часами), держит полосу репозитория бесконечно: Stage, Commit, Discard, Checkout этого репозитория встают за ним в очередь, фоновые индикаторы Repositories тоже стоят (`busy` включает `network.running`, App.svelte:2936); выход — только «Exit Anyway». Пользовательский двойник CC-009 (там фоновый probe, полосу он не держит) · Push в https-remote, во время передачи отключить VPN без разрыва соединения (или прокси, который держит соединение без ответа) → push не кончается; Stage файла → «Staging» в очереди навсегда · Для сетевых команд `-c http.lowSpeedLimit=1000 -c http.lowSpeedTime=60` и, если своей ssh-команды нет, `ssh -o ServerAliveInterval=15 -o ServerAliveCountMax=4`; отмена сетевой операции из футера — остановка дерева её процесса (`taskkill /T` по pid, как в `children::stop_all`; ссылки git пишет последним шагом под lock, прерванный fetch/push их не портит); либо поправить doc/03 и M1
CC-019 · ux · medium · frontend/src/MergeWindow.svelte:37 · Неудачный Save в окне слияния заменяет редактор текстом ошибки: `failed = String(err)` → ветка `{:else if failed}` (:47) размонтирует MergeView, выбранные стороны и ручные правки пропадают, повторить Save нечем. Отказ обычен при параллельной работе: `git add` в `write_resolution` (crates/git_engine/src/conflicts.rs:97) упирается в `index.lock` другого процесса git (терминал, IDE, полоса связанного worktree — CC-007), файл открыт другой программой без общего доступа (`std::fs::write`, os error 32), репозиторий в главном окне закрыли и открыли снова (у окна прежний `RepoId` → `RepoNotFound`). DC-002 — о закрытии окна без вопроса, это другой путь · Открыть конфликтный файл кнопкой ⧉, выбрать стороны, дописать текст; создать `.git/index.lock` в терминале → Save → в окне только «Unable to create '…/index.lock': File exists», разрешение потеряно; lock удалён — повторить нечего · Ошибку сохранения держать отдельным полем (`saveError`) и показывать над MergeView, не размонтируя его; `failed` оставить для ошибки `mergePreview`
CC-020 · race · low · frontend/src/stores/graph.svelte.ts:168 · Загрузка графа репозитория, закрытого во время неё, не отбрасывается и доходит до уведомления. `onLeave` (:85) гасит только догоняющую загрузку (`#next`); первая загрузка (граф был пуст: первый репозиторий сессии, после `graph.clear()`) идёт как `#shown`. Закрытие удаляет граф (`forget_graph`), обход останавливается на чанке без `isLast` (crates/app_state/src/graph_cache.rs:514), `#settle` получает пустое окно → повтор `#load(repo, query, false)` (:200), проверка :168 пропускает его, когда не открыто ничего (`open === undefined`), → `load_commits` → `RepoNotFound` → catch (:203–206) → App.svelte:421 «Could not load the graph». R-300 обещает, что `graph.load` для репозитория, который уже не открыт, отбрасывается · Запустить Cogit, открыть large и сразу Ctrl+W (`closeCurrent` не зовёт `graph.clear`, App.svelte:2862) → уведомление «Could not load the graph» с «id 1»; трасса graph.svelte.ts:185 → commands/mod.rs:557 → graph_cache.rs:514 → graph.svelte.ts:199–200 → :168 → :206 · В `onLeave` всегда `this.#loads += 1; this.loading = false`; в `#load` выходить при `repository.current?.repo !== repo`, в том числе когда открытого нет; то же проверять в catch и `#settle`
CC-021 · race · low · crates/git_engine/src/shared.rs:72 · R-324: «снимок gix сам следит за ссылками, HEAD, индексом… (читает с диска по запросу)». Но `SharedRepo::handle` раздаёт клоны одного `ThreadSafeRepository`, у которых общий кэш индекса (gix repository/impls.rs:62, index.rs:115: «The index file is shared across all clones») и буфер packed-refs (`refs.clone()`), а перечитываются они, только если mtime файла строго больше запомненного (gix-fs snapshot.rs:125; сам gix: «relies on sub-second precision or else is a race»). На NTFS время записи идёт шагом таймера (опыт: 83 из 200 пар lock + rename подряд с одинаковым `LastWriteTime`, шаг ≈1 мс, по умолчанию 15,6 мс). Если чтение индекса Cogit (статус worktree.rs:43 и status.rs:99, `stage_blob` conflicts.rs:101, `diff_sides`) попало между двумя записями индекса в одном тике, каждая следующая команда видит прежний индекс, пока его не запишут ещё раз позже, — watcher своё уже сообщил и молчит. До R-324 устаревший снимок жил одну команду · Терминальный `git rebase -i` или `git stash` на большом рабочем дереве, пока Files перечитывается по событиям, — изредка Files и счётчики остаются на промежуточном шаге до следующего stage; детерминированно тестом: `handle()` → `worktree_files`, переписать `index` и вернуть ему прежний mtime → следующий `handle()` отдаёт старое содержимое · Индекс для чтений брать свежим (`repo.open_index()`, как pulse.rs:176) и отдавать статусу через `status(…).index(IndexPersistedOrInMemory::Persisted(…))` вместо `index_or_empty()`; либо в `HandleCache::handle` сверять длину и хвостовые 20 байт индекса с `checksum()` снимка и переоткрывать при расхождении; packed-refs — по длине
CC-022 · race · low · frontend/src/stores/output.svelte.ts:44 · `refresh()` принимает ответ `command_log` без номера запроса, а зовётся на каждый `CommandRecorded` (`notice`, :61) и из каскада (App.svelte:475); `command_log` асинхронная (`#[tauri::command(async)]`, commands/mod.rs:784), ответы приходят в любом порядке — снимок без последней команды может лечь последним, и последней (часто упавшей) записи в открытом Output нет до следующей команды. `clear()` (:48–49) не отсекает ответ, начатый до очистки: очищенный список возвращается, хотя журнал бэкенда пуст · Открыть Output, запустить Pull и сразу нажать Clear → очищенные записи снова в списке; трасса :61 → :44, ответ после :49 · `const asked = ++this.#asked` в `refresh`, принимать ответ только при `asked === this.#asked`; `clear()` тоже увеличивает `#asked`
CC-023 · race · low · src-tauri/src/lib.rs:310 · `WorkerGuard` отдан в `app.manage`, а `App::run` завершает процесс через `std::process::exit` (tauri 2.11.5 app.rs:1346: «the process is exited directly»): деструкторы managed state не выполняются, guard не сбрасывается и не ждёт, пока поток `tracing_appender` допишет очередь. Строки выхода (`RunEvent::Exit`, :351–352: «stopped the git processes still running», «cogit stopped») уходят в канал и гонятся с `ExitProcess`, который первым делом завершает прочие потоки. CLAUDE.md держит guard ради хвоста лога и требует логировать shutdown — по логу нельзя отличить чистый выход от зависшего · Закрыть Cogit; в конце `cogit-<start>.log` строки «cogit stopped» может не быть; трасса lib.rs:350–352 → возврат из обработчика → `process::exit` · Хранить guard в `Mutex<Option<WorkerGuard>>` и в ветке `RunEvent::Exit` после последней строки делать `take()` и `drop` — drop шлёт Shutdown и ждёт, пока поток допишет очередь
