# Аудит: фронт: Repositories, Branches, Worktrees

25.09.2026 · ветка `audit/2026-09-25` (a779a38) · просмотрено: `components/repo-tree/{RepositoryList,ScanDialog,WorktreeList,AddWorktreeDialog,RemoveWorktreeDialog}.svelte`, `components/branch-tree/{RefTree,RefSortButtons}.svelte`, `components/remote/{RemoteOpsDialog,RepoSettingsDialog}.svelte`, `components/panels/{Repositories,References,Worktrees}Panel.svelte`, `stores/{repo-list,repo-groups,repo-pulse,refs,worktrees,submodules,module-forest,module-memory,scan,flow,stashes}.svelte.ts`, `lib/{repo-click,repo-groups,repo-labels,repo-list,repo-menu,repo-phase,repo-settings,repo-state,repo-sync,ref-menus,ref-nodes,ref-sort,module-memory,module-tree,pulse-queue,worktree-list,drop-open,tree,tri-state-box,remote-dialogs,remote-menu,push-to,pull-request}.ts`; для трасс — обработчики панелей в `App.svelte`, `components/menus/RefActions.svelte`, `stores/repository.svelte.ts`, `lib/{multi-select,context-menu,names}.ts`, `components/common/{Checkbox,Disclosure,Dialog}.svelte`, `git_engine/src/{worktrees,submodules,branches,stash}.rs`, `src-tauri/src/commands/mod.rs` (`popup_context_menu`); документы — `doc/05` §3.2–3.3, `doc/11` §10, `doc/12-risks.md` (R-149, R-184, R-240, R-250, R-253…R-255, R-260, R-351…R-355, R-380), F-140, F-342, F-361, F-450, F-451, `modules/M3-repo-tree.md`. Поведение git для FR-004, FR-008, FR-023 проверено консольным git 2.51 во временной папке вне репозитория.

## Проход 1

FR-001 · bug · high · frontend/src/App.svelte:1315 · Одиночный клик по неинициализированному submodule в дереве Repositories без вопроса запускает `git submodule update --init <path>` — клонирует и меняет рабочую копию; нарушает crates/CLAUDE.md «Never change the user's repository without explicit confirmation» и R-149 («Initialize с подтверждением»); комментарий говорит о двойном клике, срабатывает одиночный · Строка `not initialised` в дереве активного проекта или в лёгком дереве любого (R-352): RepositoryList.svelte:163 `onclick → open(node)` → App.svelte:3149/3150 → `openModule` 1313 → 1315 `refreshSubmodule` → 1306 `submodules.update(owner, path, true)` → crates/git_engine/src/submodules.rs:192 `--init`; двойной клик (RepositoryList.svelte:164-168) запускает три `update --init` сразу; так же ведёт себя пункт `Open Repository` меню узла (App.svelte:2579) · Для `notInitialised` звать `offerInitialise(row.key)` (App.svelte:1403, он спрашивает), а не `refreshSubmodule`; повторный вызов, пока идёт первый, отбрасывать
FR-002 · bug · high · frontend/src/App.svelte:2466 · Контекстное меню строки Worktrees не выполняет ни одного пункта: `worktreeContext` сравнивает результат `popupContextMenu` с id, а команда возвращает `()` (src-tauri/src/commands/mod.rs:1350), выбор приходит позже событием `menu-command`; там id `open` совпадает с командой палитры `{ id: "open", title: "Open Repository…" }` (App.svelte:537) — пункт `Open` открывает диалог выбора папки, `Copy Path`, `Reveal…`, `Lock`, `Unlock`, `Remove…`, `Prune`, `Repair…` молчат; в R-260 это записано как «оставлено как есть» · ПКМ по linked worktree → `Open` — вместо worktree диалог Open Repository; `Lock` — метки `locked` нет; трасса: App.svelte:2486 `chosen === null` → 2488-2502 не срабатывают → onMenuCommand App.svelte:2994-3003 → `palette.find(id)` · Как у `repoContext`: запомнить `worktreeTarget = entry`, id с префиксом (`worktree-open`, …), `runWorktreeCommand` в onMenuCommand; пункты — через `offer`/`tidy`
FR-003 · bug · high · frontend/src/components/repo-tree/RepositoryList.svelte:323 · Обычный клик по строке её «отмечает»: `applyClick` без модификаторов даёт `{root}` (lib/multi-select.ts:36) → `onmarked([root])` (RepositoryList.svelte:70-72) → `markedRepos` (App.svelte:3146) → `fetchAll` берёт отмеченные, если они есть (App.svelte:2216); после любого переключения кликом `Fetch All` (`Ctrl+Alt+Shift+F`, doc/11 §3 «все открытые репозитории») fetch'ит один репозиторий; отметки не чистятся при Close/Remove — после закрытия кликнутого `targets` пуст, и Fetch All молча ничего не делает (App.svelte:2218) · Открыть A и B, кликнуть B, `Ctrl+Alt+Shift+F` — обновлён только B; закрыть B через меню и повторить — не делается ничего · Отмечать только `Ctrl`/`Shift`-кликом (обычный клик снимает отметки); наружу отдавать только отмеченные корни, которые есть в `order` и открыты
FR-004 · bug · medium · frontend/src/components/repo-tree/RemoveWorktreeDialog.svelte:21 · `--force` предлагается только при незакоммиченных изменениях, а `git worktree remove` без `--force` отвергает и чистый worktree с инициализированными submodules («working trees containing submodules cannot be moved or removed») — такой worktree из Cogit не удалить (галочки нет, `Remove` падает) · Worktree проекта с submodules, `git submodule update --init` в нём, изменений нет → `Remove…` → `Remove` → ошибка git; `dirty` = false, галочка `--force` не показана (21-25) · Показывать `--force` и когда в worktree есть инициализированные submodules (без stash, если изменений нет), либо бэкенд сам добавляет `--force`, когда мешают только submodules
FR-005 · bug · medium · frontend/src/lib/repo-groups.ts:150 · У свёрнутой группы `walk` не заходит в дочерние группы, их репозитории не попадают в `claimed` и уходят в `Ungrouped` (157-163); все группы стартуют свёрнутыми (R-160, stores/repo-groups.svelte.ts:25-30), так что при каждом запуске репозитории вложенных групп стоят под `Ungrouped (N)` · Группы Work и Client, перетащить Client на Work, репозиторий R — в Client; перезапустить: `Work (0)`, `Ungrouped (1)`, в Ungrouped — R; раскрыть Work — R переезжает туда · `claimed` — все корни с `groups.of[root]` из `order`, независимо от свёртки; свёртка влияет только на вывод; тест с репозиторием вложенной группы при свёрнутом родителе (repo-groups-nesting.test.ts:54 проверяет только пустой список)
FR-006 · bug · medium · frontend/src/components/repo-tree/RepositoryList.svelte:247 · Поле фильтра прячется, когда строк ≤ 1, а введённый `filter` продолжает действовать: оставшийся репозиторий скрыт, очистить фильтр нечем, список пуст без сообщения (`idleMessage` для открытого — `undefined`) · Открыть alpha и beta, ввести `beta`, `Remove…` на beta → поля нет, alpha не видна, панель пуста, пока не открыть ещё один репозиторий · Показывать поле и при `filter !== ""` или сбрасывать `filter`, когда строк стало ≤ 1
FR-007 · bug · medium · frontend/src/App.svelte:2582 · `Open Repository` из меню закрытой строки сначала убирает корень из `closed` (`repoList.opened`), потом открывает; если открыть не удалось (папку перенесли, это не репозиторий), корня нет ни среди открытых, ни среди закрытых — строка пропадает из списка; клик по той же строке (App.svelte:3145) её сохраняет · Закрыть репозиторий, переименовать его папку, ПКМ по закрытой строке → `Open Repository` → ошибка, строки больше нет · Убрать `repoList.opened(root)` в 2582 — `activate` делает это сам после успеха (App.svelte:1714)
FR-008 · bug · medium · frontend/src/components/repo-tree/WorktreeList.svelte:54 · Пропавший и заблокированный worktree не убрать: кнопка `Prune` в строке активна и при `locked`, а `git worktree remove --force` (crates/git_engine/src/worktrees.rs:147) заблокированный отвергает («use 'remove -f -f' to override or unlock first»); в меню `Prune` неактивен (App.svelte:2469), `Unlock` для missing не предлагается (2467-2473 против 2479-2481), `Prune All` locked пропускает (2392); подсказка метки обещает «until it is unlocked» (lib/worktree-list.ts:31) · `git worktree lock ../wt`, удалить папку → в Worktrees `Prune` → ошибка git, разблокировать из Cogit нечем (консольный `git worktree unlock` по пропавшему пути работает, после него `remove --force` проходит) · В меню missing-записи — `Unlock` (активен при `locked`); кнопку `Prune` в строке при `locked` делать неактивной с причиной
FR-009 · ux · medium · frontend/src/App.svelte:1313 · Повторный клик по submodule, который уже в панелях, открывает его заново: в `openModule` нет проверки «уже на экране» (у репозиториев она есть, lib/repo-click.ts:24, #50) — `forgetPanelsKeepingTheTree` сбрасывает выбранный коммит, Diff, стэши, перечитываются граф, worktrees, flow; двойной клик для раскрытия (RepositoryList.svelte:164-168) открывает трижды (click, click, dblclick); двойной клик по закрытой строке — два `activate` (RepositoryList.svelte:361) · Открыть submodule, выбрать коммит и файл в Diff, кликнуть тот же submodule — выбор и Diff пропали, граф перегружается · В `openModule` выходить, если `submodules.open === row.key` и он на экране; в `ondblclick` только раскрывать — открыл уже первый клик
FR-010 · ux · medium · frontend/src/components/repo-tree/RepositoryList.svelte:82 · Фильтр Repositories не раскрывает группы: `groupRows` получает `repoGroups.collapsed`, а все группы стартуют свёрнутыми — при вводе видны только заголовки `Group (n)`, совпадения остаются внутри свёрнутых, пустые группы висят с `(0)`; в Branches то же решено наоборот (lib/ref-nodes.ts:277, R-240 «совпадение в свёрнутой папке не найти») · Две группы с репозиториями, перезапуск, ввести часть имени репозитория — строк репозиториев нет · Пока `filter` не пуст, передавать пустое множество свёрнутых и не выводить группы без совпадений
FR-011 · bug · medium · frontend/src/stores/repo-pulse.svelte.ts:88 · Закрытые строки читаются один раз за сеанс: `watch` просит только невиданные корни, `changed()` приходит лишь от наблюдателя открытых (App.svelte:977-981), у закрытого наблюдателя нет (R-351); при выключенной фоновой проверке (по умолчанию) точка, стрелки и `missing` закрытой строки не меняются до перезапуска, хотя F-451 обещает «изменить файл в закрытом репозитории — точка появляется» · Закрыть репозиторий, изменить в нём файл снаружи — точки нет до перезапуска · Перечитывать не-owned строки (прежде всего закрытые) по фокусу окна или медленным таймером через ту же очередь (2–50 мс на строку, R-353), либо поправить F-451
FR-012 · inconsistency · medium · frontend/src/components/branch-tree/RefTree.svelte:77 · Для каждой строки `checkState` и `leavesUnder` ищут узел в полном дереве (`findIndex` + `slice`, lib/ref-nodes.ts:288-299) — O(видимые × все) на каждый рендер, который идёт после каждого клика по галочке; дерево не виртуализировано — против CLAUDE.md «virtualize lists» и «Nothing blocks the UI for more than 50 ms» · Репозиторий с ~3000 remote-веток и ~2000 тегов, раскрыть origin и Tags, отметить ветку — ~2·10⁷ сравнений id и 5000 DOM-строк за одну перерисовку · Один `$derived` на дерево: позиция узла и число отмеченных листьев под каждым заголовком за один проход; строки — через `VirtualList`
FR-013 · ux · medium · frontend/src/components/branch-tree/RefTree.svelte:87 · Branches без клавиатурной навигации doc/11 §10 («одинаково во всех панелях-списках»): строки `tabindex="-1"`, у дерева нет фокуса и `onkeydown` — нет ↑/↓, ←/→, Home/End, поиска вводом; Tab обходит две-три остановки на строку (треугольник, галочка, подпись); Enter на подписи выбирает, а не делает действие по умолчанию («checkout ветки») · Кликнуть ветку, нажать ↓ — ничего; Tab — фокус на следующей галочке · Roving tabindex на `role="tree"` с обработчиком как у CommitList (`nextRow`): ←/→ → `oncollapse`, Enter → `oncheckout`/`onactivate`, Space → галочка
FR-014 · ux · medium · frontend/src/components/branch-tree/RefTree.svelte:117 · Нативные элементы формы: галочки Branches (`<input type="checkbox">` с одним `accent-color`), галочки ScanDialog.svelte:93 и RemoveWorktreeDialog.svelte:51, радио AddWorktreeDialog.svelte:63,80 и RepoSettingsDialog.svelte:146; `color-scheme` в приложении не задан — на тёмных темах светлые системные квадраты; frontend/CLAUDE.md «no native form controls», общий `Checkbox.svelte` есть ровно для этого · Тёмная тема, панель Branches — неотмеченные галочки белые · `Checkbox` с `indeterminate` (tri-state — тем же `triState` на скрытом input) и общий `Radio`, заменить во всех пяти местах
FR-015 · bug · low · frontend/src/stores/repo-pulse.svelte.ts:78 · Строка, которую панели отпускают, до нового чтения показывает pulse, прочитанный до того, как она стала owned (может быть часовой давности): `rowSync` у не-owned берёт pulse раньше overview (lib/repo-sync.ts:32), а новое чтение — не раньше 500 мс и после того, как новый репозиторий откроется и загрузит граф (очередь стоит, lib/pulse-queue.ts:80-83) · A и B открыты, активен A; переключиться на B и обратно (у A появился pulse), закоммитить и запушить всё в A, кликнуть B — у A секунды горят точка и ↑, потом гаснут · В `setOwned(root)` удалять `pulses[root]`: пока нового чтения нет, строка берёт overview, который `activate` только что перечитал `refreshList`
FR-016 · bug · low · frontend/src/components/repo-tree/RepositoryList.svelte:348 · Ветка и метка состояния (`<rebasing>` и т. п., 342) берутся из overview, который обновляет только `refreshList`, а точка и стрелки — из pulse, где ветка тоже есть (lib/ipc/bindings.ts:1324): после `git switch` в терминале в неактивном репозитории стрелки посчитаны для новой ветки, подписана старая; у закрытой строки ветки нет вовсе; M3-repo-tree.md DoD: «переключение ветки в терминале отражается в дереве в пределах 200 мс» · Открыть A и B, в терминале в неактивном A `git switch other` — в строке A прежняя ветка до ближайшего `refreshList` · `branch` в `RowSync` (pulse ?? overview) и выводить её у всех строк; на событие `refs` неактивного репозитория (App.svelte:978-981) — отложенный `refreshList` ради `state`
FR-017 · bug · low · frontend/src/App.svelte:2514 · Закрытая строка, которую список пометил `missing` (по pulse, RepositoryList.svelte:372), получает меню с активными `Open in Explorer`, `Reveal`, `Open in Terminal`, `PowerShell`, `Git Shell`: `missing: row.overview?.missing ?? false`, а у закрытой overview = null; `Open Repository` активен и при missing (lib/repo-menu.ts:72) · Закрыть репозиторий, удалить его папку, перезапустить (строка `missing`), ПКМ → `Open in Explorer` → уведомление об ошибке · `missing: row.overview?.missing ?? repoPulse.pulses.get(row.root)?.missing ?? false`; `repo-open` — через `offer(…, gone)`
FR-018 · bug · low · frontend/src/App.svelte:2612 · `Rename…` закрытой строки подставляет полный путь: `renameListed(root, undefined)` → `current = names[root] ?? folder ?? root` (2680); Enter без правки сохраняет путь как имя (lib/repo-list.ts:68 сравнивает только с `folderName`) · Закрыть репозиторий, ПКМ → `Rename…` → в поле `D:/work/foo`, Enter — строка называется `D:/work/foo` · Подставлять `folderName(root)`, когда overview нет
FR-019 · ux · low · frontend/src/App.svelte:2467 · Меню worktree прячет неприменимое вместо отключения (у missing нет `Open`/`Reveal`/`Lock`/`Remove…`, у существующего — `Prune`/`Repair…`) — frontend/CLAUDE.md «Inapplicable → disabled, not hidden»; у неактивных пунктов нет причины (R-250, `offer`), нет `tidy`; `Reveal in File Manager` идёт через `revealItemInDir` плагина (2500), а меню репозитория — через `fileMenus.revealOnDesktop` с подписью `Reveal in Explorer` (R-260), против CLAUDE.md «Platform-specific actions … go through one abstraction» · ПКМ по пропавшему worktree — пунктов `Open`/`Lock` нет; по основному — `Lock` серый без причины · Один список пунктов через `offer(id, label, reason)` + `tidy`, `Reveal in ${desktop.fileManager}` через `fileMenus.revealOnDesktop` (вместе с FR-002)
FR-020 · ux · low · frontend/src/components/repo-tree/RepositoryList.svelte:329 · Repositories и Worktrees (WorktreeList.svelte:37) обрабатывают только Enter: нет ↑/↓, Home/End, `Shift`/`Ctrl`+↑↓ для отметок, ←/→ у строк репозиториев и групп (есть только у строк submodules, RepositoryList.svelte:171-172) — против doc/11 §10 · Кликнуть репозиторий, нажать ↓ — фокус не двигается · Общий обработчик клавиш списка (как в CommitList) для обеих панелей
FR-021 · ux · low · frontend/src/components/branch-tree/RefTree.svelte:112 · Треугольник и `aria-expanded` читают `input.collapsed`, а строки развёрнуты по `foldedWhileFiltering` (50): при фильтре у свёрнутой папки видны дети, но треугольник `▶`; клик по нему меняет сохранённое состояние без видимого эффекта, и после снятия фильтра папка неожиданно раскрыта; подпись всегда `Collapse …` · Свернуть папку `feature/`, ввести фильтр `feat` — ветки видны под `▶` · Брать `node.open` из `flatten` (lib/tree.ts:24-25) для `open` и `aria-expanded`, подпись `open ? "Collapse" : "Expand"`
FR-022 · inconsistency · low · frontend/src/components/branch-tree/RefTree.svelte:133 · Двойной клик расходится с `Check Out` меню: тег (App.svelte:1199 `checkoutTag`) отсоединяет HEAD без предупреждения, которое задаёт меню (RefActions.svelte:498-505, R-254), тег не на коммит — ошибка git (в меню пункт неактивен); remote-ветка — `activateRef` (App.svelte:1196-1203) не делает ничего, хотя doc/05 §3.3 «Двойной клик по ветке — checkout», а меню создаёт локальную ветку (RefActions.svelte:493-494) · Двойной клик по `origin/feature` — ничего; по тегу — detached HEAD без вопроса · Двойной клик — через тот же `checkoutTarget`, что пункт `Check Out`
FR-023 · ux · low · frontend/src/lib/ref-menus.ts:195 · `Delete` активен у локальной ветки, которую держит другой worktree (у узла есть `worktree`, lib/ref-nodes.ts:189): подтверждение «Undo can bring it back» → `git branch -d` отказывает («cannot delete branch … used by worktree») → ошибка; то же в меню метки графа (129) · Добавить worktree на `topic`, в Branches ПКМ по `topic` → `Delete` → ошибка · Передавать метку worktree в `BranchesContext`/`RefTarget` и `offer("delete", …, "checked out in a worktree")`
FR-024 · inconsistency · low · frontend/src/App.svelte:2862 · `Ctrl+W` (`closeCurrent`) и пункт `Close Repository` (с тем же `Ctrl+W` справа, lib/repo-menu.ts:86) ведут себя по-разному: меню (`closeListed`) открывает следующий открытый репозиторий (2648-2649), у submodule возвращает к владельцу (2629-2632); `Ctrl+W` оставляет стартовый экран при открытых других, а на submodule/worktree закрывает его регистрацию, оставляя `submodules.open` и `worktrees.ownerRoot` указывать на закрытое · Открыть A и B, активен A: меню `Close Repository` → на экране B; `Ctrl+W` → «No repository open.» · `Ctrl+W` → `closeListed` для строки, которую показывают панели (для submodule/worktree — возврат к владельцу)
FR-025 · race · low · frontend/src/stores/repo-pulse.svelte.ts:115 · `forget` не убирает корень из `unknown` и `remoteAhead`, проба в пути пишет `remoteAhead` мимо `cancel` (48: `#setAhead` раньше проверки `#dropped` в очереди), `fetchEvery(0)` (125) оставляет оба множества как есть — `?` и стрелка pull висят до перезапуска, в том числе у активной строки (`rowSync` учитывает их и для owned) · Включить фоновую проверку, remote с недоступным URL даёт `?`; выключить проверку — `?` остаётся; `Remove…` и снова Open — `?` на месте · Чистить оба множества в `forget` и при `minutes <= 0`; `#setAhead` перенести в `onFetch` под проверку `#dropped`
FR-026 · ux · low · frontend/src/App.svelte:2320 · `Delete this group` удаляет группу сразу — имя, вложенность, состав — без подтверждения и Undo (2354-2357); frontend/CLAUDE.md «Destructive items confirm» · ПКМ по группе → `Delete this group` — группа исчезла, репозитории в Ungrouped · `confirmation.ask` с именем группы и числом репозиториев, уходящих в Ungrouped
FR-027 · inconsistency · low · frontend/src/App.svelte:3133 · Счётчик заголовка Repositories — `repository.openRepos.length`, а тело показывает открытые и закрытые строки (`listedRepos`, RepositoryList.svelte:76); frontend/CLAUDE.md «header counts and body read the same state» · 3 открытых и 2 закрытых → `Repositories (3)` над пятью строками · Считать строки `listedRepos(...)` или подписывать «3 open»
FR-028 · inconsistency · low · frontend/src/App.svelte:2224 · `Fetch All` идёт строго по одному (`await` в цикле), хотя M3-repo-tree.md («Известные ловушки»: «Блокирующий Fetch All последовательно — Параллельно, с ограничением одновременных задач») требует обратного; недоступный remote держит остальные до своего таймаута · Пять репозиториев, remote первого не отвечает — остальные ждут · Пул из 3–4 параллельных fetch (очередь на репозиторий это позволяет), общий счётчик как сейчас
FR-029 · inconsistency · low · frontend/src/lib/remote-menu.ts:39 · Одно действие двумя написаниями: `Initialize` в Remote ▸ Submodule (и src-tauri/src/menu/mod.rs:64), `Initialize new submodules` (lib/repo-settings.ts:81), «Initialize brings it back» (lib/remote-dialogs.ts:73) против `Initialise` в вопросе о submodule (App.svelte:1406-1409) и в Diff (components/diff/SubmoduleDiff.svelte:36); frontend/CLAUDE.md «British spelling in UI strings» · Remote ▸ Submodule ▸ Initialize и кнопка «Initialise this submodule» в Diff · `Initialise` (и `Synchronise`) во всех подписях; id команд не трогать
FR-030 · duplicate · low · frontend/src/lib/worktree-list.ts:94 · `addProblem` повторяет проверку имени ветки из lib/names.ts:6-7 (`branchNameProblem`), но без `.lock`: Add Worktree пропускает `topic.lock`, git отказывает уже после `Add` · Add Worktree → New branch `topic.lock` → кнопка активна, ошибка git · Для новой ветки звать `branchNameProblem(name, choices.map((c) => c.name))`, сообщение про Existing branch оставить своё

## Проход 2

Второй, независимый проход (25.09.2026, a779a38): пограничные состояния, пути ошибок, свежий код 23–24.09 (`repo-pulse`, `pull_probe`, лёгкие деревья, метка worktree), обещания `doc/features` (F-066, F-143, F-342, F-361, F-451) и R-184, R-353, R-354. Консольным git 2.51 во временной папке проверены FR-033 (`git pull origin`, когда ветка отслеживает другой remote) и FR-037 (`[upstream/feature: gone]` после `fetch --prune`, конфиг ветки остаётся).

FR-031 · bug · high · frontend/src/lib/repo-sync.ts:32 · Точка, ↑/↓, ветка и метка состояния строки репозитория, который на экране, берутся из `repository.openRepos` (RepoOverview), а его перечитывает только `refreshList` — при открытии, Fetch All, Pull/Push из меню строки, скане, drop и закрытии; ни `afterMutation` (App.svelte:461), ни `afterRefChange` (1125), ни `applyDiskChanges` (989) его не зовут, pulse для owned не читается (lib/pulse-queue.ts:96), а `applyDiskChanges` при этом снимает с Repositories метку устаревшего (App.svelte:1006) — отметки активной строки замирают на моменте открытия, против F-451 и DoD M3 T3.2 («переключение ветки в терминале отражается в дереве в пределах 200 мс») · Открыть чистый синхронный A; изменить отслеживаемый файл — точки нет; закоммитить — ↑ нет; запушить; двойным кликом переключить ветку в Branches — в строке прежняя ветка; кликнуть B и вернуться к A — всё верно · Для owned-строки брать отметки из `repository.current` (status: staged/unstaged/untracked/conflicted; ahead/behind HEAD-ветки из `branches`; `head`; `state`) — он свежий после каждой записи и события; либо отложенный `refreshList` в конце `afterMutation`/`applyDiskChanges`
FR-032 · bug · high · frontend/src/stores/flow.svelte.ts:13 · `flow.current` (ветка git-flow, на которой HEAD) читается только при открытии и переключении репозитория (App.svelte:1335, 1719, 1759, 2461) и после шагов самого git-flow; checkout в Branches (`switchTo` → `afterRefChange`, App.svelte:1228) и `git switch` в терминале (`applyDiskChanges`) его не обновляют, `flow.clear()` не вызывается нигде — `Git-Flow: Finish This Branch…` действует на ветку, бывшую HEAD при открытии: feature без вопроса (App.svelte:2294) сливается в develop и удаляется `branch --delete --force` (crates/git_engine/src/flow.rs:193, 206) · Git-Flow настроен, ветки feature/a и feature/b; открыть репозиторий на feature/a, двойным кликом перейти на feature/b (или на develop) → Repository ▸ Git-Flow: Finish This Branch… — слита и удалена feature/a, feature/b на месте; на develop пункт активен вместо «Not on a Git-Flow branch» · `flow.refresh(id)` в `afterMutation` (или на `head`/`refs` в `applyDiskChanges` и в `afterRefChange`), `flow.clear()` в `forgetPanelsKeepingTheTree`; `askFlowFinish` перечитывает статус и сверяет `isHead` перед запуском
FR-033 · bug · medium · frontend/src/App.svelte:2664 · `Pull` из меню строки Repositories идёт в `origin`, а не в remote, который отслеживает ветка: неактивная — `primaryRemote` (origin, иначе первый; его комментарий, 2241, обещает «the tracked one»), активная — `runNetwork` с `network.primary` (1467); кнопка Pull тулбара берёт `currentRemote(tracked.upstream)` (514, 1496) и `pullMode` из Preferences, а меню — всегда `--ff-only` (1476, 2670); Fetch All через тот же `primaryRemote` fetch'ит только origin, и ↓ ветки, отслеживающей другой remote, не обновляется · Remotes `origin` (форк) и `upstream`, `main` отслеживает `upstream/main`; ПКМ по строке → Pull → «You asked to pull from the remote 'origin', but did not specify a branch…», exit 1 (Pull в тулбаре на том же репозитории проходит) · Remote для pull — `currentRemote(upstream ветки HEAD, remotes)` в обоих путях (для неактивной — из её веток), `ffOnly` — из `settings.current.pullMode`; активную строку вести через `pullNow()`; Fetch All — remote отслеживаемой ветки (или все), комментарий привести к коду
FR-034 · inconsistency · medium · frontend/src/App.svelte:1216 · Переход «Branch is in another worktree — Switch to it?» (двойной клик по ветке и `Check Out` в меню Branches, components/menus/RefActions.svelte:492) открывает worktree через `activate(path)`: он становится отдельной строкой Repositories и попадает в сессию (`find_or_register` делает `listed |= true`, crates/app_state/src/lib.rs:1284), строка владельца без метки worktree (`worktrees.ownerRoot = null`, App.svelte:1700); R-184 прямо заменил этот вариант на `open_worktree` («Прежний вариант (`activate(path)`) добавлял ворктри в список репозиториев»), панель Worktrees так и открывает (`openWorktreeRow`, 2436) · Добавить worktree на `topic`, в основном двойной клик по `topic` → Yes — в Repositories появилась строка папки worktree, после перезапуска она на месте · Если `elsewhere` — запись `worktrees.entries`, звать `openWorktreeRow(elsewhere)`; `activate` — только для чужого пути
FR-035 · bug · medium · frontend/src/stores/repository.svelte.ts:281 · Репозиторий прошлой сессии, который при запуске не открылся (папку перенесли, сетевой или съёмный диск ещё не подключён), молча выпадает из списка навсегда: `restore` собирает `failed`, App.svelte:345 его отбрасывает, `refreshList` → `session.remember` перезаписывает сессию без него, в `repoList.closed` он не попадает, уведомления нет; закрытая строка в той же ситуации остаётся с меткой `missing` и подсказкой «Close it here, or open it again from where it moved» (lib/repo-labels.ts:14), F-361 · Открыть репозиторий на `Z:` (сетевой диск), выйти, запустить Cogit до подключения `Z:` — строки нет ни среди открытых, ни среди закрытых; подключить диск, перезапустить — не вернулась · После `restore()` для каждого `failed` — `repoList.closed(root)` (строка остаётся, pulse помечает её missing) и одно предупреждение со списком путей
FR-036 · bug · medium · frontend/src/App.svelte:393 · База PR — имя upstream самой ветки (`origin/feature` → `feature`), поэтому для обычной ветки, запушенной в одноимённую, `base === head` и `pullRequestUrl` отдаёт `null` (lib/pull-request.ts:35): `Create Pull Request` и `Copy Pull Request Link` неактивны с причиной «No GitHub, GitLab or Bitbucket remote» (784, 823) при remote на github.com; для ветки без upstream `ahead` = 0, и вопрос о push (860) не задаётся — открывается форма для ветки, которой нет на сервере; F-066 обещает форму и предложение запушить · Ветка `feature` с upstream `origin/feature` → палитра: Create Pull Request (No GitHub, GitLab or Bitbucket remote); новая ветка без push → форма PR без вопроса · База — ветка по умолчанию remote (`refs/remotes/<remote>/HEAD`, иначе main/master), не upstream; спрашивать о push и когда upstream нет; причина неактивности — по делу («already on the base branch», «not pushed yet»)
FR-037 · bug · medium · frontend/src/lib/ref-nodes.ts:72 · Ветка, чей upstream удалён на сервере (`[origin/x: gone]` у git), в Branches подписана `= origin` — «синхронна», а метка worktree на ней зелёная «clean and pushed» (lib/worktree-list.ts:125): бэкенд берёт `upstream` из конфига до проверки, что tracking-ссылка есть (crates/git_engine/src/repo.rs:201), ahead/behind остаются 0; Fetch Cogit всегда с `--prune` (crates/git_engine/src/network.rs:33), так что каждая слитая и удалённая на сервере ветка после Fetch выглядит синхронной · Запушить `feature` с `-u`, удалить её на сервере, Fetch в Cogit → `feature  = origin` (консольный git: `[upstream/feature: gone]`) · Признак `gone` в `Branch` (upstream в конфиге есть, ссылки нет) или `upstream` только при найденной ссылке; `upstreamDetail` пишет `gone`, `worktreeMarks` не считает такую ветку запушенной
FR-038 · bug · low · frontend/src/stores/repo-pulse.svelte.ts:103 · Признак «сервер ушёл вперёд» (`remoteAhead`) снимается только событием наблюдателя `refs` (App.svelte:975), а свои Fetch/Pull/Sync/Fetch All Cogit идут под `quiet` и событий не дают (crates/app_state/src/network.rs:17, 30) — после удачного Pull в Cogit ↓ с подсказкой «The remote has commits not fetched yet» висит до следующей фоновой проверки, и у активной строки тоже (lib/repo-sync.ts:31); наоборот, коммит или новая ветка из терминала (тоже `refs`) его снимают, хотя ничего не fetch'илось — R-354: «смена refs репозитория (fetch, pull)» · Включить фоновую проверку, закоммитить в клоне remote — у строки ↓; Pull в тулбаре — ↓ остаётся; в другом репозитории с ↓ `git commit` в терминале — ↓ пропал · Снимать признак (или ставить пробу `request(root, { fetch: true, first: true })`) по завершении своих сетевых операций (`runNetwork`, `runRemoteSteps`, `fetchAll`, `syncListed`); на внешнее `refs` — перепроверять, а не снимать
FR-039 · inconsistency · low · frontend/src/stores/worktrees.svelte.ts:40 · Метка `(● worktree)` в Branches и тег `dirty` в Worktrees не узнают о правках в linked worktree: список перечитывается только на события текущего репозитория (App.svelte:1013) и при открытии, а наблюдатель следит за корнем и каталогами `.git` (crates/app_state/src/lib.rs:748-754), не за папками других worktree; шаг F-342 «изменить в нём файл — точка жёлтая» не выполняется, пока в текущем ничего не случится (коммит в worktree виден — он пишет `refs/heads` в общий `.git`) · Worktree на `topic` рядом с репозиторием, в основном метка серая; изменить файл в папке worktree редактором — метка серая, `dirty` нет, пока не изменить что-нибудь в основном · Перечитывать `worktrees` по фокусу окна и медленным таймером, пока есть linked worktree, либо наблюдать их корни в бэкенде; иначе поправить F-342
FR-040 · bug · low · frontend/src/App.svelte:2564 · `Move To ▸ New Group…` с пустым именем переносит репозиторий в последнюю существующую группу: `validate: () => null` пропускает пустое, `addGroup` его отвергает (lib/repo-groups.ts:24), а «новая» группа ищется как `order.at(-1)` (App.svelte:2568) — это последняя старая; `New Group` в тулбаре то же имя проверяет `textProblem` (2309) · Группы Work и Client, репозиторий вне групп: ПКМ → Move To ▸ New Group… → пустое поле → Create — репозиторий в Client · `validate: textProblem`; `repoGroups.add` возвращает id созданной группы, `assign` — по нему
FR-041 · bug · low · frontend/src/App.svelte:2637 · `Close Repository` и `Remove…` строки владельца, пока в панелях его submodule, панели не освобождают: `isActive` сравнивает id владельца с id submodule на экране — `wasActive` ложно, `closeOne` текущий не трогает (stores/repository.svelte.ts:290), дерево остаётся у `submodules.owner` с закрытым `RepoId`; строка владельца закрыта (или исчезла), выделенного узла нет, `Update`/`Initialise` узла, Remote ▸ Submodule (stores/remote-ops.svelte.ts:103) и `submodules.refresh` идут в закрытый id и падают или молча пусты · Открыть A, кликнуть его submodule S, ПКМ по A → Close Repository — в панелях S, строка A приглушена; Remote ▸ Submodule ▸ Synchronize → ошибка · Считать строку активной и при `submodules.owner === overview.repo && submodules.open !== null`: `forgetPanels`, затем следующий открытый, как при закрытии активной
FR-042 · bug · low · frontend/src/stores/scan.svelte.ts:66 · Отмена скана (`Cancel`, крестик, повторный `Choose Folder…`) не останавливает обход: `clear()` меняет только поколение, а `scan_until` прекращается, лишь когда `on_found` вернёт `false` (crates/git_engine/src/discover.rs:59), то есть при ошибке отправки в канал (src-tauri/src/commands/mod.rs:534) — канал жив, обход на глубину 6 идёт до конца в пуле rayon и параллельно следующему скану; против CLAUDE.md «reads parallel and cancellable» · Scan Folder на `C:\` → Cancel → обход продолжается (диск и CPU); тут же второй скан — идут оба · Как у `searchFileContents`: id операции в первом сообщении и `cancelOperation` в `scan.clear()`/новом `run`, флаг отмены в замыкании `on_found`
FR-043 · bug · low · crates/app_state/src/lib.rs:465 · Скан помечает `already open` (галочка неактивна, components/repo-tree/ScanDialog.svelte:96) и репозиторий, которого в списке нет: `find_by_root` находит любую регистрацию, в том числе неперечисленные (`listed: false`) worktree и submodule, открытые из панели Worktrees или дерева · Worktrees ▸ Open для linked worktree W, вернуться в основной, Scan Folder на родителе W — W серый «already open», в Repositories его нет, сканом не добавить · `already_open` — только для регистрации с `listed == true`
FR-044 · duplicate · low · frontend/src/App.svelte:2986 · Перетаскивание нескольких папок открывает все, кроме первой, через `repository.open` — полный переход фаз панелей (новая эпоха, `opening`/`open` со сводкой другого репозитория при прежних графе и дереве), а не IPC `openRepository`, как скан (2773); `.catch` мёртв — `open` не бросает, ошибка приходит вторым путём через `repository.error` · Бросить в окно три папки репозиториев — Branches и заголовки по очереди показывают вторую и третью при прежнем графе, потом первую; чтения, начатые для прежнего репозитория, отбрасываются · Для остальных `await openRepository(path).catch((err) => errors.report(…))`, затем `activate(first)` — как `openScanned`
