# Аудит: тесты

25.09.2026 · ветка `audit/2026-09-25` (a779a38) · просмотрено: `crates/test_fixtures/src/lib.rs`, `crates/test_fixtures/tests/{states,stress,content}.rs`; `crates/app_state/tests/{undo,mutation_speed,performance,queue,graph_cache,graph_parity,discard_lines,stage_lines,repositories,pull_submodules,avatars,logging,open,handles,worktrees,credentials}.rs`; `crates/git_engine/tests/{commit_write,operations,replay,rebasing,merging,interactive,conflicts,network,pull_probe,upstream,pulse,published_parity,protected,submodule_parity,submodule_ops,gix_parity,lfs,health,surgery,stashes,stash_modes,stash_contents,staging,apply,apply_worktree,reflog,tips,worktrees,branch_ops,status,worktree_view,filemode,hook_run,children,discover,overlap}.rs`; `crates/fs_watcher/tests/watching.rs`; `crates/diff_engine/tests/{text,syntactic,patch,budget}.rs`; `crates/avatars/tests/queue.rs`; `.config/nextest.toml`, `.cargo/config.toml`, `.githooks/*`; `doc/09-testing.md`, `doc/15-benchmark.md`, R-197/R-300…R-315; `scripts/bench/*.mjs`; фронтенд — все `*.test.ts` на таймеры, холодный импорт, настенное время и пустые тесты (подробно `scrolling`, `graph-overlay`, `trace`, `pulse-queue`, `tri-state-box`, `repository`, `leaving-repository`, `investigate/menu`); для проверки — `git_engine/src/{runner,stash,maintenance,commit_write,operations,network,history,published,graph_walk}.rs`, `app_state/src/{lib,graph_cache,safety,network,rewrite}.rs`, `fs_watcher/src/watcher.rs`, `notify-8.2.0/src/windows.rs`, `frontend/src/stores/diff.svelte.ts`, лог базового прогона `D:\cogit-work\base-tests.log`. Опыты с консольным git 2.51 — в `D:\cogit-work\tmp-tests\`.

## Проход 1

TS-001 · bug · medium · crates/test_fixtures/src/lib.rs:39 · `Fixture::init` целиком перезаписывает `.git/config` после `git init`: пропадают `[extensions]` и `repositoryformatversion = 1`, которые git пишет для reftable/sha256, и `core.ignorecase = true`, который git пишет на NTFS (обходят его вручную в health.rs:9); `AMBIENT_GIT_VARS` (lib.rs:143) не чистит `GIT_DEFAULT_REF_FORMAT`/`GIT_DEFAULT_HASH` · проверено: `GIT_DEFAULT_REF_FORMAT=reftable git init r` → в config `extensions.refstorage = reftable`; после записи того же текста, что в lib.rs:42–55, `git commit` → `fatal: cannot lock ref 'HEAD': … refs/heads/.invalid.lock`. С такой переменной в окружении (и с git 3.0, где reftable по умолчанию) падает весь набор; на Windows все фикстуры регистрозависимы, в отличие от настоящих репозиториев · не перезаписывать файл, а дописывать блок `[core] autocrlf/filemode/symlinks/quotepath`, `[user]`, `[commit]`, `[gc]` в конец существующего config (поздние значения побеждают) — `[extensions]`, `repositoryformatversion` и `ignorecase` остаются; тест в test_fixtures: фикстура под `GIT_DEFAULT_REF_FORMAT=reftable` открывается и коммитит
TS-002 · race · medium · crates/app_state/src/lib.rs:784 · Причина известного флапа `undo::a_mutation_longer_than_the_quiet_window_does_not_echo_either`: окно тишины не держится «всю мутацию» (R-197), а открывается на 400 мс при входе, при выходе каждого процесса git (`command_sink`, lib.rs:823) и при drop охранника; пока один процесс git идёт дольше 400 мс, окно закрыто. `git commit` переписывает `.git/index` до pre-commit-хука (проверено: хеш index внутри хука уже новый), и если запуск `git.exe` под нагрузкой занял больше ≈275 мс, событие `Index` выходит из debouncer (100 мс) при закрытом окне, пока хук спит · undo.rs:302: `quiet()` (lib.rs:658) → `run_git` (commit_write.rs:37) → git пишет index → хук `sleep 1` → `Route::classify` (watcher.rs:183) видит `is_quiet() == false` → `RepoChanged{Index}` → assert undo.rs:327; у пользователя — лишняя перезагрузка панелей на коммите с медленным хуком · держать тишину охранником, а не таймером: `RepoWatcher::hold()` → счётчик активных мутаций, который `Route::is_quiet` проверяет наравне с `quiet_until`, на drop — `quiet_for(DEFAULT_QUIET)`; до исправления — внести `binary(undo) & test(/echo|watcher/)` в группу `timing`
TS-003 · inconsistency · medium · crates/app_state/tests/undo.rs:644 · Изолирован только git самих фикстур (lib.rs:167): код под тестом (runner.rs:320–331, gix) читает `~/.gitconfig` и системный конфиг разработчика, HOME не перенаправляется вовсе — вопреки doc/09-testing.md:83 и crates/CLAUDE.md («HOME and GIT_CONFIG_GLOBAL redirected»). `a_pull_can_be_undone` зелёный только потому, что у разработчика задан `pull.rebase` (global `false`, system `true`): `git pull origin` без `--rebase/--no-rebase` (network.rs:48) на разошедшихся ветках `with_remote` без этой настройки падает; так же `lfs.rs:46` («…in this repository only») не проверяет, что глобальный конфиг не тронут, а он настоящий · проверено консольным git 2.51 с `GIT_CONFIG_GLOBAL=<пусто> GIT_CONFIG_NOSYSTEM=1`: `git pull --progress origin` → `fatal: Need to specify how to reconcile divergent branches`, exit 128 — тест на чистой машине красный · в `Fixture::init` писать `[pull] rebase = false`; процессам тестов задать `GIT_CONFIG_GLOBAL`/`GIT_CONFIG_NOSYSTEM` (setup-script nextest через `NEXTEST_ENV`); в lfs.rs сравнить `git config --global --list` до и после; doc/09 и crates/CLAUDE.md привести к фактической изоляции
TS-004 · bug · medium · crates/app_state/tests/undo.rs:482 · Тесты запускают `std::process::Command::new("git")` мимо test_fixtures, без очистки `GIT_DIR`/`GIT_WORK_TREE`/`GIT_INDEX_FILE`, которую lib.rs:819–837 называет «a safety property, not tidiness»: undo.rs:482 (`switch dev`), undo.rs:526 (`commit --allow-empty` в submodule), reflog.rs:235 и :245 (`clone`, `fetch`), repo_settings.rs:36 (`config --get`) · набор, запущенный из окружения с `GIT_DIR` (хук, `git rebase -x "cargo nextest run"`, терминал внутри хука): undo.rs:526 коммитит «moved» в репозиторий из `GIT_DIR`, то есть в D:\cogit, undo.rs:482 переключает там ветку · `f.git_in(path, args)` / `f.git(&["clone", …])`, как в pull_submodules.rs:29; для чтения глобального конфига — сделать `git_command` публичным в test_fixtures
TS-005 · bug · medium · crates/git_engine/src/stash.rs:74 · `stash_apply` при любой ошибке `stash apply --index` повторяет `stash apply` без `--index`; если первая попытка остановилась на конфликте (маркеры уже в файле, индекс unmerged), вторая падает с `error: could not write index` / `f: needs merge`, и пользователь видит это вместо `CONFLICT` первой команды (INV-05). Это путь Undo (safety.rs:111, :130) — тестов на Undo поверх изменившегося файла нет · Discard файла → коммит другой правки той же строки → Undo: воспроизведено консольным git — `stash apply --index <oid>` → `CONFLICT (content)`, exit 1, маркеры в файле → `stash apply <oid>` → `needs merge`, exit 1 · повторять без `--index` только при отказе восстановить индекс (`Conflicts in index. Try without --index.`), иначе возвращать первую ошибку; тест в undo.rs: discard → конфликтующий коммит → `undo_last` → ошибка содержит `CONFLICT`, запись журнала осталась
TS-006 · bug · medium · crates/fs_watcher/src/watcher.rs:45 · Комментарий «An overflowed ReadDirectoryChangesW buffer lands here» неверен для notify 8.2.0: переполнение до `Err` не доходит. В `handle_event` (notify windows.rs:338–367) код `ERROR_NOTIFY_ENUM_DIR`, которым по документации завершается переполненное асинхронное чтение, попадает в ветку `_` → `request.unwatch()`: рекурсивное наблюдение за корнем снимается молча до конца сессии; при `ERROR_SUCCESS` число байт игнорируется и разбирается пустой буфер. Отсюда же `a_thousand_files_in_target_produce_no_events` (watching.rs:66) зелёный и при снятом наблюдении · checkout или сборка в не исключённую папку на тысячи файлов → буфер 16 КБ (notify windows.rs:40) переполнен → правки из терминала больше не обновляют панели · после такой пачки проверять живость наблюдения (пробный файл или повторный `watch` корня по таймеру), обновить notify, если исправлено выше 8.2.0, или обернуть своим обработчиком `ERROR_NOTIFY_ENUM_DIR` → одно событие WorkingTree + перезапуск наблюдения; тест: пачка 5 000 файлов, затем обычная правка даёт событие
TS-007 · bug · medium · scripts/bench/run.mjs:812 · `--check` пропускает сценарий без единого замера: у записи из одних ошибок `summarise([{ total: NaN }])` даёт `median: null` (run.mjs:726, :297), `null > budget.median` ложно — «all budgets met»; `row.errors` не проверяется вовсе · сломать селектор сценария (например, `button.row.header` в scenarios.mjs:30) → каждый замер падает с «nothing matches» → `npm run bench -- --check` печатает «all budgets met», exit 0 · считать превышением строку с `median === null` или `errors.length > 0`, а также отсутствие строки для набора из `--sets`
TS-008 · bug · medium · scripts/bench/fixtures.mjs:289 · Наборы бенчмарка зависят от глобального конфига машины (doc/15 §3 обещает детерминированность): ENV (fixtures.mjs:18) отключает только системный конфиг, `core.autocrlf false` ставится после `git clone` (строки 289–292), когда checkout уже прошёл с глобальным `core.autocrlf=true` (у пользователя он есть); клоны submodules (240, 259, 262) остаются с autocrlf=true навсегда. В `network` рабочие файлы в CRLF при autocrlf=false, а `remoteCommit` (run.mjs:103–109) коммитит файл целиком в CRLF · воспроизведено: `git -c core.autocrlf=true clone remote.git pusher && git config core.autocrlf false` → `git status` → ` M f.txt`; дописать строку и `commit -am` → в коммите все строки `-a` / `+a^M` · в ENV добавить `GIT_CONFIG_GLOBAL` на пустой файл (и в `git()` run.mjs:58), клонировать с `-c core.autocrlf=false`, то же для `submodule add/update`; пересоздать наборы и снять базу заново
TS-009 · inconsistency · medium · crates/git_engine/tests/gix_parity.rs:79 · Паритет с консольным git есть для staged (`diff --cached`), stash и worktrees, но не для второй половины того же чтения — unstaged/untracked списка Files (`worktree_files`, gix status) и счётчиков `repo_status`; status.rs и worktree_view.rs сверяют с ожиданиями, написанными руками, на простых случаях · не покрыто: untracked внутри новой папки, `!`-исключения .gitignore, удалённый и заново созданный файл, смена типа, `add -N`, грязный submodule, racy-файл той же длины, CRLF при `core.autocrlf=true` · модуль `unstaged` в gix_parity.rs: сверка с `git status --porcelain=v2 --untracked-files=all` на этих случаях
TS-010 · inconsistency · medium · crates/app_state/tests/discard_lines.rs:10 · Построчные stage/unstage/discard проверены только на фикстурах с `core.autocrlf=false` (LF, либо CRLF внутри blob — apply.rs:98); обычный на Windows случай — blob в LF, рабочий файл в CRLF (`core.autocrlf=true` или `eol=crlf`) — не покрыт ни одним тестом, хотя patch собирается с `lineEnding: diff.eol.old` (frontend/src/stores/diff.svelte.ts:173), а discard деструктивен · в `linear(1)` задать `core.autocrlf true`, записать файл в CRLF с двумя добавленными строками, `discard_selection` одной → проверить байты (CRLF сохранён, вторая строка на месте) и Undo; `stage_selection` → `git show :file` в LF · добавить эти тесты в discard_lines.rs и stage_lines.rs
TS-011 · bug · low · crates/git_engine/src/stash.rs:49 · Причина известного флапа `mutation_speed::discarding_a_file_is_a_single_git_invocation`: `stash_paths` всегда передаёт `--include-untracked`, и `git stash push` запускает ещё четыре своих процесса (R-315: 265 против 160 мс на small) даже для отслеживаемого файла; бюджет теста 400 мс (mutation_speed.rs:90) — полтора холостых прогона, любой нагрузки хватает · mutation_speed.rs:79 → `discard_paths` (lib.rs:623) → `stash_paths` → `stash push --include-untracked --message … -- file0.txt` → `update-index` ×2, `clean`, `reset --hard` · как в R-315: `--include-untracked` только если среди путей есть неиндексированный (поиск в индексе gix, без процесса); A/B `changes.*` до принятия
TS-012 · bug · low · crates/app_state/tests/performance.rs:138 · Причина флапа `performance::fifty_thousand_commits_meet_the_product_promise`: тест намеренно проверяет обещание продукта без запаса (300/500 мс, шапка файла), которое R-302 мерило в `--release`, а набор собирается профилем dev, где крейты воркспейса без оптимизации (Cargo.toml: `[profile.dev]` без `opt-level`, `opt-level = 2` только у зависимостей), на фикстуре без commit-graph · любой полный прогон на занятой машине; запас зависит от машины · в наборе — запас, как у остальных тестов файла (×2), обещание проверять release-сборкой (`--check` бенчмарка, `graph.full-layout` large) либо `[profile.dev.package.{graph_engine,git_engine,app_state}] opt-level = 2`
TS-013 · inconsistency · low · crates/git_engine/tests/operations.rs:131 · Abort/Continue/Skip для cherry-pick и revert проверяются только на подделанном состоянии (нулевой `CHERRY_PICK_HEAD`, пустой `rebase-apply`): результат отброшен (`let _ =`), сверяется лишь подстрока команды. Настоящий конфликт cherry-pick (replay.rs:37) и revert не доводится ни до abort (HEAD и дерево прежние), ни до continue после разрешения, `skip_operation` для них не вызывается · operations.rs:136: `git cherry-pick --abort` на нулевом `CHERRY_PICK_HEAD` падает — тест всё равно зелёный · на фикстуре replay.rs:37: конфликт → `abort_operation` → HEAD прежний, `status --porcelain` пуст; конфликт → разрешение → stage → `continue_operation` → Clean и новый коммит; то же для revert и skip
TS-014 · inconsistency · low · .config/nextest.toml:24 · Тесты, сравнивающие длительность, не попадают в группу `timing`, хотя её комментарий требует этого для всех таких: text.rs:491 `a_rewritten_enormous_line_does_not_hang_the_word_diff` (< 5 с), avatars/tests/queue.rs:238 `asking_for_a_window_does_not_block_the_caller` (< 100 мс), app_state/tests/avatars.rs:221 (< 1 с), оконные undo.rs:168, :287, :334 (400 мс) — идут в пуле из 12 потоков · полный прогон во время сборки · дополнить фильтр: `test(/does_not_hang/) | test(/does_not_block/) | test(/does_not_stall/) | (binary(undo) & test(/echo|watcher/))` или назвать их `…budget…`
TS-015 · bug · low · frontend/src/lib/scrolling.test.ts:38 · `expect(elapsed).toBeLessThan(16.6)` меряет настенное время одного холодного прохода в параллельном vitest (154 файла по воркерам): сборка мусора или вытеснение потока дают больше 16,6 мс; само свойство уже доказывает счётчик `curves` строкой выше · `npm --prefix frontend run test` во время сборки · убрать проверку времени (оставить счётчик) или перенести замер в бенчмарк `graph.scroll`
TS-016 · bug · low · crates/fs_watcher/tests/watching.rs:66 · Отрицательные тесты наблюдателя без положительного контроля проходят и при неработающем наблюдателе: `a_thousand_files_in_target_produce_no_events` и `churn_in_git_objects_is_ignored` (:84) — при снятом после переполнения наблюдении (TS-006), undo.rs:168 и repositories.rs:118 — если наблюдатель не запустился · любой сбой наблюдения → пусто → тест зелёный независимо от `is_excluded` · после «шума» записать обычный файл и ждать ровно одно WorkingTree; в undo.rs:168 проверить, что первая запись (строка 172) дала событие
TS-017 · inconsistency · low · crates/git_engine/tests/surgery.rs:332 · Регрессионные тесты проверяют только `Ok` (антипаттерн doc/09 §11): `a_file_with_a_non_ascii_name_can_be_split_off` (:332) и `file_names_that_look_like_secrets_can_be_split_off` (:375) не смотрят, что файл ушёл в отдельный коммит; `an_unknown_commit_reports_gits_words` (replay.rs:113) и `a_pull_that_cannot_fast_forward_is_refused_rather_than_merging_silently` (network.rs:142) — только `is_err`, без текста git и без проверки, что HEAD не сдвинулся · `split_off`, вернувший `Ok` без переписывания истории, эти тесты пропустят · как в surgery.rs:204: `diff-tree --name-only` нового коммита = выбранный файл; для replay/network — `GitError::Command` со stderr git и `oid("HEAD")` до и после
TS-018 · bug · low · crates/app_state/src/graph_cache.rs:172 · Кэш графа отвечает, если совпал отпечаток ссылок (history.rs:60), но граф зависит и от `.git/shallow` (graph_walk.rs:163): после `git fetch --unshallow` из терминала при неизменных вершинах отдаётся граф, обрезанный по старой границе; наблюдатель `shallow` не классифицирует (fs_watcher/src/lib.rs:74), тестов нет · shallow-клон (как reflog.rs:223) → граф → `git fetch --unshallow` → Refresh → в логе «commit graph from the cache», истории ниже прежней границы нет до первого сдвига ссылки · подмешивать в отпечаток список `shallow_commits()` (graph_walk.rs:220); тест в graph_cache.rs
TS-019 · bug · low · crates/app_state/src/graph_cache.rs:665 · К известному регрессу `repo.close` на large: `forget_graph` — `self.graph.write().graphs.remove(&repo);` — освобождает граф (50 000 строк, сегменты, тексты) в том же выражении, то есть под write-блокировкой всего кэша и в потоке `close_repository` (lib.rs:994) · закрытие large: освобождение сотен тысяч аллокаций до ответа; `graph_window` других репозиториев ждут блокировку · `let dropped = self.graph.write().graphs.remove(&repo);` и отдать `dropped` фоновому потоку (как `prefill`); A/B `repo.close`
TS-020 · inconsistency · low · doc/09-testing.md:82 · Документ расходится с фикстурами: «`stress(10_000)` создаётся один раз на сессию и кэшируется» — кэша нет, performance.rs строит `stress(10_000)` четыре раза и `stress(50_000)` трижды внутри группы `timing` с `threads-required = "num-cpus"` (весь пул ждёт); «`HOME` … перенаправляются» (строка 83) — HOME не трогается (lib.rs:160–179) · сравнить doc/09:82–83 с test_fixtures/src/lib.rs:160–179 и :335–344 · либо кэш (`OnceLock` с путём и `git clone --shared` на тест, чтобы тесты не делили состояние), либо поправить текст; про HOME — TS-003
TS-021 · dead-code · low · .config/nextest.toml:29 · Профиль `[profile.quick]` («The hook's profile») не использует ни один хук и ни один скрипт, а шапка .githooks/pre-commit:5–6 обещает, что «the whole test suite, and every test that asserts a duration» идут в pre-push, где тестов нет (pre-push:39–46) · `grep -rn "profile quick" .githooks scripts doc` — пусто · удалить профиль и поправить шапку pre-commit под doc/09 §9

## Проход 2

Просмотрено сверх прохода 1: `app_state/tests/{graph_reuse,handles,repositories}.rs`, `git_engine/tests/{branches,shared,published,pulse,submodule_parity,search}.rs`, `test_fixtures/tests/{content,states}.rs`, `scripts/bench/{scenarios,fixtures}.mjs` и `run.mjs` (`Git`, `attempt`, проходы), `page.mjs` (перехват IPC); для проверки — `app_state/src/{graph_cache,handles,safety,stashing,ref_ops,rewrite,network}.rs`, `git_engine/src/{shared,pulse,published,stash,staging,branches,module_ops,submodules}.rs`, `frontend/src/lib/checkout-refusal.ts`, `App.svelte` (Discard, autostash), `doc/modules/M9-fixtures.md`, `doc/cogit-tasks-2026-09-2{3,4}.md`, R-210, R-253, R-353, R-354; соседние области — чтобы не дублировать (FG-002, GE-025, CC-008, DC-003). Опыты с консольным git 2.51 — в `D:\cogit-work\tmp-tests\p2\`.

TS-022 · bug · medium · crates/app_state/src/stashing.rs:72 · Drop stash журналируется как `Recovery::Stash { oid }`, а Undo такой записи — `stash_apply(oid)` (safety.rs:111): Undo после Drop не возвращает запись в список, а накладывает её изменения (и её неотслеживаемые файлы) на рабочее дерево; при мешающих правках apply падает, и stash остаётся лишь висячим коммитом. doc/cogit-tasks-2026-09-23.md:320 обещает «после Undo stash вернулся», имя теста git_engine/tests/stashes.rs:170 — тоже; в app_state/tests/undo.rs Undo дропа нет вовсе · `with_stashes(2)` → `stash_drop(repo, 0)` → `undo_last` → `stashes()` — одна запись, в дереве `?? work-in-progress.txt`; консольным git: `stash drop` + `stash apply --index <oid>` → список без записи, `?? w.txt` · отдельный `Recovery::StashEntry { oid, message }` (git_engine `stash_drop` возвращает и сообщение), Undo — `git stash store -m <message> <oid>` с возвратом на прежнее место, как `rename_stash` (R-252); тест в undo.rs: список и рабочее дерево после Undo — как до Drop
TS-023 · bug · medium · crates/git_engine/src/module_ops.rs:27 · `ALLOW_FILE` (`-c protocol.file.allow=always`) снимает защиту git от CVE-2022-39253 не только в `add_submodule` (URL вводит пользователь), но и в `submodule update --init --recursive` / `--checkout` (:46, :56), `update_submodule` (submodules.rs:190) и в автоматической инициализации submodules, пришедших с pull (`init_submodules_added_since` :93 ← app_state/src/network.rs:48): локальные пути из чужого `.gitmodules`, в том числе вложенных, клонируются без вопроса. doc/modules/M9-fixtures.md:42 разрешает флаг «только на команду добавления», в 12-risks решения нет; тесты submodule_ops.rs:42 и submodules.rs:171 зелёные только благодаря этому обходу · клон репозитория с submodule по локальному пути → `git submodule update --init` → `fatal: transport 'file' not allowed` (проверено), Cogit ▸ Initialize — клонирует · флаг оставить только в `add_submodule`; update/init — без него, а на отказ `transport 'file' not allowed` — вопрос с путём submodule и повтор с флагом только для него; фикстурам `with_submodule`/`with_nested_submodule` писать `protocol.file.allow=always` в `.git/config` родителя (так уже делает fixtures.mjs:256); решение — в 12-risks
TS-024 · bug · medium · crates/git_engine/tests/staging.rs:111 · Тесты Discard правят только рабочее дерево при индексе = HEAD (`discarding_restores_the_file_from_the_index`, undo.rs:22), поэтому «из индекса» (F-023, doc/03:51 `git restore`, R-210) от «из HEAD» не отличают, а путь приложения — `stash push --include-untracked -- <путь>` (app_state/src/lib.rs:638) — откатывает к HEAD и индекс (файл MM — FG-002). Сверх FG-002: новый файл, добавленный в индекс и доправленный (AM), после Discard в Unstaged пропадает целиком — из индекса и с диска · `echo a > n.txt; git add n.txt; echo b >> n.txt` → Discard `n.txt` в Unstaged → Staged пуст, файла нет (консольным git: после `stash push -- new.txt` статус пуст) · исправление FG-002 плюс тесты в undo.rs на MM и AM: после Discard индекс прежний, рабочий файл равен индексу, после Undo — всё как до Discard; в staging.rs:111 — индекс ≠ HEAD
TS-025 · bug · medium · crates/git_engine/tests/branches.rs:64 · `checking_out_with_uncommitted_changes_reports_gits_refusal` проверяет stderr только внутри `if let Err(GitError::Command(..))`: checkout, вернувший `Ok`, или ошибка другого варианта проходят; HEAD и содержимое мешающего `dev-1.txt` не сверяются. Это единственный тест отказа checkout из-за локальных изменений, и на тексте этого отказа держится предложение autostash (`blockedByLocalChanges`, frontend/src/lib/checkout-refusal.ts:2–5), которое проверено только на stderr, написанном руками (checkout-refusal.test.ts:4–13) · заменить тело `checkout` (branches.rs:16) на `Ok(())` — тест зелёный; трасса отказа: App.svelte:1225 `offerAutostash` → checkout-refusal.ts:11 · `unwrap_err()`, `match Command(d)` с `d.stderr.contains("The following untracked working tree files would be overwritten by checkout")` и `dev-1.txt`, HEAD = `main`, файл не тронут; второй случай — изменённый отслеживаемый файл («Your local changes to the following files would be overwritten by checkout»), чтобы оба заголовка `HEADINGS` были сверены с настоящим git; лишнюю подготовку с `base.txt` убрать
TS-026 · bug · medium · scripts/bench/scenarios.mjs:323 · С 5458873 (24.09 16:50, сценарий `net.pull-check`) `net.push` меряет отклонённый push: prep `net.pull-check` кладёт коммит в remote (`remoteCommit`) и ничего не забирает, `net.push` делает `localCommit` и Push → `! [rejected] main -> main (fetch first)`; перехват IPC в page.mjs:79–86 закрывает вызов по любому из двух колбэков, не различая, а `attempt` (run.mjs:386) уведомлений не смотрит — сэмпл записывается. Таблица doc/cogit-tasks-2026-09-24.md:287 (81,6 → 75,7 мс, «same») — время отказа по обе стороны A/B; порог budgets.mjs:68 (113 мс) снят 23.09 с настоящего push, поэтому `--check` молчит. С того же раунда `net.pull` на раундах ≥ 1 сливает непушнутый локальный коммит (merge по `pull.rebase=false` из глобального конфига; без него — отказ «divergent branches»), а не fast-forward, как до 5458873 · консольным git в порядке сценариев набора network (fetch, pull, pull-check, push) — push `exit=1 … (fetch first)` (D:\cogit-work\tmp-tests\p2\net) · в `localCommit` (run.mjs:111) сперва `fetch` + `merge --ff-only origin/main` вне замера или `net.pull-check` после `net.push`; после замера сверять вершину remote с HEAD клона; в перехвате IPC помечать вызов, закрытый колбэком ошибки, и считать такой сэмпл ошибкой; замеры `net.*` после 5458873 переснять
TS-027 · race · low · crates/git_engine/tests/worktrees.rs:123 · Тесты пишут в общие пути `%TEMP%`, а не в свои временные папки: `adding_a_worktree_puts_a_branch_in_it` — `f.path().parent().join("added-wt")`, то есть `%TEMP%\added-wt`, одно имя на все прогоны, удаляется только в конце `.ok()`: упавший или прерванный прогон оставляет папку, и дальше тест падает на `git worktree add`, а два набора сразу (worktree'ы `D:\cogit-work\wN`) сталкиваются. `a_failed_open_registers_nothing` (app_state/tests/open.rs:68) создаёт `%TEMP%\cogit-app-state-not-a-repo` и не удаляет (лежит сейчас), а «не репозиторий» там верно, только пока выше `%TEMP%` нет репозитория (discovery идёт вверх) · непустая `…\added-wt` → `fatal: '…/added-wt' already exists` (проверено); `ls %TEMP%` — `cogit-app-state-not-a-repo` на месте · `tempfile::tempdir()` на тест, как `with_worktree` (test_fixtures/src/lib.rs:502), и держать её до конца теста; для open.rs — папка внутри своей `TempDir`
TS-028 · bug · low · crates/test_fixtures/src/lib.rs:48 · Фикстура ставит `core.quotepath = false` — противоположно умолчанию git, и весь набор разбирает вывод git в режиме, которого у пользователя нет. Так и прошёл баг 1493aa7 (split off не узнавал не-ASCII файл в выводе `diff-tree --name-only`): нашли вручную, а тесты возвращают умолчание руками поверх фикстуры (surgery.rs:334, line_history.rs:243); новый разбор путей без `-z` в тестах не упадёт · `git diff-tree --root --name-only -r HEAD` с файлом `файл.txt`: по умолчанию `"\321\204\320\260\320\271\320\273.txt"`, в фикстуре — `файл.txt` (проверено) · убрать `quotepath` из `Fixture::init` (заодно с TS-001 — дописывать config, а не перезаписывать), ручные `config core.quotepath true` удалить
