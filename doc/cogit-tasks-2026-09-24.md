## Cogit: производительность и правки от 24.09.2026

Цифры «сейчас» — из прогона бенчмарка. Каждый пункт производительности перемеряй тем же
бенчмарком на тех же наборах: нет выигрыша или сломалось поведение — откат. #1 раньше #2,
оба трогают граф. Запись — только через системный `git`.

### Режим работы

- Ветка `tasks/2026-09-24` от `refactor/night-2` (28baaca). Восемь агентов параллельно, каждый в
  своём worktree `D:\cogit-wt\gN` на ветке `tasks-0924/gN`; слияние в общую ветку — по одной
  ветке, после каждой — полный набор тестов.
- Группы: g1 — #1, #2, #10; g2 — #3, #6; g3 — #4, #5, #7, #8, #9; g4 — #12, #13, #14, #27;
  g5 — #11, #26; g6 — #15–#18; g7 — #19–#22, #25; g8 — #23, #24.
- Тяжёлые команды — на ядрах 16–31 через `D:\cogit-wt\heavy.cmd`, общий `CARGO_TARGET_DIR`.
- Замеры A/B — только после того, как агенты закончили, на простаивающей машине: база —
  `target/bench/exes/night2-final.exe` (прогон `night2-ab`, 28baaca), по сборке на каждый пункт
  A, те же наборы.
- Настройки графа для #11, #12, #26 заводит g8 (#23); g4 и g5 принимают их параметрами,
  проводку делает слияние.

### A. Производительность и граф

- [x] 1. Кэш графа на репозиторий
  - Сейчас: переключение на large — 274–279 мс, почти всё — повторный обход 50 000 коммитов.
    При переключении граф моргает: есть граф → нет графа → есть граф.
  - Ожидание: −200…−300 мс на large, ~−30 мс на medium.
  - [ ] Кэш переживает переключение между репозиториями.
  - [ ] Этапа «нет графа» нет: при переключении сразу показывается граф из кэша, при его
    отсутствии — прежний граф остаётся до готовности нового.
  - [ ] Сброс по изменению ссылок, в том числе внешнему (git из терминала). Тест: refs
    изменены извне → после переключения граф актуален.
  > Итог: переключение заново обходило 50 000 коммитов, в `AppState` был один граф → граф на репозиторий с LRU по байтам (96 МБ), отпечаток ссылок (`refs_fingerprint`) сверяется при каждой загрузке, так что git из терминала замечается при переключении (тест), прежний граф стоит до готовности нового (R-300; `9c2a5c7`). A/B `repo.switch`: large 278,5 → 57,9 мс (×0,21, p95 316 → 66), medium 57,8 → 45,3 мс (×0,78) — принято.
  > Проверить в сборке: large ↔ medium без мигания графа.
- [x] 2. Инкрементальный обход истории
  - Сейчас: отметка ветки и полная раскладка на large — 237–267 мс. При отметке галочки в
    Branches граф пересчитывается целиком: индикатор загрузки становится большим и по центру,
    счётчик коммитов считается заново.
  - [ ] Сначала обход до нужной глубины (видимое + запас), остальное — в фоне.
  - [ ] Отметка ветки — частичная перестройка: добавляются только новые коммиты, текущее
    положение прокрутки сохраняется.
  - [ ] Никакого индикатора загрузки на весь граф при отметке ветки — максимум ненавязчивый в
    заголовке.
  - [ ] Счётчик в заголовке Graph обновляется по мере догрузки и всегда совпадает с числом
    строк.
  > Итог: первый экран и так приходил после первого чанка; каждый обход заново читал все объекты → свой обход `ByTime` берёт время, родителей и строки из прошлого графа и читает только новые коммиты, раскладка целиком заново (инвариант лейна 0 держит тот же код, тест сверяет с обходом с нуля), прокрутка не сбрасывается, вместо большого индикатора — многоточие у счётчика в заголовке (`8e9cbaf`). A/B: `graph.tick-branch` large 270,3 → 91,2 мс (×0,34), `graph.tick-tags` large 262,1 → 95,6 (×0,37), medium 37,1 → 20,4 (×0,55) — принято.
  > Проверить в сборке: отметка ветки на large — без скелета, прокрутка на месте, заголовок `(N…)`.
- [x] 3. Ускорить запись, не уходя с `git` CLI
  - Сейчас: «stage всех» 337 мс (процесс ~300 мс), коммит ~125 мс, `stash push` ~180 мс.
  - [ ] Профиль самого `git` через `GIT_TRACE2_PERF=1`.
  - [ ] Stage всех — один `git add -A`; выбранные файлы — `--pathspec-from-file=-
    --pathspec-file-nul`.
  - [ ] Непостоянные флаги на вызов (`-c index.threads=true` и т.п.); конфиг пользователя и
    формат индекса не менять.
  - [ ] Убрать лишние процессы вокруг записи (`rev-parse` до и после) — брать из вывода команды
    или gix-чтением.
  > Итог: профиль `GIT_TRACE2_PERF` (14-profiling §8): запуск процесса git на Windows 41 мс, `git add` 2 003 путей тратит ≈290 мс на запись 2 000 loose-объектов, `commit` 44 мс ждёт свой `maintenance run --auto`, `stash push -u` — четыре своих процесса. Регресс прошлой итерации (`changes.stage-all` 420 → 446) был в `commitScope`: `includes()` по массиву на каждый staged-файл → `Set` (`e375350`): 403,7 → 374,4 мс, против `night-final` — same (регресс снят). Дальше, каждое A/B против предыдущего шага: stage всех одним `git add --all` (`e08638d`) 359,1 → 332,7 (×0,93); блобы одним pack при ≥ 200 файлах, `-c core.bigFileThreshold=1` (`3c21366`, R-312; при `core.autocrlf=true` выигрыша нет) 299,2 → 190,9 (×0,64); HEAD после коммита через gix (`0c035cb`) commit small 125,8 → 109,9, medium 150,8 → 138,3; `maintenance.auto` после коммита, а не внутри (`fb6697d`) small 113,1 → 101,0, medium 142,9 → 134,2; `stash push -u` только при неотслеживаемых (`4bac65d`) 151,4 → 126,4 — всё принято. `-c index.threads`, `fsmonitor`, `preloadIndex`, `fscache`, `fsync*`, `compression` выигрыша не дали и не вошли.
- [x] 4. `is_published` без ожидания в контекстном меню
  - Сейчас: меню открывается за 39–89 мс, ожидание ~10 мс.
  - [ ] Номера поколений commit-graph для отсечения поиска или меню открывается сразу,
    зависящие пункты в состоянии «проверяется».
  - [ ] Пока ответа нет, при ошибке и таймауте — коммит считается опубликованным.
  > Итог: меню ждало два процесса git → `is_published` в процессе обходом от удалённых вершин с отсечением по поколению commit-graph, без удалённых веток — сразу «нет», без commit-graph — прежний `for-each-ref`; пока нет ответа, при ошибке и через 200 мс — «опубликован» (R-320; `e75577c`). A/B `ui.context-menu`: large 38,8 → 4,7 мс (×0,12), medium 38,6 → 4,6 (×0,12) — принято. `protecting_refs` в меню не вызывается, его A/B — в gix-задаче, пункт 2.
- [x] 5. Переиспользовать открытый репозиторий (`ThreadSafeRepository`) вместо `handle()` на
  каждый вызов. Ожидание ~−5…−15 мс, не измерено — измерить. Инвалидация при закрытии и смене
  пути.
  > Итог: `handle()` на каждую команду заново находил репозиторий и разбирал конфиг (0,71–0,89 мс) → `git_engine::SharedRepo` держит открытый репозиторий, переоткрывает при изменении любого файла конфига (include, глобальный, `config.worktree`) и HEAD, выбрасывает при закрытии (R-324; `51c491e`). A/B (против сборки до пункта): `changes.stage-one` dirty 86,9 → 79,5 мс (×0,92), `repo.switch` dirty 66,2 → 57,9 (×0,88, p95 90 → 58), остальное same — принято.
- [x] 6. Дубли в каскаде обновления
  - [ ] `worktree_files` дважды на stage — один раз.
  - [ ] `repo_status` и `conflicted_paths` — из одного чтения статуса.
  - [ ] После коммита `repository.refresh` делает полный `open_repository` — заменить
    обновлением ссылок.
  > Итог: после записи список перечитывали и store, и App → `runMutation`, stage/unstage/discard читают раз (`81bad73`): stage-one 86,9 → 70,4 мс, unstage-one 87,0 → 70,3, unstage-all 107,9 → 91,3; счётчики и конфликтующие пути — одно чтение `working_state` (`0305e5c`): stage/unstage на dirty ×0,94–0,95; после коммита вместо `open_repository` — `repo_refs` (`6a4dc45`): общее время same (130,1 → 129,8), но вызов 7,4 → 3,9 мс и панели не проходят фазу `opening` — принято как дедупликация каскада (выигрыш по вызову, не по сценарию).
- [x] 7. `log_from_frontend` пачками. Сейчас 9–12 IPC-вызовов на действие. Буфер по объёму и
  таймеру; ошибки и события перед закрытием окна — сразу.
  > Итог: одна строка лога — один IPC-вызов → пачка на задачу или по 32 строкам, ошибки и выход — сразу (R-321; `c895228`). A/B `repo.open` medium: `log_from_frontend` 9 → 5 вызовов, 12,2 → 5,2 мс IPC; общее время same — принято по числу вызовов (цель пункта). Таймер 250 мс дал бы один вызов на действие, но удлинил бы каждый сценарий бенчмарка, который считает конец действия по последнему IPC.
- [x] 8. `set_menu_state` одним вызовом вместо шести. ~−3 мс.
  > Итог: команда уже принимала полное состояние, лишние вызовы давали перезапуски эффекта → состояние уходит раз в конце задачи и только если изменилось (R-322; `4efcfd5`). A/B `repo.close`: `set_menu_state` 2 → 1 на прогон; общее время same, обещанных −3 мс не видно — принято по числу вызовов.
  > Проверить в сборке: галочки Output/Overlap/панелей после клика в меню и после сохранения клавиш.
- [x] 9. Лишний кадр при закрытии последнего репозитория. ~−4 мс.
  > Итог: после закрытия шли `close_repository`, отдельный `repositories` и лишние записи лога → `close_repository` отвечает списком оставшихся (R-323; `292f4e8`). A/B `repo.close` small/submodules: вызов `repositories` исчез, логов 3 → 2; общее время same (9,7 → 9,6 мс), обещанного кадра не видно — принято по числу вызовов.
- [ ] 10. Упреждающая подгрузка блоков графа. Прыжок прокрутки 13,8 мс → ~10 мс.
  > Итог: упреждающая подгрузка по направлению прокрутки (`dfbc884`) — откачено: A/B `graph.scroll` same (medium 13,9 → 13,8, large 13,6 → 13,8, p95 хуже), а сценарий — одиночный прыжок из покоя, которому упреждение не помогает; упреждение из покоя добавило бы окна в каждую загрузку. Для проверки идеи нужен сценарий из нескольких шагов колеса.

### B. Graph: отображение

- [ ] 11. Подсветка отмеченных веток
  - Сейчас цвет есть только у активной ветки, остальные серые.
  - [ ] Ветка, отмеченная галочкой в Branches, рисуется своим цветом: цепочка первых родителей
    от вершины ветки до точки, где она сливается с уже подсвеченной линией или с главной.
  - [ ] Цвет устойчивый: выводится из имени ветки и не меняется между запусками; палитра —
    токены темы, различимые во всех четырёх темах.
  - [ ] Обновить раздел Graph в `CLAUDE.md` и `doc/07-graph-rendering.md`: «монохромно по
    умолчанию; отмеченные в Branches ветки — своими цветами».
  > Итог: у отрисовки не было ни полосы у отрезка, ни родителей на фронтенде → `graph_engine::paint` (чистая функция над готовыми строками) + команда `graph_overlay` + функции стиля `$lib/graph-style`; цвет — FNV-1a от имени ветки (`origin/x` — от `x`) по восьми токенам `--graph-branch-*`, контраст и различимость во всех четырёх темах проверяет тест (он поймал тёмно-серую тему, там два цвета заменены); обрубки длинных связей (#12) сшиты в одну полосу; раздел Graph в `CLAUDE.md` и `07` обновлён (R-340; `9af236f`, `9738283`).
  > Проверить в сборке: цвета отмеченных веток в четырёх темах; ветка и её `origin/…` одного цвета; тик и снятие тика.
- [ ] 12. Аватар, автор и время всегда видны
  - Сейчас: коммит с двумя длинными метками веток, длинной темой и 12 полосами графа
    выталкивает время, автора и аватар за край панели.
  - [ ] Правые колонки (аватар, автор, время) закреплены у правого края и не уходят за него
    никогда.
  - [ ] Сжимается середина: сначала метки веток (усечение посередине), затем тема
    (многоточие).
  - [ ] Длинные связи — как в SmartGit: если связь коммита с родителем тянется дальше порога
    строк, вместо сплошной полосы рисуются два коротких обрубка со стрелками — у коммита и у
    родителя. Освободившаяся колонка переиспользуется, граф становится уже.
  - [ ] Клик по стрелке прокручивает к другому концу связи и выделяет тот коммит; tooltip
    стрелки — короткий хеш и тема коммита на другом конце.
  - [ ] Порог в строках — настройка графа в Preferences (#23).
  - [ ] Если после этого полос всё равно больше, чем помещается, область графа обрезается у
    края; полосы не сжимаются.
  > Итог: у темы был жёсткий минимум 35ch, всё лишнее резал край панели → правые колонки фиксированной ширины у правого края, сжимаются сначала метки (усечение посередине), потом тема; строка управляется настройками #23; длинные связи — два обрубка со стрелками, колонка освобождается, раскладка в `graph_engine::with_long_links` (лейн 0 не рубится), клик по стрелке — к другому концу, tooltip — хеш и тема; порог по умолчанию 40 строк (чуть больше экрана на 1080p), настраивается; лишние полосы обрезаются у края с затуханием (R-330, R-331; `4d30def`, `75b22de`, `6a53156`).
  > Проверить в сборке: 12 полос и две длинные метки при сужении панели — метки, потом тема, потом обрез графа; клик и tooltip по обеим стрелкам длинной связи.
- [ ] 13. Изменение высоты Diff сдвигает граф
  - [ ] При изменении высоты панели Graph (в том числе из-за сплиттера Diff) первая видимая
    строка графа остаётся на месте.
  > Итог: при ресайзе закреплённый слой строк сдвигал `spacer`, и привязка прокрутки браузера уводила список → `overflow-anchor: none` и чистая `anchoredScrollTop` (тест), она же держит верхнюю строку при смене плотности (`f6e6fd4`).
  > Проверить в сборке: сплиттер Diff и размер окна.
- [ ] 14. Отступ между датой и аватаром слишком большой — уменьшить, но оставить явное
  разделение блоков (токен отступа, а не впритык).
  > Итог: дата была выровнена вправо в колонке 74 px и стояла на колонку от аватара → выровнена влево, зазор — токен `--sp-3` (`c31955c`).
- [ ] 27. Синяя рамка вокруг панели Graph при навигации стрелками
  - Сейчас: клик по коммиту, затем `↑` — вся панель Graph обводится синей рамкой по периметру.
  - [ ] Убрать браузерную рамку фокуса у контейнера панели (`:focus` / `:focus-visible` у
    прокручиваемого элемента). Фокус панели показывается только подсветкой заголовка,
    выбранная строка — выделением.
  - [ ] Проверить остальные панели на то же поведение.
  > Итог: на прокручиваемый контейнер действовал общий `:focus-visible` → общий класс `.key-list` убирает рамку у списка и у выбранной строки, у кнопок, полей и строки, до которой дошли Tab без выбора, рамка остаётся; навешен на Graph, References, Repositories, Worktrees, Files, VirtualList и три списка Investigate, правило в `06` (`e21eb23`).
  > Проверить в сборке: клик и `↑` в каждой панели; Tab по кнопкам и полям.

### C. Repositories

- [ ] 15. Открытое и закрытое состояние
  - Сейчас: надпись `closed` появляется только при ручном закрытии, а переключение на другой
    репозиторий выглядит как закрытие первого — при возврате он загружается заново, но не
    помечен.
  - [ ] Переключение на другой репозиторий не закрывает предыдущий: открытые остаются
    открытыми (с кэшем из #1) до явного закрытия.
  - [ ] Крестик закрытия убрать; закрыть — только через контекстное меню.
  - [ ] Надпись `closed` убрать. Закрытый репозиторий — приглушённые текст и иконка, открытый —
    без приглушения, как в SmartGit.
  - [ ] Замерить память при 5 открытых крупных репозиториях; если растёт чрезмерно — описать в
    итоге.
  > Итог: переключение и так не закрывало — бэкенд держит все открытые, а выглядело закрытием, потому что строки не различались и граф строился заново (это закрыл #1) → крестик убран, закрытие — `Close Repository` в меню или `Ctrl+W`; надпись `closed` убрана, закрытая строка приглушена; для неактивного держатся регистрация, наблюдатель, снимок строки и кэш графа (R-351). Память (тест на large, 50 693 коммита): регистрация и наблюдатель +1,3 MiB, строка +0,3 MiB, разложенный граф +23 MiB — пять открытых large ≈ 125 MiB, почти всё графы; кэш графа #1 ограничен 96 МБ с вытеснением давно не нужных (`617b6ad`).
  > Проверить в сборке: два открытых — оба без приглушения; закрыть через меню — приглушена, без надписи; реальный замер памяти при пяти открытых.
- [ ] 16. Несколько репозиториев с submodules одновременно
  - Сейчас: чтобы добраться до submodule, нужно сначала выбрать главный репозиторий и
    дождаться, пока появятся его submodules. Работа идёт сразу в нескольких проектах.
  - [ ] Дерево submodules любого репозитория в списке раскрывается без того, чтобы делать его
    активным: лёгкое чтение `.gitmodules` и gitlink-записей.
  - [ ] Можно держать раскрытыми несколько проектов одновременно и переходить в любой
    submodule любого проекта одним кликом.
  - [ ] Раскрытое состояние сохраняется между запусками.
  > Итог: дерево читалось открытием каждого submodule, а у закрытой строки нет `RepoId` → команда `submodule_outline` (`.gitmodules` через gix, gitlink из дерева HEAD, без статуса и истории), деревья любых строк раскрыты одновременно, клик по submodule чужого проекта открывает проект в фоне и сам submodule, раскрытое хранится между запусками (R-352; `56eb3d1`).
  > Проверить в сборке: раскрыть submodules двух неактивных строк (одна закрытая), клик по submodule закрытой; после перезапуска деревья раскрыты.
- [ ] 17. Индикаторы изменений, pull и push
  - Сейчас: оранжевая точка справа от имени.
  - [ ] Индикатор изменений — слева от имени. Место под него зарезервировано у всех строк,
    чтобы имена начинались с одной вертикали.
  - [ ] Доступен pull — стрелка вниз, push — стрелка вверх. SmartGit рисует их поверх иконки
    репозитория, не закрывая её. Сделать так же или предложить вариант лучше — обоснуй.
  - [ ] Статус обновляется асинхронно и для закрытых репозиториев. Лёгкий расчёт:
    ahead/behind — по локальным remote-tracking ссылкам через gix; изменения — дешёвая
    проверка индекса по времени модификации, без хеширования; по одному репозиторию за раз,
    отменяемо, с троттлингом.
  - [ ] Фоновый fetch для актуальности pull — отдельной настройкой в Preferences с интервалом;
    без запросов учётных данных; ошибка — статус «неизвестно», запись в лог, без уведомления.
  > Итог: точка стояла справа → слева от имени в слоте у всех строк; стрелки push и pull — на углах иконки, как в SmartGit (отдельная колонка съела бы 12 px у каждой строки ради чаще пустых значков), числа в подсказке; статус для всех строк через `repo_pulse` (ahead/behind по локальным remote-tracking через gix, изменения по размеру и mtime записей индекса, без хеширования), очередь по одному с паузой 300 мс, отменяемая, не начинает ничего, пока занят активный репозиторий; фоновая проверка сервера — настройка `backgroundFetchMinutes` (0 — выключено, по умолчанию), спрашивает `git ls-remote --heads` без записи и без интерактивных запросов, ошибка — `?` и строка в логе (R-353, R-354; `11ddd0e`, `c0e99dd`).
  > Проверить в сборке: изменить файл в закрытом репозитории — появляется точка; включить проверку при новом коммите на remote — стрелка вниз; недоступный remote — `?`.
- [ ] 18. Отступы вокруг треугольника раскрытия
  - [ ] Уменьшить отступы слева и справа от треугольника в Repositories и Branches; слева и
    справа — одинаковые.
  - [ ] Найти другие деревья с тем же дефектом и поправить через общий компонент.
  > Итог: коробка треугольника 20 px плюс база 8 px давали 13/11 px в Repositories и 13/9 px в Branches → коробка шириной с глиф и токен `--disclosure-side` = 6 px с обеих сторон, зона клика прежняя; правка в общем `Disclosure` покрыла Repositories, Branches, Preferences и Files (R-350; `bfc3581`).
  > Проверить в сборке: поля слева и справа от треугольника одинаковы в четырёх темах.

### D. Branches и Worktrees

- [ ] 19. Метка worktree у ветки
  - Сейчас: рядом с веткой иконка worktree и надпись `changes`.
  - [ ] Компактно: `(● worktree)`, где кружок жёлтый — есть изменения, зелёный — чисто и всё
    запушено; цвета те же, что у индикаторов в Repositories.
  - [ ] Метка есть у локальной ветки и нет у remote-tracking — это верно, worktree привязан к
    локальной ветке. Если считаешь, что у remote-ветки, являющейся upstream такой ветки, нужен
    признак, — обоснуй.
  > Итог: у ветки были иконка и слово `clean`/`changes` → метка `(● worktree)`: жёлтая — изменения, зелёная — чисто и всё запушено, серая — чисто, но не запушено, красная — папка пропала; токены `--indicator-*` общие с Repositories. У remote-ветки метки нет: worktree держит локальную ветку, и её строка рядом в дереве — вторая метка дала бы двойной сигнал (`c21caf8`).
  > Проверить в сборке: цвета метки в четырёх темах.
- [ ] 20. Repair worktree: нет статуса, долго
  - Сейчас: после переноса папок `dtv_device_master` и `dtv_device` → `Repair` → выбор папки:
    процесс идёт долго, без статуса.
  - [ ] Repair — операция в общей очереди, со статусом в футере и результатом в уведомлении.
  - [ ] Найти, почему долго: сам `git worktree repair` быстрый — вероятно, после него
    полностью переоткрывается или пересканируется репозиторий. Исправить, записать причину.
  > Итог: сам `git worktree repair` занимает ~25 мс; долго шло следующее за ним чтение списка — вне очереди, поэтому без статуса: у перенесённой папки другие stat-данные у каждого файла, и полный status каждого worktree хешировал всё, а gix индекс не пишет → Repair — операция очереди со статусом в футере и уведомлением об итоге, после него `git update-index --refresh` в починенном и текущем worktree, признак изменений — до первого; на 20 000 файлах первое чтение 3,6 с → 0,5 с, следующие 145 → 60 мс (R-360, R-361; `b333dad`).
  > Проверить в сборке: перенести `dtv_device*` на другой диск → Repair: статус в футере, уведомление.

### E. Тулбар и Diff

- [ ] 21. Выпадающее меню кнопки тулбара
  - Сейчас: при открытии меню (например, у `Rebase`) кнопка теряет выделение — меню выглядит
    оторванным; каретка меняет направление — это хорошо.
  - [ ] Пока меню открыто, кнопка в нажатом состоянии.
  - [ ] Каретка выровнена по центру строки подписи по вертикали (сейчас — в верхней половине).
  > Итог: у кнопки не было состояния «нажата», а каретку flex центрировал по строке, а не по буквам → пока меню открыто, кнопка подсвечена и обведена; подпись с кареткой в строчной вёрстке, `vertical-align: middle` (`088ed0f`).
  > Проверить в сборке: меню `Rebase` при 100, 125 и 150 %.
- [ ] 22. Убрать кнопки действий из Diff коммита — `Cherry-pick`, `Revert`, `Split Off…`,
  `Rebase…`, `Roll Back Tree` и прочие. Эти действия — только через контекстное меню.
  > Итог: в деталях коммита были кнопки действий → убраны; всё, кроме `Roll Back Tree`, уже было в контекстном меню, его добавил в меню коммита и метки ветки; тест проверяет, что каждое прежнее действие кнопки есть в меню (`add87ba`).

### F. Preferences

- [ ] 23. Настройки отображения графа
  - [ ] Переключатели: имя автора, аватар, время, хеш.
  - [ ] Время — вложенные варианты формата (относительное, дата, дата и время); неактивны,
    если время выключено.
  - [ ] Порядок и расположение колонок — перетаскиванием.
  - [ ] Прочее оформление графа: плотность строк, чередование фона, подсветка отмеченных
    веток (#11), порог обрубков длинных связей (#12).
  - [ ] Здесь же — переключатели всех режимов из #26.
  - [ ] Применяется сразу — изменения видны за диалогом.
  > Итог: настроек не было, колонки и полосы были зашиты в код → ключи `graph*` в `Settings` с проверкой при чтении; раздел Graph & History: колонки с переключателями и порядком перетаскиванием (с клавиатуры `Alt+↑/↓`), формат времени относительный / дата / дата и время (неактивен при скрытом времени; разведён с `dateFormat`, который остаётся для Blame и деталей и умеет `both`; старый `dateFormat=relative` переносится), плотность, полосы, подсветка отмеченных, порог обрубков, четыре режима #26 — `collapseMerged` неактивен при `firstParent` с пояснением; всё применяется сразу (R-370; `a79b75e`, `706ebf2`).
  > Проверить в сборке: снять Hash, перетащить Time наверх, `dateTime`, снять полосы, скрыть Time — формат неактивен, `Cancel` возвращает всё; четыре темы.
- [ ] 24. Configure Toolbar — в Preferences
  - [ ] Раздел `Toolbar` в Preferences. Пункт `Edit → Configure Toolbar…` удалить — настройка
    только в Preferences.
  - [ ] Изменения применяются сразу — видны на тулбаре за диалогом.
  - [ ] Кнопка `Undo` внутри раздела: пошаговая отмена изменений тулбара, сделанных в этом
    окне.
  > Итог: отдельный диалог → раздел `Preferences ▸ Toolbar`; пункт `Edit → Configure Toolbar…` и команда палитры удалены, правый клик по тулбару открывает раздел; изменения применяются сразу, `Cancel` возвращает раскладку; `Undo` — пошаговый стек изменений этого окна (R-371; `f927bf4`).
  > Проверить в сборке: изменения видны на тулбаре за диалогом; `Undo` по шагу; в меню Edit нет Configure Toolbar.

### G. Общее

- [ ] 25. Иконки кнопок панелей (Repositories — `Open Repository`, Files — фильтры, Branches —
  сортировка и др.) — чуть крупнее, одним токеном размера на все тулбары панелей. Умеренно:
  чтобы читались, но не спорили с кнопками главного тулбара.
  > Итог: иконки тулбаров панелей были 14–15 px разными числами → один токен `--panel-icon: 16px` в Repositories, Files и Branches; главный тулбар 22 px без изменений (`1986731`).
  > Проверить в сборке: иконки читаются и не спорят с главным тулбаром.
- [ ] 26. Режимы работы с графом — каждый включается и выключается в Preferences, раздел
  графа (#23)
  - [ ] Только первые родители (`--first-parent`): история из одной линии без веток, слитых
    через merge.
  - [ ] Подсветка ветки коммита по клику: клик по коммиту или его полосе выделяет «его ветку»
    — цепочку первых родителей, в которой лежит коммит, от точки ответвления до слияния или
    вершины.
  - [ ] Подсветка предков и потомков выбранного коммита: остальные коммиты приглушаются.
  - [ ] Сворачивание слитых веток: ветка, слитая через merge, сворачивается в одну строку у
    коммита слияния с кнопкой раскрытия; раскрытое состояние сохраняется до переключения
    репозитория.
  - [ ] Режимы совместимы между собой и с отметками веток в Branches (#11); конфликтующие
    сочетания — неактивны с пояснением.
  - [ ] Все — чистые функции поверх результата раскладки, с модульными тестами.
  > Итог: режимы — чистые функции поверх раскладки с тестами, по умолчанию выключены: только первые родители (совпадает с `git log --first-parent`), ветка коммита по клику по строке или линии (подсветка на фронтенде), предки и потомки (остальное приглушено до 0,28), сворачивание слитых веток в строку merge с раскрытием до смены репозитория; одна таблица конфликтов — `collapseMerged` неактивен при `firstParent` (R-341, R-342; `02fe55d`, `989dbb1`, `7899562`, `47ed5ab`).
  > Проверить в сборке: клик по линии, а не по кольцу; раскрытие и сворачивание, положение прокрутки.

### Итог

- [x] Одна строка на пункт: для A — до → после или «откачено: причина»; для остальных —
  `> Итог:` по правилам `CLAUDE.md`.
  > Итог: A — у каждого пункта A/B против сборки до него (6 раундов); принято 1–9, откачен 10. B–G — `> Итог:` и «Проверить в сборке»: интерфейс глазами не проверялся, поэтому галочки у них не стоят.
- [x] Обновлённая таблица бенчмарка, отсортированная по базовому времени.
  > Итог: A/B `night2-final` (28baaca) против итога ветки (96e260d), 6 раундов, все наборы, сырые данные — `doc/benchmarks/2026-09-24-tasks-ab.json`: 26 быстрее, 51 same, 7 медленнее. Медленнее были две группы:
  > - `repo.close` на всех наборах (+18…85 %) — `setOwned` очереди статусов Repositories (#17) читал строку ушедшего репозитория прямо внутри закрытия → чтение через 500 мс, после того как закрытие затихло (`a6b23f1`, тест); повторный A/B (3 раунда): medium, small, submodules — same; large всё ещё 17,3 → 27,0 мс;
  > - `branches.load` large/medium/small +16…30 % (+2…10 мс) и остаток `repo.close` large — пришли с кэшем графа и повторным обходом (#1/#2, атрибуция `night2-final` против `g1-10`: `branches.load` large ×1,29, `repo.close` large ×1,26; g4–g7 чистые). Не откачено: #1/#2 дают `repo.switch` large 278 → 58 мс и отметку ветки 270 → 91 мс; вероятные причины — отпечаток ссылок перед каждым обходом и синхронное освобождение кэша графа при закрытии. **Решение за пользователем:** исправлять отдельным шагом или принять.

| Сценарий | Набор | `night2-final`, мс (p95) | итог ветки, мс (p95) | × | 95 % интервал | Вердикт |
|---|---|---|---|---|---|---|
| `app.ready` | medium | 422.0 (511.5) | 422.8 (460.3) | 1.002 | [0.976; 1.028] | same |
| `app.first-paint` | empty | 403.8 (467.1) | 404.3 (412.9) | 1.001 | [0.981; 1.017] | same |
| `changes.stage-all` | dirty | 365.9 (378.6) | 195.2 (241.1) | 0.533 | [0.511; 0.614] | быстрее |
| `app.dom-loaded` | empty | 328.4 (390.3) | 329.0 (335.9) | 1.002 | [0.991; 1.01] | same |
| `repo.open` | large | 297.6 (322.5) | 280.9 (301.7) | 0.944 | [0.866; 1.015] | same |
| `graph.full-layout` | large | 290.9 (315.8) | 274.5 (293.5) | 0.944 | [0.867; 1.015] | same |
| `repo.switch` | large | 266.3 (287.2) | 70.4 (82.9) | 0.264 | [0.245; 0.291] | быстрее |
| `graph.tick-branch` | large | 266.2 (287.1) | 111.5 (120.5) | 0.419 | [0.374; 0.433] | быстрее |
| `graph.tick-tags` | large | 241.3 (320.5) | 91.1 (99.5) | 0.378 | [0.318; 0.395] | быстрее |
| `app.navigation-start` | empty | 199.5 (255.1) | 200.5 (208.9) | 1.005 | [0.989; 1.032] | same |
| `changes.stash` | small | 195.5 (213.7) | 145.4 (162.0) | 0.744 | [0.698; 0.94] | быстрее |
| `net.fetch` | network | 195.5 (218.4) | 181.8 (195.7) | 0.930 | [0.882; 0.985] | same |
| `net.pull` | network | 183.3 (230.2) | 169.9 (186.6) | 0.927 | [0.848; 0.97] | быстрее |
| `changes.commit` | medium | 182.9 (203.8) | 124.5 (137.1) | 0.681 | [0.622; 0.786] | быстрее |
| `changes.commit` | small | 153.5 (162.1) | 112.1 (161.4) | 0.730 | [0.701; 0.824] | быстрее |
| `files.content-search` | dirty | 116.2 (129.2) | 103.8 (122.0) | 0.893 | [0.793; 1.064] | same |
| `repo.open` | submodules | 114.1 (137.4) | 101.8 (137.5) | 0.892 | [0.803; 0.963] | быстрее |
| `changes.unstage-all` | dirty | 112.1 (116.3) | 95.5 (105.7) | 0.852 | [0.811; 0.934] | быстрее |
| `changes.stage-one` | dirty | 103.4 (111.8) | 78.6 (99.6) | 0.760 | [0.72; 0.876] | быстрее |
| `changes.unstage-one` | dirty | 91.3 (99.5) | 74.4 (78.5) | 0.815 | [0.744; 0.86] | быстрее |
| `net.push` | network | 81.6 (149.9) | 75.7 (88.5) | 0.928 | [0.74; 1.054] | same |
| `repo.open` | dirty | 79.0 (100.0) | 64.2 (79.1) | 0.813 | [0.733; 0.887] | быстрее |
| `repo.switch` | dirty | 74.5 (89.0) | 57.8 (61.9) | 0.776 | [0.698; 0.831] | быстрее |
| `changes.stage-one` | small | 70.2 (74.4) | 57.7 (66.2) | 0.822 | [0.765; 0.998] | same |
| `repo.open-worktree` | medium | 61.9 (70.4) | 53.6 (128.6) | 0.866 | [0.811; 1.773] | same |
| `repo.open-submodule` | submodules | 61.8 (66.3) | 53.7 (95.4) | 0.869 | [0.801; 1.476] | same |
| `graph.first-screen` | large | 61.0 (75.1) | 39.7 (52.4) | 0.651 | [0.61; 0.716] | быстрее |
| `repo.open` | medium | 60.1 (85.3) | 56.0 (75.3) | 0.932 | [0.791; 1.18] | same |
| `repo.switch` | medium | 57.8 (62.0) | 41.2 (49.4) | 0.713 | [0.573; 0.847] | быстрее |
| `ui.context-menu` | medium | 49.8 (106.3) | 3.2 (5.7) | 0.064 | [0.06; 0.072] | быстрее |
| `ui.context-menu` | large | 49.2 (52.8) | 3.6 (7.1) | 0.073 | [0.071; 0.104] | быстрее |
| `graph.full-layout` | medium | 45.5 (57.0) | 42.3 (56.7) | 0.930 | [0.792; 1.154] | same |
| `app.close` | medium | 44.0 (53.0) | 29.0 (47.0) | 0.659 | [0.551; 0.969] | быстрее |
| `graph.tick-branch` | medium | 41.0 (45.4) | 24.3 (24.5) | 0.593 | [0.498; 0.653] | быстрее |
| `graph.tick-tags` | medium | 41.0 (45.3) | 20.2 (20.4) | 0.493 | [0.487; 0.547] | быстрее |
| `files.toggle-tree` | dirty | 36.8 (40.8) | 32.7 (67.7) | 0.889 | [0.78; 0.991] | same |
| `repo.open` | small | 35.2 (50.0) | 35.0 (39.2) | 0.994 | [0.741; 1.006] | same |
| `branches.load` | large | 31.1 (39.4) | 39.7 (52.4) | 1.277 | [1.203; 1.405] | **медленнее** |
| `graph.first-screen` | submodules | 31.0 (37.5) | 24.7 (33.1) | 0.797 | [0.762; 0.823] | быстрее |
| `graph.full-layout` | submodules | 30.6 (37.1) | 30.8 (41.5) | 1.007 | [0.976; 1.043] | same |
| `ui.theme` | medium | 29.2 (33.6) | 30.8 (33.4) | 1.055 | [0.856; 1.321] | same |
| `files.status` | large | 29.2 (33.4) | 29.2 (33.4) | 1.000 | [0.871; 1.172] | same |
| `graph.first-screen` | dirty | 29.1 (33.0) | 18.2 (22.2) | 0.625 | [0.613; 0.811] | быстрее |
| `files.status` | submodules | 29.0 (29.2) | 29.2 (37.6) | 1.007 | [1; 1.332] | same |
| `graph.first-screen` | medium | 28.1 (53.7) | 15.6 (19.3) | 0.555 | [0.437; 0.63] | быстрее |
| `files.content-search` | medium | 25.0 (29.0) | 20.8 (26.4) | 0.832 | [0.796; 0.992] | same |
| `graph.full-layout` | dirty | 24.1 (30.3) | 23.4 (32.5) | 0.971 | [0.833; 1.152] | same |
| `branches.load` | submodules | 22.5 (29.1) | 24.7 (33.1) | 1.098 | [1.069; 1.182] | **медленнее** |
| `files.status` | dirty | 20.9 (25.1) | 20.9 (37.6) | 1.000 | [0.995; 1.197] | same |
| `diff.big` | dirty | 20.2 (20.3) | 16.2 (20.2) | 0.802 | [0.787; 0.836] | быстрее |
| `branches.load` | dirty | 18.3 (20.8) | 18.2 (22.2) | 0.995 | [0.942; 1.213] | same |
| `graph.first-screen` | small | 16.7 (20.2) | 10.4 (12.8) | 0.623 | [0.611; 0.748] | быстрее |
| `graph.scroll` | medium | 16.7 (16.8) | 16.6 (26.5) | 0.994 | [0.838; 1.186] | same |
| `files.status` | medium | 16.7 (16.7) | 16.6 (75.1) | 0.994 | [0.994; 1.006] | same |
| `graph.scroll` | large | 14.0 (16.7) | 14.0 (14.1) | 1.000 | [0.727; 1.111] | same |
| `graph.full-layout` | small | 13.9 (16.0) | 15.1 (18.4) | 1.086 | [0.968; 1.244] | same |
| `repo.close` | large | 13.9 (17.9) | 25.7 (30.0) | 1.849 | [1.274; 1.899] | **медленнее** |
| `commit.next` | large | 13.2 (13.9) | 12.1 (13.7) | 0.917 | [0.715; 1.025] | same |
| `files.status` | small | 12.5 (12.6) | 12.5 (12.7) | 1.000 | [1; 1] | same |
| `commit.next` | small | 12.1 (13.7) | 12.1 (13.7) | 1.000 | [0.81; 1.017] | same |
| `branches.expand` | large | 12.1 (12.2) | 12.1 (15.0) | 1.000 | [0.992; 1.008] | same |
| `commit.next` | submodules | 12.1 (13.7) | 12.1 (13.8) | 1.000 | [0.992; 1.041] | same |
| `commit.next` | dirty | 12.1 (14.6) | 12.1 (12.2) | 1.000 | [0.983; 1.017] | same |
| `branches.load` | medium | 12.0 (15.2) | 15.6 (19.3) | 1.300 | [1.102; 1.475] | **медленнее** |
| `commit.next` | medium | 12.0 (13.7) | 12.1 (13.8) | 1.008 | [0.948; 1.317] | same |
| `branches.expand` | medium | 12.0 (12.1) | 12.0 (14.9) | 1.000 | [0.983; 1.017] | same |
| `diff.submodule` | submodules | 12.0 (12.2) | 12.0 (49.3) | 1.000 | [0.992; 1.017] | same |
| `diff.small` | dirty | 12.0 (12.1) | 11.9 (12.1) | 0.992 | [0.983; 1.017] | same |
| `commit.select` | large | 11.9 (14.6) | 11.9 (12.4) | 1.000 | [0.992; 1.026] | same |
| `commit.select` | submodules | 11.9 (12.1) | 11.9 (12.8) | 1.000 | [0.983; 1.017] | same |
| `commit.select` | small | 11.8 (12.0) | 11.8 (12.1) | 1.000 | [0.992; 1.008] | same |
| `commit.select` | medium | 11.8 (12.5) | 11.8 (12.0) | 1.000 | [0.983; 1.008] | same |
| `commit.select` | dirty | 11.8 (11.9) | 11.9 (14.4) | 1.008 | [1; 1.017] | same |
| `diff.binary` | dirty | 11.8 (12.1) | 11.9 (12.1) | 1.008 | [1; 1.034] | same |
| `branches.load` | small | 9.9 (12.6) | 10.4 (12.8) | 1.051 | [0.86; 1.121] | same |
| `repo.close` | medium | 8.5 (9.8) | 14.7 (16.8) | 1.729 | [1.453; 1.953] | **медленнее** |
| `branches.filter` | medium | 8.4 (8.4) | 8.3 (8.5) | 0.988 | [0.679; 1] | same |
| `files.filter` | medium | 8.4 (8.5) | 8.2 (8.4) | 0.976 | [0.702; 1.012] | same |
| `branches.filter` | large | 8.4 (8.5) | 5.8 (8.4) | 0.690 | [0.667; 1.018] | same |
| `repo.close` | small | 8.3 (9.6) | 12.5 (12.6) | 1.506 | [1.157; 1.524] | **медленнее** |
| `ui.preferences` | medium | 8.3 (8.4) | 8.4 (10.0) | 1.012 | [1; 1.195] | same |
| `repo.close` | submodules | 8.3 (9.8) | 9.8 (70.9) | 1.181 | [1.032; 1.494] | **медленнее** |
| `files.filter` | dirty | 8.3 (8.5) | 8.3 (8.4) | 1.000 | [1; 1.431] | same |
| `repo.close` | dirty | 8.3 (9.9) | 12.5 (12.6) | 1.506 | [0.969; 1.506] | same |

