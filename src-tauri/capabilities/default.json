{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions granted to the main window and to its child windows.",
  "windows": [
    "main",
    "compare-*",
    "merge-*",
    "blame-*"
  ],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default",
    "clipboard-manager:allow-write-text",
    "core:window:allow-destroy",
    "updater:default",
    "process:allow-restart"
  ]
}
